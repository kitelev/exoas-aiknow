---
exo__Asset_uid: f0585b6d-2c3b-46d5-beec-b4a045592ca8
exo__Asset_createdAt: 2026-09-08T08:43:22
exo__Asset_updatedAt: 2026-09-26T10:24:51
exo__Instance_class:
  - "[[39a39239-2a97-483a-ba91-fb01bf5c85f3]]"
exo__Asset_createdBy: "[[4ef3962d-b8a7-42b5-bd28-88ec846f1d13]]"
exo__Asset_label: "Правило: integration-test-revert-verify (полный текст; пилот rules-from-graph, 2026-09-06)"
aliases:
  - "Правило: integration-test-revert-verify (полный текст; пилот rules-from-graph, 2026-09-06)"
  - integration-test-revert-verify
exo__Asset_isDefinedBy: "[[2ce77ac1-3f78-4f64-9dc1-41ea01da1ef0]]"
inbox__ExoAssistantKnowledge_decay: "[[06a5b9f9-da93-4234-883f-3f0cf65b2fba]]"
inbox__ExoAssistantKnowledge_confidence: "[[227def30-f56f-4f5f-936d-f6a81ad73c96]]"
aiKnow__Memory_aboutConcept:
  - "[[16a725fd-8ef7-425a-af5b-abdec027b325]]"
  - "[[83d44fae-29a4-40fa-ac95-bbdf02129102]]"
  - "[[40b891a1-014a-4fec-88ed-20be8e4edfe0]]"
  - "[[9173f32f-a521-4653-a27d-95e61978707d]]"
  - "[[45d37e19-7baa-467c-9b6d-df1d567442d0]]"
  - "[[473d20a4-7a63-4cbe-8fa8-510d51d5f0f5]]"
  - "[[ea2818a8-f1d5-42e1-8a5a-ec42a2fd910e]]"
  - "[[88460bff-2e69-4fa9-8073-d7dc6cada2fc]]"
  - "[[1bc20b0e-9827-40b0-aa81-d7d5ea127db6]]"
  - "[[2bdb1269-ecfa-45c6-96f6-d97e2b44cee2]]"
  - "[[290000cf-5ae2-4051-a7fa-a810d23d2499]]"
  - "[[af0e126f-cc3c-4917-a4b1-c92a28ce8303]]"
  - "[[f23bce3d-fb26-4f63-8c2b-770f2b3de7c3]]"
  - "[[b7143233-241c-4094-bf9f-8080fc324f2d]]"
---
<!-- Added by /session-retrospective 2026-05-24 — root: PR #3244 round-1 code-reviewer caught my integration test passing pre-fix AND post-fix (false-positive coverage). Same pattern almost repeated in PR #3248 until I proactively verified. -->

# Integration tests for regression fixes: revert→fail / restore→pass discipline

## Принцип

⛔ **БАРЬЕРА НЕ БУДЕТ — и это ЗАМЕР, а не лень (2026-08-22, метод задачи 7156e4c2).** Нарушение здесь СЕМАНТИЧЕСКОЕ («ось вакуумна», «мутант не различает»), а хук видит только вызов инструмента: в тексте `Bash`/`Edit` нет признака, по которому отличить ось, которая проверяет, от оси, которая зелена по независимой причине. ⇒ рецидивы (9 за 30 дней) ловятся **симптомом из таблицы**, а не гвардом; ускорить это можно только сжатием текста, не механикой.

Любой integration test, который claims «exercises the regression», обязан быть **empirically verified** двумя прогонами:

1. **Revert production fix** → test must FAIL (it should detect the absence of the fix).
2. **Restore production fix** → test must PASS.

Если test passes в обоих состояниях — он не exercises regression; он vacuously passes по другой причине (fixture defect, wrong assertion, shape mismatch). Это **false-positive coverage** — ship'нутый guard, который не guard'ит. ⛔ (квалифицировано 2026-09-02: верно для оси **на дефект**; ось на **отсутствие побочного эффекта** зелена в обе стороны ЗАКОННО — §сразу ниже.)

## ⛔ Ось на ОТСУТСТВИЕ побочного эффекта зелена в ОБЕ стороны ЗАКОННО — и это её ПРОДУКТ, а не вакуумность

Строка выше безусловна и потому применяется к **каждой** оси набора. У косметического фикса
(вычистить NUL-байты, переименовать переменную, снять мёртвый импорт) в наборе штатно живёт ось
**«поведение не изменилось»** — и она обязана быть зелёной И на предмете, И на мутанте: её
зелёность на до-фиксной половине **и есть** доказательство, что правка ничего не сломала.

- ⛔ **Дискриминатор — ПРЕДМЕТ оси, а не её исход.** Ось на **дефект** («грep без `-a` видит»,
  «носитель чист») обязана краснеть на мутанте. Ось на **отсутствие регрессии** обязана НЕ
  краснеть — иначе фикс не косметический, и это находка.
- ⛤ **Отличать от «контроля на невакуумность»** (§капабилити-латч): там соседняя ось зелена,
  потому что мутант бьёт **не в неё**. Здесь мутант бьёт **ровно в предмет** оси (заменён весь
  файл), и она всё равно зелена — по существу, а не по промаху.
- ✅ **Floor: такая ось законна ТОЛЬКО в наборе, где ≥1 соседняя ось КРАСНЕЕТ на том же мутанте.**
  Ноль краснот на весь набор ⇒ вакуумен набор целиком, и строка выше применяется без поблажек.
- ⚠ **Симптом-tell, ловится ПРИ ЧТЕНИИ набора:** ты собираешься назвать ось вакуумной — спроси,
  что она утверждает. «Ничего не изменилось» — это утверждение, и оно проверяемо.

### ⛤ Инструмент для таких фиксов: git отдаёт ПАРУ бинарной ⇒ построчного диффа нет, сверяй `cmp -l`

Приёмка косметического фикса упирается в «а ровно ли то изменено, что заявлено». При NUL-байте
в любой из ревизий `git diff --numstat` даёт `-  -`, а `git show` — `Bin N -> N bytes`:
построчного диффа **не существует**, и глазами сверить нечего.

```bash
cmp -l <(git show <sha>^:<path>) <(git show <sha>:<path>)   # rc=0 идентичны · rc=1 различия
```

- ⛔ **Не пайпить** (`| cat`, `| head`): `$?` тогда берётся у последнего звена, и `rc=1` читается
  как `0` — `bash-loop-eval-pitfalls` Г3b. Вердикт — по `rc` **или** по числу строк вывода.
- ⚠ **Смещения 1-based, значения ВОСЬМЕРИЧНЫЕ:** `0` = NUL, `40` = пробел (не «сорок»).
- ⛤ Ноль строк сверх ожидаемых = «прочих правок нет» — ровно то, что нужно приёмке; `diff -q`
  этого не даёт (он говорит лишь «Binary files differ»).

**Empirical 2026-09-02** (`91c5fef9`, приёмка NUL-фикса `save-retrospective-verify.sh`): `numstat`
пары → `-  -`; `cmp -l` → **ровно 3 строки** (4819 / 5745 / 6458, `0o0 → 0o40`) ⇒ правка = три
NUL→пробел и ничего больше. Ось «поведение не изменилось» дала `PASS=6 FAIL=0` на **обеих**
половинах при двух краснеющих соседних осях — то есть косметичность доказана, а набор не вакуумен.

## Дискриминатор (один вопрос)

> **Предъявлен ли ИСХОД, при котором эта ось КРАСНАЯ — прогоном, а не рассуждением?**

- **Да** (revert → FAIL, restore → PASS на одном дереве) ⇒ ось различает свой предмет.
- **Нет** («логика очевидна», «тест же про это») ⇒ это НЕ ось, а декорация: зелёное здесь
  неотличимо от зелёного по независимой причине.

⚠ **Второй вопрос, обязательный при «мутант не покраснел»:** сломан предмет — или сломана
МУТАЦИЯ? Прежде чем писать «ось вакуумна», проверь, что мутация вообще ЛЕГЛА (`assert n == 1`
на якорь) и что она меняет ПОВЕДЕНИЕ, а не текст сообщения.

⛔ Зелёное в ОБЕ стороны не всегда дефект: у оси на **отсутствие побочного эффекта** это её
законный продукт, а у гварда **defensive**-вида исход совпадает с негвардовым по арифметике.
Тогда дискриминатор меняется на «назови вход, где результаты расходятся» — нет такого входа
⇒ запирать надо наблюдаемое РЕШЕНИЕ, а не строку гварда.

## Tell

- ⚠ Ты пишешь в тело PR «покрыто тестом» и **не гонял** ни одного прогона без фикса.
- ⚠ `git stash` перед revert напечатал «No local changes to save» (или ты заглушил вывод) — откат
  был **вакуумным**, обе половины флипа шли на одном предмете.
- ⚠ Обе половины флипа дали **побайтно одинаковый** результат, и напрашивается вывод «фикс ничего
  не меняет»: сперва проверь, что база РЕАЛЬНО до-фиксная (маркер фикса в извлечённом файле).
- ⚠ Драйвер говорит «не различает», а вручную мутант **краснеет** — дефект в связке (имя оси как
  машинный ключ, регэксп сбора), а не в оси.
- ⚠ Красных БОЛЬШЕ предсказанного, и предсказание ты выводил из ТЕМЫ мутанта, а не из того, какие
  оси вообще исполнятся после его применения.
- ⚠ Набор зелёный, а фича сломана в проде: оси судят **промежуточную запись**, тогда как продукт —
  конечный ЭФФЕКТ, и между ними стоят ещё гейты.
- ⚠ Ожидание оси прибито к **абсолютной величине снаружи** (дата, снимок каталога, счёт живых
  файлов) — она протухнет на исправном коде, и краснота придёт не по делу.

## Когда правило срабатывает

- Writing integration test для bug fix (любая природа: parser, validator, executor, indexer)
- Writing integration test для new feature, который reintroduces a previously-fixed scenario
- Writing acceptance test после code-reviewer flag'нул "test doesn't exercise the regression"

НЕ применяется к:
- Unit tests, где input/output контракт явный (failing input → failing output detector)
- Test для new feature без regression history — здесь нечего «revert»

## Required workflow

```bash
# Step 1: Verify test fails without fix
# Fix ещё НЕ закоммичен (изменения в working tree):
# ⛤ Нужно только БАЗОВОЕ СОДЕРЖИМОЕ файла ⇒ `git show <пред-фиксный-sha>:<path> > /tmp/base` — stash не нужен ВОВСЕ (§A1).
#    Нужна копия ДЕРЕВА (мутировать нельзя) ⇒ §A10 ZERO-GIT-TOUCH. У обоих механизмов ниже — по 3+ рецидива с ущербом.
git stash push <production-file>   # OR temporarily revert the lines
<run the test>                     # Must fail
git stash pop                      # OR restore lines

# Fix УЖЕ закоммичен — stash НЕ работает (см. §A1 — stash-рецепт после commit):
git checkout origin/main -- <production-file>
<run the test>                     # Must fail
git checkout HEAD -- <production-file>

# Step 2: Verify test passes with fix
<run the test>                     # Must pass

# Step 3: Document in PR / commit
# "Empirically verified: FAILS pre-fix (N/N), PASSES post-fix (N/N)"
```

<!-- Added by /session-retrospective 2026-08-16 — root: ПЕРЕИСПОЛЬЗОВАЛ свой же harness (restore = `git checkout --`) на дереве, где лежали НЕзакоммиченные ревью-правки; restore стёр их из 2 из 3 файлов. §A2 запрещает checkout до коммита — но запрет адресован ЧЕЛОВЕКУ, а исполнял его СКРИПТ, написанный на прошлом (чистом) прогоне. -->
⛔ **ХАРНЕСС, ЧЕЙ `restore()` — `git checkout --`, ОБЯЗАН САМ ОТКАЗЫВАТЬСЯ НА ГРЯЗНОМ ДЕРЕВЕ.** §A2 («checkout-рецепт применим ТОЛЬКО после коммита») — правило для того, кто печатает команду. Но revert-verify быстро превращается в **скрипт**, и дальше его **переиспользуют**: написан он был на закоммиченном состоянии, где `checkout` восстанавливал точно, а запускается позже — поверх свежих незакоммиченных правок, которые молча уничтожает. Дисциплина тут не работает **по построению**: команду в этот момент никто не печатает.

- ✅ **Floor — три строки в начале харнесса, до первой мутации:**
  ```bash
  D=$(git -C "$WT" status --porcelain -- $FILES)
  [ -z "$D" ] || { echo "ABORT: незакоммиченные правки в $FILES — restore их СОТРЁТ:"; echo "$D"; exit 2; }
  ```
  Отдельный код возврата (`2`) обязателен: «не запускался» и «оси не различают» — разные исходы, и смешивать их нельзя.
- ⚠ **Симптом-tell ПОСЛЕ прогона, если гварда не было:** финальный «restored»-прогон даёт **не** контрольные числа (у меня `failed=1` при контроле `0/100`), либо `git status` показывает изменённым **не тот** набор файлов, что до старта. ⛔ Не читать это как «ось нестабильна» — это стёртые правки.
- ⛤ **Дешевле гварда только порядок: коммить ДО revert-verify.** Тогда `checkout` восстанавливает ровно то, что нужно, и харнесс безопасен по построению. Гвард нужен потому, что порядок соблюдается не всегда — а скрипт запускается всегда.

⛔ **Гвард выше POINT-IN-TIME, и его недостаточно: коллизия приходит ВНУТРЬ окна мутации.** Floor проверяет дерево **до** первой мутации — то есть отвечает на «была ли чужая правка, когда я стартовал». Между мутацией и restore проходят минуты прогона, и в это окно чужой актор (параллельная сессия, оркестратор, я сам) пишет в тот же файл. На старте было чисто, гвард промолчал, а restore всё равно уничтожил работу.

- ⛔ **Байтовый restore «из памяти» вместо `git checkout` эту дыру НЕ закрывает** — он тоже перезаписывает файл целиком. Разница только в том, к какому состоянию откатывает; чужая правка гибнет одинаково.
- ✅ **Floor: запомни sha256 ТОГО, ЧТО ТЫ НАПИСАЛ, и сверь ПЕРЕД restore.** Не совпало ⇒ писал не ты ⇒ ⛔ **не восстанавливать**: отложить текущее содержимое (`<file>.concurrent`) и упасть громко.
  ```python
  SRC.write_text(mutant); mine = sha(SRC)          # ← что написал Я
  run()
  if sha(SRC) != mine:                              # ← кто-то писал в окно
      shutil.copy(SRC, str(SRC) + ".concurrent")
      sys.exit("⛔ CONCURRENT WRITE: чужая правка сохранена, restore НЕ выполнен")
  SRC.write_text(original)
  ```
- ⚠ **Симптом-tell у ПОСТРАДАВШЕГО:** твоя правка «исчезла», `git status` чист, mtime файла — момент чужого restore. ⛔ Не читать как «Edit не применился»: он применился и был снесён.
- ⛤ **Обязанность симметрична и лежит на ОБОИХ.** Пишущий в общий worktree, где кто-то может гонять мутанты, обязан **сказать об этом** (иначе харнесс не знает, что теперь дерево грязное); владелец харнесса обязан **проверить перед restore**. Ни одна половина по отдельности класс не закрывает.
- **Empirical 2026-08-18** (PR #4078): раунд-2 ревьюер гонял permutation-харнесс в том же worktree; я правил комментарий внутри его окна. Его teardown (`git checkout HEAD -- <file>`) снёс правку; гвард на старте был удовлетворён. Обнаружено им же и честно отрапортовано, но правку пришлось накладывать заново.

## Common false-positive shapes

| Symptom | Underlying cause | Fix |
|---|---|---|
| Test passes both ways, assertion looks tight | Shape/contract mismatch — fixture doesn't trigger the code path | Inspect emitted triples / API calls; mirror production shape exactly |
| ⛔ Ось ИЗОЛЯЦИИ («предмет не тронул живой X») краснеет, а предмет исправен — и краснеет НЕ каждый прогон | Дискриминатор — СНИМОК общего ресурса до/после (манифест `.md`, `shasum` журнала, mtime каталога состояния). В общий ресурс пишут ТРЕТЬИ ЛИЦА (живой демон, репликация, соседний бот), и их запись, попавшая в окно прогона, читается как утечка изоляции. ⛤ Отличать от строки «КОНТРОЛЬ красный сразу после правки» ниже: там третье лицо тронуло ПРЕДМЕТ/ХАРНЕСС (код), здесь — ПРЕДМЕТ НАБЛЮДЕНИЯ самой оси | Ось обязана **атрибутировать АВТОРА**, а не фиксировать факт изменения: у прогона есть уникальный маркер (nonce вопроса, свои uid), и запись, им порождённая, его содержит ⇒ `mine` / `foreign`, красным считать только `mine`. ⛤ Границу назвать в коде: удаление маркера не оставляет ⇒ считается своим безусловно (ошибка в громкую сторону). ⚠ Вероятность считается, а не оценивается: `частота чужих записей × длительность прогона` (замер 2026-09-07: 99 записей/сутки × 310 с ⇒ треть прогонов). ⛔ Не «ослабить ось» и не «мерить реже» — оба снимают защиту. ✅ Если маркера нет по построению (предмет — чужой журнал), судить ИНВАРИАНТ («путь не боевой»), а не снимок |
| ⛔ Ось зелена под мутантом, который её предмет ЗАВЕДОМО ломает; предикат — `grep -q '<маркер>'` по ВСЕМУ выводу предмета | Маркер встречается и в СОСЕДНЕЙ строке того же вывода — той, что печатается ВСЕГДА (справка о смежном механизме, подсказка «вернуть», статус соседа) ⇒ вердикт берётся из неё, а не из статусной строки, и ось сравнивает предмет с КОНСТАНТОЙ | Судить по ОПРЕДЕЛЁННОЙ строке (`head -1` для статусной), а не по вхождению. ⚠ Tell ловится ДО прогона: маркер вердикта — общеупотребительный символ (эмодзи-статус, `OK`, `✅`), а вывод предмета многострочный. ⛤ Обнаруживается ТОЛЬКО мутантом: контроль зелен в обе стороны |
| Test passes pre-fix, fails post-fix | Test asserts buggy behaviour; the «fix» is correct but test codifies old bug | Update assertion to expected post-fix behaviour. ⛔ **И спроси, ПОЧЕМУ он был зелёным** — почти всегда он проверял НЕ ТО поле: ассерт стоял на признаке, который дефект не менял, а поле, где дефект жил, не проверялось ВООБЩЕ. ⇒ обновляя, **усиль**: допиши ось на то самое поле, иначе набор снова не поймает свой предмет. ⚠ Tell: ассерт называет ТИП/ФАКТ («это minus», «вернулось число»), а дефект — в ЗНАЧЕНИИ/ОПЕРАНДЕ |
| ⛔ Ось краснеет ПОСЛЕ фикса, хотя её ассерт ВЕРЕН и стоит на ТОМ САМОМ поле | Фикстура нереалистична, а прод **ОТБРАСЫВАЛ** значение, которое эту нереалистичность обнажает: пока возврат игнорируется (`f x` вместо `if f x`), любая статическая фикстура проходит. Фикс начинает значение ПОТРЕБЛЯТЬ ⇒ та же фикстура становится законным «отказом». ⛤ Отличать от строки выше: там ассерт кодифицировал баг, здесь ассерт верен — **дефект прода МАСКИРОВАЛ дефект фикстуры** | ⛤ **Предсказывать ДО отгрузки, а не реагировать после:** прогнать КАЖДУЮ существующую фикстуру через ново-потребляемый предикат и посчитать, сколько дают «отказ» — это и есть список осей, которые покраснеют. Он **часть работы**, а не scope drift: фикс не может лечь зелёным иначе. ⚠ Tell при чтении прода: есть вызов, чей результат не читается, и на нём стоят оси |
| ⛔ Фикс применён верно, а repro КАК БЫЛ красным, так и остался | Корень на слой глубже, чем ты решил; «применил и не помогло» само по себе НЕ говорит, где именно | ⛤ **Матрица проб по осям, а не гадание.** Взять предмет и варьировать ОДНУ ось за раз, пока не найдётся ближайшая пара «работает / не работает». Тогда корень зажат между ними и его не надо угадывать. Пример-форма: тот же оператор ВНЕ конструкции / внутри неё · внутренний аргумент КОНСТАНТА / внешняя переменная. ⚠ Пробы писать **в одном сьюте с `console.log`**, а не по одной — сравнимость даёт именно таблица |
| Test fails both ways | Fixture setup wrong (missing required props, validator skips file) | Trace via debug script: dump triples / log emission counts |
| ⛔ Ось зелена, мутант её НЕ краснит — а предикат ждёт **ТОЧНОЕ** значение (`rc == 2`), не отрицание | У предмета **ДВА разных отказа сводятся к одному коду**: «инструмент недостижим» и «прогон не помог» оба дают `rc=2 BROKEN` ⇒ ось удовлетворяется чужим отказом. §A38 сюда НЕ дотягивается — он про предикат-ОТРИЦАНИЕ (`rc != 0`), а здесь равенство, и оно ощущается точным | Грепать **СВОЙ текст** отказа (`grep -q '<инструмент> недостижим'`), а не общий маркер. ⚠ Tell до прогона: в скрипте `exit 2` встречается >1 раза (`grep -c 'exit 2' <предмет>`) ⇒ код возврата ось не различает ⛔ **И сам tell НЕПОЛОН: он считает `exit 2` в ПРЕДМЕТЕ, а путей к одной точке выхода бывает несколько по ВХОДУ** (валидный вход и мусорный аргумент дают тот же код) ⇒ счёт даст `1`, и tell промолчит. ✅ Второй tell, независимый от предмета: **все оси набора судят по ОДНОМУ наблюдаемому** ⇒ общий дефект фикстуры пройдёт набор целиком; хотя бы одна ось обязана судить по ДРУГОМУ (метка на диске, запись в журнале, побочный артефакт). _Empirical 2026-09-05: 3 оси хука зелены на фикстурах с литеральным `{VB}` в пути — `rc=2` приходит и для несуществующего пути; поймала ось, судившая по МЕТКЕ._ |
| ⛔ Мутант ПРИМЕНИЛСЯ (`cmp` показывает различие), а красных **НОЛЬ** | У предмета **ДВА механизма одного назначения на разных уровнях** (латч «уже сообщали» ↔ дедуп по подписи; гвард входа ↔ гвард выхода; кэш L1 ↔ L2), и ось упражняет **не тот**, который ты мутировал ⇒ ось корректна, неверен **ЯКОРЬ** | Грепнуть **назначение**, а не имя переменной (`grep -n 'latch\|LATCH'`); носителей >1 ⇒ выбрать тот, что стоит на пути фикстуры ЭТОЙ оси. ⚠ Tell: пустая дельта при успешном `cmp` — это НЕ «ось вакуумна» (вакуумная даёт [] и на мутанте, бьющем в ЕЁ механизм). ⛤ Побочно вскрывает ПРОБЕЛ: механизм, который ты мутировал, не упражняется ничем — оставить мутант с ожиданием `[]` как **детектор пробела**, он позеленеет ровно когда ось заведут |
| ⛔ Мутант-ДЕМОНСТРАТОР (построен показать claim: «здесь покрытие сузится МОЛЧА») дал ОСМЫСЛЕННЫЙ, но НЕ демонстрируемый исход — ждал зелёный, получил красный | Ты заменил ПРЕДИКАТ целиком (поведенческий → хардкод-список) ⇒ снял и его **побочную** функцию (отсев соседей), и наблюдаемое стало про неё, а не про твой предмет | Демонстратор **аддитивен**: механизм сохранить, предмет демонстрации ДОБАВИТЬ к нему (`skip = <поведенческий> ∪ {две безобидные}`). ⚠ Tell: ты заменяешь выражение целиком, а не меняешь его значение. ⛤ Отличать от §A36: там вторая мутация ломает КОМПИЛЯЦИЮ (`BROKEN`/`Tests: 0`), здесь — соседний РАБОЧИЙ механизм, прогон идёт и врёт осмысленно |
| ⛔ Мутант краснит ось, которой по смыслу **не касается** | Фикстура этой оси удовлетворяет **лишнему** правилу предмета ⇒ ось СМЕШАНА и отвечает на два вопроса сразу; её зелёный больше не означает того, что написано в её имени | **Изолировать фикстуру** (снять лишнее условие), а НЕ дописывать ось в ожидания мутанта — второе благословляет смешение навсегда. ⛤ Дискриминатор «изолировать vs дописать»: следует ли краснота из семантики мутанта **на предмете именно этой оси**? Да ⇒ законна, перечислить явно. Нет ⇒ смешение, чинить фикстурой |
| ⛔ То же, но изоляция фикстуры НЕ помогает: сканер у осей ОБЩИЙ и **агрегатный по построению** | Ассерт ключуется на **счётчике** этого сканера (`… == 1`) ⇒ утверждает «сломан РОВНО ОДИН предмет во ВСЁМ наборе» — это про НАБОР, не про свой. Внешний мутант, сломавший соседний, делает счётчик `2`, и ось краснеет **ложной причиной**: текст обвиняет гвард, тогда как гвард различает и предмет назвал | Атрибуцию давать **ИМЕНЕМ своего предмета в выводе** (`grep -q '<имя пары>'`), а не числом; агрегат НЕ трогать — его `rc` остаётся законным конъюнктом. ⛔ Ослабление `== 1` → `>= 1` тавтологично рядом с `rc = 1` (сканер даёт `1` ⟺ счётчик ≥ 1) ⇒ снимать конъюнкт целиком. ⚠ Tell: в конъюнкте число из вывода ОБЩЕГО сканера — оно о наборе, не о предмете оси. ⛔ Приёмка требует ОБЕ половины (мутант A краснит ровно {общая, A}; B — {общая, B}). ⛤ Диагностика расхождения — §A54 ниже; тот же принцип, что `aggregate-hides-dead-stream` §Floor п.1 «состав, а не объём», потребитель другой — АССЕРТ |
| ⛔ Ось на ГОНКУ зелена при снятом гварде, хотя мутация применилась | Тест не дошёл до момента, где гонка вообще возможна: «подождать ход» смоделировано **фиксированным числом тиков** (`await Promise.resolve()`, `setTimeout(…,0)` ×N), а предмет уходит в ожидание глубже — и к моменту мутации мира уже отработал целиком **на новом** мире | Ждать **фактического состояния**, а не тиков: предмет выставляет флаг в точке подвеса, тест крутит `while (!suspended) await new Promise(r => setTimeout(r, 0))`. См. §Ось на ГОНКУ ниже |
| ⛔ КОНТРОЛЬ набора красный СРАЗУ после твоей правки (а приёмка требовала N/N) | Краснота может быть **пред-существующей**: третье лицо тронуло харнесс или его предмет между снятием базовой линии и твоим прогоном. «Покраснело после моей правки» — корреляция, а не причина | Прогнать **ТОТ ЖЕ** харнесс против ДО-правочного предмета: `git show HEAD:<путь> > /tmp/pre.sh && chmod 0755 /tmp/pre.sh` (⛔ бит исполнения обязателен — `harness-invocation-surface`), затем `SUT=/tmp/pre.sh <харнесс>`. Вердикты **идентичны побайтово** (та же ось, та же строка, тот же rc) ⇒ регрессии нет, дефект пред-существующий → отдельным тикетом, scope не расширять; расходятся ⇒ регрессия твоя. ⛤ Гнать это надо и для правки, которая «не может ничего сломать» (комментарий, docs): замер стоит один прогон, а спор «это же только комментарий» не является доказательством |
| ⛔ Вердикты РАЗОШЛИСЬ (⇒ «регрессия твоя»), а предмет правки — НОВАЯ проверка в ОБЩЕМ наборе | Твой предикат верен для ПРОДА и ложен для ФИКСТУР соседей: они строились под ДРУГИЕ инварианты и не обязаны удовлетворять твоему — синтетический хелпер набора создаёт объекты без поля, которое ты требуешь | ⛔ Не ослаблять предикат «вообще» и не править чужую фикстуру. ✅ Замерить класс НА ЖИВОМ графе: носителей 0 ⇒ исключить его из предиката ЯВНО **и печатать их число** рядом с вердиктом (`без актора=N`) — тогда предикат строг к проду, слеп к синтетике, а исключение наблюдаемо. ⚠ Tell: краснеет ось, чьей ТЕМЫ твоя правка не касается, при зелёной твоей оси |
| ⛔ Мутант применился (`n == 1`), ось по смыслу его предмет, а набор ЗЕЛЁН — и естественный вывод «ось слепа» ЛОЖЕН | Мутант **СНЯЛ ЗВЕНО ПАЙПА**, и вместе с логикой изменилась **сигнальная семантика**: `grep -q` выходит на первом совпадении и закрывает пайп ⇒ вышестоящее звено получает **SIGPIPE** ⇒ rc ПАЙПЛАЙНА становится **141**, а не 0 ⇒ условие `if ! …` **инвертируется**, и мутант ведёт себя как исправный код. ⚠ Форма «убрать `\| tail -N \|`» этим и опасна: она выглядит самой чистой мутацией предиката | ✅ Мутировать **ПАРАМЕТР** звена, а не убирать звено: `tail -6` → `tail -100` расширяет окно, `tail` читает до EOF, SIGPIPE не возникает, ось краснеет детерминированно. ⛤ Дискриминатор до разбора — **замерить rc самого предиката**, а не гадать: вставить в копию прода строку `<пайплайн>; echo "rc=$?" >> /tmp/probe` (rc=141 ⇒ мутант невалиден, а не ось слепа). ⛔ Не записывать «различимость непредъявима», не сняв этот замер: он стоит один прогон, а запись живёт годами и снимает работу с преемника |
| ⛔ Эталон и финал дали РАЗНЫЕ вердикты — а строка выше велит читать это как «регрессия твоя» | Ось **нестабильна**: у флаки-оси ОДИН прогон эталона отвечает случайно ⇒ бинарная сверка «эталон ↔ финал» даёт вердикт броском монеты. Строка выше молча предполагает детерминированность оси | ⛤ Считать по **N прогонам**, а не по паре — и выборка обычно УЖЕ есть бесплатно: revert-verify оставил логи (мутант A, мутант B, до/после). Доля зелёных на **неизменной ревизии** и есть ответ: 6/7 ⇒ флаки, 0/7 ⇒ регрессия. ⚠ Tell: соседние оси того же пути зелены во ВСЕХ прогонах ⇒ путь здоров, нестабильна одна ось. ⛤ Второй бесплатный дискриминатор — окно: покраснение пришлось на прогон под нагрузкой, тихий прогон зелен. ⛔ Найдя флаки — не чинить в своём scope: отдельный тикет |

<!-- Added by /session-retrospective 2026-08-26 (vteam-431820a5, StageSpecification) — root: спецификация фикса «возврат send_line отброшен» обязана была назвать, какие оси харнесса покраснеют. Замер пробой на живых фикстурах: статическая (оси F/M) оставляет field_busy = TRUE ⇒ после фикса тот же вход даёт SEND_FAILED, динамическая (ось N) — FALSE ⇒ остаётся зелёной. Класс не был задокументирован: строка «passes pre-fix, fails post-fix» объясняет красноту ассертом-кодифицирующим-баг, а здесь ассерт верен и покрасит его нереалистичная фикстура, которую прод маскировал. -->
⛤ **Предсказание стоит ОДНОГО прогона и снимает спор «это регрессия или часть работы».** Замер 2026-08-26 (`self-compact.sh`, фаза 1): прод звал `send_line "$CMD" "$i"` и печатал `SENT`, **не читая возврата** ⇒ ось F (`rc=0`) и ось M (`rc=5`) на статической фикстуре зелены, хотя `field_busy` после прогона = TRUE, то есть команда не сдана. Фикс (терминальный `exit 8` + эскалация) делает обе красными **по существу** — и это назвали ДО реализации, вместе с формой исправления фикстур.

<!-- Added by /session-retrospective 2026-08-11 — root: ось C9 («правило портировано во вторую петлю») краснела и на мутанте M6 («порядок правил»), потому что её фикстура не имела продуктивного исхода и потому удовлетворяла ещё и правилу B. Соблазн был дописать C9 в ожидания M6; правильным оказалось добавить в фикстуру продуктивный исход — после чего M6 краснит РОВНО свою ось. Замер: до изоляции M6 давал 2 красноты, после — 1. Класс не был задокументирован: grep «одна причина краснеть» по всему корпусу правил давал 0 вхождений. -->
⛔ **ЗЕЛЁНЫЙ НАБОР НЕ СУДИТ ЗАМЫСЕЛ: оси проверяют, верна ли РЕАЛИЗАЦИЯ задуманного, и бессильны, когда задумано неверно.** Все формы выше — про ось, которая не различает свой предмет (вакуумна, кодифицирует баг, мерит не то). Здесь оси **исправны**: мутанты краснят, revert-verify пройден, покрытие полное — а фича всё равно не делает того, ради чего заводилась.
- ⚠ **Tell ловится ДО прогона одним вопросом:** назови СЦЕНАРИЙ, ради которого фичу просили, и пройди его по своей реализации целиком. Не можешь пройти — набор тут ни при чём.
- ✅ **Floor: одна ЖИВАЯ проба на боевом пути** (не фикстура): единственный источник, видящий замысел. Оси мерят реализацию **по построению**.
- ⛤ Найденное ею — правка ДИЗАЙНА, значит отдельный коммит и **новая ось** (старые верны для своей половины).

**Empirical 2026-08-30** (кнопки напоминаний): 18 осей / 13 мутантов зелены, живой тап показал, что отсрочка гасит клавиатуру ЦЕЛИКОМ — то есть отложенное, ради которого «Готово» и заводилось, закрыть нечем.

⛤ **Симптом ловится ДРАЙВЕРОМ, а не глазами — если он печатает и ожидаемые, и ФАКТИЧЕСКИЕ красноты.** Драйвер, который проверяет только «покраснели ли объявленные», к смешению слеп по построению: лишняя краснота его не волнует. Печатать обе величины стоит одну строку и превращает смешение в наблюдаемое.

<!-- Added by /session-retrospective 2026-08-14 — root: ALP-задача 2c73c41f. Добавил в гвард ПЕЧАТЬ якорей, написал под неё две оси (AD/AD2) + два мутанта, получил 72/0 и 30/30 — и отгрузил поведенческую регрессию: печать звала float() на значении, до которого цикл не доходит. Нашёл code-reviewer сравнением с РОДИТЕЛЬСКИМ коммитом; мои оси зелены by construction. -->
## Anti-pattern

⛔ «Test passes → ship it» без revert-проверки. Особенно опасно для:
- Tests с synthetic vault fixtures (validator may silently skip files with missing required properties)
- Tests против graph databases (subject/predicate scope mismatch hides regression)
- Tests с complex JSON Schema validators (default to «no errors» if shape doesn't match scope)

<!-- Added by /session-retrospective 2026-08-14 — root: ТРИ вакуумные оси подряд за одну сессию, все на харнессах тиков автономных петель; каждая падала на СВОЁМ раннем выходе (узкое окно лога · нет снимка лимитов ⇒ burn-rate-unknown · нет живой tmux-сессии ⇒ worker-died), и каждая находилась только прогоном мутанта. -->
⛔ **Предмет — ДЛИННЫЙ СКРИПТ С МНОЖЕСТВОМ РАННИХ ВЫХОДОВ (тик петли, cron-джоб, диспетчер)? Перечисли ВСЕ выходы ВЫШЕ проверяемого блока и удовлетвори КАЖДЫЙ — иначе ось вакуумна по построению.**

Такой скрипт устроен как лестница гвардов: блокировка · темп расхода · память · занятый слот · cooldown. Твой предмет лежит **ниже** них, поэтому фикстура, не удовлетворившая любой один, останавливает исполнение **до** предмета — и ось зеленеет/краснеет по причине, не имеющей к нему отношения.

- ⚠ **Симптом-tell — ВЕРДИКТ НЕ ПО ТЕМЕ:** ось про закрытие слота печатает `burn-rate-unknown`, ось про уведомление — `worker-died`. Исход, называющий **чужой** гвард, = до предмета не дошли. ⛔ Не «ось краснеет, значит различает»: краснеть она может на гварде.
- ✅ **Floor: `grep -n 'finish\|exit\|return' <тик> | head -40` ДО написания фикстуры** — получаешь список выходов; для каждого спроси «моя фикстура его проходит?». Дёшево и однократно.
- ⛤ **Часть выходов удовлетворяется КОПИРОВАНИЕМ БОЕВОГО СОСТОЯНИЯ, а не синтетикой:** снимок лимитов (`cp rate-limits.json`), живая tmux-сессия-пустышка, непустой файл слота. Синтезировать их «правильно» дороже, чем скопировать то, что уже лежит рядом.
- ⛔ **Мутант ЭТО НЕ ЛОВИТ.** Мутант проверяет, различает ли ось предмет; вакуумная ось не доходит до предмета и краснеет на мутанте **тоже** — просто по другой причине. Единственный детектор — **прочитать исход**, а не факт красноты.

<!-- Added by /session-retrospective 2026-08-26 (vteam-32c29269, StageStandards) — root: релиз предъявил «61 из 61 в проде» как подтверждение фикса, а все 61 шли успешным путём, где обе ревизии печатают побайтно одну строку. Step 3a: 0 по «в агрегате»/«предвестник»/«взаимозаменяемы», канарейка 'выборк' = 17 файлов. -->
⛔ **ВЫБОРКА обязана совпадать с ОБЛАСТЬЮ ДЕЙСТВИЯ механизма — иначе журнал врёт В ОБЕ СТОРОНЫ**

- ⛔ **Вакуумное ЗЕЛЁНОЕ:** «N из N в проде после фикса» не доказывает ничего, если в окне не исполнялась **исправленная ветвь** — на успешном пути обе ревизии печатают побайтно одно и то же, и метрика не может покраснеть (`self-satisfying-metric-weak-verifier`).
- ✅ **Дискриминатор — счёт ПРЕДВЕСТНИКА ветви в том же окне** (строка, которую она логирует **до** отказа). Ноль ⇒ ветвь не исполнялась ⇒ прод о фиксе молчит. Прод доказывает, что ветвь **жива и в агрегате не нема**; **различимость** — только флип двух ревизий (§A51). ⛔ Не взаимозаменяемы.
- ⛔ **Ложное КРАСНОЕ:** отсекай строки **старше самого механизма** — до его появления они нарушают инвариант ПО ПОСТРОЕНИЮ. Возраст брать `git log -S '<маркер>' -- <файл>`, не по памяти.
- ⚠ **Tell:** «немых» больше, чем называл предыдущий этап ⇒ проверь ГРАНИЦЫ ОКНА, а не предмет.

**Empirical 2026-08-26** (`self-compact.sh`, `32c29269`): пост-фикс 64 `COMPACT_UNCONFIRMED` = 64 `WOKEN`, `WAKE-FAILED` 0, предвестник `RETRY[wake5]` **0** ⇒ фикс прод не проверял; «немых 19 против названных 2» оказались окном `2026-08-09T23`, а ветвь введена 2026-08-10 (`99cd711`).

<!-- Added by /session-retrospective 2026-08-27 (vteam-44bf27cc). Step 3a: 0 по «точк.*инъекц», «дедуплик». -->
⛔ **ДЕДУПЛИКАЦИЯ точки инъекции СХЛОПЫВАЕТ раздельность осей, которые её сторожат.** N осей на N копий механизма существуют **ровно потому**, что копии гниют независимо. Свёл копии в общую функцию — одна мутация краснит **все** оси, и «каждая от своей, по одной за прогон» недостижимо **по построению**, а не по качеству реализации.

- ⚠ **Tell ДО правки:** постановка требует и «убрать дубль», и «оси краснеют каждая от своей». Совместимо, только если различимость даёт **ФОРМА строки** (дискриминатор в ней самой), а не **число точек**.
- ✅ **Floor: строки развести, КЛАСС закрыть отдельным ратчетом.** Слияние чинит якорь ценой атрибутируемости; раздельность обычно **ОБЪЯВЛЕНА** в комментарии самих осей — прочитай прежде, чем сливать.

**Empirical 2026-08-27** (`vteam-supervise-verify.sh`): **9** классов побайтно одинаковых env-строк у **5** пар помощников (тикет называл 1).

<!-- Added by /session-retrospective 2026-08-27 (vteam-41543fbc, StageStandards) — root: floor выше (44bf27cc, тот же файл и день) области не объявляет и предписал бы НЕ сливать. -->
⛔ **Floor выше — про оси, сторожащие СОДЕРЖИМОЕ копий; у оси, сторожащей САМУ ИНЪЕКЦИЮ, он ИНВЕРТИРУЕТСЯ.** Дискриминатор — **сколько осей** сторожит точку: **N** (по одной на копию) ⇒ разводить + ратчет; **ОДНА** (инвариант «изоляция действует») ⇒ **сливать**, иначе ось дублирует env-префикс и проверяет СВОЮ КОПИЮ, оставаясь зелёной при любой мутации прода (§A14). ⛤ Сливать ДО уровня, где ось одна; дальше — нет.

- ⚠ **Tell:** чтобы прогнать call-site дважды, надо **скопировать** его env-префикс ⇒ ось вакуумна ещё до написания. Единая точка тут не «чище», а **предусловие выполнимости** критерия.

**Empirical 2026-08-27** (тот же файл, `41543fbc`): 3 префикса → `mrun()` ⇒ канарейка `_mcan` упражняет ТОТ ЖЕ код (I5: 0→2 ↔ мутант 2→2); `mrun`/`irun` **не** слиты — осей две, класс закрыт ратчетом.

<!-- Added by /session-retrospective 2026-08-28 (vteam-25f1de4f) — root: конвенция якоря жила комментарием ВНУТРИ харнесса; автор следующего мутанта его не откроет. -->
⛔ **Развёл строки — ЗАФИКСИРУЙ ЯКОРЬ: работает только ЦЕЛАЯ строка (с отступом); ФРАГМЕНТ неуникален ПО ПОСТРОЕНИЮ**, и счёт его вхождений растёт с каждым новым помощником (переименование ради уникальности запрещено).

- ⚠ **Tell:** взял фрагмент, получил fail-loud `n=7` — это НЕ возврат дефекта, а неверный якорь.
- ✅ **Отсюда предикат ГВАРДА — ПОБАЙТНЫЙ:** строки, различающиеся лишь отступом, как целые **различны** ⇒ якорь ложится ⇒ гвард краснеет РОВНО когда якорь ломается.
- ⛔ Конвенцию писать **у гварда И в корпусе** — комментарий в файле до следующего автора не доходит.

**Empirical 2026-08-28** (`25f1de4f`): фрагмент `VTEAM_SUP_TMUX="$TSTUB"` — 7 вхождений, `VTEAM_SUP_SILENCE_H=6` — 19; целостроковый якорь — `count == 1` у 13 из 13.

<!-- Added by /session-retrospective 2026-09-07 (vteam-33ae6e4e, MemberDeveloper) — root: floor «ЦЕЛАЯ строка» ИСПОЛНЕН, якорь всё равно дал n=2. -->
⛔ **ГРАНИЦА того же floor'а: ЦЕЛАЯ строка НЕуникальна, если СОСЕДНЯЯ НАЧИНАЕТСЯ С НЕЁ.**
`str.count()` матчит префикс внутри более длинной строки ⇒ `n=2` при формально целом якоре.
⛤ Провокатор — сам корпус: приняв **форму-прецедент** соседнего блока (то, что рекомендуется),
ты СБЛИЖАЕШЬ свою строку с его, и различает их только хвост.
- ⚠ **Tell:** `n=2` сразу после правки, принявшей форму соседнего блока ТОГО ЖЕ файла.
- ✅ **Floor: якорь ДВУСТРОЧНЫЙ** — прибавить предыдущую строку блока: у соседа она другая
  по построению (разные имена переменных разбора), тогда как хвост различает не всегда.

_(2026-09-07, `33ae6e4e`: строитель `OWNER_MAP` после фикса стал префиксом строителя
`TRANSITION_MAP` — обе строки `for st in sorted(m): print(… ",".join(sorted(m[st])) …)`,
у второй хвост `' 2>/dev/null)`. Поймано fail-loud `assert n == 1`, цена — один прогон.)_

<!-- Added by /session-retrospective 2026-08-30 (vteam-1f963144, StageRelease) — root: tell выше при n=2 гонит искать уникальную строку; у оси-канарейки она ЕСТЬ и не работает. -->
⛔ **ГРАНИЦА floor'а выше: у оси-КАНАРЕЙКИ предмет мутации — ПАРА ветвей `ok`/`bad`, а не строка.** Предикат аудита матчит маркер в ЛЮБОЙ ветви ⇒ мутация одной целой строки **не краснит**, даже когда она уникальна (`count == 1`).

- ⚠ **Tell выше здесь ВВОДИТ В ЗАБЛУЖДЕНИЕ:** `n=2` по фрагменту читается как «неверный якорь», ты ищешь уникальную строку — она **есть** и бесполезна.
- ✅ **Дискриминатор — ПРОБА, не рассуждение: мутировать по одной ветви и посмотреть.** Обе зелены ⇒ предмет = пара ⇒ якорь берётся фрагментом с **выведенной из предмета** арностью (`want = 2`), а не константой `1`.
- ⛤ Замер 2026-08-30 (`vteam-dispatch-verify.sh`, Т8): `ok`- и `bad`-строки `count == 1` каждая; мутация только `ok` → OK, только `bad` → OK, обеих → RED.

<!-- Added by /session-retrospective 2026-08-31 (vteam-1f963144, StageStandards) — root: floor выше заменил константу 1 на константу 2; замер 371 оси даёт арность до 5. -->
- ⛔ **Но `want = 2` — ТА ЖЕ КОНСТАНТА, сдвинутая на единицу: арность СЧИТАЕТСЯ, а не назначается.** Замер 2026-08-31 (14 харнессов, **371** ось): `2` у **89 %**, `1` — 5 %, `3` — 5 %, есть `4` и `5` (`vteam-burn-verify.sh:E1`) ⇒ при `want = 2` ложно-BROKEN у **11 %**, включая `A16`/`A17` того же `vteam-dispatch-verify.sh`.
  - ✅ **Floor — ОДИН вызов вместо N проб:** `want=$(grep -cE '(^|&&|\|\||;)[[:space:]]*(ok|bad) "<ось>\b' <харнесс>)`. Проба по ветви при арности 5 — 5 прогонов, и числа она не даёт.
  - ⛔ **Предикат счёта обязан видеть ветвь в ЛЮБОЙ позиции строки.** Якорь `^\s*(ok|bad)` пропускает инлайн-форму (`[ -x "$SUT" ] && ok "A0 …" || bad "A0 …"`) и даёт **137 вместо 371**, а на `A0` — **0**, то есть fail-loud «якорь протух» на живой оси. ⚠ Tell: счёт ветвей = 0 при заведомо существующей оси.

<!-- Added by /session-retrospective 2026-09-01 (vteam-45481887) — root: §5 тикета предписывал предъявить поведенческую ось ПОЛНЫМ прогоном на фикстурном корпусе; замер показал, что контроль краснеет на ЛЮБОЙ фикстуре и прогон возвращается ДО измеряемого блока у ОБЕИХ ревизий. Step 3a: «живой топологи|боевую топологию» → 0. -->
## ⛔ Ось НЕПРЕДЪЯВИМА НА ФИКСТУРЕ, когда ранний КОНТРОЛЬ ключуется на ЖИВОЙ ТОПОЛОГИИ — замена не «фикстура получше», а ФЛИП ДВУХ РЕВИЗИЙ на одном входе

§A56 — про мета-инструмент, чью правку не берёт его же таблица мутантов. Здесь мутанты **есть и различают**, а непредъявимой оказывается **поведенческая** ось (прогон целиком).

⛤ **Механизм:** ранний гвард («контроль красный ⇒ вердикты не читать») ключуется на **абсолютных путях живой среды** (`plist` → `ProgramArguments`, `settings.json`). Фикстурный корпус таких ключей не даёт **никогда** ⇒ контроль краснеет ⇒ возврат **до** измеряемого блока — у ОБЕИХ ревизий.

- ⚠ **Tell:** две фикстуры, различающиеся как угодно (схлопнутая и зеркалящая прод), дают **одинаковое** число краснот ⇒ вердикт даёт не вход.
- ✅ **Floor:** один вход подать ОБЕИМ ревизиям (`git show <base>:<путь>` + живой файл), вызывая измеряемые функции **напрямую**, + контроль невакуумности на входе, где ревизии обязаны совпасть.
- ⛔ Обход `main()` уносит его калибровку ⇒ самопроверки — отдельной пробой (`qa-engineer` §обходя main).

<!-- Added by /session-retrospective 2026-08-18 (child 1668d525) — root: контракт «stdout: ОДНА строка TSV» объявлен ДВУМЯ соседями движка; `admit` держал его на всех исходах, `record` молчал на ВСЕХ 21 ненулевом. Оба харнесса зелены, дефект прожил до e2e-прогона. Step 3a: класс в корпусе отсутствовал (0 совпадений по «на ПАРУ»/«шов»/«контракт СВОЕГО»). -->
## Разборы случаев — ВЫНЕСЕНЫ В ГРАФ (справочник on-demand)

⛤ Детальный разбор каждого случая (§A1-§A56: механизм, floor, эмпирика) живёт **ассетом графа**,
а не во флоре: его читают, когда ось **уже соврала**, а не при написании каждого теста.
Таблица ниже = **индекс по симптому**, разбор = справочник.

⛔ **Ключ ТРОЙНОЙ (§A + дата + тема), и это не украшение.** Замер 2026-08-11: **37 входящих
указателей** из правил, скиллов И харнесс-скриптов (`.claude/bin/*.sh`, `scripts/*.py`), из них
**11 — по ТЕМЕ** (`§ZERO-GIT-TOUCH`, `§дублирующие гварды`, `§ПЯТАЯ механика`). Чекер темы
**не судит** (они «проза») ⇒ сломались бы молча. Даты тоже неуникальны: `2026-08-05` — пять
разделов, `2026-08-07` и `2026-08-09` — по четыре, `2026-08-03` — три ⇒ **датой одной не
адресоваться**. В справочнике две переходные таблицы: тема → §A и дата → §A.

| # | дата | Симптом ⇒ действие |
|---|---|---|
| **§A1** | `2026-06-11` | `git stash push <file>` печатает «No local changes to save» (или ты заглушил `-q`) ⇒ фикс УЖЕ закоммичен, stash — no-op, обе половины флипа идут на одном предмете ⇒ базу брать `git show <пред-фиксный-sha>:<path> > /tmp/base`, после реверта `grep <маркер-фикса>` обязан быть ПУСТ. ⛔ `stash pop` тянет ЧУЖОЙ стэш (общий на репо) ⇒ разбор §A1, радикально — §A10 |
| **§A2** | `2026-06-12` | После `git checkout HEAD -- <file>` твоя НЕзакоммиченная правка исчезла молча, `git status` чист ⇒ checkout-рецепт применим ТОЛЬКО после коммита своих правок; до коммита — копия в `/tmp` либо отдельный worktree. Verify после restore — `grep <твой-новый-символ> <file>`, а НЕ «status чист» |
| **§A3** | `2026-06-21` | `Test suite failed to run` + `error TSxxxx`, `Tests: 0 total` ⇒ это НЕ красный, а сломанная компиляция твоего мутанта ⇒ мутант обязан быть type-preserving: заменить ФИНАЛЬНЫЙ `return <результат>` на `return <вход>`, mismatched lookup key, `void v;`. ⛔ Не `if (false && cond)`, не `if (cond \|\| true) return` (хвост недостижим ⇒ TS2345) ⇒ формы §A3 |
| **§A4** | `2026-06-22` | Фича УЖЕСТОЧАЕТ ветку, которая сейчас пропускает всё ⇒ красно→зелёного revert-verify нет **по построению** (старый код пропускает всё, что пропускает новый). ⇒ ось смещается на **hole-case** — вход, который старый код ложно ПРОПУСКАЛ, а новый корректно заваливает; positive-demo осью не является (проходит обоими способами) |
| **§A5** | `2026-07-04` | Наблюдаемость фичи гейтится числовой КОНСТАНТОЙ (тик каждые N, sampling каждые K, batch при size≥M) ⇒ revert = вытолкнуть ПОРОГ за диапазон (`STEP = 100_000_000`), а не комментировать emit и не `if(false)`: type-safe, faithful, работает ДО коммита. Несколько порогов ⇒ выталкивать все нужные и убедиться, что независимые фичи остались GREEN |
| **§A6** | `2026-07-19` | Общий хелпер (rollback / cleanup / delete) зовётся из ≥2 веток, а ось написана на одну ⇒ вторая НЕ locked, дроп её аргумента оставит набор зелёным. ⇒ перечислить ВСЕ call-site грепом имени и revert-locked'ить КАЖДЫЙ отдельно; труднодостижимую `catch`-ветку форсировать реальным throw через instance-override step-runner'а и ассертить error-shape |
| **§A7** | `2026-08-02` | Добавил ранний `return`/гвард — свои оси зелёные и CI зелёный, а нижний гвард перестал исполняться (его покрытие обнулилось МОЛЧА) ⇒ перечислить гварды НИЖЕ своего и нейтрализовать каждый: ни один не покраснел = покрытие потеряно. Чинится тестом на конфигурацию, где ТВОЙ гвард ПРОПУСКАЕТ. ⛤ Зеркало в том же разделе: правка МАСКИРУЕТ пред-существующий дефект (меняет семантику агрегации ⇒ он перестаёт проявляться), и оси его не видят, потому что написаны под НОВУЮ семантику |
| **§A8** | `2026-08-03` ⛔ | Набор зелёный, покрытие 100 %, а гвард всё равно снимается незамеченным ⇒ единый зонд: нейтрализуй КАЖДЫЙ гвард по одному и посчитай красные; ноль = не под тестом (чужие call-site · затенён · дублирован · тело покрыто, а ВЫЗОВ нет). ⚠ Мутантов считать по ДИФФУ, не по плану: `git diff -U0 \| grep -c 'if (this.<flag>)'` ⇒ ловушки зонда §A8 |
| **§A9** | `2026-07-30` ⛔ | Чинил несколько вещей разом, симптом исчез — но неизвестно, от какой ⇒ отреверть КАЖДУЮ правку по отдельности: симптом не вернулся = правка НЕ требуется, откатить байт-в-байт и вынести отдельным наблюдением. Особенно в shared infra: «не нужен для симптома» ≠ «неверен», но это ОТДЕЛЬНОЕ решение, а не бесплатный довесок |
| **§A10** | `2026-07-30` ⛔ | Revert-verify грозит чужим стэшем либо потерей правки ⇒ **ломай КОПИЮ** (`ZERO-GIT-TOUCH`): `copytree`/`cp -al` → literal-splice с abort-if-not-found → каждое состояние ОТДЕЛЬНЫМ процессом (в одном кэш модулей склеит варианты); рабочий файл не трогается, коммитить фикс заранее не нужно. ⚠ После `cp -al` замер по `ctime` ложен ⇒ §A10 |
⛤ **Изоляция — не единственный законный способ; второй — МАРКИРОВКА, и у неё своё условие.**
Харнесс, чей предмет пишет в ОБЩИЙ журнал, вправе не подменять путь, а метить свои записи
уникальным префиксом прогона и считать **дельту** своих строк (`grep -c` до/после). Условие
ровно одно: фикстурная запись обязана быть **отличима человеком**, который придёт разбирать
инцидент. ⛔ Дискриминатор — прочитать свою же фикстурную строку глазами и спросить «отличу ли
я её от боевой через месяц»: `[sc-verify-12345] COMPACTING` отличима, `прогнано=3 красных=1`
— нет. Замер 2026-08-21 (свип 15 харнессов): у одного была неотличимая запись (56 строк
ложных инцидентов), у остальных — либо изоляция, либо маркировка; то есть оба способа живут
в корпусе, и выбор между ними надо делать осознанно, а не по тому, какой вспомнил.

| **§A11** | `2026-08-03` ⛔ | Харнесс поднял ЖИВУЮ сессию / отправил реальное сообщение; либо проверка краснеет при исправном коде сразу после смены дефолта ⇒ путь эффекта сделать ПОДМЕНЯЕМЫМ (`SPAWN="${SPAWN:-<боевой>}"`) и подставлять заглушку в ОБЩЕМ хелпере запуска — тогда «харнесс не спавнит» свойство КОНСТРУКЦИИ, а не дисциплины; режим/флаг, от которого зависит исход, передавать ЯВНО в каждой проверке и покрывать ОБА режима |
| **§A12** | `2026-08-03` ⛔ | Нейтрализовал — набор зелёный, и рука пишет «ось вакуумна» ⇒ СПЕРВА проверь, что МУТАЦИЯ легла и меняет ПОВЕДЕНИЕ (`grep` маркера + `assert src.count(old) == 1`): no-op формы — операнд дизъюнкции (`false \|\| X`), мёртвая ветка перед живой, мутация в КОММЕНТАРИЙ ⇒ надёжно `if (false)` / вырезать блок ⇒ §A12 |
| **§A13** | `2026-08-05` ⛔ | Ось проверяет режим, до опасного кода не доходящий («сюда он всё равно не дойдёт») ⇒ фикстуры туда не передал ⇒ **revert-verify гоняет ту же ось против СЛОМАННОЙ копии, у которой раннего выхода НЕТ** — и она уходит в боевой источник (живой `ps`, сеть, продовое состояние). Передавать фикстуры в КАЖДУЮ ось и закрывать КАЖДЫЙ внешний канал, а не только источник данных |
| **§A14** | `2026-08-05` ⛔ | В одном харнессе часть осей извлекает предикат из прода (`sed`), часть держит РУЧНУЮ КОПИЮ ⇒ копия проверяет сама себя и зелена при любой мутации прода ⇒ извлекать блок целиком и `eval` с bail-out при неудаче извлечения, мутировать СМЫСЛ (инверсия условия), а не наличие блока ⇒ грабли пере-извлечения §A14 |
| **§A15** | `2026-08-05` ⛔ | Соседний харнесс покраснел на оси, никак не связанной с твоей правкой ⇒ первая гипотеза «я сломал логику» неверна: прочитай, ЧТО вырезает ЕГО мутант — строки в проде нет ⇒ сломан его ЛИТЕРАЛЬНЫЙ анкер, а не прод. Чинить анкер переводом с ДОСЛОВНОГО состава на ФОРМУ строки + `assert` на число замен. ⇒ правя прод, прогоняй ВСЕ харнессы над ним (`ls ~/.claude/bin/*verify*`), а не только свой |
| **§A16** | `2026-08-05` ⛔ | Пишешь тест на ИНСТРУМЕНТ, который запускает другой код (харнесс, раннер, диспетчер) ⇒ рефлекс мутирует ДРАЙВИМОЕ, а гарантия — про инструмент («харнесс передаёт копию»), и ось её не проверяет ⇒ мутант применять к ФАЙЛУ инструмента (копии) + `assert` на число замен; ось на драйвимое — ВТОРАЯ, а не единственная. Tell: формулировка оси называет не тот субъект, что формулировка гарантии |
| **§A17** | `2026-08-06` ⛔ | Ассерт вида `toEqual([])` / `toHaveLength(0)` над величиной, которую ПРОИЗВОДИТ обход ⇒ зелен и при полностью мёртвом производителе ⇒ **засеять реального члена**, чтобы ожидание стало положительным (`toEqual(["<заведомый элемент>"])`); мутант производителя (ранний `return`) обязан краснить — до засева не краснит. ⛤ Гипотезу ревьюера («а если бы X был застаблен?») закрывать НОВОЙ ОСЬЮ, а не прозаическим опровержением |
| **§A18** | `2026-08-07` ⛔ | Ось исполняет извлечённую из прода строку, но берёт ПЕРВОЕ совпадение якоря ⇒ покрыт первый из N однотипных путей, остальные молча нет ⇒ дискриминатор арифметический: число путей в проде (`grep -Fn '<якорь>'`) против числа ИНДИВИДУАЛЬНО проверенных; якорить номером строки либо своим комментарием ⇒ §A18 |
| **§A19** | `2026-08-08` ⛔ | В прогоне есть `⚠ мутация не применилась` при `FAIL=0` ⇒ rc остаётся 0, набор читается «зелёным», а ось НЕ проверена: якорь мутанта втянул ПРОЗУ и протух вместе с ней (перестановка блоков вынуждает захватить пояснение между ними) ⇒ мутация ВСТАВЛЯЕТ копию, а не переставляет — тогда якорь = маркер шага + команда, ноль прозы. И `mutate()` возвращает ПУСТО при непринятом якоре, ось потребляет через `\|\| bad` ⇒ протухший мутант роняет rc **по построению**, а не по дисциплине чтения вывода |
| **§A20** | `2026-08-09` ⛔ | Перенёс харнесс к соседу и «аккуратно» СУЗИЛ guard образца — теперь он обрывается `exit 2` на легитимном мутанте ⇒ ось мертва, а мутанта на неё нет, поэтому смерть невидима (набор зелёный) ⇒ сузив guard при порте, прогони мутанта ЭТОЙ оси немедленно; якорить guard на СОСЕДНИЙ инвариант, которого мутант не касается, а не на предмет оси. ⛔ Не откатывать ужесточение целиком — узкий якорь на инвариант строго лучше широкого |
| **§A21** | `2026-08-09` ⛔ | Мутант краснит ось ЧЕРЕЗ РАЗ при неизменном коде (порядок ФС, `PYTHONHASHSEED`, время, сеть) ⇒ брать ДЕТЕРМИНИРОВАННЫЙ: флаки-ось за 2-3 прогона получает ярлык «шум» и обесценивает ВЕСЬ набор; детерминированного нет ⇒ дать гварду ПРЕДМЕТ (ничья для сортировки, дубль для дедупа) ⇒ §A21 |
| **§A22** | `2026-08-09` ⛔ | Прода ЕЩЁ НЕТ (новый precondition / конфиг / впервые отгружаемая форма) ⇒ floor «извлекай из прода» неприменим, и копию держит **ОТГРУЗКА**: харнесс собирает проверяемую форму своим кодом, апплай — своим, расходятся молча ⇒ отгрузка ЧИТАЕТ форму из АРТЕФАКТА харнесса (`matrix-full.json` → `variants.BASE`), а не собирает заново. Остался второй источник ⇒ байт-в-байт гвард с ТЕРМИНАЛЬНЫМ выходом. Tell: проверяемая форма встречается в репо ДВАЖДЫ |
| **§A23** | `2026-08-09` ⛔ | Добавляешь ось, которой у ОБРАЗЦА (прецедента, откуда копировал) НЕТ ⇒ прогони её ПРОТИВ образца: краснеет = дефект унаследован из источника и жив там, потому что покраснеть было НЕЧЕМУ. Это третий и самый дешёвый детектор «прецедент несёт латентный дефект» — исполнением, а не чтением. ⛔ Исход не обязан быть правкой образца (он может исполнять тебя же), но follow-up обязан нести порт ОСИ и МУТАНТА, а не только строчку фикса |
| **§A24** | `2026-08-11` ⛔ | Гвард сам решает, работать ли (сверяет путь корпуса с эталоном — храповик, базовая линия, allow-list) ⇒ на КОПИИ дерева он выключает себя и рапортует «чисто» ⇒ инъекция идёт в ДАННЫЕ гварда (убрать имя из линии), гасящий мутант обязан КОМПОНОВАТЬСЯ поверх неё (fail-loud при якоре ≠ 1) ⇒ §A24 |
| **§A25** | `—` | Дописал строку НИЖЕ цикла с `continue` (печать, счётчик, метка «чтобы всегда»), оси зелены и мутанты различают ⇒ они слепы ПО ПОСТРОЕНИЮ: фикстуры дают вход, где до `continue` не доходит ⇒ перечислить РАННИЕ ВЫХОДЫ выше вставки и завести ось на КАЖДЫЙ, фикстура — ИНВЕРСИЕЙ условия ⇒ §A25 |
| **§A26** | `2026-08-26` ⛔ | Ось на ГОНКУ (подвесить → изменить мир → снять гвард) зелена и с мутантом, и без ⇒ «дать дойти до подвеса» смоделировано ФИКСИРОВАННЫМ числом тиков, а предмет уходит в ожидание глубже ⇒ ждать ФАКТА: предмет выставляет флаг в точке подвеса, тест крутит `while (!suspended) await …` ⇒ §A26 |
| **§A27** | `2026-08-23` ⛔ | Фикс обоснован СИММЕТРИЕЙ («у имени валидация есть, у значения нет») ⇒ пара осей, объявленная самим req, регрессию НЕ ловит: слишком широкий гвард удовлетворяет обе ⇒ ТРЕТЬЯ ось на форму, проходящую по строгому предикату и отсекаемую скопированным; ширину выводить ЗАМЕРОМ живых носителей ⇒ §A27 |
| **§A28** | `2026-08-16` ⛔ | Мутант краснит МЕНЬШЕ предсказанного (вплоть до нуля) ⇒ сперва посмотри, сколько СЬЮТОВ вообще запустилось: у копии вне репозитория ломается резолюция модулей и `Tests: 0` выглядит успехом ⇒ прогнать НЕМУТИРОВАННУЮ копию ПЕРВОЙ и требовать точный baseline ⇒ §A28 |
| **§A29** | `—` | Набор массово красный, мутанты пересвечивают, а контроль ЗЕЛЁН ⇒ смотри не на предмет, а на НАГРУЗКУ: под параллельной работой прогон не успевает произвести разбираемый вывод, оси читают `None` ⇒ tell по ФОРМЕ — пересветившие наборы КОМПЛЕМЕНТАРНЫ, поля разбора ПУСТЫ; гнать в тишине ⇒ §A29 |
| **§A30** | `2026-08-16` ⛔ | Мутация применилась, окно верное — а предмет ВЕРНУЛ состояние сам: мутируемый артефакт для него не источник истины, а ПРОИЗВОДНЫЙ КЭШ ⇒ прогнать ОБА состояния (с самовосстановлением и без), дискриминатор искать в ИСТОЧНИКЕ. ⚠ Tell: рядом `refresh-*`/`*-cache.*` либо шапка «Cache: …» ⇒ §A30 |
| **§A31** | `2026-08-17` ⛔ | Набор зелёный, а на живом прогоне сообщение гварда битое ⇒ оси написаны на КОД ВОЗВРАТА, вторая поставка гварда — ДИАГНОСТИКА ⇒ ≥1 ось ассертит ПОДСТРОКУ сообщения. ⛔ ДВА зеркала: составной ассерт краснеет по **B**, а обвиняет **A** (читай КОНЪЮНКТЫ), и в `ok "…"` нельзя класть то, чего нет в `if` ⇒ §A31 |
| **§A32** | `2026-08-17` ⛔ | Контроль КРАСНЕЕТ группой осей одной темы, rc «данных не найдено», а `git log` по предмету ПУСТ ⇒ фикстура наследовала аномалию ЖИВЫХ данных, а её ПОЧИНИЛИ ⇒ ⛔ не откатывать починку и не ослаблять оси: фикстура обязана СЕЯТЬ аномалию сама (`build_vault(ambiguous=True)`, мутация ровно один раз, fail-loud при `n != 1`) ⇒ §A32 |
| **§A33** | `2026-08-18` ⛔ | Мутант применился (`n == 1`), а проверка зелена ⇒ «ось не различает» ЛОЖНО: проверка — ДИЗЪЮНКЦИЯ, свидетельство осталось в других дизъюнктах ⇒ посчитать его ДО мутации (`grep -c`): больше одного ⇒ одноместная мутация бессильна ПО ПОСТРОЕНИЮ ⇒ усечь по маркеру блока либо сменить жертву ⇒ §A33 |
| **§A34** | `2026-08-20` ⛔ | Восстановил после флипа, `shasum` сошёлся — а CI краснеет ТВОИМИ осями с ДО-фиксным поведением ⇒ `git checkout <ref> -- <file>` пишет И рабочую копию, И ИНДЕКС; `cp` вернул первую, откат остался во второй, и bare `git commit` его унёс ⇒ сверять `git status --porcelain`, коммитить PATHSPEC'ом, восстанавливать `git restore --staged --worktree` ⇒ §A34 |
| **§A35** | `2026-08-21` ⛔ | Мутант снял гвард — НИ ОДНА ось не покраснела ⇒ гвард DEFENSIVE: состояние, при котором он срабатывает, даёт тот же результат и без него (`if (n < 2) return NaN` при `0/0`) ⇒ дискриминатор АРИФМЕТИЧЕСКИЙ: посчитать выражение при условии гварда; совпало ⇒ мутировать наблюдаемое РЕШЕНИЕ, гвард пометить defensive В КОДЕ ⇒ §A35 |
| **§A36** | `2026-08-21` ⛔ | Мутант применился, а драйвер печатает `BROKEN` / `Tests: 0` ⇒ `if (false)` снял не только истинность, но и TYPE-NARROWING, и блок перестал компилироваться (`TS18047`) ⇒ мутант сохраняет ФОРМУ выражения и меняет только истинность (тот же вызов с аргументом, который не совпадёт никогда); драйвер обязан печатать первую строку `error TS` ⇒ §A36 |
| **§A37** | `2026-08-21` ⛔ | Мутант покраснил все оси, КРОМЕ осей на выходы/исключения (escape-маркер, разрешённый инструмент, путь вне зоны) ⇒ фикстура такой оси не несёт ЗАПРЕЩЁННОЙ формы, и вход прошёл бы и без выхода ⇒ мысленно снять выход: вход обязан блокироваться, не блокируется ⇒ ось меряет не то ⇒ парная отрицательная ось §A37 |
| **§A38** | `2026-08-21` ⛔ | Ось на отказ в форме `[ "$rc" != 0 ]` зелена и под мутантом, снимающим её же guard ⇒ снятый guard даёт не «принял», а `124` от `timeout` — тоже ненулевой ⇒ исключать И `0`, И `124`; `timeout` в харнессе обязателен. ⛤ Класс шире rc: всякий предикат-ОТРИЦАНИЕ удовлетворяется МНОЖЕСТВОМ исходов ⇒ §A38 |
| **§A39** | `2026-08-21` ⛔ | Мутант краснит НАБОР осей без общей темы ⇒ это не «сильный мутант», а УТЕЧКА СОСТОЯНИЯ: `beforeEach` сбрасывает модульное состояние ТЕМ ЖЕ публичным средством, которое само есть предмет одной из осей ⇒ изоляция из СРЕДЫ (`vi.resetModules()` + свежий динамический импорт), не из предмета ⇒ когда множественность законна §A39 |
| **§A40** | `2026-08-18` ⛔ | Инвариант ОБЩИЙ для двух соседей (формат вывода, словарь кодов) — и оба их харнесса зелены ПО ПОСТРОЕНИЮ: у шва нет владельца ⇒ завести ось, чей ПРЕДМЕТ — инвариант, и гонять её на ОБОИХ соседях одним кодом, нарушителей перечислять ПОИМЁННО (`файл:строка`) ⇒ сперва чинить ДЕТЕКТОР (разбор по структуре) §A40 |
| **§A41** | `2026-08-19` ⛔ | `assert s.count(old) == 1` падает `x0` на строке, которую ты ВИДИШЬ на экране, а файл между чтением и правкой не менялся ⇒ анкер скопирован из ТВОЕГО ЖЕ вывода, напечатанного с префиксом (`sed 's/^/  /'` ради читаемости) ⇒ анкер СТРОИТЬ ИЗ ФАЙЛА: найти строку по подстроке, отступ брать у файла (`ind = len(l) - len(l.lstrip())`), многострочный блок править ПО ИНДЕКСАМ. ⚠ Второй подвид — отступ, выведенный ПО ПАМЯТИ о структуре. ⛤ Печатать с делимитерами (`print("%4d \|%s\|" % …)`), ⛔ не `cat -A` (BSD его не знает) |
| **§A42** | `2026-08-21` ⛔ | Обе половины флипа дали ПОБАЙТНО одинаковый результат ⇒ ⛔ не «фикс ничего не меняет»: ОТКАТ вакуумен (`git stash` при чистом дереве — no-op, обе половины на одном предмете) ⇒ откат ЯВНЫМ извлечением `git show <fix-sha>^:<путь>` + ПРЕД-прогонная канарейка по МАРКЕРУ предмета (⛔ не по `sha`) ⇒ §A42 |
| **§A43** | `2026-08-21` ⛤ | Мутант снял ровно тот кусок, который ось называет ПО ИМЕНИ, а ось зелена ⇒ ⛔ не усиливать: у гварда форма «ЯВНОЕ ИСКЛЮЧЕНИЕ + УЗКИЙ ДЕТЕКТ», исключения покрыты ДВАЖДЫ, держит второй заслон ⇒ причину фиксировать В ДВУХ носителях (spec `_note_<ось>` + комментарий у оси); дискриминатор вакуума — сломать ОБА ⇒ §A43 |
| **§A44** | `2026-08-21` ⛔ | Прогон печатает «ожидалось [A1], покраснели [A1 A2]» ⇒ ⛔ это НЕ смешение осей: ожидание выведено из ТЕМЫ мутанта, а оси не независимы — предикат одной включает условие другой ⇒ по КАЖДОЙ оси спросить «исполнится ли она, если мутация применена»; расхождение НАЗЫВАТЬ в харнессе ⇒ зеркало §A44 |
| **§A45** | `2026-08-21` ⛔ | Создал условие срабатывания (сироту, ошибку, фикстуру), а вывод ПУСТ — читается как «детектор не видит» ⇒ твой блок стоит НИЖЕ раннего выхода по УСПЕХУ (`[ … -eq 0 ] && exit 0`) ⇒ назови ПУТЬ типичного прогона и проверь, лежит ли блок на нём; пробу гнать в ОБОИХ состояниях предмета ⇒ §A45 |
| **§A46** | `2026-08-21` ⛔ | Мутант применился (`n == 1`), поведение идентично, красных нет — «ось не различает» ⇒ якорь взят по СТРОКЕ СООБЩЕНИЯ, а у гварда две части: сообщение и РЕШЕНИЕ (`return 1` / `bad += 1` / `sys.exit`) ⇒ якорь — ПАРА строк: сообщение оставить, решение обнулить. ⛤ Пара-якорь чинит и уникальность: голое `bad += 1` встречается многократно, в паре с конкретным сообщением — ровно раз. ⚠ Tell: не можешь сформулировать эффект мутации иначе как «сообщение станет другим» ⇒ поведение не тронуто |
| **§A47** | `2026-08-22` ⛔ | Мутант краснеет РУКАМИ, а драйвер говорит «не различает» ⇒ имя оси — МАШИННЫЙ КЛЮЧ: драйвер извлекает красноту регэкспом (`⛔ (A\d+\w?)`), осмысленное имя в него не попадает ⇒ имя оси = `A<цифра>[буква]` всегда, смысл — в ТЕКСТЕ оси; у jest-драйвера имя = ПОСЛЕДНИЙ сегмент `●`-строки ⇒ §A47 |
| **§A48** | `—` | Драйвер печатает «красные = НИ ОДНОЙ — ось не различает», а вручную мутант краснеет ⇒ у spec СКРЫТОЕ ПЕРЕЧИСЛЕНИЕ: регэксп сбора перечисляет ПРЕФИКС имени (`A\d+`), и ось с другим префиксом для него не существует ⇒ инвертировать полярность (`[A-Z]\d+`) — тогда следующая ось попадает в сбор ПО ПОСТРОЕНИЮ. ⛔ Не переименовывать ось под регэксп: это чинит экземпляр и оставляет класс. ⚠ Tell: «не различает» получила НОВАЯ ось, а старые краснеют штатно ⇒ различие между ними не логика, а ИМЯ |
| **§A49** | `2026-08-22` ⛔ | В боевом канале нашлись уведомления про несуществующие предметы (твои фикстуры) ⇒ инъекция канала ЕСТЬ, ею забыли воспользоваться, дисциплина тут бессильна ⇒ гвард: признак пробы (`<X>_DIR`) задан И изоляция канала не задана ⇒ **fail-loud**, ⛔ не тихий стаб; ось ПАРНАЯ ⇒ §A49 |
| **§A50** | `2026-08-22` ⛔ | Канарейка доставки говорит «подмена не доезжает», сменил способ — вердикт тот же ⇒ дело не в способе: ось судит по КОДУ ВОЗВРАТА, а пустой/подменённый предмет выходит нулём ⇒ требовать СЛЕД РАБОТЫ (маркер, который печатает только настоящий предмет) ⇒ сперва «что харнесс сделает с ПУСТЫМ предметом» §A50 |
| **§A51** | `2026-08-24` ⛤ | Этап релиза: предмет — ОПУБЛИКОВАННЫЙ артефакт, мутировать нельзя, «фикс доехал» зелено при любом состоянии продукта ⇒ флип берётся ПРЕДЫДУЩЕЙ ОПУБЛИКОВАННОЙ версией (`npx <pkg>@<prev>`, `gh release download <prev-tag>`): дефект ЖИВ там и МЁРТВ здесь. ⛔ Версию-предшественника брать ЗАМЕРОМ (`npm view <pkg> time`) ⇒ §A51 |
| **§A52** | `2026-08-24` ⛔ | Оси зелены, мутанты различают — а набор проверяет арифметику ПРИ величине, не саму величину ⇒ фикстура подаёт КОНСТАНТОЙ то, что прод обязан ВЫЧИСЛЯТЬ (потолок, супремум, нормировку) ⇒ разделить ВХОД (литерал законен) и ВЫХОД (не подавать вовсе) либо ось на КРАЙ области. ⚠ Tell: одно число во ВСЕХ записях ⇒ §A52 |
| **§A53** | `2026-08-24` ⛔ | Набор зелен сегодня и покраснеет на ИСПРАВНОМ коде без единой правки ⇒ ожидание прибито к АБСОЛЮТУ, а величина осмысленна только относительно `now` ⇒ фикстура принимает СДВИГ (`time.time() + 3*3600`), и мутант тоже; ожидание выводить из ИСТОЧНИКА предмета, не из наблюдения момента ⇒ ПЯТЬ носителей дрейфа §A53 |
| **§A54** | `2026-08-27` ⛔ | Мутантная матрица СПЕЦИФИКАЦИИ предсказала «краснеют РОВНО A+B», прогон даёт A+B+C ⇒ ⛔ не «оси смешаны»: спека гоняла флип на НИЖНЕМ слое (ляжет ли мутация), а утверждала про вердикт ОСЕЙ ⇒ утверждение об осях доказывает прогон ОСЕЙ. ⚠ Tell: флип трогает функцию, предсказание — про имена осей ⇒ §A54 |

| **§A55** | `2026-08-27` | Мутант покраснил ГОРАЗДО больше предсказанного (снял бит исполнения, опустошил файл) ⇒ предмет НЕИСПОЛНИМ, краснеть обязана КАЖДАЯ ось, что его запускает, а оставшаяся ЗЕЛЁНОЙ вакуумна ⇒ аудит: выписать зелёные и спросить, чем удовлетворились. ⛔ ВТОРОЙ исход — харнесс УПАЛ, строки `PASS=` нет вовсе ⇒ §A55 |

| **§A56** | `2026-08-28` | Правишь МЕТА-ИНСТРУМЕНТ (аудит, линтер, детектор) — его собственная `MUTANTS`-таблица твою правку тронуть НЕ МОЖЕТ ⇒ флип ДВУМЯ РЕВИЗИЯМИ инструмента на копиях (`git show HEAD:<путь>` против живого), судить по СОСТАВУ вердиктов, ⛔ не по `rc` (он =1 в обеих при вне-scope красноте); состав резать по МАРКЕРУ БЛОКА ⇒ §A56 |
| **§A57** | `2026-09-06` | Изолируешь харнесс от боевых каналов, перечислив env-инъекции ⇒ ⛔ часть каналов НЕинъектируема ПО ПОСТРОЕНИЮ (путь лога захардкожен `2>>"$HOME/…"`), и перечисление закрывает N−1 из N ⇒ подменять ВСЮ КОМАНДУ (`<TOOL>_PING_CMD=<стаб>`), а не окружение. ⚠ Tell ДО прогона: грепни эмиттер на `>>"$HOME` ⇒ §A57 |
| **§A58** | `2026-09-07` | Ось-REVERT, доказывающая различимость, строит свой мутант ИЗ ТОГО ЖЕ инъектируемого источника, который подменяет ВНЕШНИЙ мутант приёмки ⇒ при его глобальной подмене её якорь даёт 0 совпадений и роняет прогон `BROKEN` ⇒ «краснеет ровно одна ось» недостижимо **по построению**, и это свойство ПАРЫ, а не дефект оси. ⚠ Tell до прогона: приёмка требует иммунитета оси к мутанту, который подаётся ТЕМ ЖЕ каналом. ✅ Floor: у оси-revert СВОЙ, НЕподменяемый источник (константа боевого пути + параметр `src_path`), у прогонов — инъектируемый; иммунитет тогда *by construction*. ⛔ Не «компоновать поверх инъекции» (§A24): компоновка решает ВНУТРЕННИЙ мутант, а здесь внешний. ⛤ Разбора в справочнике НЕТ — floor целиком в этой строке |

⛤ **Справочник:** `1d04c4d9-3c93-43b6-8641-f0eed781ff10`, якоря **§A1-§A57, §A64, §A68-§A74, §A76, §A79-§A83, §A86-§A103** (§A58-§A63, §A65-§A67, §A75, §A77-§A78, §A84-§A85 — floor целиком в строке индекса, см. ⛔ под таблицей).
| **§A59** | `2026-09-07` | Подменил ВСЮ команду стабом (§A57) и считаешь канал закрытым ⇒ ⛔ той же подменой ты вывел **боевую ветку резолва** (`os.environ.get("X") or <дефолт>`) из-под ВСЕХ осей: инъекция стоит в каждой, правая половина не исполняется НИ РАЗУ. А вызов обязан быть fail-open (`try/except` + `check=False` + `DEVNULL`) ⇒ сломанный дефолт молчит и в ПРОДЕ. ⚠ Tell, ловится ЧТЕНИЕМ набора: у оси есть инъекция пути/команды, и НИ ОДНА ось не гоняет предмет БЕЗ неё. ✅ Floor: один прогон БЕЗ подмены команды, но с уводом побочных каналов в НАБЛЮДАЕМЫЙ файл — он доказывает, что дефолт резолвится (в файле формат РЕАЛЬНОГО эмиттера, а не стаба), и тем же прогоном служит флипом оси изоляции, которая иначе остаётся без предъявленного флипа. ⛤ Разбора в справочнике НЕТ — floor целиком в этой строке _(2026-09-07, приёмка `1bae87a4`: снял `VTEAM_PING_CMD` на копии ⇒ красноты ровно `[A12, A14]`, боевой журнал 1→1)_ |
| **§A60** | `2026-09-07` | Заслон №2 объявлен ЭКВИВАЛЕНТНЫМ заслону №1 по замеру («множества совпали побайтно»), и ты пишешь терминальный отказ на ЛЮБОЕ расхождение ⇒ равенство держится на СВОЙСТВЕ ДАННЫХ, а не на механизме, и стороны расхождения не равны: `окно \\ плоскость` ОПАСНО (отобрана запись, писавшаяся верным путём), `плоскость \\ окно` БЕЗОБИДНО (запись отсечена вторым критерием намеренно) ⇒ разводить НАПРАВЛЕНИЯ (первое терминально, второе печатается поимённо), а не сравнивать множества. ⚠ Tell: фикстура, зеркалящая прод «чуть строже», роняет прогон при БЕЗУПРЕЧНОМ отборе. ⛤ Разбора в справочнике НЕТ — floor целиком в этой строке |
| **§A61** | `2026-09-07` | Ось изоляции обвиняет тебя в порче БОЕВЫХ данных на прогоне, который боевой предмет не открывал ⇒ префикс фикстуры (`fix-`, `test-`, `tmp-`) КОЛЛИДИРУЕТ с боевыми идентификаторами, и предикат `LIKE '<префикс>%'` мерит не своё множество ⇒ префикс делать заведомо уникальным (случайный токен внутри) — ⛤ то же для ИМЁН ПРОЦЕССОВ-фикстур (`pkill/pgrep -f 'sleep 3031'`): ПАРАЛЛЕЛЬНЫЙ прогон того же харнесса убивает/считает чужие стабы (2026-09-15 ф3: A141 `rc` вместо `network`, 3 мутанта «шире») ⇒ тег на прогон `$((3100+RANDOM%800))` в env. ⚠ Дискриминатор арифметический, а не чтение: прогон не создал у боевого предмета ни одного побочного артефакта (штампа, backup, записи), а все пути были явными ⇒ обвинение ложно. ⛔ Не «чинить» боевые данные по такой находке. ⛤ Разбора в справочнике НЕТ — floor целиком в этой строке _(2026-09-07: `fix-` совпал с 4 боевыми `session_id` апреля-июля)_ |
| **§A62** | `2026-09-07` | Инъекция источника введена РАДИ СТОИМОСТИ И ДЕТЕРМИНИЗМА (боевой запрос ~15 с x десятки прогонов; без неё вердикт = функция живого vault) ⇒ floor §A59 «один прогон БЕЗ подмены» здесь НЕИСПОЛНИМ: он возвращает обе беды, ради которых подмену и ввели. ⛔ Значит точка, которую инъекция обходит, не покрыта НИЧЕМ, и это невидимо — набор зелёный, инъекция «правильная». ⚠ Tell, ловится ЧТЕНИЕМ фикстуры: подменяется уже РАЗОБРАННАЯ структура (карта, список, словарь), а не сырой ответ источника. ✅ Floor: стабить на УРОВЕНЬ НИЖЕ — подменять ОТВЕТ источника (фикстурный JSON), тогда разбор исполняется на фиксированном входе, а цена и детерминизм сохранены. ⛤ Разбора в справочнике НЕТ — floor целиком в этой строке _(2026-09-07, `33ae6e4e`: `VTEAM_OWNER_MAP_FIX` подменяет готовую TSV-карту ⇒ `sorted(m[st])[0]`, схлопывающий N владельцев этапа в одного, не исполнен НИ ОДНОЙ из 27 осей)_ |
| **§A63** | `2026-09-07` | §Приёмка ПРЕДШЕСТВЕННИКА формулирует ветку ОТРИЦАНИЕМ («в артефакте НЕТ подстроки X») ⇒ предикат зелен и когда артефакта НЕТ ВОВСЕ: ветка «два владельца» уронила тик `BROKEN`, промпт не создан — критерий прошёл. ⛔ §A55 этот класс знает, но адресует ОСИ под мутантом; носителя «критерий, написанный предшественником по этапу» он не называет, и потому floor молчит, когда ты ЧИТАЕШЬ чужую приёмку, а не пишешь свою ось. ⚠ Tell: критерий требует ОТСУТСТВИЯ в артефакте, который сам может не появиться. ✅ Floor: ось-отрицание — КОНЪЮНКЦИЯ «артефакт существует ∧ подстроки нет»; ⛔ формулировку отрицанием НЕ менять на положительную (та зелена у реализации, оставившей ложное утверждение рядом с верным). ⛤ Разбора в справочнике НЕТ — floor целиком в строке _(2026-09-07, `33ae6e4e` StageSpecification)_ |
| **§A64** | `2026-09-07` | Мутант применился, `bash -n` ЧИСТ, а набор дал катастрофический пересвет ⇒ ⛔ это НЕ «предмет неисполним» (§A55): у харнесса с ENV-ПРЕФИКСНОЙ цепочкой вставка МЕЖДУ `\`-продолжениями рвёт команду ⇒ снимаются ВСЕ инъекции разом и прогон уходит в БОЕВЫЕ каналы — ломается ИЗОЛЯЦИЯ, не вердикт ⇒ §A64 |
| **§A65** | `2026-09-09` | Ось на ГРАНИЦУ сравнения (`rem >= limit`), где одна сторона вычисляется от **wall-clock** ⇒ ⛔ мутант «сменить СТРОГОСТЬ» (`>=`→`>`) ВАКУУМЕН по построению: фикстура ставит величину от СВОЕГО `now`, предмет берёт СВОЙ (позже) ⇒ точка равенства недостижима, обе формы дают ОДИН исход, и «не различает» читается как дефект оси ⇒ мутант делать **СДВИГОМ границы** (`>= limit + 1.0`): он краснит РОВНО ось с зазором ~0 и не трогает ось с зазором в часы (предъявить парным контролем). ⛔ И граница запирается **С ДВУХ СТОРОН**: одна ось у края ловит сдвиг лишь ВВЕРХ, а сдвиг ВНИЗ проходит незамеченным до зазора ближайшей противоположной оси (замер: S11 с зазором 22ч не краснела до сдвига ≥22ч) ⇒ нужна ПАРА осей по обе стороны края + свой мутант-сдвиг каждой. ⚠ Направление «подавляет ИСТИННОЕ» обычно дороже «пропускает ложное» — его и покрывать первым. ⛤ Разбора в справочнике НЕТ — floor целиком в этой строке |
| **§A66** | `2026-09-10` | Набор зелёный, а фича сломана в проде ⇒ ⛔ оси судят ПРОМЕЖУТОЧНУЮ ЗАПИСЬ (файл переписан, поле снято), тогда как продукт фичи — КОНЕЧНЫЙ ЭФФЕКТ (сообщение доставлено, задача создана): между ними стоят ЕЩЁ гейты, и любой из них гасит эффект при безупречной записи ⇒ ≥1 ось обязана доходить до эффекта, а не до артефакта. ⚠ Tell при написании набора: ни одна ось не зовёт того, КТО ЧИТАЕТ запись. ✅ И зеркало в проде: ОТРИЦАТЕЛЬНЫЙ предикат («расклад пуст ⇒ никто не отправил») удовлетворяется и НЕРАЗОБРАННЫМ входом, поэтому дрейф формата чужого инструмента даёт не ошибку, а УВЕРЕННЫЙ ЛОЖНЫЙ ВЕРДИКТ ⇒ рядом обязана стоять канарейка ПОЛНОТЫ входа (независимый счёт того же множества — у CLI это его собственная строка `Summary: N`); расхождение ⇒ вердикт не выносится вовсе. ⛤ Разбора в справочнике НЕТ — floor целиком в этой строке _(2026-09-10: 19 осей зелены при отсрочке, не возвращавшей напоминание 8 суток; 44 записи «ничего не отправлено» печатались РОВНО при удачном push)_ |
| **§A67** | `2026-09-11` | Флип требует состояния ДО, а взять его неоткуда: предмет **git-free** (каноничный vault) ЛИБО это **АГРЕГАТ**, у которого «маркера правки» нет ⇒ §A42-канарейка (сверить маркер предмета в извлечённой базе) НЕИСПОЛНИМА по построению, и рука тянется написать «не установлено» ⇒ реконструировать ДО **инъекцией в ТОТ ЖЕ снимок** (копия инструмента в scratchpad, ⛔ боевой скрипт не править), а модель валидировать **воспроизведением НЕЗАВИСИМО снятых величин** — не одной, а нескольких: сошлись 308 рёбер + 3 цикла + состав третьего цикла, снятые предшественником ⇒ дельта читается как ФАКТ, и открытый вопрос закрывается разностью множеств. ⛔ ОДИН агрегат совпасть может случайно — брать ≥2 числа И состав. ⚠ Tell: собираешься объявить неустановленным, потому что «машины времени нет». ⛤ Разбора в справочнике НЕТ — floor целиком в этой строке |
| **§A68** | `2026-09-12` | Ось — РАВЕНСТВО обёртки и композиции (`expect(w).toBe(f(g(x).part))`), мутант композиции краснит кейс ⇒ ⛔ покраснел СОСЕДНИЙ ассерт на литерал: эталон вычисляет сам тест теми же `f`/`g`, мутация сдвигает обе стороны разом ⇒ запирать мутацией ТЕЛА обёртки на фикстуре с расходящимися входами. ⚠ Tell: эталон — ВЫЗОВ, не константа; судит НОМЕР СТРОКИ упавшего ассерта |
| **§A69** | `2026-09-12` | Ось «константа НЕ дублируется» ищет ТЕКУЩЕЕ значение ⇒ ⛔ мутант «завести дубль» протухает ТИХО после смены значения: мутация ложится, драйвер винит ОСЬ («не различает»; не §A19 — там якорь, ГРОМКО) ⇒ предикат ловит ФОРМУ (`[0-9a-f]{64}` с границами), мутант вставляет заведомо ЧУЖОЕ значение. ⚠ Tell: значение стоит и в оси, и в мутанте — смена обезвредит оба |
| **§A70** | `2026-09-12` | Прогон СТОИТ: ни `PASS=`, ни красноты, ни падения — от «мутант не различает» неотличимо ⇒ ось с `mp.Barrier` виснет на НЕПРИГОДНОМ предмете (канарейка доставки драйвера даёт `#!/bin/sh`): `exec_module` падает у ВСЕХ воркеров ДО барьера, родитель ждёт `q.get()` вечно ⇒ старт по ЧАСАМ, `q.get(timeout)`, недобор ответов = BAD. ⚠ Tell: вывод оборван на заголовке оси |
| **§A71** | `2026-09-12` | Мутант дал «якорь 0 раз», а его текст ты не трогал ⇒ проверь, не менял ли В ЭТОМ ЖЕ заходе СИГНАТУРУ функции, на вызов которой он якорится — якорь протух от СВОЕЙ правки ⇒ после правки предмета гонять ДРАЙВЕР, и ВСЕ спеки субъекта, не только свою (`spec-anchor-sweep.py` в pre-PR хуке) ⇒ §A71 |
| **§A72** | `2026-09-12` | Строковый лист схемы запёрт ПРЕФИКСОМ (`re.match` без `$`, `startswith`) ⇒ ⛔ хвост неограничен — лист БЕЗ проверки: мутант «стемы промпта в broken» зелен при PASS=61 ⇒ `re.fullmatch` у ВСЕХ строковых листов, не только у найденного — класс, не экземпляр. ⚠ Детект: `grep -nE 're\.match\|startswith' <ось>`; tell — ось на ФОРМУ значения без якоря конца в шаблоне |
| **§A73** | `2026-09-12` | Мутант не покраснил НИ ОДНОЙ оси, драйвер гонит КОПИЮ ⇒ ⛔ не «ось вакуумна»: предикат САМОЙ ОСИ ключуется на АБСОЛЮТНЫЙ литерал пути (`$HOME/<репо>/…`) и на копии судит БОЕВОЙ файл — зеркало §A24 (там выключается гвард ПРЕДМЕТА, здесь ОСЬ) ⇒ путь в предикате брать от `dirname(SUT)`, абсолют — вторым условием. ⚠ Tell: в оси стоит `$HOME`, а драйвер запускает копию |
| **§A74** | `2026-09-12` | ТУ ЖЕ ось краснит и ЧУЖОЙ мутант ⇒ ⛔ имя оси ЗАНЯТО: §A47 задаёт ФОРМУ `A<N>`, уникальность не проверяет никто, две оси под одним именем неразличимы ⇒ ПЕРЕД именованием `grep -oE '\bA[0-9]+\b' <харнесс> \| sort -u -V`, брать выше максимума; причину номера — комментарием у оси. ⚠ То же с `§A<N>` справочника при параллельных сессиях |
| **§A75** | `2026-09-12` | Боевой путь у САМОГО предмета (SUT берёт пути из `$HOME`), правка SUT запрещена ⇒ ⛔ §A57 неприменим ⇒ ✅ подменять `HOME` при вызове SUT — он сам выведет пути в изолированный каталог. ⚠ Предъявлять: подмена доехала И tmux целы. ⛔ Ось «0 новых файлов в боевом каталоге» ложно-красна — пишет ТРЕТИЙ процесс ⇒ ключевать на свои префиксы. ⛤ Floor в строке _(`ebe325e4`)_ |
| **§A76** | `2026-09-12` | Ревью коммита шло на РАБОЧЕМ дереве, пока автор правил те же файлы ⇒ ⛔ вердикт — артефакт гонки (4 мутанта — побайтно одинаковые 18/21 красных; вручную TypeError смены сигнатуры); §A53-4 адресован автору харнесса, здесь актор — РЕВЬЮЕР ⇒ ревью только на копии `git show <sha>` в scratchpad, без драйверов при чужом прогоне. ⚠ Tell: ревьюер и писатель в одном каталоге |

| **§A77** | `2026-09-13` | Прогон мутантов идёт ЧАС+ и даёт **0 байт** в файле вывода ⇒ ⛔ не «завис»: python БУФЕРИЗУЕТ stdout при редиректе в ФАЙЛ (Г28 покрывает только хвост-пайп — его floor тут ИСПОЛНЕН и не спасает), а SIGTERM стирает буфер ЦЕЛИКОМ ⇒ полтора часа прогона исчезают без частичного результата. ⇒ у драйвера печать построчная (`print(..., flush=True)`); дискриминатор «жив или деградировал» — возраст ДОЧЕРНЕГО прогона, не размер файла. ⛤ Floor в строке _(2c.8)_ |

| **§A78** | `2026-09-13` | Ассерт на ПОЛЕ библиотеки, чьё ИМЯ совпадает с гарантией (Commander `Option.required` читается как «обязательна», а означает «принимает значение» — истинно и у `--registry`, и у `--self`) ⇒ ⛔ ассерт тождественно-зелёный; поймало ревью, а не мутанты — флип шёл на ДВЕ оси файла, не на КАЖДУЮ гарантию ⇒ у каждой гарантии свой мутант (`requiredOption → option` обязан покраснить), поле брать по ДОКУ (`.mandatory`), не по имени. ⚠ Tell: `expect(x.<булево-поле>).toBe(true)` без мутанта, поле выбрано по имени. ⛤ Floor в строке _(PR #4228 round 1)_ |
| **§A79** | `2026-09-14` | База флипа берётся `git show <sha>:<путь>`, а ПУТЬ — СИМЛИНК ⇒ ⛔ `git show` отдаёт ТЕКСТ ССЫЛКИ, не содержимое, молча и с rc 0; обе половины «различаются», вердикт о базе ложен ⇒ базу извлекать по РЕАЛЬНОМУ пути (`readlink -f`) + канарейка размера. ⚠ Tell: «файл» помещается в одну строку ⇒ §A79 |
| **§A80** | `2026-09-15` | Правишь предмет в WORKTREE по пути `.claude/hooks/<x>` ⇒ ⛔ это АБСОЛЮТНЫЙ симлинк на главный чекаут: запись мутирует ЖИВОЙ файл, а `SUT` гоняет живой хук — изоляция worktree вакуумна ⇒ `readlink` ДО правки, предмет — реальный `bin/<x>`. ⚠ Tell: `diff --stat` worktree пуст, а `~/dotfiles status` — нет ⇒ §A80 |
| **§A81** | `2026-09-16` | Фикстурный харнесс ВНУТРИ харнесса X читает предмет из `${SUT:-…}` ⇒ ⛔ X получил `SUT` из окружения и ЭКСПОРТИРУЕТ его детям — фикстура гоняет ПРЕДМЕТ X как свой ⇒ у фикстуры СВОЁ имя переменной (`MSUBJ`). ⚠ Tell: ось красна текстом драйвера о доставке, хотя доставку ты не трогал ⇒ §A81 |
| **§A82** | `2026-09-17` | Две ветки пишут ВРЕМЯ, а фикстура — FROZEN clock ⇒ обе дают ОДНУ строку, и guard между ними не запирается ничем (мутант «снять guard» зелёный) ⇒ ТИКАЮЩИЙ clock + КОНТРОЛЬНЫЙ прогон на другом свойстве: guarded-ветка обязана потребить на один вызов `now()` меньше. ⚠ Tell: ось зелена под ВСЕМИ мутантами ⇒ §A82 |
| **§A83** | `2026-09-17` | База флипа — литеральный до-фиксный файл, а новые оси `spyOn`-ят СИМВОЛ, введённый САМИМ фиксом ⇒ на базе они падают ОДНОЙ структурной ошибкой, и «7 failed» читается как RED, хотя краснота не поведенческая ⇒ базу подписывать мутантом **M0**: снять ПОВЕДЕНИЕ, сохранить ФОРМУ; матрица называет и ЗЕЛЁНЫЕ оси ⇒ §A83 |
| **§A84** | `2026-09-17` | Флип ЛИНТЕРА через stdin (`git show origin/main:<f> \| npx eslint --stdin --stdin-filename <f>`) дал **0 problems / rc 0** на файле с ЗАВЕДОМЫМ долгом — те же 9 problems (5 err + 4 warn) он показывает с ДИСКА ⇒ ⛔ typed/`obsidianmd/*`-правила по stdin МОЛЧАТ, и обе половины флипа (main = 0, ветка = 0) неразличимы — ложно-зелёный revert-verify ⇒ флип ТОЛЬКО на диске: записать main-версию поверх файла (config уже без override), `eslint --max-warnings=0`, restore побайтно (`shasum` до/после). ⚠ Tell: stdin-форма выдала 0 там, где ты сам видел долг. ⛔ **НАПРАВЛЕНИЕ ошибки НЕ ИНВАРИАНТ, и Tell выше из-за этого ловит лишь половину класса** (уточнено 2026-09-26): здесь stdin дал **БОЛЬШЕ** проблем, чем диск — одно и то же до-фиксное содержимое, `с диска 7` против `через stdin 8` ⇒ признак «stdin выдал 0 / подозрительно мало» МОЛЧИТ, когда stdin завышает. ✅ Рабочий Tell — не величина, а **АРИФМЕТИКА ИНСТРУМЕНТОВ**: обе половины флипа обязаны быть сняты ОДНИМ способом; сравнил «до» через stdin с «после» с диска ⇒ сравнил два ИЗМЕРИТЕЛЯ, а не две ревизии (`self-authored-claim-loses-its-provenance` §Второй признак — один предмет, разные ответы ⇒ сломан измеритель). ⚠ И цена тут ДРУГАЯ, чем у исходного случая: там ложно-ЗЕЛЁНЫЙ revert-verify, здесь гейт не страдает вовсе — страдает durable-утверждение в теле PR «ничего не изменилось» _(2026-09-26, PR #4407: заявлено «eslint unchanged 8→8» при фактических 7→8; поймало round-2 ревью, не автор)_. ⛤ Floor целиком в строке _(PR #4255, `7c02970c`)_ |
| **§A85** | `2026-09-17` | Пишешь ось-флип со `jest.spyOn(obsidian, "requireApiVersion")`, а jest падает `TypeError: Cannot redefine property` ⇒ ⛔ фабрика `jest.mock("obsidian", () => ({ ...actual }))` ТЕРЯЕТ non-enumerable `__esModule` ⇒ `import * as obsidian` получает `__importStar`-обёртку с non-configurable getters, spyOn невозможен по построению ⇒ `{ __esModule: true, ...actual }` в фабрике — тогда namespace-import = сам объект мока. ⚠ Tell: spyOn работает в соседнем тесте БЕЗ фабрики и падает в тесте СО spread-фабрикой. ⛤ Floor целиком в строке _(PR #4255)_ |
| **§A86** | `2026-09-17` | Ось-revert зелена под мутантом, снимающим ЕЁ гвард, а мутант применился (`n == 1`) ⇒ ⛔ сперва проверь ФИКСТУРНЫЙ мутатор, а не ось: он «менял» подстроку, которой в seed НЕТ ⇒ вход байт-в-байт, штампа нет по построению ⇒ в мутатор фикстуры канарейку `expect(next).not.toBe(current)` ⇒ §A86 |
| **§A87** | `2026-09-17` | Jest-конфиг с `resetMocks: true` ⇒ ⛔ `jest.fn(() => …)` ВНУТРИ `jest.mock`-фабрики теряет реализацию ДО первого теста (`spawn()` вернул `undefined`) ⇒ в фабрике plain-функции либо `let impl` + присвоение в `beforeEach`; `spyOn` на `import * as ns` даёт `Cannot redefine property` — мокать модуль фабрикой ⇒ §A87 |
| **§A88** | `2026-09-18` | Заменил РАЗБОР текста (regex / `split`) на СВОЙ сканер ⇒ ⛔ твои оси зелены — они на НОВЫЕ формы, а СТАРЫЕ регрессировали молча при rc 0 ⇒ матрица форм из корпуса (прогон head↔main, вердикты обязаны совпасть) + дифференциальный fuzz против РЕФЕРЕНСНОГО парсера ДО PR ⇒ §A88 |
| **§A89** | `2026-09-18` | Ось пинит mtime фикстуры и ассертит РАВЕНСТВО с тем, что ПЕРЕДАЛА ⇒ ⛔ зелено локально (APFS), красно в CI (ext4): ФС хранит округлённый timespec, и ось меряет конверсию раннера, а не предмет ⇒ пинить ЦЕЛЫЕ секунды и сравнивать с величиной, ПРОЧИТАННОЙ ОБРАТНО через `stat` ⇒ §A89 |
| **§A90** | `2026-09-19` | Матрица «мутант на каждый гвард» собрана грепом по `if (`/`continue`/`.has(` — а обход стартует от ЛИТЕРАЛЬНОГО множества корней (`queue = [X]`), и оно тоже гвард ⇒ поддерево отсечено молча, ни один `if` не соврал ⇒ гварды диффа = каждая строка, где множество ЗАДАЁТСЯ литералом. ⚠ Tell: visited шире queue ⇒ §A90 |
| **§A91** | `2026-09-19` | Драйвер мутантов гонит сьюты ДВУХ пакетов ⇒ ⛔ два тихих промаха: ключ оси уникален лишь внутри ОДНОГО харнесса (core `I1` и cli `I1` склеились в общий `RED`), и cli-сьюты импортируют core из `dist` — мутация src без `npm run build -w core` НЕ доезжает ⇒ уникальность ключей по ВСЕМ спекам + пересборка перед каждым core-мутантом ⇒ §A91 |
| **§A92** | `2026-09-19` | Нужна ось «предмет БЕЗ инъекции даёт дефолт под $HOME», а у предмета ГВАРД ПРОБЫ на ДРУГУЮ инъекцию ⇒ ⛔ с ней ось недостижима, без неё предмет читает дефолтный каталог ⇒ не задавать её вовсе: фикстура ПО ДЕФОЛТНОМУ пути под подменённым HOME, зависимость — стабом. ⚠ Tell: гвард и ось спорят за переменную ⇒ §A92 |
| **§A93** | `2026-09-19` | Объявил конъюнкт гварда «defensive по control-flow», выведя арифметику из ЭМИТТЕРА ⇒ ⛔ арифметика defensive-гварда идёт по ВХОДАМ ПУБЛИЧНОГО API предмета, а не по одному производителю ⇒ ось + мутант обязательны, «defensive» снимается. ⚠ Tell: перечисляешь формы «которые только и бывают» по коду ПИСАТЕЛЯ, гвард же в ЧИТАТЕЛЕ ⇒ §A93 |
| **§A94** | `2026-09-17` | Второй мутант на ТУ ЖЕ строку предмета, и рука тянется расширить якорь «чтобы не делить его» ⇒ ⛔ ложно: драйвер применяет КАЖДОГО мутанта к НЕТРОНУТОМУ src, уникальность `from` требуется В ПРЕДМЕТЕ ⇒ якорь минимальный (строка + отступ), различие мутантов — в `to`. ⚠ Tell: в `_note` стоит «чтобы не делить якорь» ⇒ §A94 |
| **§A95** | `2026-09-20` | Не пишешь мутанта на гвард, объявив его defensive (снятие не меняет исход НА ТВОЁМ входе) ⇒ ⛔ арифметика посчитана на ОДНОМ элементе, а гвард стоит ПЕРЕД записью в ОБЩЕЕ состояние: без него мусор ЗАНИМАЕТ ключ и под first-wins носитель проигрывает ⇒ дефенсивность доказывается входом из ДВУХ ⇒ §A95 |
| **§A96** | `2026-09-20` | Свой драйвер мутантов ЗЕЛЁН и матрица воспроизведена дословно ⇒ ⛔ это НЕ доказывает, что твои оси меряют СВОЙ предмет: фикстура могла опираться на механизм ЧУЖОГО тикета ⇒ правишь файл с чужой спекой — прогони ЕЁ драйвером; изолировав фикстуру, предъяви ОБЕ половины флипа ⇒ §A96 |
| **§A97** | `2026-09-20` | Мутант дал **BROKEN** (компиляция сломана, `Tests: 0`), и рука ставит `expect: []` ⇒ ⛔ `BROKEN` — это ОТСУТСТВИЕ замера, а не замер: у мутанта, снявшего type-narrowing (§A36), вывод драйвера НЕ отличим от «ось не различает», а диагнозы противоположны ⇒ переписать в type-preserving форму и ПЕРЕ-МЕРИТЬ ⇒ §A97 |
| **§A98** | `2026-09-20` | Контроль драйвера падает `Unexpected token export` на КОПИИ дерева ⇒ ⛔ в монорепо потребитель резолвит зависимость из ВЛОЖЕННОГО `packages/<p>/node_modules`, а `symlink_node_modules` перечисляет только корневой ⇒ ESM читается как CJS и НИ ОДНА ось не исполняется ⇒ перечислять корневой И КАЖДЫЙ `packages/*/node_modules` ⇒ §A98 |
| **§A99** | `2026-09-20` | `spec-anchor-sweep` отвечает «broken 0» ⇒ ⛔ не CLEAN: читать ДВЕ величины — `touched 0` при непустом `git diff` есть BROKEN. ⛔ Второй механизм с тем же выводом: subject спеки внутри САБМОДУЛЯ (дифф даёт один gitlink) ⇒ якоря своих спек проверять ПРЯМЫМ прогоном драйвера ⇒ §A99 |
| **§A100** | `2026-09-20` | Полный прогон в СВЕЖЕМ worktree даёт падения в сьютах, читаемые как регрессия твоей правки ⇒ ⛔ сперва проверь СРЕДУ: свежий worktree не несёт САБМОДУЛЕЙ, и тесты с TBox-данными падают ДО связи с предметом ⇒ `ls packages/exoas-exocmd \| wc -l`, лечение `git submodule update --init` ⇒ §A100 |
| **§A101** | `2026-09-20` | Хочешь предъявить НЕЙТРАЛЬНОСТЬ правки байт-сравнением с ОПУБЛИКОВАННЫМ артефактом ⇒ ⛔ контроль неверен по построению при расхождении версий — дифф покажет разницу ВЕРСИЙ под видом твоей правки ⇒ дешевле и сильнее: перечислить ПОТРЕБИТЕЛЕЙ правленого поля (`git grep`) ⇒ §A101 |
| **§A102** | `2026-09-21` | Data-guard над структурированным файлом читает значение ПОСТРОЧНЫМ регэкспом, а прод его ПАРСИТ ⇒ ⛔ ось falsifiable обычным YAML (block-scalar, одноэлементный список): прод рендерит кнопку, гвард рапортует ноль, все оси зелены ⇒ читать ТЕМ ЖЕ парсером, что прод; мутанты — на КАЖДУЮ форму записи ⇒ §A102 |
| **§A103** | `2026-09-22` | ДОБАВИЛ ось — покраснела ЧУЖАЯ, которой ты не касался, при НЕТРОНУТОМ предмете ⇒ ⛔ не «мутант шире» (§A44 про ЧТЕНИЕ результата) и не дефект предмета: соседние оси читают `$rc`/`$out` ОТ ПРОГОНА ПРЕДЫДУЩЕЙ — связь СКРЫТАЯ, в их коде происхождения переменных не видно, и ВСТАВКА между ними их перезаписывает ⇒ ось, делающая СВОЙ прогон, ставится ПОСЛЕ последнего потребителя чужих `$out`/`$rc`, а причина пишется ⛔-строкой над ней (иначе следующий автор вернёт её «поближе по смыслу» к дискриминантной паре). ⚠ Tell ловится ФЛИПОМ, а не чтением: без правки 37/0, с правкой 37/1. ⛤ Floor целиком в строке _(2026-09-22, desc-gate A61 между A17 и A28: «A28 вход не построен: ожидался rc=3, получен 1»)_ |
| **§A104** | `2026-09-22` | Драйвер печатает «красных НЕТ, но rc харнесса ≠ контрольного» на КАЖДОМ мутанте ⇒ ⛔ не «мутант уронил харнесс»: `red_re` берёт имя сразу после `›`, а `it("@req:… Y1 …")` ставит туда тег ⇒ имена невидимы при настоящей красноте ⇒ имя оси ПЕРВЫМ токеном (в одном файле законны оба порядка) ⇒ §A104 |
| **§A105** | `2026-09-22` | Мутант на ФИЛЬТР не покраснил свою ось-контроль ⇒ ⛔ не «ось не различает» и не «defensive»: фикстура могла не доводить предмет ДО фильтра (поле без `minCount` не входит в цикл) ⇒ ось несёт ОТСЕИВАЕМОЕ ЗНАЧЕНИЕ (`minCount: 0`), а не отсутствие поля ⇒ §A105 |
| **§A106** | `2026-09-22` | Откатил рабочий файл на `origin/main` ради baseline и вернул «копию» ⇒ ⛔ снятая РАНЬШЕ последней правки копия возвращает УСТАРЕВШУЮ редакцию (обе зелены, отличаются ценой) ⇒ базу мерить на ОТДЕЛЬНОЙ копии дерева (§A10), а тронув рабочее — сверять МАРКЕР правки ⇒ §A106 |
| **§A107** | `2026-09-20` | Ось КРАСНАЯ, и её текст обвиняет ПРЕДМЕТ — а фикстура НЕ ПОСТРОИЛАСЬ ⇒ ⛔ зеркало §A63, и сигнатура ОБРАТНАЯ: там непостроенный вход даёт вакуумную ЗЕЛЕНЬ у оси-отрицания, здесь у ПОЛОЖИТЕЛЬНОЙ оси он даёт ложно-атрибутированную КРАСНОТУ — пустой вывод читается как «предмет не напечатал», хотя печатать было нечего, и чинить уходят ИСПРАВНЫЙ предмет ⇒ конъюнкт «вход построен ∧ <предикат>» обязателен у ОБЕИХ полярностей, а непостроенный вход — свой исход BROKEN, не `bad`. ⚠ Tell: текст красноты называет отсутствие того, чего фикстура и не создавала. ⛤ Floor целиком в строке _(находка ребёнка `descgate`, ход 2026-09-20: ось A6 на пустой фикстуре)_ |
| **§A108** | `2026-09-20` | Мутант «сменить предикат» НЕ краснит ось, мутация ЛЕГЛА (`n == 1`) ⇒ ⛔ не «ось вакуумна» (§A12 — там мутация не легла) и не дрейф ОЖИДАНИЯ (§A53): протух сам ВХОД — фикстура написана под ПРЕЖНИЙ предикат и после его смены перестала различать, оставшись СИНТАКСИЧЕСКИ ВАЛИДНОЙ, то есть молча. ⇒ меняя предикат, пере-проверять КАЖДУЮ кормящую его фикстуру вопросом «даёт ли она ещё РАЗЛИЧАЮЩИЙ вход», а не «валидна ли она». ⚠ Tell: ось зелена и до, и после мутации предиката, при том что фикстуру ты не трогал. ⛤ Floor целиком в строке _(находка ребёнка `descgate`, тот же ход: мутант по `exceeds` на фикстуре прежней шкалы)_ |

| **§A109** | `2026-09-22` | Краснота контроля СТАРШЕ твоей правки ⇒ ⛔ «pre-existing» ≠ «не наше»: корпус мог править ты же накануне ⇒ ДАТИРОВАТЬ по журналу раннера (`verify-harness-runner.log.d/*.out`, прогон/сутки), атрибутировать по коммитам окна, состав сверять ПОИМЁННО. ⚠ Tell: виновного зовёшь чужим. ⛤ Floor в строке _(6v: A92 1→5 = `64433499`+`1b6a86b5`)_ |
| **§A110** | `2026-09-22` | Два мутанта — ОДИН набор красных ⇒ ⛔ не доказывает двух запертых конъюнктов: `red_re` берёт ИМЕНА осей, ветвь различает СООБЩЕНИЕ (§A31) ⇒ предъявить ветвь пробой на копии либо ассертить подстроку. ⚠ Tell: у оси ≥2 конъюнктов. ⛤ Floor в строке _(6v A75: MA1 → плейсхолдер, MA2 → off/on 318→425 б)_ |
| **§A111** | `2026-09-22` | Не пишешь мутанта на гвард — «в фикстуре нет различающего входа» ⇒ ⛔ это свойство ФИКСТУРЫ, не предмета: §A35/§A93 судят УЖЕ написанного мутанта, промах же РАНЬШЕ — на решении не заводить ⇒ спросить, породит ли такой вход ПРОД; да ⇒ расширить фикстуру, а не звать гвард defensive. ⚠ Tell: «такого не бывает» — про СВОЮ фикстуру. ⛤ Floor в строке _(req 07509cf9: дедуп и порядок; свойство с двумя доменами одной цепочки законно ⇒ 9→11 мутантов)_ |
| **§A112** | `2026-09-22` | Два мутанта РАЗНЫХ гвардов дали ОДИН набор красных, и хотя бы один вводит ПРОЦЕСС-ШИРОКОЕ состояние (`globalThis`, модульный кэш) ⇒ ⛔ это не §A110-вакуум: состояние переживает ТЕСТЫ, предыдущая ось греет его на том же входе, и набор красных становится функцией ПОРЯДКА, а не гварда ⇒ чинить ФИКСТУРУ (дать осям вход, которого не трогает ни одна другая ось), а не ослаблять ось. ⚠ Tell: мутант «кэш навсегда» краснит ось про «внутри одного вызова». ⛤ Floor в строке _(9e19f141: M13/M14 [P6a,P6b] → [P6b])_ |
| **§A113** | `2026-09-22` | Ось КРАСНА на исправном коде ⇒ ⛔ не находка: значение фикстуры прошло ПАРСЕР, он сохранил её ЛЕКСИЧЕСКУЮ форму, сериализатор счёл готовой ⇒ байты те же с гвардом и без ⇒ подать значение предмету НАПРЯМУЮ, минуя фикстуру. ⚠ Tell: записано «как в файле», ассерт «как в памяти» _(afb25c43: кавычки ВНУТРИ строки)_ |
| **§A114** | `2026-09-22` | Ось красна на ИСПРАВНОМ предмете, падает ассерт ПОСЛЕ ожидания ⇒ ⛔ не чинить предмет: `expect.poll` гейтился предикатом СЛАБЕЕ своих же ассертов и вышел в переходном окне (одна поверхность UI отрисована, вторая нет) ⇒ ждать ТУ ЖЕ форму, что ассертишь. ⚠ throw из генератора `poll` НЕ ловит — попытка рвёт весь цикл: try/catch |
| **§A115** | `2026-09-22` | ТО ЖЕ наблюдаемое, что §A103, но ВТОРОЙ механизм — разводить по тому, ЧТО перезаписано: там переменные прогона (`$rc`/`$out` соседней оси), здесь ФАЙЛ ФИКСТУРЫ. Вставил ось между чужими блоками — покраснели оси, которых ты не трогал ⇒ ⛔ не чинить их логику: твой блок начинается с `rm -f "$T"/*-verify.sh "$T"/*.spec.json` и ПЕРЕЗАПИСЫВАЕТ общую фикстуру соседнего блока (у меня спека пересоздалась без `"shards":2` ⇒ соседние оси получили `shard=0/1` вместо `1/2` и читались как дефект ротации) ⇒ блок ставить ПОСЛЕ тех, чью фикстуру он затирает, и давать СВОИ имена фикстур (`tmo-*`, `plan-*`). ⚠ Tell, ловится ДО прогона: твоя вставка несёт `rm`/`mk` по именам, которые уже встречаются ВЫШЕ в файле (`grep -n '<имя-фикстуры>' <харнесс>` даёт совпадения вне твоего блока). ⛤ Floor целиком в строке индекса _(2026-09-22, тикет 4be3bbfa: S1d затёр фикстуру S2/S2b; FAIL=3 при одной новой оси)_ |
| **§A116** | `2026-09-22` | Мутант снял гвард обхода, а драйвер ВИСИТ без вердикта вовсе ⇒ ⛔ не «ось не различает»: обход крутится в МИКРОТАСКАХ, и per-test таймаут jest НЕ срабатывает (прогон убит снаружи, rc=124) ⇒ `timeout` на КАЖДОМ вызове харнесса, а не только на сьюте, который «должен» виснуть: спин идёт на любом входе, где обходу есть чем ветвиться, поэтому висят и соседние сьюты; rc=124 ЛЮБОГО вызова печатает ось цикла красной — незавершение ровно то, что она утверждает. ⚠ Tell: у мутанта на гвард цикла нет НИ ОДНОЙ строки вердикта. ⛤ Floor целиком в строке |
| **§A117** | `2026-09-22` | Консолидация перенесла гвард в ДРУГОЙ файл, `spec-anchor-sweep` даёт count 0 у ЧУЖОГО мутанта ⇒ ⛔ перецелить его нельзя, у драйвера `subject` ОДИН: мутант ПЕРЕЕЗЖАЕТ в спеку нового носителя вместе со своим гвардом, а прежний `expect`-список становится проверкой добросовестности переезда — совпал побайтно ⇒ перенесён гвард, а не написан новый. ⚠ Tell: текст якоря ты не правил, а УДАЛИЛ. ⛤ Floor целиком в строке |

| **§A118** | `2026-09-23` | Мутант не покрасил НИ ОДНОЙ оси ⇒ ⛔ диагнозов ТРИ, а не два: к «ось слаба» и «гвард defensive» добавляется **условие МЁРТВОЕ — его снятие не ослабляет гвард, а ЗАКРЫВАЕТ в нём дыру**. Дискриминатор: **назови вход, на котором условие ЗАЩИЩАЕТ законную форму**. Не называется (её пропускает ДРУГОЙ конъюнкт) ⇒ это не defensive, а ОБХОД — снимать из прода, не документировать. ⚠ Tell: обоснование звучит «чтобы не ловить законную форму X», но у X срабатывает другой конъюнкт. ⛤ Два ДЕЙСТВУЮЩИХ диагноза для контраста: дублирующее покрытие (форму держит второй заслон ⇒ оставить, причину в код) и defensive по КОНТРАКТУ вызываемой программы (`--help` говорит, что флаги парные ⇒ различающего входа прод не произведёт). ⛤ Floor целиком в строке _(`check-then-mutate-guard`: конъюнкт «на строке с `<<` нет `&&`» освобождал от проверки любую команду с `&& echo ok`; обход rc=0 при контроле rc=2, после снятия rc=2)_ |
| **§A119** | `2026-09-23` | Прогон мутантов ВИСИТ без единой строки вердикта — читается как «мутант тяжёлый» ⇒ ⛔ проверь, не ЗОВЁТ ЛИ ОСЬ СВОЙ ЖЕ ПРЕДМЕТ: у самопроверяющегося инструмента (харнесс и предмет — ОДИН файл) мутант, снимающий предусловие завершения, делает вызов **рекурсией без дна**, и каждый уровень порождает полный набор осей. Наблюдаемое неотличимо от §A70/§A116, механизм и лечение другие. ⚠ Tell ловится ЧТЕНИЕМ оси ДО прогона: в её теле запуск `$SUT` с флагом, чья обработка живёт в ТОМ ЖЕ предмете. ✅ Floor: такой оси нужно **дно** — метка глубины в окружении дочернего вызова (`env=dict(os.environ, <PREFIX>_DEPTH="1")`), а блок таких осей пропускается, когда метка стоит: внутренний прогон завершается, вердикт внешнего остаётся полным. ⛔ Не «смягчать мутанта» и не снимать ось — рекурсию вносит МУТАНТ, ось обязана его ловить. ⛤ Floor целиком в строке _(`mutant-coverage-verify`: драйвер висел 25 мин и умер по таймауту, ноль строк вердикта)_ |
| **§A120** | `2026-09-24` | Мутант ПРИМЕНИЛСЯ, но покраснела ЧАСТЬ ожидаемых осей ⇒ ⛔ прежде чем искать вакуумную ось, спроси, **не берёт ли сам МУТАНТ значение из ОКРУЖЕНИЯ**: `os.environ.get("X")` без дефолта даёт строку в интерактивной сессии (где `expect` и снимали руками) и `None` под launchd, а обходчики схем на `None` выходят сразу — судить нечего, и оси на ФОРМУ значения молча зеленеют. ⚠ Дефект невидим автору ПО ПОСТРОЕНИЮ: руками не воспроизводится НИКОГДА, всплывает только в плановом прогоне, у шардированной спеки — раз в N суток. ✅ Floor: значение мутанта — свойство МУТАНТА, не среды (дефолт либо литерал); ⛔ не подгонять `expect` под факт и не звать оси вакуумными — они исправны, вырожден ВХОД. ⚠ Tell: в `to` есть чтение окружения без дефолта. ⛤ Не §A53 и не §A108 (там протухла ФИКСТУРА — тут она цела). ⛤ Floor целиком в строке _(`rules-symptom-retrieval` шард 3/22: ожидал 12 красных, дал 9; после дефолта `"cli"` флип дал 17)_ |
| **§A121** | `2026-09-24` | САМОДЕЛЬНЫЙ цикл мутантов (таблица ВНУТРИ харнесса, без `mutant-driver`) не гоняет КОНТРОЛЬ ⇒ ⛔ ось, красная ВСЕГДА (её фикстура отстала от предиката предмета), попадает в `expect` и обрастает объяснением «и это ЗАКОННО, потому что…» — ожидание совпало с наблюдением ПО СОВПАДЕНИЮ; чинишь фикстуру — и СОСЕДНИЕ мутанты «разъезжаются», хотя сломал их не ты. ⛔ Не §A44 и не §A32: здесь ОДНА ось, давно, и именно она в `expect`. ✅ Floor: судить РАЗНОСТЬ `red − control`; драйвер закрывает это ГЕЙТОМ (красный контроль ⇒ rc=1, мутанты не гоняются), самодельный цикл — ничем. ⚠ Tell: в `expect` стоит ось, чью красноту автор объясняет прозой рядом — открой её вердикт в КОНТРОЛЕ. ⛤ Второй носитель ищи в ТОМ ЖЕ файле: сценарий оси часто скопирован внутрь цикла мутантов (§A33). ⛤ Floor целиком в строке _(`reminder-multimount-dedup`: T4 строила ключ голым uid после перехода предмета на `_fired_key(uid, at)`; M2/M4 разошлись после починки)_ |
| **§A124** | `2026-09-24` | Мутант снял ИМЕННО тот конъюнкт, о котором говорит проверяемая фраза, мутация ЛЕГЛА — ось зелена ⇒ ⛔ не «ось вакуумна» и не «defensive»: предикат отсекает вход НЕСКОЛЬКИМИ конъюнктами разом, одноместная мутация бессильна ПО ПОСТРОЕНИЮ (§A33 на ПРЕДИКАТЕ, не на выводе). Замер: `isSyncablePath` отвергал `<p>.md.conflict.local.txt` уже по `endsWith('.md')`, и он же нёс `.local.` — каждый из двух предикатов отсекал имя ДВАЖДЫ, снятие `.conflict.` не меняло ничего. ⚠ Tell — ЧТЕНИЕ ИМЕНИ входа ДО прогона: в нём БОЛЬШЕ одного токена, который тот же предикат исключает. ✅ Floor: вход, где проверяемый токен — ЕДИНСТВЕННАЯ причина отказа (`alpha.conflict.md`), плюс позитивный контроль на обычном имени — иначе пара `false` зелена и от сломанного импорта. ⛤ Найдено ПРОГОНОМ, не чтением: чтение показывает, ЧТО предикат отвергает вход, и не показывает ЧЕМ. Floor целиком в строке _(PR #4342 MEDIUM-2)_ ⛤ _(был §A121, коллизия — расцеплено 24.09; ссылок 0)_ |
| **§A125** | `2026-09-25` | Мутант даёт «мутация НЕ ЛЕГЛА» (`count != 1`) — а его ИСКОМЫЙ ЛИТЕРАЛ написан в ТОМ ЖЕ файле, внутри его собственного heredoc ⇒ ⛔ выборка включает САМУ СТРОКУ ОПРЕДЕЛЕНИЯ, и дефект **меняет симптом со временем**: пока реального call-site нет, `count == 1` достигается ЛИТЕРАЛОМ — мутант правит СВОЁ определение, предмет не трогает, и ось рапортует «гвард не различает» (§A12 уводит чинить ось); как только call-site появляется, `count` становится 2 и тот же дефект читается как «якорь протух» (§A71 уводит искать свою правку сигнатуры). ⇒ ни один из двух готовых диагнозов не верен, а мутант был **вакуумен с рождения**. ✅ Floor: считать и заменять ПО СТРОКАМ с явным исключением строки-определения (`[i for i,l in enumerate(lines) if FRAG in l and not l.lstrip().startswith("FRAG")]`), либо вырезать heredoc-блоки до выборки — терминатор ПРИВЯЗАН К ТЕГУ (backreference), иначе `.*?` тянется до ближайшего тега от ЛЮБОГО heredoc и съедает реальные call-site. ⚠ Tell, ловится ДО прогона ЧТЕНИЕМ: предмет мутации — строка, которую ты ЖЕ И ЦИТИРУЕШЬ в коде мутанта/сканера. ⛤ Класс рецидивирует В ОДНОМ ФАЙЛЕ: за одну задачу он соврал ТРИЖДЫ — дважды в сканере (`_callscan` метил себя 1 из 11; наивная вырезка heredoc схлопнула 11 до 2) и один раз в мутанте той же оси. ⛤ Floor целиком в строке _(2026-09-25, `vteam-supervise-verify.sh`, dotfiles `d62bc3e3`: U3c 143/1 → 144/0)_ |
| **§A122** | `2026-09-24` | Правка ДОБАВЛЯЕТ ЧТЕНИЕ существующей переменной/состояния (инициализатор к `let`, второй потребитель поля) ⇒ ⛔ та же величина может питать **ВТОРОЙ КАНАЛ**, и твои оси слепы **по построению**: фикстуры строятся под ТВОЙ сценарий, поэтому вход, на котором каналы расходятся, в наборе отсутствует вовсе. Замер: `lastCreatedPath` в `executeComposite` обслуживала ЦЕЛЬ шага (`stepUsesCreatedPath` → `stepPath`, req `b00acde4`) и вводимое ЗНАЧЕНИЕ; сидирование её из родительского композита молча перенацелило шаг с `targetsCreatedInstance` во ВЛОЖЕННОМ композите с click-target на ассет РОДИТЕЛЯ — регрессия уже отгруженной фичи при **пяти зелёных осях и восьми совпавших мутантах**. ⚠ Tell, ловится ЧТЕНИЕМ ДИФФА, до прогона: ты дописываешь инициализатор к `let`, объявленному БЕЗ него, либо второго читателя к живому полю. ✅ Floor: грепнуть ВСЕХ читателей величины и по каждому спросить «меняется ли ЕГО вход»; наследование/обогащение применять **в точке передачи СВОЕМУ потребителю**, а не к общей переменной — тогда чужой канал не может сдвинуться by construction. ⛤ Ось на чужой канал писать ОТДЕЛЬНО (у меня A6: `body_template` во вложенном композите пишет в click-target) и мутантом на ВОЗВРАТ сидирования. ⛤ Floor целиком в строке _(2026-09-24, PR #4344 HIGH; корпус-греп по 280 носителям дал 0 при живой канарейке — класса не было)_ |
| **§A123** | `2026-09-24` | Прирост **warnings** после монтирования нового ассетспейса ⇒ ⛔ не регрессия: монтирование делает класс резолвимым, его инстансы ВПЕРВЫЕ получают тип, и шейпы, ранее не исполнявшиеся вовсе, исполняются впервые — находки всплывают как «новые» при НЕИЗМЕНИВШИХСЯ файлах ⇒ разделить оси: **violations** жёстко (0 новых), **warnings** мягко (прирост допустим при ПРЕДЪЯВЛЕННОМ механизме — чей класс стал резолвиться · сколько у него инстансов · существуют ли цели новых находок в том vault, где дельта нулевая). ⚠ Tell: дельта 0 в одном vault и +N в другом, а mtime целей — месячной давности. ⛤ Зеркало §A7: там ранний `return` ОБНУЛИЛ покрытие молча, здесь монтирование его ВКЛЮЧИЛО. ⛤ Floor целиком в строке _(F3.4: vault-my 402→402, tbank 482→485, exodev 613→616; все 6 — два из четырёх `lit__Book`, цели живут в vault-my)_ |
⛔ **§A58, §A59, §A60, §A61, §A62, §A63, §A65, §A66, §A67, §A75, §A77, §A78, §A84, §A85, §A109, §A110 разделов в справочнике НЕ имеют** <!-- floor-edit-ok: §A84/§A85/§A86 — строки индекса, floor в строке --> — их floor целиком в строке индекса (замер 2026-09-13: `grep -c '^## §A'` даёт **64** — §A1-§A56, §A68-§A74 и §A76). `awk`-рецепт по ним вернёт пусто, и это не поломка справочника.
Рецепт адресного чтения (один на корпус) — `rule-carrier-follows-function` §«Читать справочник АДРЕСНО».


## Cross-references

- `sparql-iri-form-pre-verify.md` §Addendum 2026-08-05 — ⛤ **единственный ТРАП-НЕЗАВИСИМЫЙ детектор
  всех дефектов оси: ожидаемое значение, ВЫВЕДЕННОЕ ДО прогона.** Он ловит и ловушки, ещё не
  описанные ни здесь, ни там (замер: 3 домена — SPARQL · файловый замер · shell-харнесс —
  и ловушки трёх разных классов). ⚠ **Ссылка нужна ИМЕННО ЗДЕСЬ:** тот файл назван `sparql-`,
  и автор shell-харнесса в него не грепнет никогда — дискаверабилити-дефект, **предсказанный
  самим правилом** и подтверждённый в 4-й раз 2026-08-07 (harness `dlp-finish-prompt-verify.sh`:
  три дефекта оси подряд, все задокументированы в корпусе, ни один не предотвращён; поймало
  каждый — ожидаемое число красных на мутанта).
- `self-verification.md` — общий принцип verify-before-done
- `dry-run-preview-not-real-output.md` §Addendum 2026-08-03 (сухой режим ЧАСТИЧЕН) — сосед: там сухой режим ЧАСТИЧЕН в
  продовом коде; здесь побочный эффект приходит из ХАРНЕССА и лечится инъекцией, а не флагом
- `test-fixture-realism.md` — fakes must mirror real API contract (related, narrower scope)
- `multi-agent-review-vs-user-validation.md` — почему code-reviewer agent ловит то, что author не ловит

⛤ **Эмпирика** (5 фрагм.) → `~/.claude/rules-archive/integration-test-revert-verify.md`

## Счётчик

⛤ **Числа в ЭТОЙ СТРОКЕ нет намеренно.** `~/dotfiles/scripts/rules-effectiveness.py` берёт первое
`**<N>` из строки `^## Счётчик` и делает его счётом повторных нарушений правила; величина, взятая
из другого окна или из памяти, молча подменила бы метрику корпуса (`~/dotfiles/.claude/rules/self-authored-claim-loses-its-provenance.md`).
Пока ручной счёт не ведётся, величина остаётся **производной**: скрипт считает её сам по `rules/` + `rules-archive/`.
Замер 2026-09-11 для этого правила — **8** (`python3 -I ~/dotfiles/scripts/rules-effectiveness.py`).

⛤ **Независимый замер — другой актор и другое окно:** отметок в ретро-таблицах нарушений правил
за 30 суток — **13** (`python3 -I ~/dotfiles/.claude/bin/rules-label-extract.py`, снимок 2026-09-11).

⚠ Две величины меряют РАЗНОЕ (упоминания внутри текста правила против отметок в чужих ретро) и одна
к другой не сводится — поэтому обе названы с командой, а в заголовок не поднята ни одна.
