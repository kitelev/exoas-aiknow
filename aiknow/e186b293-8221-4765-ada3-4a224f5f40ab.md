---
exo__Asset_uid: e186b293-8221-4765-ada3-4a224f5f40ab
exo__Asset_createdAt: 2026-09-08T10:17:32
exo__Asset_updatedAt: 2026-09-25T00:09:03
exo__Instance_class:
  - "[[39a39239-2a97-483a-ba91-fb01bf5c85f3]]"
exo__Asset_createdBy: "[[4ef3962d-b8a7-42b5-bd28-88ec846f1d13]]"
exo__Asset_label: "Правило: self-satisfying-metric-weak-verifier (полный текст; пилот rules-from-graph, 2026-09-06)"
aliases:
  - "Правило: self-satisfying-metric-weak-verifier (полный текст; пилот rules-from-graph, 2026-09-06)"
  - self-satisfying-metric-weak-verifier
exo__Asset_isDefinedBy: "[[2ce77ac1-3f78-4f64-9dc1-41ea01da1ef0]]"
inbox__ExoAssistantKnowledge_decay: "[[06a5b9f9-da93-4234-883f-3f0cf65b2fba]]"
inbox__ExoAssistantKnowledge_confidence: "[[227def30-f56f-4f5f-936d-f6a81ad73c96]]"
aiKnow__Memory_aboutConcept:
  - "[[4537abf3-69ce-48e7-b3f8-7964626dc98d]]"
  - "[[16a725fd-8ef7-425a-af5b-abdec027b325]]"
  - "[[56abf919-8afb-4665-a196-d55b47644788]]"
  - "[[8b1b210a-4a31-4000-b9e5-e90cc110cc42]]"
  - "[[4b542c1f-7f3c-47d0-a6d4-4e4e6c63efc2]]"
  - "[[9173f32f-a521-4653-a27d-95e61978707d]]"
  - "[[e346e3da-3266-4f28-a1c8-be3753b2fc47]]"
  - "[[14547501-9254-4c6a-a84d-de78bcdfdd4b]]"
  - "[[3616e9c2-b8fb-450b-998d-8e61ceaa23dd]]"
  - "[[986f3774-bda9-4da9-ac6d-1141f82fef5b]]"
  - "[[81ce8585-183e-48e0-bac8-4a69f328286f]]"
  - "[[782e4137-edd0-4dae-a60d-96553efdc371]]"
  - "[[93fa0c24-4a9d-4ffd-a492-3be7526785cb]]"
  - "[[712c4846-d553-468a-9f9a-7ddff958d9c1]]"
---
# Метрика, которую контролирует проверяемый, удовлетворяется тождественно — «слабый верификатор»

## Принцип

Метрика или приёмочный критерий, чьё значение **производит тот же актор, чью работу она оценивает**, не может дать отрицательный сигнал. Она выполняется **по построению**, с первого же прогона, независимо от качества работы. Внешне выглядит как строгая проверка — измеримая, с числом, фальсифицируемая на вид, — и именно поэтому проходит ревью.

Канон Loop Engineering называет это **weak verifier** («лиса охраняет курятник») одним из главных анти-паттернов автономных петель: петля усиливает то, что внутри неё, включая её собственную ошибку измерения.

## Дискриминатор (одна строка)

> **Может ли эта метрика показать ПЛОХОЙ результат, если работа сделана плохо?**

Не «легко ли её выполнить», а **существует ли вообще исход, при котором она красная**. Если не существует — метрика не измеряет ничего.

Второй вариант того же теста: **кто производит число?** Если тот же актор, чью работу оценивают, — метрика замкнута.

⛔ **Он СЛАБЕЕ первого, а не острее (прежняя формулировка «более острый» СНЯТА, §A24).** При НЕЗАВИСИМОМ источнике числа он отвечает «всё в порядке», хотя метрика мертва по АРИФМЕТИКЕ НОРМИРОВКИ (числитель ограничен сверху, знаменатель растёт ⇒ порог недостижим). ⇒ решает всегда ПЕРВЫЙ вариант — «существует ли исход, при котором она красная».

## Tell

- ⚠ Ты доволен тем, что метрика **измерима и выражена числом**, и делаешь из этого вывод, что она
  проверяет. Измеримость — свойство формы, а не силы: спроси про ИСХОД, а не про единицу.
- ⚠ Гейт **ни разу не краснел** за всё время жизни, и это читается как «нарушений нет». Прежде чем
  так читать — предъяви вход, на котором он обязан покраснеть.
- ⚠ Единственная метрика автономной роли — **счётчик её собственных действий** (пингов, прогонов,
  закрытых листов): их производит тот, кого оценивают.
- ⚠ Критерий — **связка** («тесты зелёные и достаточные», «ранги различны и объяснимы»): измеримую
  половину ты закрыл прогоном, а ощущение «пройдено» перенеслось на оценочную.
- ⚠ Приёмка **расщепления** (разбить цикл, раздробить монолит) сформулирована СУММОЙ по частям —
  сумма сохраняется по построению, исхода «улучшилось» у неё нет.
- ⚠ В диагностической строке есть **число, которого нет ни в одном `if`**: посчитано и напечатано ≠
  учтено, а ревьюер читает его как учтённое.
- ⚠ Ожидаемое значение проверки — **ноль**, и канарейки рядом нет: ноль неотличим от мёртвого запроса.

## Тест на самоудовлетворение (одна строка)

⛤ **Ядро дискриминатора продублировано в §Принцип роутера** — оно читается ПЕРЕД написанием
метрики, а не после того, как она соврала. Ниже — тот же текст плюс подсекции-разборы.

> **Может ли эта метрика показать ПЛОХОЙ результат, если работа сделана плохо?**

Не «легко ли её выполнить», а **существует ли вообще исход, при котором она красная**. Если не существует — метрика не измеряет ничего.

Второй вариант того же теста: **кто производит число?** Если тот же актор, чью работу оценивают, — метрика замкнута.

⛔ **Он СЛАБЕЕ первого, а не острее (прежняя формулировка «более острый» СНЯТА, §A24).** При НЕЗАВИСИМОМ источнике числа он отвечает «всё в порядке», хотя метрика мертва по АРИФМЕТИКЕ НОРМИРОВКИ (числитель ограничен сверху, знаменатель растёт ⇒ порог недостижим). ⇒ решает всегда ПЕРВЫЙ вариант — «существует ли исход, при котором она красная».

<!-- Added by /session-retrospective 2026-08-14 — root: ревью 116a3a6/b469c92, ДВА гварда одной сессии, оба предъявлены прогоном. §A17/§A19 владеют классом «область охвата ⊊ поверхность», но НАЗЫВАЮТ его абстрактно («шире фактически используемого», «структура, а не литерал»); дешёвого позитивного теста, который отвечает «а какую форму я пропустил», в ядре не было. -->
### ⛤ ПОЗИТИВНЫЙ тест охвата: пропущенная форма — почти всегда ТА, КОТОРОЙ ПОЛЬЗУЕТСЯ ПРОД РЯДОМ

Тесты выше отвечают «может ли метрика покраснеть». Они не отвечают на соседний вопрос, с которого начинается дыра в **гварде**: «какую форму я не детектирую?» Абстрактный совет «брать структуру, а не литерал» его не закрывает — структуру тоже можно взять слишком узко, и узость видна только на форме, которой ты не подумал.

✅ **Дешёвый тест — одна команда:** выпиши форму, которую твой гвард ДЕТЕКТИРУЕТ, и грепни прод на **соседние** формы того же действия.

```bash
grep -nE '<альтернативные формы: > | : > | mv | echo 0 > | truncate>' <прод-файл> | cat
```

Нашлась форма, которую гвард не видит, **и она уже употребляется в этом же (или соседнем) файле** ⇒ дыра не гипотетическая: следующий автор скопирует **локальную** идиому, а не ту, под которую написан гвард.

- ⛤ **Почему тест работает:** идиомы в файле однородны, потому что их копируют друг у друга. Значит «форма, которой прод пользуется» — не один из многих кандидатов, а **самый вероятный** будущий вход гварда. Гвард, её не видящий, охраняет форму, которая **не будет** использована.
- ⛔ **Вывод сильнее, чем «добавь ещё одну форму»: инвертируй полярность.** Перечисляй **безопасные** формы и считай кандидатом всё прочее — тогда неизвестная форма попадает в детектор **по построению**. Цена — громкая ложная краснота на новой безопасной форме вместо тихого пропуска; ошибка уходит в безопасную сторону.
- ⚠ **Симптом-tell при ревью гварда:** в его условии стоит **одна** конкретная операция (`rm -f`, `[ -e ]`, `== "completed"`), а действие, которое он охраняет, выразимо **несколькими** способами. Спроси не «верна ли эта форма», а **«сколько форм у этого действия и сколько я вижу»**.

<!-- Added by /session-retrospective 2026-08-14 — root: ALP 0c629415. Фикс `[A-Z]+` → `[A-Z][A-Z0-9]*` был подписан «якорь на ФОРМУ имени, а не на перечень встреченных форм» СО ССЫЛКОЙ на эту самую секцию — и остался перечнем: `L2b`/`M1a`/`AA2x`/`Q_7`/`l2` пропускались молча. Поймал `code-reviewer` замером, не я. -->
- ⛔ **ПОЧИНКА перечня перечнем: расширение класса на ОДИН шаг выглядит структурой, особенно когда ты цитируешь это правило в том же коммите.** Класс символов, набор расширений, список имён — любое **положительное** перечисление остаётся перечислением, сколько его ни удлиняй; а ссылка на «брать структуру» в комментарии рядом создаёт **ложную уверенность, что проверка уже сделана** (форма №6 в чистом виде: вывод «надо расширить» верен, обоснование «это структура» ложно).
  - ✅ **Дискриминатор стоит одну минуту и решает: НАЗОВИ СЛЕДУЮЩУЮ ФОРМУ, которую твой якорь всё ещё пропускает.** Смог назвать (`L2b` после `L2`) ⇒ это перечень, инверсия не сделана. Не можешь назвать **ни одной** — потому что принимается всё, кроме явно безопасного, — ⇒ полярность действительно инвертирована.
  - ⚠ **Цитирование правила НЕ засчитывается за его исполнение.** Проверять надо замером на именах/входах следующего поколения, а не соответствием формулировки комментария тексту правила.

<!-- Added by /session-retrospective 2026-08-27 (vteam-5c888d98, StageStandards) — root: инверсия полярности была НЕПРИМЕНИМА (имя сессии произвольно), и правило оставляло только «расширить перечень». -->
- ⛤ **ТРЕТИЙ выход, когда инверсия НЕПРИМЕНИМА: сменить ОСЬ НАБЛЮДЕНИЯ.** Инверсия требует перечислимости безопасных форм; при произвольных (имя сессии, свободный текст) единственным ходом кажется «добавить ещё форму». Спроси: **какую ДРУГУЮ величину даёт тот же вызов** и не структурна ли она.
  - ✅ Типовой случай — guard судит по **тексту ошибки** при доступном рядом **коде возврата**: `case "$V" in *"not found"*)` видит одну форму недостижимости бинаря, а `rc=127` — **обе** (`command not found` и `No such file or directory`) и не коллидирует с «бинарь есть» (0 или 1).
  - ⛔ Текстовый перечень **не снимать** — он ловит соседний класс (`rc=0` + пустой вывод). Две оси, не замена.
  - ⚠ **Tell:** пишешь очередную альтернативу в `case`, а строкой выше стоит невостребованный `$?`.
  - ⛤ **ТРЕТИЙ носитель того же класса — КАНАРЕЙКА ДОСТАВКИ у драйвера мутантов**, и она врёт
    в **ложно-мёртвую** сторону: судит «доехала ли подмена» по совпадению списков красных осей
    (`red_re`), а у харнесса может быть СВОЙ способ отвергнуть заглушку, чья строка под шаблон
    не подходит ⇒ оба списка пусты ⇒ равны ⇒ «подмена не доезжает» на ИСПРАВНОЙ доставке.
    ⚠ **Tell:** драйвер обвиняет доставку, а `env_var` спеки и чтение его в харнессе совпадают —
    значит врёт не доставка, а её измеритель. ✅ Floor тот же: судить по **наблюдаемому исходу**
    (`red` ∪ `rc`), а не перечислять формы отказа — при настоящей недоставке харнесс гоняет
    боевой предмет, поэтому ОБА поля идентичны, и расширение условия канарейку не ослабляет.
    ⛤ Empirical 2026-08-31 (`rules-floor-budget-verify`): контроль rc=0/PASS=3, проба rc=2 —
    различитель БЫЛ и не читался; до фикса лечили частные формы (пустой файл, гвард `[ -x ]`),
    то есть чинили перечень перечнем. Флип: исправный spec → rc=0, тот же с мёртвым `env_var`
    → rc=2 BROKEN (dotfiles `3f3c567`).
  <!-- Added by /session-retrospective 2026-08-27 (vteam-52215d7f, StageStandards) — root: три этапа подряд отвергли ось доводом «вакуумна», рассмотрев только поведенческий класс. -->
  - ⛔ **ВТОРАЯ форма — вывод «оси НЕ МОЖЕТ БЫТЬ».** Предикаты потребителей к предмету нечувствительны ⇒ поведенческая ось вакуумна ⇒ пишешь «заглушка хуже отсутствующей» и оставляешь инвариант без сторожа. Довод верен для ПОВЕДЕНЧЕСКОЙ оси и ложен как обобщение: **структурная** (скан ТЕКСТА артефакта + ратчет-база + мутант на различимость) краснеть умеет и поведения потребителя не требует. ⚠ **Tell:** объясняешь ОТСУТСТВИЕ оси её вакуумностью — спроси, какой КЛАСС рассматривал и нет ли структурного гварда в ЭТОМ ЖЕ файле как прецедента. ⛤ Empirical 2026-08-27 (`52215d7f`): дефолт отвергнут тремя этапами; через ~2 ч в тот же файл лёг ратчет `U1/U2` — структурная ось. Мутант: снятие не краснит ни одной из 101 оси ⇒ остаток жив, тикет `6632a011`.
  <!-- Added by /session-retrospective 2026-08-27 (vteam-6632a011, StageStandards) — root: остаток, названный строкой выше, закрыт; и его закрытие вскрыло floor, которого в корпусе не было (греп по двум ключам — 0 при живой канарейке). -->
  - ✅ **ОСТАТОК ЗАКРЫТ 2026-08-27 (`6632a011`) — floor подтверждён ИСПОЛНЕНИЕМ, а не только рассуждением:** структурная ось `U3`/`U4` отгружена (`_defscan` — скан текста харнесса; dotfiles `bf39dcf`, дельта `PASS` 101 → 103). Замер этапа стандартов на **опубликованном** блобе: без мутанта `rc=0` «без дефолта 0», с мутантом (снят РОВНО дефолт) — `rc=1` «без дефолта 1» + пара названа поимённо. ⛤ Хвост строки выше («остаток жив») **ложен с 2026-08-27** и сохранён как история: его замер верен про своё время.
  - ⛔ **НО структурная ось ТОЖДЕСТВЕННО-ЗЕЛЕНА, если её предикат не исключает КОММЕНТАРИИ предмета — а комментарий там почти всегда ЕСТЬ.** Сканер ищет литерал в блоке; тот же литерал стоит в шапке блока, где ПРЕДЫДУЩИЙ автор объяснил, зачем строка нужна. Замер: в блоке `irun` литерал встречался **дважды** (комментарий + код), после снятия кода остаётся 1 ⇒ наивный предикат ЗЕЛЁН **на дыре**, строгий (комментарии и heredoc исключены) — КРАСЕН.
    - ⛤ **Симметрия, делающая класс устойчивым:** чем лучше задокументирован дискриминатор, тем вероятнее гвард на него слеп — комментарий и пишется ровно потому, что строка неочевидна.
    - ✅ **Floor: предикат = «строка НЕ комментарий ∧ НЕ внутри heredoc ∧ содержит `VAR=`»**; якорь МУТАНТА — **целая** env-строка блока (`count == 1` по файлу), `assert n == 1` **в пределах строки**: по блоку `n = 2`, и падение ассерта читается как «мутация не легла» вместо «неверный якорь».
    - ⚠ **Tell до прогона:** предмет твоего сканера — литерал, который в том же файле объясняется словами. Посчитай его вхождения **в блоке**: `> 1` ⇒ наивный предикат уже тождественно-зелён.
<!-- Added by /session-retrospective 2026-08-29 (vteam-1f963144) — root: предикат аудита сматчил литерал в АРГУМЕНТЕ мутатора; греп корпуса «литерал-аргумент» → 0. -->
    - ⛔ **ТРЕТИЙ носитель цитаты, и стриппер комментариев его НЕ снимает ПО ПОСТРОЕНИЮ: СТРОКОВЫЙ ЛИТЕРАЛ-АРГУМЕНТ** (якорь мутации, текст сообщения, регэксп в переменной). Floor выше исключает комментарий и heredoc — то есть носители, которые кодом **не являются**; аргумент кодом является, поэтому проходит любой `code_only()`.
      - ⚠ **Tell:** совпадение стоит **внутри кавычек как аргумент** другой команды (`mutate … "if rc == 0:"`), а не самостоятельным выражением. Смотреть не на литерал, а на его синтаксическую позицию.
      - ✅ **Floor: якорить на ПОЗИЦИЮ, а не на подстроку** — предмет обязан начинать строку (после отступа) либо стоять слева от `=`/`(`; исключать аргументы мета-функций (`mutate`, `expect`, `assert_*`).
      - ⛤ Empirical 2026-08-29 (`ce5a29ef`): `RE_NEEDS_CANARY` (`== 0`) сматчил якорь мутанта `vteam-burn-verify.sh:553` ⇒ ложное «нужна канарейка» у харнесса, где осей-по-отсутствию нет вовсе.

**Empirical 2026-08-14** (ревью `116a3a6`, оба найдены `code-reviewer`, оба предъявлены прогоном на копиях прода): ось негативного контроля судила по `[ -e "$p" ]` ⇒ `echo 0 > "$p"` проходил зелёным — а именно `echo 0 >` прод чистит эти же переменные **двумя функциями выше**. Дискриминатор применимости другой оси искал литерально `rm -f "$NOTIFIED"` и видел **1 форму из 6**; невидимой была `rm -f "$STATE_DIR/notified"` — та, которой прод стирает этот файл **в том же файле**, строкой 638. Обе дыры закрыты инверсией полярности + проверкой содержимого.

<!-- Added by /session-retrospective 2026-08-19 — root: ревью PR #4089 нашло HIGH в гварде, который я добавил ПО ПРЕДПИСАНИЮ этой же секции. Позитивный пол был, но КОНСТАНТОЙ (40) против измеримой популяции (114) — 65 % обвала принимались молча. Секция учит ДОБАВИТЬ пол и молчит о том, из чего брать его ЧИСЛО. -->
### ⛔ ЦЕНА непокрытой половины измерена: 4 дефекта из 10 — и все ЖИЛИ незамеченными

Свип/раннер, охватывающий один каталог из нескольких, обычно защищают доводом «остальное
редко ломается». Замер 2026-08-21 его опровергает: раннер verify-харнессов сканировал
`~/.claude/bin` (17 наборов) и не видел `~/dotfiles/scripts` (11 наборов). Ручной прогон
непокрытой половины дал **4 красных из 10 живых**:

| дефект | почему жил |
|---|---|
| **3 харнесса не импортировались** — fail-loud `BOTLOG_VAULT` (правка прода) уронил их | blast-radius мерился грепом **прод**-импортёров («ровно один»); харнессы в популяцию не вошли |
| **ось изоляции тождественно-красная** — считала штатное поле прода своим следом | краснела с первого боевого хода бота, но красноту никто не видел |
| **данные бота 0644** — писатель без `chmod` перед `os.replace` | приватный класс данных читаем всеми; ratchet на эту форму существует и не гонялся |

⛤ **Вывод сильнее, чем «расширь охват»: непокрытая половина накапливает дефекты ИМЕННО
потому, что непокрыта.** Покрытая половина зелена не потому, что качественнее, а потому что
её краснота кому-то показывается. ⇒ доля дефектов в непокрытом **выше**, чем в покрытом, и
довод «там редко ломается» перевёрнут: там ломается так же часто, просто молча.

⚠ **Симптом-tell при ревью охвата:** предмет свипа задан ОДНИМ каталогом, а артефакты того
же класса лежат в нескольких (`grep -rl` по классу имён даёт больше одного корня). Проверка
стоит одну команду и отвечает числом, а не мнением.

### ⛔ Позитивный пол КОНСТАНТОЙ оставляет слепую зону ровно между константой и измеримой популяцией — выводи число, а не назначай

Секция выше предписывает **позитивное** доказательство охвата — и на этом останавливается. Отказ следующего порядка: пол добавлен, он позитивный, всё сделано «по правилу» — а число взято **с запасом**, потому что точное казалось хрупким. Тогда зазор между константой `K` и фактической популяцией `N` есть **молча принимаемый обвал**: любой прогон, покрывший `K ≤ x < N`, объявляется полноценным.

- ⛔ **Дефект тише отсутствующего пола, потому что пол ЕСТЬ и он «сработал бы».** Ревью не спрашивает «а есть ли охват?» — охват предъявлен числом; спросить надо «а откуда это число?».
- ⚠ **Симптом-tell, ловится за один вопрос к себе:** ты можешь назвать `N` **замером** (у меня — 114 файлов на диске), но записал `K` заметно меньше. Значит зазор `N−K` ты принял, не назвав.
- ✅ **Floor: выводить ожидание из ТОЙ ЖЕ структуры, которой пользуется прогон.** Часто оно **точное**, а не приблизительное: если конфиг объявляет `include: <всё под каталогом>`, то каждый файл — корень программы и обязан быть в выдаче ⇒ `на диске == в листинге` тождественно. Точный вывод снимает и вторую беду — **ложную краснотỳ при законном сжатии** предмета: выведенный пол движется вместе с ним, константный — нет.
- ⛔ **У ДЕРИВЕРА своя ветка отказа, и без неё лекарство хуже болезни:** источник, из которого выводится ожидание, может быть сломан ровно тем же событием (каталог исчез/переименован) ⇒ выведет `0` ⇒ пол разоружится **именно тогда, когда предмет пропал**. `expected === 0` — отдельный терминальный исход с сообщением «сломан ДЕРИВЕР», а не «предмет мал».
- ⛤ **Обе стороны обязаны считать ОДНО множество.** Исключение, применённое на одной стороне (у меня `.d.ts` вычитался из дискового счёта, но принимался фильтром листинга), делает пол мягче **ровно на его размер** — дефект того же класса, только внесённый самой починкой.

⛤ **Отличать от §A11** (там ожидание бралось из **канарейки**, то есть из соседней проверки): здесь источник законный и независимый — проблема в том, что число **назначено**, а не **выведено**.

**Empirical 2026-08-19** (PR #4089, `kitelev/exocortex`): `MIN_COMPILED_SRC_FILES = 40` против 114. Ревьюер предъявил на стабе, что `45 файлов + 0 диагностик` даёт вывод **байт-идентичный** `114 + 0` («база устарела, запусти `--update`») — то есть подчинение сообщению пишет **пустую** базу, пока 69 из 114 файлов покинули программу. И это **ровно тот вход**, который ловил снятый в том же коммите zero-guard ⇒ добавление «правильного» позитивного пола было **регрессией силы гейта**. Флип после выведения: `rc=1 → rc=2`.

### ⛔ ОБЪЯВЛЕНИЕ инверсии в теле коммита НЕ доказывает её применения ко всем слоям: считай, из СКОЛЬКИХ перечислений состоит критерий

Тест выше («назови следующую форму») адресован **автору** гварда. Зеркальный, ревью-временной вопрос:
коммит **пишет в своём теле**, что инвертировал полярность («перечисляем безопасное, неизвестное — в
счётчик по построению»), — и это утверждение читается как выполненное, потому что оно осознанное,
сформулировано механизмом и часто подкреплено прогоном.

⛤ **Но критерий почти никогда не состоит из ОДНОГО перечисления.** Типовая связка: перечисление
**предмета** (какие каталоги / файлы / переменные смотрим) + перечисление **операций** над ним
(что считаем безопасным) + перечисление **написаний** имени (как предмет назван в коде). Инверсия
применяется к тому слою, который болел, а соседние остаются белым списком — и остаточный отказ
**того же класса** доживает до следующего ревью.

- ✅ **Floor ревьюеру: прочитай в теле коммита фразу об инверсии и спроси «сколько перечислений у
  этого критерия?» — затем проверь КАЖДОЕ.** Дискриминатор дешёвый: перечисление опознаётся по
  литеральному списку (`DIRS = [...]`, `case "$1" in a|b)`, `$0 ~ /\$VAR/`), а не по объёму кода.
- ⚠ **Симптом-tell:** тело коммита само называет число («реализован ДВУМЯ перечислениями, и
  промахнулись ОБА»), а дифф трогает одно. Названное автором число — готовый чек-лист, и расхождение
  видно без анализа.
- ⛔ **Самая тихая пара — ЗНАЧЕНИЯ выведены из источника, а КЛЮЧИ захардкожены.** Механизм,
  собирающий данные из живого источника, обычно **гордится** этим в докстринге («перечень
  ВЫВЕДЕН из самой политики, а не перечислен в харнессе — иначе разъехался бы МОЛЧА»), и
  ровно эта гордость глушит подозрение: читатель видит слово «выведен» и не спрашивает,
  **что именно** выведено. А выведены **значения**; **предикаты**, по которым их собирают,
  остаются литеральной альтернацией (`^flow__StatusModel_(?:statusProperty|activeStatus):`).
  Добавили в модель третий предикат — сбор его не увидел, и разъезд произошёл ровно тем
  способом, от которого докстринг обещал защиту.
  - ⚠ **Симптом-tell, ловится ЧТЕНИЕМ ОДНОЙ СТРОКИ:** в коде есть фраза «выведено из
    источника, а не перечислено» — и **в той же строке** литеральный список ключей/полей,
    по которым идёт выведение. Гордость и дефект стоят рядом.
  - ⛤ **Цена ассиметрична и маскируется:** отказ приходит не туда, где корень. Fail-closed
    гейт, требующий разрешимости нового предиката, роняет ВЕСЬ набор (`rc=3`), причём оси,
    ожидающие `rc=3`, остаются **тождественно-зелёными** ⇒ набор одновременно краснеет и
    зеленеет неверно, и диагностика уходит в движок вместо харнесса.
  - ✅ **Floor: добавляя предикат политики в потребителя, добавь его в СБОРЩИК тем же заходом**
    — и проверь **числом** (сколько терминов было / стало), а не чтением.

  **Empirical 2026-08-20** (`vteam-select-verify.py`, задача `165adee3`): `policy_terms()` собирал
  стабы по `statusProperty|activeStatus`; добавленный в политику `boardStatus` стабов не получал ⇒
  fail-closed резолюция падала на каждом прогоне, контроль красный целиком (29 осей), а S4/S5/A8
  зелены. Фикс — **одно слово** в альтернации: терминов 4 → 6 (`+Backlog, +Draft`), контроль
  `rc=0`, мутант «переименовать board-статус» → `rc=3 BROKEN`.

- ⛤ **Особый под-случай — сбор ВХОДА против логики фильтра.** Инверсия фильтра (что пропускаем)
  видна при чтении кода; инверсия матчера (что вообще попало на вход) — нет, потому что пропущенное
  не доходит до фильтра и не оставляет следа. ⇒ спрашивать надо не «верна ли логика», а **«что
  физически не попадает на вход этого гварда»**.

#### ⛔ Считать перечисления НЕДОСТАТОЧНО: у цитируемой модели есть ещё и НАПРАВЛЕНИЯ, и половина берётся тише

Floor выше («сколько перечислений у этого критерия?») ловит **ширину** охвата. Сегодняшний
рецидив прошёл сквозь него нетронутым: перечисление было **ровно одно** и покрыто полностью — а
недобрана оказалась **вторая половина модели**, на которую я сам же и сослался.

⛤ Форма: ты берёшь за образец существующий механизм («оба сиблинга ключуются на множестве пар»),
реализуешь его **основное** направление и превращаешь встречное в **уведомление**. Тело коммита
при этом честно называет сиблингов моделью — и именно это делает дефект невидимым: цитата читается
как доказательство, что модель перенесена.

- ⛔ **Уведомление вместо отказа — не «мягче», а ДРУГАЯ гарантия.** Ратчет, который на исчезнувшей
  записи только сообщает, перестаёт быть ратчетом: база, которую никто не подрезает, превращается
  в **лицензию**. Замер: файл с 17 вхождениями остаётся «17» навсегда ⇒ вычистив 16, можно молча
  добавить 16 новых. Множество пар **сужает** окно подмены с общерепного до пофайлового, а закрывает
  его именно встречный отказ.
- ⚠ **Симптом-tell, ловится ЧТЕНИЕМ ОБРАЗЦА, а не своего кода:** открой цитируемого сиблинга и
  посчитай, сколько **исходов** он различает. Их два, а у тебя один ⇒ модель перенесена наполовину.
  Заголовки таких механизмов обычно объясняют вторую половину прямым текстом — то есть проверка
  стоит один `grep` по образцу, который ты и так назвал.
- ⛤ **Дискриминатор формулируется как вопрос к ПАРЕ состояний:** «какие два разных факта мой
  критерий сводит к одному выводу?» У меня — «долг вычищен» и «файл ушёл из корпуса»: одинаковая
  строка, одинаковый код возврата, и различить их мог только человек, вручную перегенерировавший базу.
- ⛔ **Половинная реализация РОЖДАЕТ следующий дефект, а не просто оставляет пробел.** Достроив
  встречный отказ, я обнаружил, что соседний тест изолировался **мёртвыми** переменными: под
  односторонним поведением это было безвредно, под двусторонним — все 56 записей базы читаются
  как «исчезли», и каждая зелёная ось того файла краснеет по причине, которой не проверяет.
  ⇒ добирая вторую половину, перечисли **потребителей** первой.

**Empirical 2026-08-19** (PR #4092, `kitelev/exocortex`): пара-множественная база принята у двух
сиблингов, встречный отказ — нет; `check-cli-types.mjs:416` и `check-test-types.mjs:368` делают
`process.exit(1)`, мой гвард печатал уведомление и выходил 0. Найдено адверсарным ревью — **четвёртый
раунд подряд один класс**, и предыдущие три floor'а (охват · перечисления · инверсия полярности)
его не покрывали, потому что все три меряют ШИРИНУ, а не ЧИСЛО ИСХОДОВ.

**Empirical 2026-08-14** (ревью-пакет `8486ba03`: шесть коммитов `~/dotfiles`, шесть раундов
`code-reviewer` по одному): **все 5 HIGH — один класс, 5/5**. `6246376` объявил sibling-sweep и
охватил 3 порога из 16 · `f84de71` написал «промахнулись ОБА перечисления» и инвертировал одно
(имена), оставив каталоги белым списком + нерекурсивным обходом · тот же `f84de71` объявил промах
«громким», не заведя слушателя (краснота двое суток без читателя) · `0625557` инвертировал список
операций, оставив перечисление ДВУХ написаний имени файла (4 формы teardown из 6 уходили тихим
пропуском) · он же оставил силу маркера на НЕЗАПИСАННОМ инварианте сида фикстуры. ⛤ Симметрия видна
**только пакетом**: пер-коммитное ревью каждый раз находило «частный недосмотр», и лишь пять
вердиктов рядом показали, что это устойчивая форма отказа, а не пять совпадений.

## Типичные формы (все встречались)

| Форма | Почему тождественна |
|---|---|
| «Доля работ, выбранных **по рангу**, который сам и проставил» | Ранжирующий ранжирует, потребитель берёт верх списка → 100% всегда |
| «Число закрытых черновиков» как метрика **качества** разбора | Агрессивное закрытие улучшает метрику; корректность не измеряется |
| «Все N элементов обработаны» без независимого перечисления N | Обработавший сам и считает знаменатель |
| «Тест проходит» там, где тест писал тот же, кто чинил, и revert-verify не делался | Тест может проходить обоими способами (`integration-test-revert-verify`) |
| «Инструкция соблюдена» по самоотчёту агента | Отчёт производит тот, кого проверяют (`verify-before-assert` §A2 (PROGRESS-ping ≠ authoritative)) |

<!-- Added by /session-retrospective 2026-08-12 — root: у Олиного бота `rc` от многорепного `exosync pull` = 1 в 40 прогонах из 40 (штатно: двухпроходная схема, перекрёстно-приватные репо). Ядро правила описывало только тождественно-ЗЕЛЁНУЮ сторону; корпус-греп `тождественно-красн` дал 0. -->
### ⛔ АГРЕГАТ над пустым паттерном ФАБРИКУЕТ строку — и канарейка бессильна ПО ПОСТРОЕНИЮ

Канарейка (§A7) лечит ложный ноль: «пусто ⇒ проверь, жив ли запрос». Она **не применима**, когда
запрос возвращает **непустой** результат, будучи мёртвым. Так ведёт себя агрегат: замер
2026-08-15 на этом движке — запрос по **несуществующему** предикату с `GROUP BY` вернул
**одну строку `n=0`**, без агрегата — 0 строк. (По SPARQL 1.1 при `GROUP BY` и отсутствии решений
результат обязан быть пустым.)

- ⛔ **Следствие: метрика «не превышен ли лимит» тождественно зелёная при сломанном запросе.**
  `SELECT ?stage ?limit (COUNT(?t) AS ?wip) … GROUP BY ?stage ?limit` на опечатке в предикате
  отдаёт `wip=0`, что читается как «свободно». Дисциплина «ноль засчитывать только с канарейкой»
  сюда не дотягивается — результат **не** ноль строк.
- ✅ **Floor: `HAVING (COUNT(?x) > 0)` в САМОМ запросе** — фабрикованная строка не проходит,
  живая выдача не задета. Одна строка, гвард механический, а не дисциплина потребителя.
- ⛔ **`FILTER(BOUND(?limit))` НЕ РАБОТАЕТ — проверено флипом, и это опровергает прежнюю
  редакцию этого floor'а** («проверять связанность group-переменных»): на мёртвом 2-hop она
  по-прежнему отдаёт `1 строка {"wip":"0"}`, то есть гвард тождественно пропускает. Замер
  2026-08-16 на живых данных: без гварда 1/114 · `FILTER(BOUND)` 1/— · `HAVING` **0/114**.
  ⛤ Урок про класс: floor был выведен из механизма («несвязанная переменная = мёртвый запрос»)
  и звучал убедительно — но не был **прогнан**. Механизм назван верно, лекарство от него не
  следует; проверять надо исполнением, а не правдоподобием (`integration-test-revert-verify`).
- ⚠ **Симптом-tell:** агрегат вернул ровно одну строку, все переменные группировки пусты, а
  счётчик — ноль. Это подпись сломанного паттерна, а не пустой популяции.
- ⛤ Родственно §A24 (порог недостижим по арифметике): там метрика не может покраснеть из-за
  области значений, здесь — из-за того, что движок **дорисовывает** строку, которой в данных нет.

<!-- Added by /session-retrospective 2026-09-01 (vteam-45481887, StageStandards) — root: канарейка «выдуманный uid → 0» взята как `ffffffff`; hex-строка в hex-корпусе даёт 5 коммитов ⇒ ожидаемый ноль недостижим. §АГРЕГАТ выше — про бессилие канарейки от МЕХАНИЗМА, здесь — от ВЫБОРА КЛЮЧА. -->
### ⛔ У канарейки ДВА направления, и НЕГАТИВНАЯ вырождается ВЫБОРОМ КЛЮЧА

Корпус документирует **позитивную** канарейку (§A7, §A10): «предикат жив» — заведомо СУЩЕСТВУЮЩЕЕ → ожидаем непусто. У **негативной** («предикат РАЗЛИЧАЕТ»: заведомо ОТСУТСТВУЮЩЕЕ → ожидаем 0) своя вырожденность: ключ из **алфавита предмета** отсутствовать не может ⇒ ноль недостижим, вердикт тождественно-положителен.

- ⚠ **Tell, ловится ДО прогона:** твой «заведомо отсутствующий» ключ построен из тех же символов, что предмет — hex для UUID-корпуса, общее слово для прозы, `XX`/`TODO` для шаблона.
- ✅ **Floor: ключ негативной канарейки — ВНЕ алфавита предмета** (`zzzzzzzz` для hex — ⛔ но ЭТОТ литерал приведён как ФОРМА, а не как ключ: для грепа ПО КОРПУСУ он уже мёртв, см. ⛔-строку ниже) и **прогнан**: непустой ответ = вырождена канарейка, а не богат корпус.
  - ⛔ **Ключ, ПРИВЕДЁННЫЙ В КОРПУСЕ как пример, перестаёт быть вне-алфавитным для грепа ПО КОРПУСУ: floor подрывает себя АКТОМ ЗАПИСИ.** Замер 2026-09-01: строка выше вписана коммитом `20a9716`, и через часы `git log -S'zzzzzzzz' -- .claude/` даёт **1** — сам этот коммит ⇒ floor велит прочитать ИСПРАВНУЮ канарейку как вырожденную.
  - ⚠ **Tell:** совпадений 1-2 и ВСЕ в `rules/`, а не в предмете ⇒ смотреть, ГДЕ совпало, а не только сколько.
  - ✅ **Для грепа ПО КОРПУСУ ключ брать РАЗОВЫЙ** (нигде не публикуемый) либо отсеивать файл самого floor'а.
  - ⛔ **Традиционные «фиктивные» hex (`deadbeef`, `cafebabe`, `ffffffff`) ЗАНЯТЫ ПО ПОСТРОЕНИЮ — их берут ХАРНЕССЫ как заведомо-несуществующие uid** ⇒ самый напрашивающийся «выдуманный» ключ и есть худший. ⚠ Tell: ключ придуман «на ходу» и **узнаваем как шутка**; узнаваем ⇒ его уже придумал автор харнесса. ✅ Брать НЕпроизносимый **и РАЗОВЫЙ** — ⛔ не копировать пример ИЗ ЭТОГО ПРАВИЛА: строкой выше сказано, что опубликованный ключ мёртв, а сам пример публикацией себя и убил (замер 2026-09-07, `61a72179`: `-S'qwxvpltk'` по `.claude/` даёт **1** — коммит, вписавший эту строку). ⚠ Tell: канарейка «заведомо отсутствующего» вернула 1-2, и всё в `rules/`. ✅ Генерировать: `openssl rand -hex 4`. _(2026-09-06, `6440265e`: `deadbeef` живёт в 4 файлах — `vteam-dispatch-verify.sh`, `rules-graph-carry-verify.py`, `req-active-preflip-guard.test.sh`, `founder-notify.sh`; `-S` по нему дал **4** и **9** при вердикте «ключ отсутствует», то есть исправный предикат читался как шумящий.)_
- ⛤ Тот же дефект у ДИСКРИМИНАТОРА: плейсхолдер (`session_XX`) вместо идентификатора даёт «чужой» тождественно — и вердикт случайно верен, что скрывает дефект.

**Empirical 2026-09-01** (`45481887`): `git log -S'ffffffff' -- .claude/` → **5**, `zzzzzzzz` → **0**.

### ⛔ ЗЕРКАЛО: тождественно-КРАСНЫЙ индикатор — тоже слабый верификатор, и он тише зелёного

Ядро выше — про метрику, которая **не может покраснеть**. Обратная форма: индикатор, который **не может позеленеть**, — составной код возврата, где ОДИН штатно-недоступный элемент роняет весь результат (многорепный синк с перекрёстными правами, батч с одним заведомо-пропускаемым элементом, агрегат по разнородным источникам).

- ⛔ **Опаснее зелёного зеркала: красное не расследуют, к нему ПРИВЫКАЮТ.** Тождественно-зелёное усыпляет, но хотя бы читается; тождественно-красное перестают читать вовсе — и настоящая авария выглядит в логе **идентично** норме. То есть индикатор не просто бесполезен, он **занимает место** рабочего.
- ⚠ **Дискриминатор — доля, а не значение:** `rc=1` в **40 из 40** прогонов ≠ «сорок аварий», это свойство конфигурации. Сравни с соседом той же природы: у однопроходных ботов было 195/197 и 21/22 `rc=0` — там индикатор информативен, и различает их именно распределение.
- ✅ **Floor: логировать СОСТАВ исхода, а не только агрегат** — имена упавших элементов и счётчики. Тогда сигналом становится **изменение набора**, а не сам факт красноты, и норма отличима от аварии без чтения кода.
  <!-- Added by /session-retrospective 2026-08-31 (claude-bloat латч) — root: тот же СОСТАВ, взятый ключом дедупа, требует третьего решения по каждой величине, и оба крайних дают тождественно-мёртвый латч ЛИБО слепой канал. -->
  - ⛤ **Тот же СОСТАВ, взятый КЛЮЧОМ ЛАТЧА, требует РЕШЕНИЯ ПО КАЖДОЙ ВЕЛИЧИНЕ — и оба крайних решения дефектны.** Оставить всё как есть ⇒ подпись меняется каждым тиком (`etime`, `rss` текут сами) и латч **не срабатывает ни разу**; выбросить все меняющиеся ⇒ канал становится **слепым** к росту предмета. Решений три, не два: **оставить** (стабильное) · **выбросить** (самотекущее без смысла) · ⛤ **КВАНТОВАТЬ** (величина несёт смысл, но дрожит).
  - ✅ **Дискриминатор — «какое изменение этой величины обязано ПРОБИТЬ латч?»** Ответ «никакое» ⇒ выбросить; «скачок на порядок» ⇒ квантовать до этого порядка (footprint → ГБ: 5→9 ГБ пробивает, 5413→5418 МБ нет; процент → десятки: 39↔38 не тревога, 39→12 тревога).
  - ⚠ **Симптом-tell тождественно-мёртвого латча:** повтор уходит каждый тик, а подписи **все разные**. Симптом слепого — предмет вырос на порядок, а канал молчит. Оба ловятся сверкой ДВУХ соседних подписей, а не чтением кода.
  - ⛤ **Латч взводит тот, кто знает ИСХОД доставки** (`ok=True`), а не вход в отправку: взвод до отправки подавил бы повтор после сетевого сбоя — то есть тревога терялась бы ровно когда канал барахлит (`waiting-decision-ping-before-selector` §A3). Пропуск писать в лог вслух: молчание неотличимо от «находок не было».
  - **Empirical 2026-08-31:** раздутый MCP (5413 МБ footprint при 10 МБ RSS) прожил **11 дней** при тике раз в 60 с ⇒ один и тот же текст ушёл в Telegram десятки тысяч раз. Точка вставки — общая функция отправки, а не конкретный ярус: тогда дедуп получают все ярусы разом.
- ⛔ **И что КАЖДЫЙ СЧЁТЧИК вердикт-строки попадает в УВЕДОМЛЕНИЕ.** Раннер печатал `мутанты-красные=N` в лог и 🧬-строки в stdout, а письмо фаундеру собирал ТОЛЬКО из `red`; ветка «контроль зелен, мутанты красные» выходила `exit 1` ДО отправки ⇒ мёртвый мутант (M3, якорь протух 02.09) три дня лежал в логе `rc=2` и НИ РАЗУ не был доставлен — при том что класс намеренно сделан «счётным». ⚠ Tell: в итоговой строке лога есть счётчик, имени которого нет в тексте письма ⇒ эта категория нема для человека. ✅ Floor: сообщение собирается ОДНОЙ функцией из ВСЕХ ненулевых категорий, и её зовут все терминальные ветки; ось — фикстура с красным мутантом под `SCHEDULED` ⇒ письмо содержит 🧬. _(2026-09-07, `verify-harness-runner`)_
- ⛔ **Проверь заодно, что причина вообще попадает в лог.** Смежный дефект: в запись кладут «последнюю строку вывода», а инструмент печатает последней **строку таймингов/итога** — то есть ошибка выше неё не сохраняется **никогда**. Красивый агрегат в логе при скрытой причине — признак этой пары.
<!-- Added by /session-retrospective 2026-08-30 — root: предикат находки мерил ПУСТОТУ результата вместо КОДА ВОЗВРАТА, а rc лежал в той же записи и не читался; ложная тревога скрывала настоящую. -->
- ⛔ **ЧАСТНЫЙ СЛУЧАЙ, у которого дискриминатор лежит В ТОЙ ЖЕ ЗАПИСИ: предикат меряет ПУСТОТУ РЕЗУЛЬТАТА («ничего не отправлено», «0 строк обработано», «нет изменений»), тогда как сущность — КОД ВОЗВРАТА («не смог»).** Пустой результат у исправного механизма, которому нечего делать, — норма, поэтому серия растёт **вечно по построению**, и floor «мерить долю» её не спасает: доля будет 100 % **честно**.
  - ⚠ **Симптом-tell, ловится ОДНИМ грепом по детектору:** `grep -c 'rc\|returncode\|exit' <детектор>` = **0**, при том что писатель код возврата **пишет**. Признак сущности рядом, а читается признак формы.
  - ⛔ **Цена не «лишний шум», а СОКРЫТИЕ НАСТОЯЩЕГО ДЕФЕКТА.** Замер 2026-08-30: два бота с побайтно одинаковыми внешними признаками (пустой расклад, растущая серия) — у одного `rc=0` во всех 77 записях (исправен, отправлять нечего), у другого `rc=1` во всех 4 (push падал, 14 пинов в общем репо). Один предикат смешивал их, и настоящий тонул среди ложных **месяцами** — его не искали, потому что «эта тревога всегда горит».
  - ✅ **Floor: тревожить по ОТКАЗУ, а пустой результат отправлять в журнал с явной формулировкой** («серия N при rc=0 у всех — нечего отправлять»). Тогда норма отличима от аварии без чтения кода, а счётчик остаётся видимым.
  - ⛔ **Негативный контроль обязателен и отдельный НА КАЖДЫЙ call-site:** «серия при rc=0 ⇒ НЕ тревога». Без него «ужесточён предикат» неотличимо от «сломан», а общий мутант «снять проверку rc везде» покрасит всё сразу и не покажет, что каждая ось запирает свою точку.
<!-- Added by /session-retrospective 2026-08-28 (vteam-033dfb7f) — root: аудит охвата отдавал `rc=2` BROKEN на ОБА направления дрейфа, хотя данные для продолжения уже собрал; красен с 22-23.08, вскрыт 28.08 попутно. -->
- ✅ **Floor ПЕРЕД логированием состава: у КАЖДОГО направления отказа спроси, есть ли у прогона ДАННЫЕ, чтобы продолжить.** Терминальный код (`rc=2` «сломан сам инструмент») законен там, где гарантия подорвана; там, где прогон уже собрал нужное, терминальность есть **отказ использовать собранное** — и делает индикатор тождественно-красным на **штатном** событии (в множестве появился новый член). ⚠ Симптом-tell, ловится ЧТЕНИЕМ кода: ниже по функции стоит строка, строящая рабочий набор из **фактического** состояния, а не из перечня, чьё расхождение объявлено фатальным. ⛤ Развести: подрывающее гарантию — терминально; не подрывающее — краснит (`rc=1`) и печатается поимённо, но не обрывает.
<!-- Added by /session-retrospective 2026-08-28 (vteam-033dfb7f, StageStandards) — root: первая редакция того же фикса слила ПЕЧАТЬ и РЕШЕНИЕ, и терминальное направление съело сведения о нетерминальном; floor строкой выше ведёт в этот дефект напрямую. -->
  - ⛔ **Разводя терминальность, разведи и ПЕЧАТЬ с РЕШЕНИЕМ — иначе терминальное направление СЪЕДАЕТ печать нетерминального.** Форма `if lost: print(…); return 2` / `elif extra: print(…)` при ОДНОВРЕМЕННЫХ расхождениях печатает только первое ⇒ одно прячет второе, и наблюдаемость становится ХУЖЕ, чем до разведения (прежний общий `rc=2` печатал оба). ✅ Порядок: сперва напечатать ВСЕ направления, затем принять терминальность. ⚠ Tell при ревью своей же правки: `return`/`exit` стоит в ТОЙ ЖЕ ветке, что `print`. ⛤ Ось обязана подавать ОБА расхождения разом — поодиночке они зелены у обеих редакций.

**Empirical 2026-08-12:** `pull rc=1` у бота выглядел аварией; замер прав токенов показал покрытие 7/7 объединением двух проходов (данные доезжали). Дефект был не в синке, а в том, что оба индикатора — `rc` и `tail -1` — по построению не могли сообщить ни о норме, ни об аварии. Фикс: `failed[]` + `quarantined` в лог (dotfiles `fe22dd8`), revert-verify `scripts/bot-pull-observability-verify.py` (5 осей + 3 мутанта).

<!-- Added by /session-retrospective 2026-08-16 (проход 4) — root: три артефакта подряд (снапшот 1, снапшот 2, ретро прохода 3) пометили «вехи 0» красным как незакрытый пробел; фаундер ответил, что категория избыточна. Гейт краснел на ОТСУТСТВИИ того, чем система не пользуется. -->
#### ⛔ Тождественно-красный БЕЗ агрегата: гейт краснеет на ОТСУТСТВИИ категории, которой система не пользуется — а отчёты отмывают это в «открытый пункт»

Секция выше — про **составной** индикатор (один недоступный элемент роняет агрегат). Здесь агрегата нет вовсе: гейт написан так, что **ноль = дефект**, и позеленеть он может только если кто-то заведёт сущность, которая никому не нужна. Метрика при этом выглядит образцово: измерима, выражена числом, красна честно.

- ⛤ **Дискриминатор — ПОВТОРЯЕМОСТЬ ФОРМУЛИРОВКИ, а не значение.** Настоящий долг либо закрывается, либо растёт/меняет форму. Находка, воспроизводящаяся **дословно** в N последовательных отчётах при неизменном состоянии мира, — свойство **измерителя**, не мира. Один взгляд на прошлый отчёт стоит секунды.
- ⛔ **Дороже обычного ложно-красного, потому что отчёты его ОТМЫВАЮТ.** Каждый добросовестный снапшот/ретро переносит фальшивую находку в раздел «открытые пункты», и она приобретает вид накопленного долга. Через три отчёта это уже «известная проблема», а не артефакт гейта.
- ⛔ **Своими силами не чинится по построению:** отчёт спрашивает «закрыт ли пункт», а нужный вопрос — «а нужен ли сам критерий», и его задаёт только владелец предмета. ⇒ обнаружив N-й дословный повтор, **выносить фаундеру не пункт, а КРИТЕРИЙ**: «этот гейт краснеет N-й раз подряд, он вообще про то, что нам нужно?»
- ✅ **Floor при написании гейта: спроси, при каком состоянии мира он ЗЕЛЁН, и достижимо ли оно без заведения новой сущности.** Недостижимо ⇒ это не гейт, а требование. Требования формулирует владелец, а не измеритель.
- ⚠ **Симптом-tell у автора отчёта:** ты второй раз подряд пишешь один и тот же 🔴 и предлагаешь «закрыть пробел». Второй раз — уже повод проверить критерий; третий — гарантия, что проверять надо было раньше.
- ⛤ **Смежный floor того же прохода (охват свипа):** греп, которым устанавливают **множество** (а не ищут пример), ⛔ нельзя пайпить в `head` — усечённая выдача читается как полная. Сперва `grep -c`, и печатать всё; `head` законен только когда ищешь **образец**. Замер 2026-08-16: `head -20` скрыл вторую половину файла, где лежала целая секция того, что свип и искал.
  - ⛔ **Класс ШИРЕ грепа и бьёт ДВУМЯ разными исходами** (2026-08-17, два срабатывания за одну сессию — значит это не невнимательность, а форма). Любое **перечисление** — `grep`, `ls`, `find`, `-v`-вывод детектора — усечённое `head`/`tail`, даёт либо **(1) неполное МНОЖЕСТВО**, читаемое как полное (детектор Д2 показал 5 элементов вместо 7 — `| head -30`), либо **(2) не тот ЭЛЕМЕНТ**, если перечисление **неупорядочено по нужной оси** (`ls <кэш>/* | head -1` дал версию 16.187.1 из 104 кэшированных при проде 16.223.0 — глоб-порядок ≠ порядок версий).
  - ⛤ **Второй исход опаснее первого:** усечённое множество хотя бы выглядит списком, и «мало» может насторожить; выбранный элемент выглядит **единственным** и не несёт следа выбора вовсе. Проверять его нечем, кроме вопроса «а по какой оси это отсортировано?» — на который `head` ответа не даёт.
  - ✅ **Floor: `head` только на выдаче, УЖЕ отсортированной по нужной оси** (`sort -V | tail -1` для версий), либо — когда ось не задаётся сортировкой — **резолвить замером** (спросить у самого инструмента, что он считает актуальным) и `fail-loud`, если найденного нет. ⚠ Симптом-tell: пишешь `| head -1`, чтобы «взять нужный», а не «взять пример».
  - ⛔ **ТРЕТИЙ исход, и он объясняет, почему floor выше сформулирован про `head`, а укусит `tail`: у инструмента, печатающего СЧЁТЧИК, `tail` срезает встроенный дискриминатор полноты.** `exocortex-cli query --format json` печатает `✅ Found N result(s)` **в начале** (замер 2026-08-23: 54 строки вывода, счётчик — строка **6**, JSON `[` — строка **8**) ⇒ `head -N` при `N ≥ 6` его **сохраняет**, а `tail -N` при `N ≤ 48` **выбрасывает**. То есть на такой выдаче `tail` строго опаснее `head`: он отнимает ровно то число, которым усечение и обнаруживается.
    - ⚠ **Буква floor'а («нельзя пайпить в `head`») этот случай НЕ покрывает по ДВУМ осям сразу** — там `grep`, здесь вывод CLI; там `head`, здесь `tail`. Класс тот же, и именно поэтому floor надо читать как «перечисление не усекают **ничем**», а не как запрет одной команды.
    - ✅ **Floor: инструмент печатает счётчик ⇒ читать его и сверять с числом элементов**, а не полагаться на длину хвоста. Ноль строк `Found` в твоём выводе = ты смотришь на усечённое (§`Found N result(s)` в `sparql-iri-form-pre-verify` — там тот же счётчик служит различителем «законный ноль ↔ CLI не отработал»).
    - **Empirical 2026-08-23** (приёмка тикета `f867897f`, `StageAcceptance`): `tail -80` на перечислении переходов дал вывод «из `StageAcceptance` объявлен **ровно ОДИН** переход» — и на нём я начал рассуждать, как принимать красный тикет без ветки отказа. Пере-перечисление **без усечения**: переходов **два**, второй — `TransitionAcceptanceToDefect` → `StageGrooming`. Ошибка меняла исход приёмки с «вынужденный Release» на легитимный возврат; поймана до действия только потому, что вывод «ветки отказа нет» показался неправдоподобным для роли приёмщика.
  - ⛔ **ЧЕТВЁРТЫЙ исход — не усечение вовсе: фильтр по ТИПУ строки выбрасывает ЗАГОЛОВКИ ГРУПП, и детали приписываются не тому владельцу.** Три исхода выше режут выдачу по **длине** (`head`/`tail`), поэтому все три tell'а ключуются на «мало». Здесь `grep -E '<интересные типы строк>'` отдаёт **всё**, что просили, выдача не выглядит усечённой — а группирующие строки (заголовок репо, имя файла, разделитель секции) в перечень типов не попали, и каждая деталь молча наследует **предыдущий видимый** заголовок.
    - ⚠ **Симптом-tell, ловится счётом:** строк-деталей в выдаче больше, чем строк-заголовков, а решение принимается по **принадлежности** детали. Принадлежность в такой выдаче не выражена **по построению** — её домысливает читатель.
    - ✅ **Floor: у сгруппированного вывода фильтровать от ЗАГОЛОВКА (`grep -A N '<шаблон заголовка>'`), а не по типам деталей** — либо читать целиком. Дискриминатор при написании фильтра: «есть ли в выводе строка, которая ОТНОСИТ последующие к владельцу?» Есть ⇒ она обязана быть в перечне типов.
    - **Empirical 2026-08-26** (`StageStandards`, тикет `431820a5`): `exosync-parity | grep -E 'exoas-exodev|pending-local|Summary'` показал `pending-local-edit: flow/ebd2bafc-…` сразу под моим репо ⇒ вывод «в дельте чужая незакоммиченная правка, push блокирован». Пере-прогон `grep -A3` по заголовкам репо: файл принадлежит **`exoas-flow`**, а в моём было ровно `M2 diffs 1` — мой единственный файл. Ложный вывод стоил бы раунда ожидания вместо отгрузки.

**Empirical 2026-08-16** (проход 4, арка «вехи»): `/pmbok-status` §4/§6c помечал «вех 0» красным гейтом. Снапшот 1, снапшот 2 и ретро прохода 3 воспроизвели находку **дословно**, каждый раз предлагая её закрыть. На четвёртый раз я вынес это фаундеру **вопросом о датах вех** — и получил не даты, а отмену категории («Даты вех не нужны ни сейчас ни в будущем. Это избыточно»). Цена: три отчёта несли фальшивый открытый пункт; исправление пришло от владельца, ни один из трёх проходов сам критерий под сомнение не поставил.

<!-- Added by /session-retrospective 2026-08-29 (vteam-7494fe23, StageStandards) — root: три этапа независимо диагностировали критерий, не способный позеленеть по построению; tell протухшего снимка дал ложный отбой. -->
#### ⛔ ТРЕТИЙ механизм: критерий пинит РОБАСТНУЮ статистику ПОПУЛЯЦИИ, работа меняет ПОДМНОЖЕСТВО

Статистика **верна, воспроизводима, не протухла** и нечувствительна к предмету — медиана робастна
к сдвигу внутри хвоста ⇒ работа над долей популяции внутри хвоста не сдвинет её **никогда**.

- ⚠ **Tell протухшего снимка даёт ЛОЖНЫЙ ОТБОЙ** (`graph-record-outlives-its-referent`
  §ПРИЁМОЧНЫЙ КРИТЕРИЙ): число воспроизводимо и достигалось раньше ⇒ «инвариант, сужать нечего».
  Верный tell **арифметический**: назови ДОЛЮ трогаемой популяции — сдвинет ли она статистику?
- ✅ **Floor автору: мерить ТУ популяцию, которую работа трогает.** «Дифференциал вместо абсолюта»
  недостаточен: дифференциал ПО КОРПУСУ так же мёртв. Брать чувствительное к сдвигу подмножества:
  прирост на **перенесённых** · максимум · худший элемент.
- **Empirical 2026-08-29** (`7494fe23`): сплит ключа 98 связей (21 %) — медиана при том же M
  **5.63 → 5.63** (три этапа), худший ключ 2.84 → 3.99, максимум 98 → 31.

<!-- Added by /session-retrospective 2026-08-16 — root: сравнение с зафиксированным SHACL-базлайном дало «6 добавленных / 6 удалённых»; изменений не было — расходился МОЙ нормализатор, и это доказал digest, воспроизведённый из самого базлайна. -->
### ⛤ Сравнивая с ЗАФИКСИРОВАННЫМ базлайном: сперва воспроизведи его digest ИЗ НЕГО ЖЕ — ты валидируешь СВОЙ ИНСТРУМЕНТ, а не данные

Канарейка выше (§A7) отвечает на «жив ли запрос». У сравнения с **сохранённым перечнем** есть вторая, независимая ось отказа: перечень хранится в **сокращённой форме** (`<uid8> <Предикат> -> <uid8> [<Класс>]`), а как именно сокращали — обычно нигде не записано. Значит ты пишешь **нормализатор по догадке**, и он становится частью измерительного прибора.

- ⛔ **Расхождение нормализатора неотличимо от изменения данных.** Оба дают «N добавлено / M удалено» с правдоподобными строками. Мой первый прогон показал 6/6 — при **нулевом** изменении: базлайн сокращал не-uuid-адреса (символьный IRI свойства, percent-encoded имя, label-named файл) **последними 40 символами**, а я брал первые 8, как у uuid.
- ✅ **Floor, если базлайн хранит digest: пересчитай его ИЗ СОХРАНЁННОГО перечня ДО того, как считать свой.** Совпал — конвенция склейки (сортировка, разделитель, хвостовой перевод строки) у тебя верна; не совпал — чинить нечего в данных, чини склейку. Это ground truth, лежащий **внутри самого базлайна** и бесплатный.
- ✅ **Затем прогони свой нормализатор и сравни digest'ы.** Равны ⇒ множества тождественны **посимвольно** — вывод сильнее, чем «ADDED 0 / REMOVED 0», потому что не зависит от того, верно ли ты реализовал разность множеств.
- ⚠ **Симптом-tell, ловится ДО расследования данных:** добавленные и удалённые строки **паруются один-к-одному** по субъекту и предикату, отличаясь лишь написанием адреса. Настоящая дельта так не выглядит — она либо добавляет, либо убирает, но не «переименовывает» ровно столько же.
- ⛤ **Базлайн БЕЗ digest'а — уже дефект:** без него нормализатор непроверяем, и любая дельта содержит неизвестную долю артефактов прибора. Фиксируя базлайн, клади рядом digest **и** одну строку о конвенции сокращения.

<!-- Added by /session-retrospective 2026-09-07 (vteam-7e3f90b8) — root: floor «дельта, а не абсолют» неисполним, если baseline не снят; чем его заменить, корпус не называл (грепы → 0). Floor incomplete, подвид «условие». -->
- ⛔ **Базлайна НЕТ ВОВСЕ (не снял до первой мутации) ⇒ дельта неисполнима, а абсолют её НЕ заменяет:** «0 нарушений сегодня» не отличает «я не сломал» от «столько же было до меня». ✅ **Выход — АТРИБУЦИЯ вместо сравнения: ни один из СОЗДАННЫХ тобой UID не встречается в выводе валидатора** (`--format json`, поиск своих uid8 по всей выдаче). Сильнее абсолюта: отвечает про **твой вклад**, а не про корпус. ⛔ Ноль засчитывать с канарейкой — сколько РАЗЛИЧНЫХ UID выдача вообще содержит. ⚠ Tell: пишешь «SHACL 0 нарушений» о прогоне, чьего baseline у тебя нет. _(2026-09-07: 19 ассетов; выдача — 940 UID, моих 0.)_

**Empirical 2026-08-16** (Шаг 1 vteam, SHACL-базлайн `0 / 548`, digest `9b58241ef6ad9b70`): первый нормализатор дал 6/6 «расхождение состава»; воспроизведение digest'а из сохранённого перечня подтвердило конвенцию склейки, восстановленная форма сокращения схлопнула дельту в **0/0** с тем же digest'ом. Без этой канарейки 6 пар ушли бы в отчёт как реальное изменение множества.

<!-- Added by /session-retrospective 2026-08-16 (Шаг 2 vteam, b2ed707f) — root: оба ✅-floor'а выше ПРОШЛИ, а нормализатор был всё ещё неверен; поймал только subset-check против чужого сохранённого перечня. -->
#### ⛔ Оба floor'а выше ПРОХОДЯТ при неверном нормализаторе — они не упражняют РЕНДЕР строки. Дискриминатор — ВКЛЮЧЕНИЕ, а не равенство

Два ✅-floor'а выше молча предполагают, что **популяция не менялась**. Как только базлайн сдвинулся (сохранён `0 / 548`, на диске `0 / 558`), они оба перестают ловить:

| floor | что реально проверяет | почему пропускает |
|---|---|---|
| «пересчитай digest ИЗ СОХРАНЁННОГО перечня» | сортировку · разделитель · хвостовой перевод · хеш | ⛔ строки перечня **уже отрендерены** — твой код рендеринга не исполняется ВООБЩЕ |
| «прогони свой нормализатор и сравни digest'ы» | посимвольную тождественность множеств | ⛔ **неприменим**: 548 ≠ 558 ⇒ digest'ы обязаны отличаться, и расхождение формы неотличимо от роста популяции |

⇒ ровно в практическом случае (базлайн сдвинулся — а он сдвигается от любого чужого `pull`) проверка вырождается: первый floor вакуумен по построению, второй недоступен.

- ✅ **Floor, работающий при сдвинутой популяции: сохранённое множество обязано быть ПОДМНОЖЕСТВОМ твоего.**
  ```python
  saved ⊆ mine   # потеряно из сохранённого — 0
  len(mine - saved)  # сверх — ровно те, чей источник ты можешь назвать
  ```
  Он **упражняет рендер** (сравниваются твои строки с чужими) и **не требует равенства популяций**. Потеряна хоть одна сохранённая строка ⇒ расхождение формы, а не данных.
- ⚠ **Симптом-tell тот же, что у пары «добавлено/удалено», но теперь виден на ОДНОЙ строке:** запись пропала из «потеряно» и появилась в «сверх», отличаясь единственным полем. У меня это была одна строка из 548, у которой поле класса стало пустым.
- ⛤ **Корень пропуска обычно — ВТОРАЯ ФОРМА сообщения источника.** Регэксп пишется по той форме, что попалась в выборке (у меня 557 из 558), а редкая форма (1 из 558) даёт пустое поле. Проверять надо не «сошлось ли число», а **перечислить формы сообщений** в источнике: `Counter(re.sub(r'<[^>]+>','',m).split(':')[0] for m in messages)`.
- ⛤ **Побочно: сойдясь, subset-check даёт БОЛЬШЕЕ, чем канарейка** — мой digest на 558 совпал с digest'ом, независимо записанным предыдущим шагом (`57a243af6d19ca8a`), то есть форма подтверждена **вторым, посторонним** источником, а не только собственной сверкой.

**Empirical 2026-08-16** (Шаг 2 vteam, `b2ed707f`, тот же базлайн): floor «пересчитай из сохранённого» дал `9b58241ef6ad9b70` ✅ — и при этом мой нормализатор терял класс у 1 строки из 558. Subset-check показал `потеряно 1 / сверх 11` вместо ожидаемых `0 / 10`; расхождение оказалось второй формой сообщения (`does not conform to expected class` против `not validated against`). После правки — `потеряно 0 / сверх 10`, digest сошёлся с чужим. Дельта самой задачи была `0/0` в **обеих** редакциях нормализатора, то есть собственное сравнение «мой базлайн ↔ мой после» дефект не показывало **никогда**.

## Чем заменять (по возрастанию силы)

1. **Post-hoc исход** — измерять не выбор, а его последствие: релиз вышел, задача закрылась, дефект не вернулся. Сигнал обычно **уже собирается** инфраструктурой и стоит ноль. Минус — шумный (зависит и от исполнителя).
2. **Held-out / слепая проба** — часть случаев намеренно обрабатывается иначе (случайный элемент из топ-N вместо топ-1), сравнение исходов показывает, несёт ли решение информацию. Единственная форма, где «решение бесполезно» **видно**. Минус — часть ёмкости тратится не на лучшее.
3. **Внешний арбитр** — человек или отдельный агент с чистым контекстом сверяет выборку. Сильнее всего, но стоит времени того, ради кого автоматизация и строилась, — поэтому применять **выборочно**, а не сплошь.

Плюс независимо от выбора: **разделять метрику производительности и метрику корректности**. «Сколько сделано» и «какая доля сделанного пережила проверку» — разные числа, и первое без второго вознаграждает объём.

<!-- Added by /session-retrospective 2026-08-02 — root: dlp-apply-decisions; интервью решило «завести ВТОРОЕ поле для арбитра», а сильнее оказалось переключить ключ метрики на уже существующий акт, который измеряемая сторона физически совершить не может. -->
### ⛤ Сильнее «внешнего арбитра»: ключевать метрику на акт, который измеряемая сторона совершить НЕ МОЖЕТ

Естественный фикс замкнутой метрики — **завести второе поле**: «петля пишет свой вердикт, арбитр — свой, метрика читает второй». Это работает, но защита здесь **конвенциональная**: новое поле лежит в том же графе и физически записывается тем же процессом. Ты заменил «лиса считает кур» на «лиса не должна трогать вторую ведомость».

⛤ **Строго сильнее — не заводить поле, а найти УЖЕ СУЩЕСТВУЮЩИЙ акт арбитра, механически недоступный измеряемой стороне**, и ключевать метрику на нём. Признаки такого акта: он уже смоделирован (статус, переход, approve-гейт), у него есть **enforcement** (хук / права / отдельный канал), и его совершение и есть содержание решения — то есть подделать его = подделать само решение, а не только его запись.

- **Тест на пригодность:** «может ли измеряемая сторона привести это значение в нужное состояние, не будучи остановленной?» Нет → метрика надёжна. Да → это второе поле, а не арбитр.
- ⛤ **Побочная выгода — метрика перестаёт быть отдельной сущностью.** Числитель и знаменатель становятся срезами того, что и так живёт в системе, поэтому не могут «забыть заполниться» и не требуют дисциплины. Заведённое поле требует.
- ⛤ **Дискриминатор актора часто приходится СОЗДАТЬ, и это может быть чужая правка.** Enforcement обычно упирается в «а как отличить, кто пишет». Изоляция рабочего пространства (свой каталог / свой процесс / свои права) даёт различие **по пути**, и только после этого гейт исполним. Такие две правки — изоляция и гейт — применяются **вместе**: по отдельности первая выглядит удобством, вторая — благим намерением.
- ⚠ Ограничение: приём применим, когда решение арбитра **уже оцифровано**. Если арбитр решает устно — сначала оцифровать акт, потом ключевать метрику; иначе получится ровно второе поле.

**Empirical 2026-08-02** (RFC разведочной петли): панель нашла конвергентно, что поля вердикта пишет сама петля ⇒ метрика «доля дублей» была **0 % по построению** (гейт дедупа пропускал только вердикт `Novel`). Интервью решило разделить поля (пред-вердикт петли ≠ пост-вердикт арбитра) — верно, но недостаточно. При применении решения обнаружилось, что в графе **уже** есть 8 статусов RFC + 13 объектов-переходов, а решение фаундера в новой модели ролей = ровно один переход `Proposed → Approved`, охраняемый хуком с дискриминатором по каталогу. Метрики перевели на него: числитель — статус `Approved`, знаменатель — «покинул `Proposed`». Итог: два поля из трёх запланированных **не понадобились вовсе** (`verdictBy`, `verdictAt` — дублировали лифт статусов и были бы записываемы измеряемой стороной), осталось одно, где существующего соседа нет.

## Когда правило срабатывает

- Пишешь §Метрики успеха или Acceptance Criteria в RFC, требовании, PMBOK-хартии.
- Проектируешь автономную петлю / агента, который отчитывается о себе.
- Ревьюишь чужой (или свой) DoD — особенно там, где критерий звучит «работа выполняется согласно X», а X производит сам исполнитель.

## Запреты

- ⛔ Оставлять в durable-артефакте метрику, не прошедшую дискриминатор из §Принцип.
- ⛔ Принимать «метрика измерима и выражена числом» за «метрика проверяет».
- ⛔ Ставить единственной метрикой автономной роли счётчик её собственных действий.

<!-- Added by /session-retrospective 2026-08-16 — root: задача 264d7e73, ось на fail-closed при незарегистрированном имени host-функции. -->
## Разборы случаев — ВЫНЕСЕНЫ В ГРАФ (справочник on-demand)

⛤ Детальный разбор каждого случая (§A1-§A39: механизм, floor, эмпирика) живёт **ассетом графа**,
а не во флоре: его читают, когда метрика **уже соврала**, а не при написании каждого гейта.
Таблица ниже = **индекс по симптому**, разбор = справочник.

⛔ **Ключ ТРОЙНОЙ (§A + дата + тема).** Замер 2026-08-11: **45 ссылок** на правило, из них **18
адресуют раздел**, и большинство — по ТЕМЕ («якорь, ставший цитатой» — четырежды). Чекер темы
**не судит** (они «проза») ⇒ сломались бы молча. Даты неуникальны. В справочнике две переходные
таблицы: тема → §A и дата → §A; там же зафиксирован один давно висячий указатель
(`§ось наличия vs ось смысла` — раздела с таким именем здесь не было никогда, понятие живёт в
`integration-test-revert-verify` §A14).

⛤ **«Монитор молчит / тревог нет» покрывают СЕМЬ строк — разводить по ВОПРОСУ, не по механизму:**
тревога не СФОРМИРОВАНА (§A7 ноль неотличим от мёртвого запроса · §A9 на вход ничего не пришло ·
§A17/§A18 гвард охраняет НОЛЬ · §A24 порог недостижим по арифметике) · сформирована, но не
ДОСТАВЛЕНА (§A8) · подавлена СВОИМ ЖЕ fail-open (§A22) · подавлена ПИСАТЕЛЕМ, глушащим свои
ошибки (§A12). ⛔ Универсальный тест ядра («кто производит число?») для §A24 даёт ЛОЖНО-успокаивающий
ответ — источник там независимый; фолбэк на ядро здесь хуже, чем ничего.
⛤ Соседи по симптому: `aggregate-hides-dead-stream` (агрегат непуст за счёт соседей) ·
`activity-signal-is-not-progress` (тихо ≠ работает).

| # | дата | Симптом ⇒ действие |
|---|---|---|
| **§A1** | `2026-08-02` | Предохранитель гасит систему ровно тогда, когда её ЧИНЯТ (ручной kill при отладке), и при ПОЛНОМ успехе (умер в эпилоге) ⇒ «нет прощального пинга» покрывает ТРИ события ⇒ дискриминатор — явный маркер намерения (`manual-stop`), счётчики сбоев и прерываний РАЗДЕЛЬНЫЕ, судить по наблюдаемому ИСХОДУ работы ⇒ §A1 |
| **§A2** | `2026-08-02` | Показатель улучшился в прогоне, где ты правил ПРЕДСТАВЛЕНИЕ измеряемого (сжимал документ, консолидировал секции) ⇒ счётчик меряет форму, сдвиг всегда «в лучшую сторону» ⇒ машиночитаемый ЯКОРЬ с приоритетом над подсчётом упоминаний; ⛔ он сам начинает ЦИТИРОВАТЬСЯ ⇒ дискриминатор по контексту §A2 |
| **§A3** | `2026-08-07` | Снял ключ и грепнул его имя, чтобы убедиться в отсутствии — получил ровно 1 при КОРРЕКТНОЙ правке ⇒ цитату породила ТА ЖЕ правка (ты задокументировал её в прозе) ⇒ якорить на ФОРМУ ключа (`grep -cE '^<ключ>:'`), не на подстроку, и выводить ожидаемое N ДО прогона ⇒ ТРИ формы (ключ · фрагмент · прозаическая фраза) §A3 |
| **§A4** | `2026-08-02` | Блокировка есть, но её собственный ТЕКСТ объясняет, как пройти дальше не выполнив требование ⇒ escape бесплатен, и обход неотличим от послушания, если ранний выход стоит ДО детекции ⇒ детекцию гонять ВСЕГДА, escape применять на ветке вердикта (`BYPASS_after_<gate>` против `COMPLIED`), приглашение из текста убрать ⇒ §A4 |
| **§A5** | `2026-08-02` | Гейт проходит, потому что «X **был**» — а X РАСХОДУЕМЫЙ (ответ на вопрос, одноразовый токен, выданное одобрение, взятый лок) ⇒ один давно потраченный артефакт удовлетворяет гейт бесконечно, и блока нет вовсе (журнал зелен) ⇒ условие обязано включать СОСТОЯНИЕ РАСХОДОВАНИЯ («открыт ли X»), а не наличие («был ли X»). Tell: условие ссылается на событие в прошлом. ⛤ Найдя один экземпляр — искать второй В ТОМ ЖЕ файле: у них общий корень |
| **§A6** | `2026-08-03` | Классификатор по СОСТОЯНИЮ не отличает штатный обрыв от успеха — протокол обрыва сам помечает работу выполненной ⇒ ветка обрыва недостижима при живом имени в коде ⇒ порядок ветвей: самоотчёт актора → явный маркер обрыва → состояние мира → провал, достижимость предъявить ИСПОЛНЕНИЕМ ⇒ §A6 |
| **§A7** | `2026-08-03` ⛔ | Инвариант с ожидаемым НУЛЁМ рапортует «чисто» ⇒ ноль неотличим от сломанного запроса (переименован класс, уехал префикс, не загрузился граф) ⇒ канарейка на КАЖДОМ прогоне через ТУ ЖЕ машинерию, пустая канарейка = **BROKEN**, не CLEAN; вердиктов ТРИ. ⛤ Гнать её только при пустом результате ⇒ §A7 |
| **§A8** | `2026-08-03` ⛔ | Монитор пишет верные вердикты, а тревога НЕ приходит (не тот путь к секрету, протухший токен, чужой chat_id) ⇒ у него ДВЕ независимые оси отказа, и канарейка проверки вторую не покрывает ⇒ отправка fail-loud: вердикт из ПОЛЯ `ok` ответа, не из exit-кода `curl`; selftest с ТРЕМЯ исходами ⇒ §A8 |
| **§A9** | `2026-08-03` ⛔ | «Просмотрено 0, нарушений 0» неотличимо от «просмотрено 500, нарушений 0» ⇒ сканер жив, вердикт честен, а на вход ничего не пришло ⇒ рапортовать РАЗМЕР ВХОДА рядом с числом находок, источник сделать инъектируемым (иначе флип непредъявим), развести подменяемый `TICK` и эталонный `PROD_TICK` ⇒ §A9 |
| **§A10** | `2026-08-03` ⛔ | Канарейка ЕСТЬ, зелёная, гоняется каждым прогоном — и всё равно ничего не доказывает ⇒ три формы неполноты: проверяет НЕ ТОТ термин · читает КЭШ (`--no-cache`) · покрывает ОДНОГО члена множества ⇒ образец на КАЖДОГО, вердикт называет мёртвого ПО ИМЕНИ. ⚠ Tell: членов > 1, образец один ⇒ §A10 |
| **§A11** | `2026-08-06` ⛔ | Взял размер входа ИЗ КАНАРЕЙКИ ⇒ ветка «популяция пуста» недостижима (её гейт уже отсёк), а совпадение «канарейка == популяция» ничем не охраняется ⇒ машинерию и покрытие мерить РАЗНЫМИ запросами, оставил fallback — помечать в выводе (`scanned=N (from canary)`) ⇒ §A11 |
| **§A12** | `2026-08-03` ⛔ | Твоя красная проба гейта отправила БОЕВУЮ тревогу о несуществующем инциденте ⇒ эскалацию делать opt-in по флагу, который ставит ТОЛЬКО расписание (ручной прогон печатает вердикт и молчит). ⛔ И писатель, чью запись аудитит гейт, обязан логировать свои сбои — иначе оба слоя схлопываются в ноль ⇒ §A12 |
| **§A13** | `2026-08-03` | Проверка «утечки нет» КРАСНЕЕТ, хотя защита сработала ⇒ сообщение отказа ЦИТИРУЕТ отвергнутый аргумент, и твой grep находит канарейку в тексте самой защиты ⇒ дискриминатор не должен пересекаться с тем, что защита может напечатать: «отказано **И** ничего не создано» вместо «канарейки нет в выводе». Tell: красное ровно на той форме входа, где значение попадает в текст ошибки (`--flag=value` цитируется целиком, `--flag value` — обычно нет) |
| **§A14** | `2026-08-06` ⛔ | Самоаудит рапортует НАРУШЕНИЕ правила, а на деле сработал ХУК: `exit 2` блокирует вызов, но запись `tool_use` в транскрипте остаётся ⇒ работающий барьер засчитан как дефект ⇒ `tool_use` = ПОПЫТКА: коррелировать с `tool_result` и разделять исполнено / hook-заблокировано / исполнено-и-упало ⇒ §A14 |
| **§A15** | `2026-08-05` | Проверка «я не тронул живую систему» краснеет на прогоне, который заведомо ничего не трогал (либо зеленеет, когда наблюдаемый простаивал) ⇒ дискриминатор мерит величину, которую наблюдаемый МЕНЯЕТ САМ ⇒ мерить то, что в норме меняешь только ты (побайтовое сравнение · монотонность · свой маркер) ⇒ §A15 |
| **§A16** | `2026-08-05` | «Классифицировано 100 %» ⇒ это симптом ОТКАЗА, а не качества метода: доказательством взят признак, который есть у КАЖДОГО объекта (структурная ссылка вместо смысловой) ⇒ перечислить частоту КАЖДОГО вида доказательства; вид с частотой = размеру корпуса структурный ⇒ allow-list смысловых §A16 |
| **§A17** | `2026-08-06` ⛔ | Логика гварда верна, разность множеств честна, набор зелёный — а НОВЫЙ член в сравнение просто не попадает ⇒ дефект в СБОРЕ ВХОДА, и потому невидим при чтении кода ⇒ область охвата задавать ШИРЕ используемого (класс символов, `find` по каталогу); мутант обязан ВВОДИТЬ нового члена ⇒ §A17 |
| **§A18** | `2026-08-09` ⛔ | Floor «расширь ОБЛАСТЬ до всего каталога» исполнен дословно — а гвард охраняет НОЛЬ ⇒ стартовый путь оказался СИМЛИНКОМ, `find` внутрь не идёт, обе стороны сверки схлопываются одинаково ⇒ `find -L` + канарейка ОХВАТА отдельной осью (число хешей > 1); свёртка порядок-зависима ⇒ §A18 |
| **§A19** | `2026-08-11` ⛔ | Структурный ratchet («ищу `os.replace` и требую рядом `chmod`») всё равно обходится ⇒ у структуры свои члены вне охвата: другое ИМЯ ПЕРЕМЕННОЙ, окно до соседнего исправленного вызова ⇒ якорить на ТОТ ЖЕ синтаксический объект. ⚠ Tell: в условии литерал имени ИЛИ числовая ширина окна ⇒ §A19 |
| **§A20** | `2026-08-07` | Измеритель проверен канарейкой, результат чист — и ты пишешь «подтверждено» ⇒ канарейка закрывает валидность ИЗМЕРИТЕЛЯ, а не достаточность ВЫБОРКИ ⇒ посчитать `(1−p)^n`: при ставке ≈20 % и n=4 «чисто» — ОЖИДАЕМЫЙ исход при живом дефекте ⇒ ⛔ не подписывать вывод словами «замер подтвердил» §A20 |
| **§A21** | `2026-08-10` ⛔ | Ось, запирающая ЗНАЧЕНИЕ константы, зелена при ЛЮБОМ её значении ⇒ фикстура ВЫВЕДЕНА ИЗ ТОЙ ЖЕ константы и подстраивается под что угодно ⇒ фикстура ФИКСИРОВАННАЯ, по одной на КАЖДЫЙ край допустимой полосы, числа брать из механизма; красноту читать ПО ИМЕНИ оси ⇒ §A21 |
| **§A22** | `2026-08-11` ⛔ | Опечатка в конфиге (запятая вместо точки в пороге) снимает гвард ЦЕЛИКОМ и выглядит штатным fail-open ⇒ разделить классы: сломан ИСТОЧНИК ДАННЫХ = fail-open, сломан ПОРОГ (наша политика) = **fail-CLOSED** со СВОИМ вердиктом. ⛔ «Своё имя исхода» верно ТОЛЬКО для терминальной ветки ⇒ §A22 |
| **§A23** | `2026-08-05` | Журнал называет виноватым НАБЛЮДАЕМОГО («воркер не закрылся сам»), а убил его компенсатор в тот же момент ⇒ ярлык несёт ПРИЧИННОЕ утверждение, которого замер не делал ⇒ ярлык описывает НАБЛЮДЕНИЕ («снят: тихо, задача Done»); чужой ярлык сверять МОМЕНТОМ с журналом компенсатора ⇒ §A23 |
| **§A24** | `2026-08-11` ⛔ | Гейт ни разу не срабатывал, «холдов нет» читается как «давления нет», а число даёт НЕЗАВИСИМЫЙ источник (дискриминатор ядра молчит) ⇒ мёртвость от АРИФМЕТИКИ НОРМИРОВКИ: числитель ограничен, знаменатель растёт ⇒ посчитать ОБЛАСТЬ ЗНАЧЕНИЙ и печатать достижимый максимум рядом с порогом ⇒ §A24 |
| **§A25** | `2026-08-11` ⛔ | Причина смерти выведена из ПОСЛЕДНЕГО КАДРА САМОГО ПОГИБШЕГО, а журнал наблюдателя чист (§A23 молчит) ⇒ причина ТРЕТЬЯ, внешняя обоим, свидетель одинаков в обоих классах ⇒ он обязан ПЕРЕЖИТЬ событие и быть внешним: перепись окружения от другого супервизора, дискриминатор — просадка популяции ⇒ §A25 |
| **§A26** | `2026-08-11` ⛔ | Гвард сработал корректно, а система стала хуже ⇒ область ДЕЙСТВИЯ шире НАЗНАЧЕНИЯ: ранний выход гасит весь тик, а гвард написан против ОДНОГО блока ⇒ перечислить всё ниже выхода и разделить «пропускать правильно» против «неверно»; среди подавленного бывает средство против его же триггера ⇒ §A26 |
| **§A27** | `2026-08-18` | Цепочка «собрать артефакт → проверить артефакт» дала два зелёных вердикта, а артефакт **0 байт** ⇒ ПУСТОЙ ВХОД делает проверку тождественно-зелёной (`>` усекает цель при разборе перенаправления, пустая строка валидна для парсера) ⇒ проверять СОДЕРЖИМОЕ (`grep -c '<маркер>'` + `[ -s <файл> ]`), не успешность шагов ⇒ §A27 |
| **§A28** | `2026-08-16` | Ось выглядит идеально (предмет отказывает, мутант краснеет) — а верный и неверный дизайн дали бы на ней ОДИН исход ⇒ у элементов есть поле ПОЛИТИКИ (`onUnknown`/`fail_mode`) с перекошенным распределением, и типичный элемент дизайны не различает ⇒ посчитать РАСПРЕДЕЛЕНИЕ и строить ось на РЕДКОМ значении ⇒ §A28 |
| **§A29** | `2026-08-19` | Отсев дал правдоподобное число, а внутри МОЛЧА исчезла целая модель ⇒ предикат «покрыт И не удовлетворяет признаку» судит и ту модель, что признака НЕ ОБЪЯВЛЯЛА, хотя верный ответ «нечем судить» ⇒ третий флаг `<признак>_judgeable`, отсеивать лишь `judgeable and not <признак>` ⇒ §A29 |
| **§A30** | `2026-08-21` | Отсев тождественно-неактивен при безупречной логике гварда ⇒ он читает НОВЫЙ предикат, а носители лежат в СТАРОМ (миграция переносит гварды по одному) ⇒ счёт носителей КАЖДОГО предиката + канарейка на соседний предикат namespace; фикс аддитивен — читать ОБА ⇒ §A30 |
| **§A31** | `2026-08-21` | Одноразовая проба (сентинел + самоочистка) промолчала ровно на боевом событии, ради которого заведена ⇒ сентинел израсходовал ТВОЙ ЖЕ харнесс, если хоть один прогон шёл без инъекции пути ⇒ сентинел и журнал инъектируемы С НАЧАЛА, после харнесса перевооружить (`rm <сентинел>`) и вычистить фикстуры ⇒ §A31 |
| **§A32** | `2026-08-21` | Надзор оформлен средствами наблюдаемой системы (дорожка той же доски, джоб того же планировщика) ⇒ он встаёт вместе с ней ровно тогда, когда нужен, и молчание неотличимо от «находок нет» ⇒ наблюдатель живёт ВНЕ контура, исходов ТРИ (`CLEAN` · `НАХОДКИ` · `BROKEN`); тот же класс у планового джоба ⇒ §A32 |
| **§A33** | `2026-08-21` | Класс починен, а свип по признаку выдаёт файл из твоего же последнего коммита ⇒ рефлекс исключает правленые файлы, а второй экземпляр живёт в ДРУГОЙ функции того же файла ⇒ свип по ВСЕМУ дереву без исключений + сверять НОМЕРА СТРОК с диффом: совпали — артефакт, нет — второй экземпляр ⇒ §A33 |
| **§A34** | `2026-08-21` | После инцидента «красная проба послала БОЕВУЮ тревогу» рука навешивает opt-in на всё, что уведомляет ⇒ гвард нужен ТОЛЬКО тому, что гоняют РУКАМИ; тик под расписанием и событийный пинг — нет ⇒ форма `<PREFIX>_SCHEDULED=1`, флаг ставит расписание (⛔ не `DRY=1`-опт-аут) ⇒ §A34 |
| **§A35** | `2026-08-22` | Сторож будит текстом о серии отказов ПОСЛЕ того, как корень закрыт ⇒ серия «до первого успеха» ЗАЛИПАЕТ: сбрасывает её только успех, а у редкого события он наступит через дни ⇒ серия активна, лишь если ПОСЛЕДНИЙ отказ свежий (≈2× интервала наблюдателя); протухшую понижать в ЖУРНАЛ ⇒ §A35 |
| **§A36** | `2026-08-24` | Критерий написан связкой («ранги различны **и объяснимы**», «тесты зелёные **и достаточные**») ⇒ измеримую половину закрыл прогоном, и ощущение «пройдено» перенеслось на оценочную, у которой шкалы нет ⇒ расщепить и отчитаться РАЗДЕЛЬНО: измеримое — исполнением, оценочное — ФАКТ + имя владельца суждения ⇒ §A36 |
| **§A37** | `2026-08-24` | Ранний fail-closed гвард падает — о здоровье поздних шагов не известно НИЧЕГО, но пустота достраивается теорией («движок сломан», «не хватает данных») ⇒ чинить ранний гвард ПЕРВЫМ и снимать картину ЗАНОВО: починка и есть дешёвый способ увидеть систему ⇒ §A37 |
| **§A38** | `2026-08-24` | В диагностической строке гварда есть число (`возраст 907с`, `sup 4.1667`), и ревьюер читает его как учтённое ⇒ величина ПОСЧИТАНА и НАПЕЧАТАНА, но в ветвлении не участвует ⇒ у КАЖДОЙ печатаемой величины спросить, стоит ли она в `if`. ⚠ Tell: `grep` находит имя только в `print` ⇒ §A38 |
| **§A39** | `2026-09-11` | Приёмка «файлы из каталога сняты» зелена, а остаток есть ⇒ предикат меряет ГЛУБИНУ 1: `[ -d ]` судит о существовании каталога, `listdir` не видит подпапок (ложно-ЗЕЛЁНЫЙ, и он тише) ⇒ считать РЕКУРСИВНО (`os.walk` / `find -type f`) и печатать ВТОРУЮ величину рядом (было N → осталось 0) ⇒ §A39 |
| **§A40** | `2026-09-11` | Замер «у скольких членов популяции есть признак» дал ноль у ЧАСТИ, а общий результат НЕпуст ⇒ канарейка §A7 не срабатывает по построению, и ноль уходит в durable как факт о ПОПУЛЯЦИИ, хотя он факт о ПРЕДИКАТЕ ⇒ канарейку ставить НА КАЖДЫЙ КОНЪЮНКТ отдельно; проверить ИСТОЧНИК ⇒ §A40 |
| **§A41** | `2026-09-11` | Операция ЗАДУМАНА как расщепление целого на части (разбить SCC, раздробить монолит, вынести модуль), а метрика приёмки — **СУММА по частям** («узлов в циклах», суммарный размер, общее число элементов) ⇒ ⛔ сумма СОХРАНЯЕТСЯ по построению (7 → 2+5 = 7), исхода «улучшилось» не существует; а парная метрика «ЧИСЛО частей» при этом **инвертирована** — растёт при успехе (2 → 3). ⇒ приёмка расщепления судится **МАКСИМУМОМ по частям** (наибольший SCC 7 → 5), а не суммой и не счётом. ⚠ Tell, ловится ДО прогона: глагол задачи — «распадается / разделить / вынести», а величина критерия начинается со слова «всего» либо «число». ⛔ Оба ложных показателя печатает САМ инструмент рядом с верным (детектор циклов даже предупреждает в выводе, что число SCC растёт при улучшении) — то есть приёмщик прочитает `9 = 9` и `2 → 3` как провал успешной работы, если несущая величина не названа явно. ⛤ Разбора в справочнике НЕТ — floor целиком в этой строке |
| **§A42** | `2026-09-12` | Пере-замер подтвердил число, ты пишешь «сошлось» ⇒ ⛔ совпадение НЕ подтверждает заявленную **ОБЛАСТЬ**, если величина к ней нечувствительна: значение одинаково в суженной области и в объединении ⇒ оно совпало бы и при неверном ярлыке, а исхода «не сошлось» у него нет по построению ⇒ по КАЖДОЙ величине замерить её в СУЖЕННОЙ области; совпало ⇒ пометить «область НЕ проверена», и на такие величины вердикт не опирать. ⚠ Tell: в одном приёмочном критерии несколько чисел под ОДНИМ ярлыком области — часть из них почти наверняка нечувствительна, и она маскирует соседнее неверное. ⛤ Сосед — `self-authored-claim-loses-its-provenance` §A34 (области ВЛОЖЕНЫ): там сверяют области ДО опоры, здесь — обнаруживают, что сама сверка величиной их не различает. ⛤ Разбора в справочнике НЕТ — floor целиком в этой строке _(2026-09-12, AC8 RFC `de073e8d`: шесть чисел `[три vault]`, ярлык проверяют три; `lit__Book` 9 оказалось `[vault-my]`, `place` 18 и `my-tbox` 6 — при любой)_ |
| **§A43** | `2026-09-12` | Мутант краснит ДЕСЯТОК осей, а целевая МОЛЧИТ ⇒ ⛔ не «ось не различает» и не повод усиливать ось: ЧИСЛО КРАСНЫХ — дискриминатор АДРЕСНОСТИ мутации (1–2 = адресна; много = точка не та) ⇒ переносить мутацию к ГВАРДУ (строка записи, не вызов), пока целевая не покраснеет ОДНА. Не §A55 integration-test: предмет исполним. Tell: A47 зелена при 13 красных |
| **§A44** | `2026-09-12` | Детектор срезал комментарии (§A3) и всё равно пометил харнесс глаголом, которого тот НЕ зовёт ⇒ ⛔ у цитаты ВТОРОЙ носитель — СТРОКА-СООБЩЕНИЕ оси (`ok "… create не зван"`): строковый литерал, не комментарий ⇒ единица счёта — ВЫЗОВ (позиция команды / AST), не токен. ⚠ Tell: помечены харнессы CLI-конвейеров, чьи сообщения обязаны называть проверяемый глагол |
| **§A45** | `2026-09-13` | Размер входа напечатан и НЕнулевой (§A9 исполнен), а измеритель слеп РОВНО к измеряемому классу ⇒ вход собирает ОТБОРЩИК ФИКСТУР, и его ГИГИЕНИЧЕСКИЙ фильтр выбрасывает предмет замера (438 из 1176, 37 %), тогда как гвард «мерить ДО гейта» стоял у СОСЕДНЕГО фильтра ⇒ печатать, ЧТО отсеял отборщик. ⚠ Tell: «до/после» не двигается |
| **§A46** | `2026-09-13` | «Ставка пилота ≥ контрольной» при МАЛОМ n_p неинформативна неделями (ожидаемое = rate_c·n_p: 0.53 при n_p=2 против 17.7 по частоте симптомов), а окно «с рождения ПРЕДМЕТА» смешивает дни ДО рождения МЕХАНИЗМА (тела 09.09, канал 12.09 04:51) ⇒ печатать окна рядом; вердикт — от рождения механизма, ожидание — по частоте симптомов. ⚠ Tell: закрытие окна уезжает на месяц |
| **§A47** | `2026-09-16` | CI-гейт репо A (`audit assetspace-depends --self A`) ЗЕЛЁН на ребре A → приватный B, которого A не объявляет ⇒ ⛔ не «ребро судится в CI B»: merged vault CI = A + объявленные deps, приватного B там НЕТ ⇒ цель `not-found` (informational), вердикта нет НИ В ОДНОМ CI (B судит только рёбра с источником B). Класс «TBox расщеплён между аудиториями» ловит ТОЛЬКО локальный аудит по vault с примонтированным B. ⚠ Tell: три `not-found` на один UID в выводе гейта при OK. ⛤ Floor в строке _(51e97b54: `exoas-public → exoas-my` Property_domain×3; CI public run 35056754121 OK при локальном FAIL)_ |
| **§A48** | `2026-09-16` | Гвард «остаток ⇒ отказ» проверяет ВЫХОД нормализатора (он режет всё после `\|`) ⇒ ветвь `\|` тождественно-ложна, `[[uid\|alias]] see also` проходит как `"[[uid]]"`, и живая ветвь маскирует мёртвую ⇒ валидировать СЫРОЙ вход, нормализатор — ПОСЛЕ проверки; фикстура несёт форму, которую он РЕЖЕТ ⇒ §A48 |
| **§A49** | `2026-09-20` | Диагностика/замер берёт СНИМОК изменяемой коллекции (`Object.keys(props)`), а команда дописывает в неё ПОЗЖЕ (инъекция дефолта, нормализация, обогащение) ⇒ ⛔ всё дописанное невидимо предмету по построению, при этом код читается верно и оси зелены: у снимка нет признака, что он устарел. ⚠ Tell АРИФМЕТИЧЕСКИЙ, ловится ЧТЕНИЕМ: номер строки снимка МЕНЬШЕ номера строки записи в ту же коллекцию (`grep -n '<коллекция>\['` против строки снимка) — а рядом обычно стоит чужой комментарий «finalise … AFTER any … injection», то есть кодовая база УЖЕ знает про этот порядок и говорит о нём соседнему потребителю. ✅ Снимок для ПОТРЕБИТЕЛЯ брать в точке передачи, из ФИНАЛЬНОГО состояния; перечисление известных инъекций — не фикс (следующая выпадет молча). ⛔ И НЕ сливать два снимка в одну переменную «чтобы не разъехались»: если у потребителей РАЗНЫЕ объявленные области (здесь `validate()` документирован как «raw USER keys only — the CLI injects its own keys downstream»), это ДВА ПОНЯТИЯ, и совпадение значений сегодня — свойство момента, а не конструкции. Одна переменная на ОДНО понятие. ⛤ Источник дефекта бывает УКАЗАНИЕМ РЕВЬЮЕРА («держи один набор для обоих») — оно снимает работу и потому не перепроверяется; дискриминатор тот же, контрактами потребителей _(2026-09-20, PR #4295 r1 HIGH: `create` инъектит `ems__Effort_status` на :474/:482 после снимка на :394 ⇒ конфликт по нему не эмитился бы НИКОГДА — регрессия против main, где `collect()` его репортил; мутант «вернуть ранний снимок» красит РОВНО одну ось)_ |
| **§A50** | `2026-09-25` | Наблюдатель раз за разом рапортует УСПЕХ доставки/починки, а величина, которую он этим меряет, НЕ УХОДИТ (345 × `DELIVERED` при неизменной дельте; девять подряд «при дельте 11») ⇒ ⛔ не «доставка сломана» и не §A24 (там порог недостижим по АРИФМЕТИКЕ, здесь источник числа исправен): **предикат величины включает то, что исполнитель не устраняет ПО ПОСТРОЕНИЮ** — счётчик брал `(pending|quarantine)`, а доставщик карантинные файлы пропускает молча, их снимает другой глагол. ⇒ зелёный исход НЕДОСТИЖИМ, пока в системе есть хоть один такой элемент, а они копятся; и — дороже всего — **настоящая величина МАСКИРУЕТСЯ**: «всё сделано» и «10 неустранимых + 3 невывезенных» дают ОДИН вердикт с ОДНИМ числом. ⚠ Tell, ловится ЧТЕНИЕМ ЖУРНАЛА без всякого замера: строка успеха повторяется с ОДНИМ И ТЕМ ЖЕ числом — успех, не меняющий своего предмета, успехом не является. ✅ Floor: разделить ДВА счёта — «что исполнитель может устранить» (он запускает действие и печатается как дельта) и «что он устранить не может» (печатается рядом отдельным числом с именем того, кто ЭТО снимает); предикат покоя/отпечатка при этом оставить по ОБЕИМ группам — смена неустранимого тоже событие. ⛔ Фикстуры харнесса почти наверняка несут только устранимую группу ⇒ ось на этот класс отсутствует не по недосмотру, а потому что вход не построен (§A105 [[integration-test-revert-verify]]). ⛤ Floor целиком в строке _(2026-09-25, `vault-quiescent-deliver` dotfiles `769e6e85`: вся дельта vault-exodev = 10 × `quarantine-pinned`, `pending` ноль; 16/3 → 19/0, мутант «считать пины дельтой» красит РОВНО {A8,A8б,A8в})_ |
⛤ **Справочник:** `6a3946cd-6d4c-457f-8d26-97442344cc01`, якоря **§A1-§A40, §A43-§A44, §A48** (§A41-§A42, §A45-§A47, §A49-§A50 — floor целиком в строке индекса; разбор §A46 — тело листа 2d.1 `2d8701dd` §Отчёт п.4–5).
Рецепт адресного чтения (один на корпус) — `rule-carrier-follows-function` §«Читать справочник АДРЕСНО».


## Cross-references

- `~/dotfiles/.claude/rules/decision-surface-must-derive-from-mechanism.md` §форма №6 — что происходит, когда верное решение подписано ложным обоснованием (прямое следствие Addendum 2026-08-07 выше).
- `~/dotfiles/.claude/rules/bash-loop-eval-pitfalls.md` — Г17 (`ln -sf` сквозь цель-каталог): конкретная мутация, к которой был слеп узкий манифест из Addendum 2026-08-06.
- `~/dotfiles/.claude/rules/homoiconic-external-engine-config.md` §A12 (две формы пустого ответа CLI — секция вынесена в справочник) — как именно ломается детект пустоты, на котором держится такой гейт.
- `~/dotfiles/.claude/rules/integration-test-revert-verify.md` — тот же класс на уровне тестов: тест, проходящий обоими способами, = false-positive coverage. Revert-verify и есть проверка «а может ли быть красным».
- `~/dotfiles/.claude/rules/verify-before-assert.md` — самоотчёт актора не авторитетен; §«транзитивный путь раздувает счёт» — родственная арифметическая слепота.
- `~/dotfiles/.claude/rules/adversarial-panel-before-foundational-rfc.md` — панель ловит этот класс; §циркулярность — зеркальный дефект (метрика, чей ноль есть следствие обсуждаемого пробела).
- `~/.claude/skills/rfc/SKILL.md` Phase 0 п. 2.6 — сосед по происхождению: там про то, что счёт бывает сломан; здесь — про то, что счёт бывает бессмысленным.
- `~/dotfiles/.claude/rules/activity-signal-is-not-progress.md` §Addendum 2026-08-05 — зеркало по оси атрибуции: там артефакт не говорит, КЕМ сделана работа; здесь наблюдатель сам вписывает неверное «кем» в свой вердикт.

⛤ **Эмпирика** (1 фрагм.) → `~/.claude/rules-archive/self-satisfying-metric-weak-verifier.md`

## Счётчик

⛤ **Числа в ЭТОЙ СТРОКЕ нет намеренно.** `~/dotfiles/scripts/rules-effectiveness.py` берёт первое
`**<N>` из строки `^## Счётчик` и делает его счётом повторных нарушений правила; величина, взятая
из другого окна или из памяти, молча подменила бы метрику корпуса (`~/dotfiles/.claude/rules/self-authored-claim-loses-its-provenance.md`).
Пока ручной счёт не ведётся, величина остаётся **производной**: скрипт считает её сам по `rules/` + `rules-archive/`.
Замер 2026-09-11 для этого правила — **2** (`python3 -I ~/dotfiles/scripts/rules-effectiveness.py`).

⛤ **Независимый замер — другой актор и другое окно:** отметок в ретро-таблицах нарушений правил
за 30 суток — **21** (`python3 -I ~/dotfiles/.claude/bin/rules-label-extract.py`, снимок 2026-09-11).

⚠ Две величины меряют РАЗНОЕ (упоминания внутри текста правила против отметок в чужих ретро) и одна
к другой не сводится — поэтому обе названы с командой, а в заголовок не поднята ни одна.
