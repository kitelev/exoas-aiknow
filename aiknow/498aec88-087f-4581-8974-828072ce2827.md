---
exo__Asset_uid: 498aec88-087f-4581-8974-828072ce2827
exo__Asset_createdAt: 2026-08-30T00:26:37
exo__Asset_updatedAt: 2026-08-30T17:07:23
exo__Instance_class:
  - "[[b0474610-5fa7-4ec4-a947-f85f26e93455]]"
exo__Asset_createdBy: "[[4ef3962d-b8a7-42b5-bd28-88ec846f1d13]]"
exo__Asset_label: "Разбор Auto-Company: мандат роли-критика «вето, но не бесконечная задержка» — для конвейера vteam"
aliases:
  - "Разбор Auto-Company: мандат роли-критика «вето, но не бесконечная задержка» — для конвейера vteam"
exo__Asset_isDefinedBy: "[[2ce77ac1-3f78-4f64-9dc1-41ea01da1ef0]]"
aiKnow__Memory_aboutConcept: "[[8f6133b6-e298-4bcc-bf9e-ec39d001e436]]"
---


Разбор `github.com/MaxMiksa/Auto-Company` (2490⭐, Python, 14 агентов-персон) на предмет идей для конвейера vteam. Замер 2026-08-29.

## Их модель

Демон крутит `scripts/core/auto-loop.sh` (743 строки). Каждый цикл — **независимый CLI-вызов**; единственное кросс-цикловое состояние — `memories/consensus.md` («эстафетная палочка», «must be updated before cycle end»). Внутри цикла LLM **сам формирует сквад 2-5 агентов** под задачу из 14 персон-архетипов (`ceo-bezos`, `cto-vogels`, `critic-munger`, `qa-bach`…), спавнит их как `general-purpose` с инъекцией файла роли в промпт; артефакты в `docs/<role>/`.

## ГЛАВНАЯ находка — мандат роли-критика

CLAUDE.md, строка 15, дословно:

> Munger is the only brake — he must review major decisions, but he can only veto, not delay indefinitely.

Это формулировка, закрывающая **обе** стороны риска у этапа-ревьюера, тогда как из корпуса выводится только одна:

| конфигурация | отказ |
|---|---|
| выход один | этап вырождается в «принять» — ревью формальное (это floor корпуса, `autonomous-process-scope-filter` §ЧИСЛО доступных выходов) |
| ветка отказа БЕЗ предела | тикет ходит по кругу, конвейер стоит на одном критике |

⇒ заводя этап-инвертор в vteam, мандат обязан нести **обе** границы: вето есть, бесконечная задержка — нет. Предел возвратов задавать числом.

⛤ Зачем это vteam: задокументированный пробел — пять этапов, пять акторов, **ноль внешних ревью**; каждый исполнитель рапортует свои зелёные оси, чужой дифф не читает никто.

## Что брать НЕ стоит

- **Персоны-архетипы** («ты Безос»): у vteam роли определены **мандатом и этапом** — это проверяемо запросом к графу; архетип задаёт стиль, а не границы.
- **Реализацию circuit breaker**: у них `MAX_CONSECUTIVE_ERRORS=5` → cooldown → reset, то есть счёт **подряд идущих** ошибок. Несёт обе известные корпусу слабости — серия «до первого успеха» залипает после починки, и чередование «живой→тихий→живой» держит счётчик на 1 бесконечно. Идея размыкателя полезна, счёт делать по свежести последнего отказа.
- **Дашборд**: у vteam наблюдаемость уже через надзор + уведомления фаундеру; второй канал того же.

## Где vteam строго сильнее (не менять)

| ось | Auto-Company | vteam |
|---|---|---|
| состояние | один `consensus.md`, отсюда нужда в `consensus rollback` | граф: queryable, история, связи |
| переходы | проза в промпте, дисциплина агента | `flow__Transition` объявлены данными, с актором; `rc=7` на необъявленном |
| допуск | rate-limit wait + breaker | гварды темпа, слота, cooldown, свежести замера |
| качество | QA-персона | revert-verify: оси + мутанты, краснящие ровно своё |

## Подтверждение уже принятого решения

Их «сквад под задачу» не даёт vteam нового механизма, но подтверждает выбор фаундера: ревью **отдельной задачей очереди**, следующий воркер читает коммит предыдущего своим свежим окном (решение лежит в `d4be8d85`). Их архитектура — независимый аргумент в пользу его реализации.

## Guardrails — того же класса, что мои хард-флоры

Не трогать `~/.ssh`, `~/.config`, `~/.claude`; не коммитить секреты; новые проекты только под `projects/`.

## ⛔ Поправка 2026-08-30 — рекомендация «восстановить размыкатель» имеет НЕНАЗВАННУЮ зависимость

Раздел «Что брать НЕ стоит» выше заключает: «идея размыкателя полезна, счёт делать по
свежести последнего отказа». Вывод верен, но **неисполним сегодня**, и причина не в форме
счёта, а в том, что у размыкателя **нет предиката, который он считал бы**.

Три артефакта графа, каждый верен по отдельности, и **ни один не ссылается на два других**
(замер: перекрёстных ссылок 0, все входящие — записи развилок):

| артефакт | что говорит |
|---|---|
| этот разбор | «размыкатель полезен, счёт по свежести отказа» |
| `556ddda5` (Done, 19.08) | размыкатель **снят**: `selfDisabled` читался, но **никем не писался**. Парный `cooldown` починен переводом на граф (`flow__Run_startedAt` — то, что реально пишет recorder); у breaker аналога **нет** — в графе нет чистого признака «провал» |
| `7c0f8c1d` (Backlog) | ближайший кандидат в такой признак — `flow__Outcome_isOutput` — **сломан**: `worker-completed` пишется в `vteam-dispatch.sh:255` **сразу после спавна**, до всякой работы, и помечен выходом |

⇒ **Порядок исполнения, которого нет ни в одном из трёх:** сперва `7c0f8c1d`, затем
размыкатель. Обратный порядок даёт размыкатель, который считает **спавны** и потому не
может отличить «воркер произвёл артефакт» от «воркер порождён и умер через минуту» — то есть
воспроизводит ровно тот дефект, ради защиты от которого его и восстанавливают.

⛤ **Лечение уже сформулировано и измерено** — в отклонённой опции развилки `a027e4c3`:
«переход в `StageDone` с отметкой времени, спавном не подделывается». Это третий вариант из
таблицы `7c0f8c1d` (считать выход по **последствию**, а не по самоотчёту актора), и он
единственный, чей предикат размыкатель подделать не сможет.

⚠ Опция помечена `OutcomeRejected` — то есть в своей развилке **не была выбрана**; это не
вердикт «не делать», а незапущенная работа с готовой формулировкой.

## ⛤ Независимое подтверждение чисел разбора (вторая точка замера)

Числа выше сняты одним прогоном 2026-08-29. Прочитаны повторно, другой сессией, из исходников
Auto-Company: `MAX_CONSECUTIVE_ERRORS=5` · `COOLDOWN_SECONDS=300` · защита консенсуса
`backup → validate → cmp → FAIL + restore + счётчик`. **Совпало.** ⇒ ни одно несущее число
этого разбора больше не является утверждением из одной точки замера.
