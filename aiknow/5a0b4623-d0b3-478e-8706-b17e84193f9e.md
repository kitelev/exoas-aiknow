---
exo__Asset_uid: 5a0b4623-d0b3-478e-8706-b17e84193f9e
exo__Asset_createdAt: 2026-09-08T15:56:43
exo__Asset_updatedAt: 2026-09-10T23:03:13
exo__Instance_class:
  - "[[39a39239-2a97-483a-ba91-fb01bf5c85f3]]"
exo__Asset_createdBy: "[[4ef3962d-b8a7-42b5-bd28-88ec846f1d13]]"
exo__Asset_label: "Правило: self-authored-claim-loses-its-provenance (полный текст; пилот rules-from-graph, 2026-09-06)"
aliases:
  - "Правило: self-authored-claim-loses-its-provenance (полный текст; пилот rules-from-graph, 2026-09-06)"
  - self-authored-claim-loses-its-provenance
exo__Asset_isDefinedBy: "[[2ce77ac1-3f78-4f64-9dc1-41ea01da1ef0]]"
inbox__ExoAssistantKnowledge_decay: "[[06a5b9f9-da93-4234-883f-3f0cf65b2fba]]"
inbox__ExoAssistantKnowledge_confidence: "[[227def30-f56f-4f5f-936d-f6a81ad73c96]]"
---




<!-- СЖАТО 2026-08-26 (/rule-compress): 104.9 КБ → ~37 КБ (−64%). 24 датированных разбора
     (81 КБ) вынесены в граф как справочник on-demand; во флоре остались ЯДРО (принцип, три формы,
     дискриминатор, floor'ы по формам, три признака) и ТАБЛИЦА «чем соврал → действие».
     Причина: флор корпуса дошёл до 762K ток (76% окна 1M), 22 сессии за 48ч падали с
     Prompt is too long. Полный текст до сжатия — git history этого файла.
     STEP 1 baseline: сигнатура «греп журнала без -a» = 1003 вызова / 175 сессий / 30 дней при
     1 задокументированном инциденте ⇒ FP ≈ 99.9% ⇒ хук невозможен, сжатие правомерно. -->

<!-- Added by /session-retrospective 2026-08-16 — root: ТРИ самоопровержения за одну 45-часовую сессию, все три одного класса: утверждение произвёл я сам, и оно потеряло метку происхождения. -->
# Утверждение, которое произвёл ТЫ САМ, теряет метку происхождения — и дальше потребляется как внешний факт

## Принцип

У чужого утверждения есть **источник**, к которому можно сходить: документация, чужой замер, чужой
код. У **своего** источника нет — он и есть ты. Поэтому собственное утверждение, записанное час
назад, при повторном чтении **неотличимо от установленного факта**: оно лежит в твоём же
документе, сформулировано уверенно, и внешней инстанции, с которой его сверить, не существует.

⛔ Дефект **тише** обычной ошибки по построению: сомнение возникает от расхождения с внешним
источником, а тут источник — ты. Поэтому такое утверждение не перепроверяют **никогда**, пока
кто-то посторонний не наступит на него.

⛤ Отличие от соседей — по **АВТОРУ и ПОТРЕБИТЕЛЮ**, а не по предмету:

| правило | кто произвёл | кто потребляет | чем ловится |
|---|---|---|---|
| `verify-before-assert` | внешний мир | я | свежий grep/Read |
| `decision-surface-must-derive-from-mechanism` | я | **фаундер** в момент выбора | производность от механизма |
| `retracted-claim-outranks-its-correction` | я, но **опровергнуто ниже в том же файле** | следующий читатель | сверка верха с низом |
| `graph-record-outlives-its-referent` | мир изменился под записью | любой | сверка с миром |
| **это правило** | **я** | **я же, позже** | ⛔ ничем — внешней сверки НЕТ |

### Три формы (все три — одна сессия 2026-08-14/16)

| форма | как выглядит | что оказалось |
|---|---|---|
| **число в прозе** | «потолок 2-3 воркера, выше — гонки на файлах» — в моём же документе решений, подпирало два решения | источника **нет**: ни константы в коде, ни правила в корпусе. Фактический механизм — одиночный слот; наблюдаемый максимум **9** |
| **артефакт своего замера** | «максимум 30 одновременных детей» | артефакт **метода**: 25 детей без терминального события остались «живыми» навсегда. Пере-замер по пересечению интервалов → **9** |
| **вывод из индикатора** | «`loop-health` загружен и падает (`rc=1`) ⇒ сторож слеп» | `rc=1` у чекера = **«найдены тревоги»** (`return 1 if bad else 0`), `stderr` 0 байт. Сторож был исправен и **кричал** |

⛤ Формы разные, корень один: **я произвёл значение и тут же перестал считать его своим**.

### Дискриминатор (один вопрос, задавать СЕБЕ)

> **Могу ли я назвать ОДНОЙ строкой, чем это установлено?**

Не «уверен ли я», не «правдоподобно ли» — **чем**. Ответ вида «я это где-то мерил», «так было
раньше», «это же очевидно из `rc`» = утверждение **не установлено**, оно **сформулировано**.

⚠ Уверенность тут — не сигнал: собственная формулировка ощущается тем надёжнее, чем лучше
написана. Красиво сформулированное неустановленное утверждение убеждает **сильнее**, чем
коряво сформулированное измеренное.

⛤ Формы 4-5 (замерен СОСЕДНИЙ механизм · ПРОГНОЗ о механизме) и таблица «чем именно
соврал измеритель» — ниже по роутеру и в графе; у них свои дискриминаторы.

## Три формы (все три — одна сессия 2026-08-14/16)

| форма | как выглядит | что оказалось |
|---|---|---|
| **число в прозе** | «потолок 2-3 воркера, выше — гонки на файлах» — в моём же документе решений, подпирало два решения | источника **нет**: ни константы в коде, ни правила в корпусе. Фактический механизм — одиночный слот; наблюдаемый максимум **9** |
| **артефакт своего замера** | «максимум 30 одновременных детей» | артефакт **метода**: 25 детей без терминального события остались «живыми» навсегда. Пере-замер по пересечению интервалов → **9** |
| **вывод из индикатора** | «`loop-health` загружен и падает (`rc=1`) ⇒ сторож слеп» | `rc=1` у чекера = **«найдены тревоги»** (`return 1 if bad else 0`), `stderr` 0 байт. Сторож был исправен и **кричал** |

⛤ Формы разные, корень один: **я произвёл значение и тут же перестал считать его своим**.

## Дискриминатор (один вопрос, задавать СЕБЕ)

> **Могу ли я назвать ОДНОЙ строкой, чем это установлено?**

Не «уверен ли я», не «правдоподобно ли» — **чем**. Ответ вида «я это где-то мерил», «так было
раньше», «это же очевидно из `rc`» = утверждение **не установлено**, оно **сформулировано**.

⚠ Уверенность тут — не сигнал: собственная формулировка ощущается тем надёжнее, чем лучше
написана. Красиво сформулированное неустановленное утверждение убеждает **сильнее**, чем
коряво сформулированное измеренное.

<!-- Added by /session-retrospective 2026-08-27 (vteam-7fa61e16, StageSpecification) — root: замерил «пустая env не включает питоновский дефолт» (верно) и распространил на СМЕЖНЫЙ механизм «ссылка на необъявленную переменную даст пустую строку» (ложно: под set -u это терминальный обрыв). Дискриминатор ядра ответил ДА — замер был, просто не тот. -->
### ⛔ ЧЕТВЁРТАЯ форма: замерен СОСЕДНИЙ механизм — дискриминатор выше отвечает ДА и потому УСЫПЛЯЕТ

У трёх форм источника нет вовсе; здесь он **есть и верен**, но относится к другому механизму ⇒
на «чем установлено?» ты честно называешь замер, и ядро молчит по построению.

- ⚠ **Tell:** замер и вывод — о **разных подлежащих** («пустая переменная» ↔ «необъявленная»).
  Спрашивать «мерил ли я ИМЕННО ЭТО подлежащее», а не «мерил ли я».
- ✅ **Floor: подлежащее вывода прогнать своей пробой** — обычно однострочна и часто опровергает.
- ⛤ Вывод при этом **может устоять**: ложно обоснование, не решение (`decision-surface-must-derive-from-mechanism`
  §форма №6) ⇒ коррекцию **показать**, а не переписать молча.

_Empirical 2026-08-27 (`7fa61e16`): замерил «пустая env не даёт питоновский дефолт» (верно) →
записал «ссылка на необъявленную даст пустую строку»; под `set -uo pipefail` это **терминальный
обрыв прогона**, а не пустая строка. Ушло в тело тикета до коррекции._

#### ⛔ Подвид ЧЕТВЁРТОЙ формы: подлежащее — КРИТЕРИЙ, сформулированный ТОБОЙ ЖЕ абзацем выше

Механизм замерен ВЕРНО и критерий выведен ВЕРНО — ложен **охват**: критерий рождён на одном экземпляре и там же применён, по популяции не прогнан ⇒ работает как оправдание частного случая, а не как предикат. ⛔ Дороже обычной формы: вывод уходит в durable **как доказанный**, а рядом стоит убедительный вывод механизма.

- ⚠ **Tell, ловится перечитыванием СВОЕГО текста:** есть фраза «X ⇒ обязан Y — это **факт данных**, а не выбор», и рядом ровно ОДИН пример. Спроси «сколько носителей ему удовлетворяют», а не «подходит ли этот».
- ✅ **Floor: критерий, выведенный в ЭТОМ ЖЕ артефакте, немедленно прогнать по ВСЕЙ популяции** тем же предикатом, которым нашёл первый экземпляр — одна команда, она же даёт число для текста.

_Empirical 2026-09-09 (RFC `8e2ebde0`): «range = X ⇒ обязан зависеть» применено к ОДНОЙ ссылке ⇒ «DAG ацикличен»; свип тем же предикатом — **22** ссылки, 16 таких же `range` ⇒ ацикличного объявления нет. Прошло само-ревью и гейт 6/6 дважды; поймала панель._

### ⛔ ПЯТАЯ форма: ПРОГНОЗ о механизме — дискриминатор ядра к нему НЕ ПРИМЕНЯЕТСЯ, потому что будущее «ещё не установлено» ПО ОПРЕДЕЛЕНИЮ

Четыре формы выше — про **измеритель**, который соврал (число, греп, ключ, соседний механизм);
у всех есть замер, верный или нет. Пятая обходится **без замера вовсе**: утверждение стоит в
БУДУЩЕМ времени («после сброса окна темп станет достоверным», «рассосётся само», «поедет»), и
потому читается как **план**, а не как claim. Вопрос ядра «чем установлено?» к нему субъективно
неприменим — будущего же ещё не было.

⛤ **Ложна именно эта неприменимость.** «После X станет Y» — утверждение не о будущем, а о
**ветке кода, исполняющейся после X**, и она существует и читается **прямо сейчас**. То есть
проверка стоит один `Read`, а не ожидание.

- ⚠ **Tell:** пишешь «после X станет / поедет / рассосётся Y» и не можешь назвать **строку**,
  где Y происходит. ⛔ Не «подожду и увижу» — это отдаёт проверку времени.
- ✅ **Floor: прочитать ветку, исполняющуюся ПОСЛЕ события**, а не ту, что к нему приводит.
  Типовой промах — прочитать сам триггер (`points = []`) и не дочитать, что идёт следом
  (`points.append(...)` ⇒ одна точка ⇒ охвата нет ⇒ **обратный** исход).
- ⛔ **Цена выше, чем у форм 1-4: прогноз ОСТАНАВЛИВАЕТ работу.** Неверное число зовёт
  пересчитать; неверный прогноз зовёт **ждать** — и ожидание выглядит дисциплиной, а не
  дефектом. Плюс он уезжает в durable как «почему НЕ потребовало вмешательства».
- ⛤ **Рецидивирует ПАЧКОЙ в одном ходе:** прогноз попадает и в тикет, и в отчёт фаундеру, и в
  собственный план — из одного непрочитанного места. ⇒ найдя, чинить **все** носители сразу.

_Empirical 2026-09-02 (простой конвейера vteam): записал в тикет `6440265e` и дважды сообщил
фаундеру «после 06:00 сброс окна вылечит `tempo_trusted`». Чтение `vteam-burn.sh` показало
обратное: `:202` обнуляет ряд, `:207` кладёт одну точку, `:227` `base[0] == now` ⇒
`trusted = False` **сразу после** сброса; доверие возможно не раньше `MIN_SPAN_H` (`:117`, 1 ч)
и только при монотонном ряде. Три носителя правились одним заходом._

## Floor — по форме

1. **Число в прозе.** Пишешь число в durable-артефакт — **пиши рядом ЗАПРОС/КОМАНДУ**, которой
   оно получено, а не слово «проверено». Тогда следующий читатель (в т.ч. ты) пере-выведет за один
   вызов вместо того, чтобы наследовать.
2. **Свой замер.** Прежде чем называть результат фактом о мире — спроси: **«что мой метод НЕ
   видит?»** У счётчика «сколько одновременно» слепая зона — записи без терминального события; у
   `grep -c` — несколько вхождений на строке; у выборки — распределение параметра, двигающего
   ответ.
   - ⛔ **ПЕРВЫЙ прогон нового наблюдателя — на случае, ОТВЕТ КОТОРОГО ТЫ УЖЕ ЗНАЕШЬ.** Запуск
     сразу на неизвестном оставляет тебя без независимой сверки: непустой правдоподобный ответ
     неотличим от верного. Калибровочный случай почти всегда под рукой и бесплатен — заведомо
     живой ребёнок · сессия, чью стоимость видно в футере · лист с известной длительностью ·
     чекер на заведомо чистом входе. Расхождение на нём = дефект **дискриминатора**, а не факт
     о мире.
   - ⛤ **Это НЕ канарейка** (`self-satisfying-metric-weak-verifier` §A7): та ловит **ожидаемый
     ноль** («пусто ⇒ жив ли запрос»). Здесь ответ **непустой и правдоподобный** — канарейка
     зелена, а число неверно. Калибровка отвечает на другой вопрос: «мой дискриминатор вообще
     различает то, что обещает?»
   - ⚠ **Симптом-tell частоты:** второй наблюдатель за сессию, чей первый прогон соврал, — уже
     не совпадение. Замер 2026-08-16: **5 из 5** построенных за сессию наблюдателей (сторож ×2,
     парс стоимости, парс длительностей, чтение `rc` чекера) имели дефект дискриминатора на
     первом прогоне; все пять пойманы поздно и случайно, потому что калибровочного прогона не
     было ни у одного. Каждый стоил бы одной команды.
3. ⛤ **Вывод из индикатора — САМАЯ дешёвая проверка и самая часто пропускаемая.** Индикатор
   (`rc`, статус, счётчик, цвет) есть **СВОДКА** чего-то. Прежде чем судить по нему — **открой
   то, что он сводит**: лог, вывод, тело. Один `tail` дешевле любого разбора.
   - ⛔ **«То, что он сводит», бывает НЕ логом, а СОСЕДНЕЙ ПОДКОМАНДОЙ того же инструмента** — и тогда floor читается как неприменимый: открывать вроде нечего, вывод уже перед глазами. ⚠ Tell: индикатор — СТРОКА В ВЫДАЧЕ (`quarantine-pinned`, `deferred-*`, `skipped`), а не код возврата; у такой строки почти всегда есть профильный верб (`<tool> quarantine list`), и он даёт ДРУГОЙ ответ. ✅ Floor: `<tool> --help` перед выводом из строки-индикатора — один вызов. _(2026-09-05: «~450 пинов блокируют синк» из parity; `quarantine list` → 0 открытых в одном vault и 7 в другом.)_
   - ⛔ **Код возврата ≠ «сломан».** У чекера/аудита/гейта `1` штатно означает **«нашёл»**.
     Дискриминатор — `stderr` (пуст ⇒ не крах) плюс конвенция в коде (`grep -n 'return 1\|sys.exit'`).
   - ⛔ **И конвенция НЕ ОДНА на корпус — она ИНВЕРТИРУЕТСЯ между соседями по одному флоту.**
     У детекторов `structural-detect-tick` `1`=находки / `2`=BROKEN; у `dlp-invariant-check.sh`
     (джоб `dec-anchor-check`) ровно наоборот: `1`=BROKEN, `2`=нарушение найдено. ⇒ **обзор флота
     по колонке `rc` детектором НЕ является**: он даёт ложную тревогу на исправном (`2` прочитан
     как поломка) и ложное успокоение на сломанном (`1` прочитан как «нашёл»). ⚠ Симптом-tell:
     ты перечислил джобы с ненулевым `rc` и делаешь вывод, **не открыв ни одного** их лога.
     ✅ Floor: `rc` даёт **кандидатов**, вердикт — конвенция ИМЕННО ЭТОГО скрипта
     (`grep -n 'exit [0-9]' <скрипт>`) плюс его лог. ⚠ И он **протухает**: `launchctl` хранит
     код ПОСЛЕДНЕГО прогона, поэтому починенный предмет светит красным до следующего запуска
     джоба — воспроизводи прогоном, а не читай колонку. _(Empirical 2026-08-31: `rc=2` у
     `dec-anchor-check` прочитан мной как BROKEN; фактически — VIOLATION инварианта DEC#1,
     эскалированная фаундеру в 09:37 и уже заведённая тикетом. Тревога ложная, находка настоящая.)_
   - ⚠ Симптом-tell: ты пишешь «X сломан/слеп/не работает», а **лог X не открывал**.
   - ⛔ **ЗЕРКАЛО, и оно ИНВЕРТИРУЕТ смысл: молчание ERROR-КАНАЛА — признак ЗДОРОВЬЯ.**
     `StandardErrorPath` джоба и любой `*.err`/`poll.log`, куда механизм пишет **только сбои**,
     не растёт, пока всё хорошо. ⇒ «лог не обновлялся 82 часа» читается как «механизм умер», а
     означает «82 часа без единой ошибки». ⚠ Tell: ты собираешься объявить смерть по **отсутствию**
     записей — спроси, что этот файл вообще пишет (`grep -n 'StandardErrorPath\|>&2' <plist/скрипт>`).
     ✅ Вердикт даёт **выходной артефакт** механизма (его `state.json`, его записи в графе), а не
     журнал ошибок. _(Empirical 2026-08-31: два бота «молчали 82ч» с оборванной строкой посреди
     записи — выглядело как двойное зависание; `state.json` обоих обновлён секундой назад, оба
     живы. Снято с ТРЕТЬЕЙ попытки: лог → «мертвы», vault → «сомнительно», состояние → «живы».)_
   - ⛔ **И floor «открой лог» НЕДОСТАТОЧЕН: у механизма бывает не тот ПОТОК.** Диагностический
     вывод часто идёт в `stderr` (`say() { printf … >&2; }`), а «журнал» механизма — отдельный
     файл; под супервизором `stderr` уходит в **третье** место (`StandardErrorPath` джоба).
     Тогда «ноль строк провала в журнале» — **правда о файле и ложь о механизме**: fail-loud
     сработал, просто написал в другой поток.
   - ⚠ Симптом-tell второго порядка: у механизма ЕСТЬ объявленный fail-loud (ты прочитал его в
     коде), а следов **ноль**. Молчащий fail-loud — почти всегда не «не сработал», а «пишет не
     туда, куда ты смотришь». Проверяется одной командой: `grep -nE '>&2|LOG=' <механизм>` плюс
     `StandardErrorPath` его джоба.
   - ⛤ **Цена промаха — не лишний шаг, а НЕВЕРНЫЙ ДИАГНОЗ:** «провалов нет ⇒ вызова не было»
     уводит искать несуществующий ранний выход, тогда как причина лежит в первой строке чужого
     лога. Замер 2026-08-23: 10 строк `⛔ запись прогона НЕ УДАЛАСЬ rc=6` с точным текстом
     `Wikilink … file not found` ждали в `StandardErrorPath`, пока я перебирал гипотезы по коду.
   <!-- Added by /session-retrospective 2026-08-23 — root: процитировал строку лога «SCANNED: 11 vault, проблем: 0, parity: пропущен», взял из неё «проблем: 0» и написал «алертов после фикса 0», хотя дискриминатор («какая ветка отработала») стоял в ТОЙ ЖЕ строке. -->
   - ⛔ **ТРЕТИЙ подвид, и он тише двух первых: дискриминатор лежит в ТОЙ ЖЕ СТРОКЕ, что индикатор,
     а ты прочитал строку ЧАСТИЧНО.** Оба floor'а выше предполагают, что надо куда-то **пойти**
     (открыть лог, найти другой поток). Здесь идти некуда — надо **дочитать до конца**, и потому
     проверка не срабатывает: субъективно ты источник уже открыл.
     - ⚠ **Симптом-tell отличается от двух первых:** ты не «лога не открывал» — ты **цитируешь**
       строку и берёшь из неё половину. Тель: в твоей цитате есть запятая/разделитель, а в выводе
       фигурирует только первое поле.
     - ✅ **Floor: индикатор, живущий в СТРОКЕ С ПОЛЯМИ, читается ПОЛЯМИ.** У строки вида
       `<итог>, <счётчик>, <РЕЖИМ>` вердикт даёт **режим**, а не счётчик: «проблем 0» при
       `parity: пропущен` означает «ветка не исполнялась», а не «ветка чиста».
     - ⛤ **Цена — ложное «фикс отработал в бою»:** ноль честен как факт («не сломал остальное») и
       ложен как вывод («исправленная ветка упражнена»). Отличить их можно **только** дочитав поле
       режима — то есть ошибка неопровержима изнутри собственной цитаты.
     - ⛔ **И ЗЕРКАЛО того же промаха при разборе постфактум: строки, различающиеся ИНТЕРВАЛОМ,
       принадлежат РАЗНЫМ авторам.** Прогон, стоящий через 5-9 мин от предыдущего при
       `StartInterval 1800`, — **ручной**, а не плановый. Не выяснив этого, чужой (свой же
       ручной) прогон читается как боевое подтверждение фикса. Дискриминатор — один
       `plistlib.load` + разность таймстемпов.
     - **Empirical 2026-08-23** (сессия `7120c0d9`): «алертов после фикса — 0» ушло в отчёт;
       поправку внёс оркестратор. Пере-замер показал ДВА слоя: (а) последний **плановый** прогон
       parity-ветку пропустил, (б) три прогона с `parity: прогнан` после коммита были **моими
       ручными** (интервал 5-9 мин против 1800 с) — то есть боевого подтверждения не было ни
       одного, а выглядело оно как три.

## ⛔ Самый дорогой исход — не ошибка, а ПРИСВОЕНИЕ чужой работы

Когда «сломанным» объявлен **работающий наблюдатель**, ты не просто ошибаешься — ты потом
**переоткрываешь то, что он давно сообщил**, и подаёшь как свою находку.

Замер 2026-08-16: `loop-health` ежечасно **41 час** репортил мёртвый слот воркера и **доставил
фаундеру** стерильность разведки. Оба факта я «обнаружил» тем же вечером независимо — один
положил в закрытие проекта как находку. Инструмент, объявленный слепым, работал лучше меня; его
вывод просто никто не читал.

⇒ ⚠ **Tell:** твоя «находка» касается предмета, за которым **что-то наблюдает**. Прежде чем
записывать её как новую — прочитай, что этот наблюдатель уже сказал.

## Симптомы-tell (сводно)

- ⚠ В твоём тексте стоит «проверено» / «известно» / «потолок такой-то», и на вопрос «чем?» ты
  отвечаешь **пересказом**, а не командой.
- ⚠ Число **круглое** («2-3», «~30», «полдня») — круглость есть подпись оценки, не замера.
- ⚠ Ты судишь о работоспособности по **одному полю** (`rc`, статус, цвет), и лог не открывал.
- ⚠ Ты объявляешь «находкой» то, что лежит в домене работающего монитора.
- ⛤ **Счёт по сессии:** второе самоопровержение за сессию — совпадение, третье — **свойство
  режима работы**. Тогда чинить надо не конкретное число, а привычку писать числа без источника.

⛤ **Признак один и он дешёвый: число противоречит прямому наблюдению.** Дважды за сессию,
разобранную в §A1, я видел нужное собственными глазами в сыром выводе — и всё равно сначала
поверил счётчику. ⇒ при расхождении
**наблюдение авторитетнее счётчика**, а не наоборот: у наблюдения нет промежуточного слоя,
который мог бы соврать.

⛤ **Второй признак — ОДИНАКОВЫЙ ответ там, где условия обязаны различаться** (три версии CLI
дали «строк=1 слэшей=0»; два мутанта — «ничего не покраснело»). Единообразие на различающихся
входах = сломан измеритель, а не предмет.

<!-- Added by /session-retrospective 2026-08-23 (сессия 2a0989c0) — root: шесть ложных замеров за одну сессию, и НИ ОДИН не пойман вниманием; все шесть — арифметическим противоречием внутри собственной выдачи. -->
⛤ **ТРЕТИЙ признак — САМЫЙ дешёвый, потому что не требует ВНЕШНЕЙ ОПОРЫ: два числа В ОДНОЙ
ВЫДАЧЕ не сводятся.** Оба признака выше опираются на что-то вне замера — первый на то, что ты
видел глазами, второй на знание, что условия обязаны различаться. Здесь опора не нужна:
противоречие **арифметическое и самодостаточное**, и потому срабатывает даже там, где о предмете
не знаешь ничего.

- ⚠ **Формы, в которых оно приходит:** число больше размера популяции (34 «красных» при контроле
  27/27) · ноль строк по фильтру при непустом счёте того же файла (0 после отсечки при 85
  вхождениях искомой формулировки) · «N ссылающихся» при имени, которое является **префиксом**
  чужих имён · «45 упоминаний» против **0 вызовов в позиции команды**.
- ✅ **Floor: печатать РЯДОМ вторую величину, из которой первая обязана следовать** — размер входа
  возле числа находок, знаменатель возле доли, счёт популяции возле счёта подмножества. Тогда
  противоречие объявляет себя само, а не ждёт, пока его заметят.
- ⛔ **Замер эффективности за сессию, ради которого признак и записан: аккуратность поймала 0 из 6,
  арифметика — 6 из 6.** Все шесть замеров были написаны внимательно и каждый выглядел очевидным
  ⇒ «буду внимательнее» как митигация этого класса **опровергнута исполнением**; работает только
  вторая величина в той же выдаче.
- ⛔ **ЗЕРКАЛО §A33 на стороне ПИШУЩЕГО: число без ОБЛАСТИ ЗАМЕРА порождает то расхождение,
  которое §A33 учит расследовать.** §A33 — floor ЧИТАТЕЛЯ («разошлось с чужим числом ⇒ сверь
  состояние входа»); у автора, чьи числа ВЕРНЫ, он не срабатывает вовсе — сверять нечего, пока
  их не с чем сравнить.
  - ⚠ **Tell при отправке отчёта/пинга:** в строке стоит число, а слов «на чём мерил» рядом нет.
    Своя область очевидна ТЕБЕ и невидима адресату — у него своя, и твоё число он прочтёт как своё.
  - ✅ **Floor: ярлык области — часть ЧИСЛА, а не комментарий к нему** («54 дескриптора [клон]»,
    «50 рёбер [vault-exodev]»). Пять символов против целого раунда сверки.
  - ⛤ Особенно у ЭТАПА КОНВЕЙЕРА: соседние этапы мерят ТОТ ЖЕ предмет на РАЗНЫХ корпусах (клон
    против монтирования), и совпадение чисел там — исключение, а не норма.
  _(2026-09-10, O4.2.2 `154ac631`: пинг «shortName 47, dependsOn 51» против «59 / 50» у
  оркестратора; оба верны, области разные — 47+12=59, 50+1=51.)_

- ⛤ **Отличать от третьего подвида §выше** (дискриминатор в ТОЙ ЖЕ СТРОКЕ, прочитанной частично):
  там выдача **непротиворечива**, и дочитать её надо до конца; здесь она **противоречит себе**, и
  дочитывать нечего — надо пересчитать.
- ⛔ **ЧЕТВЁРТАЯ форма, на которой ТРЕТИЙ признак МОЛЧИТ: ПЕРЕЛИВ `awk`-ДИАПАЗОНА в ИСХОДНИКЕ.**
  §A18 предписывает резать по МАРКЕРУ БЛОКА и выведен на **выводе** харнесса, где блоки идут
  подряд. В **файле** якорь конца (`/^# ⛔ …/`) может лежать за сотни строк ⇒ `awk`-диапазон
  честно исполняет floor и захватывает чужие блоки, а счёт по нему **не противоречит себе** —
  он просто велик и правдоподобен. То есть floor соблюдён, исход неверен.
  - ⚠ **Tell (единственный работающий): ненулевое число там, где ты ждал ноль по СВОЕЙ свежей
    правке.** Свою вставку ты знаешь построчно; расхождение с ней и есть сигнал — арифметика
    выдачи тут молчит.
  - ✅ **Floor: у СВОЕЙ вставки диапазон известен ТОЧНО ⇒ якоря не нужны вовсе** — `sed -n 'A,Bp'`
    по номерам из `grep -n` собственной правки. Нужны якоря — печатать `| wc -l` рядом со счётом
    и сверять с ожидаемым размером блока.

- ⛔ **Частота этого класса не падает от аккуратности**: все семь написаны внимательно и каждая
  выглядела очевидной. Падает она только от **калибровки** — floor этого правила («первый прогон
  нового наблюдателя — на случае, ответ которого ты уже знаешь») стоил бы одной команды в каждом
  из семи случаев.
- ⚠ **Цена асимметрична по направлению ошибки.** Счётчик, занизивший результат, поднимает тревогу
  и чинится; счётчик, **подтвердивший желаемое** (0 % flake, «мутанты ничего не красят» = «оси
  вакуумны»), закрывает вопрос неверным ответом и уводит чинить не то.


- ⛔ **ЗЕРКАЛО той же формы, и оно ТИШЕ: маркер отказа — ПОДСТРОКА строки УСПЕХА.**
  §A21 разбирает «счётчик даёт 0, а вердикт FAIL» (маркеров несколько). Обратный случай:
  счётчик даёт **FAIL**, а предмет зелен — потому что `grep -oE 'FAIL' | tail -1` матчит
  подстроку внутри итоговой строки `PASS=7 FAIL=0`. ⇒ измеритель докладывает провал
  ровно там, где напечатан успех.
  - ⚠ **Tell — арифметический и в ОДНОЙ выдаче:** `rc=0` при вердикте «FAIL». ⛔ Не читать
    как «харнесс сломан, но не сигналит»: у пяти предметов подряд одинаковый противоречивый
    ответ = сломан ОБЩИЙ слой, то есть твой grep (§A13).
  - ✅ **Floor: якорить на ПОЛНУЮ форму вердикта** (`PASS=[0-9]+ FAIL=[0-9]+`), а не на слово;
    и судить по `rc`, если он есть — он не является подстрокой ничего.
  - ⛤ Empirical 2026-08-30: 5 харнессов, все `rc=0` / `PASS=N FAIL=0`; наивный grep объявил
    провалившимися все пять. Восьмой ложный измеритель за сессию, снова пойман арифметикой.
## ⛤ ХАРНЕСС покрывает МЕХАНИЗМ; замер, которым механизм направляется, не покрывает НИЧТО

Инвестиция в верификацию растёт по **механизмам** (хук, скрипт — у них есть предмет, который
можно мутировать). У ad-hoc замера предмета нет: он пишется в моменте, исполняется раз и
выбрасывается ⇒ харнесс к нему невозможен **по построению**. Чем полнее покрыты механизмы,
тем заметнее непокрытая половина — числа, которыми эти механизмы направляются.

- ⚠ **Симптом-tell:** ты только что закрыл N механизмов осями — и следующий вывод делаешь
  числом, полученным командой, набранной в этом же ходе.
- ✅ **Floor: замер без харнесса обязан нести ВТОРУЮ величину в той же выдаче** (знаменатель
  рядом с долей, размер входа рядом с числом находок). Противоречие тогда объявляет себя само.
- ⛔ «Буду внимательнее» здесь **опровергнуто счётом**: аккуратность поймала 0 из 3, арифметика
  и чтение выдачи — 3 из 3.

**Empirical 2026-08-27:** сессия создала **122** harness-verify (из 126 в dotfiles) — и в том же
окне три ad-hoc замера соврали, два из них **в пользу** проверяемой гипотезы.

## ⛔ Проба-ЧТЕНИЕ, которая СОЗДАЁТ свой предмет: `sqlite3 <путь>` на несуществующем пути заводит пустую БД, и `.tables` даёт правдоподобное «пусто»

Ошибся путём к БД (`~/.claude/analytics.db` вместо `~/.claude/telemetry/analytics.db`) — `sqlite3` не падает, а создаёт файл 0 байт с `rc=0`; `.tables` печатает ничего, и это читается как «таблиц нет / телеметрия пуста». Ложный ноль тише обычного: после пробы предмет СУЩЕСТВУЕТ, и следующая проверка (`test -f`) его подтвердит.

- ✅ **Floor:** `test -f <путь>` ДО открытия — существование проверяет только он. `sqlite3 -readonly` страхует от СОЗДАНИЯ (на отсутствующем пути rc=1, файла нет; без флага — файл 0 б, rc=0), ⛔ но его отказ НЕ различает «файла нет» и «WAL-БД без `-wal`/`-shm`» — на живой `analytics.db` (WAL) он дал тот же `unable to open database file` (замер 2026-09-10, воспроизведено на фикстуре). Пустую БД, если уже создал, — удалить: она переживёт сессию и следующему покажет тот же ноль.
- ⚠ **Tell:** размер файла 0 байт рядом с «таблиц нет»; путь к БД взят по памяти, а не из `ls` / кода писателя.
- ⛤ Тот же класс у `>` (`self-satisfying-metric-weak-verifier` §A27) и `open(path,"w")` (`vault-asset-creation` §A5): открытие на запись — мутация независимо от того, писал ли ты.

_(2026-09-10: `sqlite3 ~/.claude/analytics.db '.tables'` → пустой файл 0 б, удалён; настоящая БД — `~/.claude/telemetry/analytics.db`, 2 206 skill-событий за 94 дня.)_

## ⛤ ТАБЛИЦА: чем именно соврал измеритель (главный носитель — опознание за секунды)

⛤ Все 24 разбора — вариации ОДНОГО класса («мой ad-hoc замер соврал»), различаются **инструментом**.
Строка отвечает на «чем соврал ⇒ что делать»; механизм — в справочнике по номеру §A.

| # | дата | Симптом ⇒ действие |
|---|---|---|
| **§A1** | `2026-08-19` | Семь ad-hoc счётчиков за день соврали, и все опознаны ОДНИМ признаком — противоречием с тем, что видел глазами ⇒ таблица инструментов (`--stat` режет колонки · `grep -c 'it('` матчит подстроку · `/runs/{id}/jobs` отдаёт ПОСЛЕДНЮЮ попытку · zsh-`echo` разворачивает escape · `rfind('---')` ловит маркер превью · jest без `--verbose` не печатает `✕`) ⇒ при расхождении **наблюдение авторитетнее счётчика** |
| **§A2** | `2026-08-21` | Одна группа занимает 30 %+ выдачи, а её образец подозрительно конкретен (`Exit code 127`, `HTTP 429`) ⇒ нормализация ключа (`\d+ → N`) схлопнула ЗНАЧАЩЕЕ число, и образец показан от первого члена ⇒ нормализовать ТОЛЬКО то, что не несёт различия, о котором спрашиваешь; распечатать распределение внутри группы |
| **§A3** | `2026-08-19` | Список красных ПУСТ при честно краснеющих тестах ⇒ ANSI-коды раскрасили вывод и разорвали твой якорь (`× …\x1b[2m > \x1b[22mdescribe`) ⇒ `\| sed $'s/\033\[[0-9;]*m//g'` ПЕРВЫМ звеном пайпа; ⛔ не читать пустоту как свойство предмета |
| **§A4** | `2026-08-23` | `grep` дал ноль по строке, которая ЗАВЕДОМО в файле (сам только что отправил ЛИБО журнал заведомо непуст по этой теме), БЕЗ строки `Binary file … matches` ⇒ один оборванный многобайтовый символ (обычно от `cut -c` по русскому тексту) делает файл «бинарным», и вывод подавляется ЦЕЛИКОМ ⇒ вердикт-несущий греп по журналу — всегда `grep -a`; дискриминатор `grep -c` против `grep -ac`. ⛔ **И НЕ подставляй фолбэк-ЧИСЛО: `grep -c … \|\| echo 0` УНИЧТОЖАЕТ дискриминатор** — он схлопывает «файла нет» и «вывод подавлен» в один напечатанный ноль, а именно ПУСТАЯ строка при `rc=1` и есть tell подавления. ⚠ Tell при написании: ты страхуешь счётчик фолбэком «на случай, если файла нет» — сперва `test -f`, число печатать только из живого грепа (2026-09-02, `7e5c4b19`: `OWNMUT` дал «0» из моего же `echo`, честный `grep -ac` — тот же 0, но при канарейке `END`=1993, то есть первый замер был непригоден) |
| **§A5** | `2026-08-25` | Ноль по ЧАСТИ перечисленного при непустом ответе по остальному — читается как утрата данных ⇒ общий суффикс вне группы (`^(A\|B\|prefix_):`) убил ветвь-префикс ⇒ члены альтернации делать однородными ЛИБО (дешевле) читать предмет целиком; результат мутации проверять `yaml.safe_load`, а не грепом |
| **§A6** | `2026-08-25` | Греп кириллицей по JSONL даёт **0** при валидном файле и твоей же свежей записи ⇒ писатель звал `json.dumps` без `ensure_ascii=False` ⇒ в файле `\uXXXX`, искомой подстроки нет ⇒ журнал разбирать ПАРСЕРОМ (`json.loads` построчно); `grep -a` тут НЕ помогает |
| **§A7** | `2026-08-25` | `git log -S '<цитата из тела коммита>'` → 0 при строке, которую видишь в `git log -1 --format='%B'` ⇒ `-S` ищет по СОДЕРЖИМОМУ ФАЙЛОВ ⇒ по сообщениям — `--grep=`, по файлу вперёд — `git log <sha>..HEAD -- <путь>`; ноль засчитывать только с канарейкой |
| **§A8** | `2026-08-22` | Свип «кто зовёт компонент» дал N вызывающих ⇒ ⛔ считал УПОМИНАНИЯ: комментарии соседей, строка присваивания пути, имя в XML-комментарии plist ⇒ предикат по имени проверять КОНТРПРИМЕРОМ с обоих концов (заведомо НЕ-вызов + заведомо иначе названная форма) |
| **§A9** | `2026-08-22` | Находка на пути/имени, чей ХВОСТ обрезан ровно до более короткой альтернативы (`.jsonl` → `.json`) ⇒ регэксп берёт ПЕРВУЮ подошедшую ветвь, а не длинную ⇒ длинную ставить первой (`jsonl\|json`) либо якорить конец |
| **§A10** | `2026-08-19` | Свип нашёл «нарушителей» среди файлов, чьи авторы заведомо знают правило ⇒ ключевался на ИМЕНИ переменной, а имена локальны (`SDT_`, `SENTINEL_`) ⇒ свип даёт КАНДИДАТОВ, вердикт даёт ПРОБА: подать вход и посмотреть исход |
| **§A11** | `2026-08-22` | Правишь измеритель второй раз подряд, и число каждый раз падает ⇒ сходимость к нулю НЕ значит приближение к истине ⇒ когда кандидатов стало единицы — ЧИТАТЬ их, а не уточнять предикат; искать свойство через его РЕАЛИЗАЦИИ, а не через слова |
| **§A12** | `2026-08-21` | Отгружаешь работу, которую никто, кроме тебя, не читал, при зелёных гейтах ⇒ отсутствие внешнего ревьюера в артефакте НЕВИДИМО (PR выглядит образцово) ⇒ назвать одной строкой в теле PR/отчёта: «без внешнего ревью, проверено revert-verify + N гейтов»; догоняющий проход по коммиту дёшев |
| **§A13** | `2026-08-21` | Отрицательный вердикт у ВСЕХ осей/мутантов разом ⇒ почти всегда общий слой — ПОДМЕНА НЕ ДОЕХАЛА (env читается как `$1`, имя переменной иное) ⇒ перед серией подмен одна проба заведомо непригодным входом; выдача ОБЯЗАНА измениться, иначе терминальный `BROKEN`. ⛤ **ЗЕРКАЛО, где этот floor НЕИСПОЛНИМ:** флип РЕГРЕССИОННЫЙ («не сломал ли фикс соседей») ⇒ ожидается ИДЕНТИЧНОСТЬ, и «регрессий 0» неотличимо от «подмена не доехала» ⇒ судить по СОСТАВУ вердиктов (`diff` строк `✅/❌` двух прогонов): единственное допустимое расхождение — строка, называющая файл под тестом; ноль расхождений = `BROKEN`. ⛔ Не путать с `integration-test-revert-verify` §A42 (там идентичность = вакуумный откат) |
| **§A14** | `2026-08-26` | Цитируешь строку чужого вывода и берёшь из грепа ноль, хотя `❌` видишь глазами ⇒ ad-hoc предикат построен ПО ПАМЯТИ о формате (`^❌` против `  ❌`, `# ---` против `# ──`) ⇒ над чужим выводом не якорить: читать целиком либо брать якорь ИЗ файла. ⛤ Тот же ноль у СВОЕГО текста, написанного минуту назад: слова маркера разделены `\|` markdown-таблицы (`\| W7 \| … \| fail-loud …`) ⇒ подстроки «W7 fail-loud» не существует НИКОГДА, и ноль читается как «контент не доехал». ⚠ Tell: проверяешь ДОСТАВКУ своего текста маркером, собранным по памяти о формулировке; ✅ якорь брать из ФАЙЛА (`\| W7 \|`) либо судить побайтно (`diff -q local remote`) _(2026-08-28, vteam-37efd39c)_. ⛔ **ТРЕТЬЯ форма, и оба tell'а выше её НЕ ловят: рецепт выдан ДОСЛОВНО** (в спеке, промпте, floor'е корпуса), **а ты его УЖЕСТОЧИЛ** — дописал `^`, `\b`, `-w`, кавычки, которых в нём не было. Память тут ни при чём: предикат стоял перед глазами, и «уточнение» ощущается аккуратностью, а не отсебятиной. ✅ Floor: выданный предикат исполнять ПОБАЙТНО; хочешь усилить — прогони ОБЕ формы и сверь счёт. ⚠ Tell тот же арифметический (§ТРЕТИЙ признак): сумма исходов не сходится с частями _(2026-09-09, приёмка `57de43cd`: спека дала `D4 пора переключить…`, я написал `^D4 пора…` ⇒ «эмиссий 0» при 63 латча + 0 ≠ 67 ПОРА)_ |
| **§A15** | `2026-08-22` | Канарейка доставки говорит «не доезжает» у харнесса, в котором ты ВИДИШЬ `"$SUT"` в строке запуска ⇒ проба непригодна ПО ПРАВАМ, а не по содержимому (пустой файл без бита исполнения отвергает стартовый гвард) ⇒ непригодность делать содержимым: пустой файл С `chmod 0755` |
| **§A16** | `2026-08-21` | Доля «дефектного» правдоподобно тревожна (22 из 56) ⇒ считал НАЛИЧИЕ АРТЕФАКТА (`.spec.json`) вместо наличия СВОЙСТВА («умеет покраснеть») ⇒ назвать свойство словами и перечислить ЕГО носители; подозрение включать на ПЛОХИХ числах, а не только на подозрительно хороших |
| **§A17** | `2026-08-21` | Ждун, рассчитанный на десятки минут, вернулся СРАЗУ ⇒ `until grep -q '<маркер>' "$LOG"` истинно с первой секунды: журнал НАКОПИТЕЛЬНЫЙ, маркер там от прошлых прогонов ⇒ ждать ПРИРАЩЕНИЯ (`BASE=$(grep -c …)` → `-gt "$BASE"`); спрашивать «переживает ли артефакт прошлые прогоны» |
| **§A18** | `2026-08-21` | Ложный вывод УЖЕ уехал в правило и в тело задачи, потому что выглядел не числом, а объяснением ⇒ читал вывод МУТАНТА, приняв за контроль (`grep -c '❌'` дал 34 при контроле 27/27 — больше, чем всего осей) ⇒ у структурированного вывода резать по МАРКЕРУ БЛОКА; коррекцию ПОКАЗАТЬ в обоих носителях. ⚠ Самый частый носитель — **вывод харнесса**: у него блоки помечены именами осей (`❌ U3`, `❌ U4`), и `grep -A N` перельётся между ними **всегда** — N подобрать нельзя, длина блока не твоя. Резать `awk '/❌ U3/{f=1} f&&/U4/{exit} f' \| cat`; tell: в выдаче «одной» оси стоят ДВА разных предмета _(08-27: так едва не записал «U3 назвал две пары»)_ |
| **§A19** | `2026-08-21` | Улика упавшего прогона — 1 байт, читается как «сбор не отработал» ⇒ повтор (сорванный таймаутом) ЗАТЁР её: один файл на предмет ⇒ имя улики несёт МЕТКУ ВРЕМЕНИ; дискриминатор — журнал прогонов (два и более END ⇒ перезапись) |
| **§A20** | `2026-08-21` | Число в выводе пробы не совпадает с тем, что ты сам туда положил («прогнано=10» при одной фикстуре) ⇒ два ложных измерителя подряд: инъекция не доехала (`VHR_DIRS` вместо `VHR_DIR`), затем соврала фикстура ⇒ канарейка доставки печатает РАЗМЕР ВХОДА; краснеет после доказанной соседней оси ⇒ подозревать ФИКСТУРУ |
| **§A21** | `2026-08-21` | Твой счётчик красных даёт 0, а вердикт файла говорит `FAIL` ⇒ маркер отказа в корпусе НЕ ОДИН (39 харнессов печатают `⛔`, 15 — `❌`) ⇒ посчитать МАРКЕРЫ прежде находок (`grep -oE '⛔\|❌\|FAIL' \| uniq -c`); унификация — не решение |
| **§A22** | `2026-08-21` | `grep -cF` по маркеру verify даёт 0 при заведомо доехавшем файле ⇒ фразу выбрал глазами по экрану, а в файле она разорвана `\n` ⇒ маркер брать ОДНОСТРОЧНЫЙ и заведомо целый (заголовок, UID, имя файла); два маркера разной природы |
| **§A23** | `2026-08-24` | Произносишь вывод о собственной работе и НЕ открывал артефакт в этом ходе ⇒ пересказ дрейфует в сторону более сильного утверждения («частично дублирует» → «дублирует») ⇒ ПРОЦИТИРОВАТЬ классификацию дословно; разошлось — артефакт авторитетнее, а замер предмета разрешает спор |
| **§A24** | `2026-08-24` | Уточняешь оракул третий раз, и меняется не количество, а ПРИРОДА промаха ⇒ у оракула классификации инверсия полярности НЕПРИМЕНИМА (обратное утверждение имеет те же FP) ⇒ остановиться после ВТОРОГО вида FP и сказать «механический путь исчерпан»; у остатка обычно есть НЕ-оракульный ход |
| **§A25** | `2026-08-27` | ДВА ТВОИХ измерителя одного предмета дали разные числа ⇒ ⛔ не объявлять один испорченным и не сочинять ему механизм: воспроизвести ОБА в одном ходе (расхождение чаще исчезает) + спросить ИСТОРИЮ предмета (существовало ли меньшее состояние). Не воспроизвелось ⇒ писать «причина не установлена», а НЕ вторую гипотезу. ⚠ Цена — ложный floor уезжает в durable-артефакт и отменяет корпусную форму, пережившую много прогонов. ⛔ **Расхождение в ДВУХ переменных сразу (путь И паттерн) «воспроизведением обоих» НЕ изолируется** — варьировать по ОДНОЙ: A(старый путь,новый паттерн) · B(новый,новый) · C(новый путь,старый паттерн). ⚠ **Tell, ловится ДО объяснения: сочинённый тобой механизм — КЛАСС, УЖЕ ЗАДОКУМЕНТИРОВАННЫЙ в корпусе** (симлинк, dual-IRI, Г-грабля). Знакомость подменяет замер и ОСТАНАВЛИВАЕТ проверку: правдоподобие растёт от того, что класс существует, а не от того, что он здесь сработал. _(2026-09-07: греп по `~/.claude/bin` дал 0, объяснил симлинком; изоляция — A=2 B=2 C=0 ⇒ виноват ПАТТЕРН, симлинк ни при чём.)_ ⛤ **Предмет ПОД КОНТРОЛЕМ ВЕРСИЙ ⇒ внешний арбитр ЕСТЬ** (`git status --porcelain -- <путь>`) и он СИЛЬНЕЕ воспроизведения: отвечает одной командой и терминально ⇒ спрашивать его ПЕРВЫМ, когда оба измерителя считают ФАЙЛЫ |
| **§A26** | `2026-08-27` | Основание правки корпуса — строка КОМПАКТ-САММАРИ («тогда-то X отверг форму Y»), и назвать команду, которой это установлено, ты не можешь ⇒ компакция стрипует провенанс сильнее всего: наблюдение возвращается СИСТЕМНОЙ строкой рядом с замеренными фактами и читается как установленный ⇒ пере-мерить предмет ОДНИМ прогоном ДО правки, и мерить ВСЕ формы разом (дискриминатор «резолюция прошла» — одинаковый ПОСЛЕДУЮЩИЙ отказ у всех форм, а не `rc=0`). Замер опроверг — писать «причина не установлена», ⛔ не вторую гипотезу. ⛤ **Тот же носитель, второй потребитель — ВЫБОР РАБОТЫ:** «хвост открыт» из саммари читается как факт о мире, а работа за это время могла быть сделана (в т.ч. тобой же) ⇒ проверять ПРЕДМЕТОМ до взятия, а не статусом/заметкой; замер стоит ОДИН вызов против цикла работы над сделанным. ⚠ Tell: берёшь задачу, чьё «не сделано» установлено только твоей прошлой записью |

| **§A27** | `2026-08-28` | Сравниваешь ДВЕ ревизии, и вывод о предмете получен через КЛЮЧ СРАВНЕНИЯ (имя извлечённого файла, свёртка вывода) ⇒ ключ обязан нести СОДЕРЖАНИЕ и только его, а ломается он в ОБЕ стороны: **потерял** различающую часть (деривация имени `tr -d '^'` стёрла ровно символ ревизии ⇒ обе половины в ОДИН файл) ⇒ дифференциал ВАКУУМЕН и вердикт инвертируется в «фикс ничего не меняет» на исправной поставке; **приобрёл** координаты (`grep -n`, `--stat`) ⇒ сдвиг строк от правки ВЫШЕ по файлу читается как «задело чужой участок» ⇒ имена ЛИТЕРАЛАМИ (`pre`/`fix`/`head`), канарейка «`shasum` двух половин РАЗЛИЧАЮТСЯ», сравнение `grep -h` / `git show <rev>:<path>`. ⚠ Tell схлопывания АРИФМЕТИЧЕСКИЙ: размер одной половины равен ТРЕТЬЕЙ известной величине (`HEAD`); загрязнения — различие там, где ты не правил. ⛤ Не §A19: там улику стирает ПОВТОРНЫЙ прогон и лечит метка времени, здесь коллизия внутри ОДНОГО прохода между РАЗНЫМИ входами. ⛔ **ТРЕТЬЯ форма (2026-08-29): ключ НЕВАЛИДЕН КАК ИМЯ ПУТИ** — человекочитаемая метка ревизии отдана в `mkdtemp(prefix=…)`/имя файла, а слэш внутри заставляет создавать ВЛОЖЕННЫЙ путь ⇒ `FileNotFoundError`. Отказ ГРОМКИЙ (обе формы выше тихие), но стоит цикла. ✅ Floor: метка для ПЕЧАТИ и ключ-ИМЯ — РАЗНЫЕ аргументы (`label` человеку, `slug='pub'/'pre'` в путь). ⚠ Tell до запуска: в `prefix`/имя уходит переменная, которую ты составлял для ЧЕЛОВЕКА |

| **§A28** | `2026-09-07` | Ждун уведомил о завершении, а в напечатанном хвосте вердикта НЕТ — только строки прогресса ⇒ шаблон, расширенный «чтобы не пропустить отказ», поймал маркер ЭТОГО ЖЕ прогона, помечающий ПРОГРЕСС (`✅ Loaded N triples` под альтернацией `✅`), и ждун больше не сработает ⇒ у ждуна ДВА шаблона: успех якорить на ФОРМУ терминальной строки (`^ИТОГ:`, `conforms to`, `PASS=[0-9]`), ⛔ голый `✅`/`❌`/`OK` в альтернацию не класть; отказ (`Traceback\|Killed\|FAILED`) расширять свободно — там ошибка уходит в безопасную сторону. ⛤ Не §A17 (там маркер от ПРОШЛЫХ прогонов, лечит `-gt "$BASE"`) и не Г28 (там лог пуст из-за пайпа): здесь журнал свежий и маркер настоящий, просто он не про исход. ✅ Проверять шаблон СВОИМ ЖЕ грепом по уже напечатанному хвосту ДО того, как положиться: `grep -cE '<шаблон>' <лог>` при живом прогоне обязан дать 0. ⛤ Дешевле обоих — ждать ПРОЦЕСС (`run_in_background`), маркер нужен только для чужого запуска. ⛔ **ХУКА НЕ БУДЕТ — замер, а не умолчание** (2026-09-09): за 30 дней **269** ждунов `until`+`grep`, голый маркер в ПРЕДИКАТЕ — **3**, и один из трёх ложно-положителен (`^(✅\|❌) S13 ` — форма терминальной строки, а не голый значок) ⇒ ~2 настоящих случая в месяц при FP ≈ 33 %. ⚠ Считать надо ПО ПРЕДИКАТУ (между `until` и `do`): наивный греп по всей команде дал 100 из 291, потому что `✅` живёт в постобработке ПОСЛЕ `done`. ✅ Взамен совет о выборе маркера вписан в текст отказа `until-pgrep-selfmatch-blocker.sh` — барьер на этой поверхности уже стоит |

| **§A29** | `2026-09-07` | Число получено ВЫЧИТАНИЕМ (`целое − измеренные части`) — арифметика верна, и проверку она ПРОХОДИТ ⇒ ⛔ ложна не величина, а СОСТАВ остатка: его ты не мерил ни разу, он по построению «всё прочее» ⇒ называть остаток остатком, а его состав перечислять ТОЛЬКО наблюдением (прочитать, что фактически лежит в контексте / логе / выдаче). ⚠ Tell при написании: во фразе есть «остальное — это …» / «на X приходится …», а замера ИМЕННО этой части назвать не можешь |
| **§A30** | `2026-09-08` | Бюджет ждуна/окна взят из ШАПКИ предмета («быстрый режим 6.7 мин») ⇒ таймаут `rc=4` на ИСПРАВНОМ догоне в 1 ч 59 мин, читается как «завис» и рождает второй ждун ⇒ ⛔ число в комментарии — СНИМОК замера (шапка мерилась ДО стадии мутантов, а новая стадия предмета шапку не обновляет — тот же носитель, что компакт-саммари §A26) ⇒ бюджет = длительность ПОСЛЕДНЕГО прогона ТОГО ЖЕ режима из журнала предмета (`grep 'прогнано=' <лог> \| tail`, START→сводка) × 2; и коррекцию класть В ШАПКУ предмета датой, не только в свой ждун. ⚠ Tell: круглое число в комментарии старше последней стадии предмета. ⛤ Разбора в справочнике НЕТ — floor целиком в строке |
| **§A31** | `2026-09-10` | Сравниваешь ДВА СВОИХ замера одного графа, и направления рёбер получаются ВЗАИМНО ОБРАТНЫМИ ⇒ ⛔ дело не в данных: **шапка CSV-выдачи `exocortex-cli query --format csv` — `o,p,s`, обратный порядок относительно `SELECT ?s ?p ?o`** ⇒ позиционный разбор ИНВЕРТИРУЕТ каждое ребро, и вывод «кто от кого зависит» становится противоположным при правдоподобных числах. ⚠ Tell: два твоих замера одного предмета расходятся не величиной, а НАПРАВЛЕНИЕМ (замер: 5 импортов `public→ems` против 1 обратно). ✅ Floor: `--format json` (он keyed) ЛИБО разбор по ФАКТИЧЕСКОЙ шапке — ⛔ никогда по позиции; шапка лежит НЕ первой строкой (перед ней прогресс-строки CLI), поэтому `next(reader)` берёт прогресс за шапку и молча уводит на позиционный разбор. ⛤ Разбора в справочнике НЕТ — floor целиком в этой строке |
| **§A32** | `2026-09-10` | Предикат «жив ли демон» по `launchctl list` дал **7** при **8** фактически живых ⇒ ⛔ ты матчишь ВТОРУЮ колонку как `[0-9]+`, а это **last-exit-status**, и он бывает **ОТРИЦАТЕЛЬНЫМ** (`4621⇥-15⇥com.kitelev.reminder-checker.poll` — процесс ЖИВ, предыдущий инстанс убит SIGTERM) ⇒ живость судить **только по ПЕРВОЙ колонке** (числовой PID); вторая о живости не говорит ничего. ⛔ Ошибка идёт в ОПАСНУЮ сторону: предикат «все остановлены = 0» удовлетворяется **при работающем писателе**, и окно тишины считается наступившим. ⚠ Tell — §ТРЕТИЙ признак: launchd-счётчик **7** против `pgrep` **8** в одной выдаче; аккуратность молчит, ловит только вторая величина. ⛤ Разбора в справочнике НЕТ — floor целиком в этой строке |

| **§A33** | `2026-09-10` | Твой замер разошёлся с ЧУЖИМ числом, ты поставил эксперимент (убрал одну переменную) — и он **подтвердил** твою гипотезу ⇒ ⛔ подтверждённая изоляцией гипотеза ещё НЕ причина: два разных вмешательства могут давать ОДИН сдвиг результата, и эксперимент их не различает (у меня «убрать `public/person` из `--move`» и «довести отстающий vault» оба убирали лишний узел из SCC) ⇒ при расхождении с чужим числом первым делом сверять **СОСТОЯНИЕ ВХОДА** контрольными счётчиками, а не процедуру (§A6 — про процедуру, её мало), и требовать **дискриминатор**: величину, чувствительную к одной гипотезе и нечувствительную к другой (число рёбер `100` = вход не доведён, `99` = доведён). ⚠ Tell ДО объяснения: в расхождении фигурирует сущность, которой твоя правка не касается вовсе — своя правка не может добавить узел, которого не трогала |
| **§A34** | `2026-09-10` | Опираешься на ЧУЖОЕ число, оно ВЕРНО и расхождения НЕТ ⇒ ⛔ §A33 (ловит расхождение) и §ЗЕРКАЛО (адресовано пишущему) молчат ПО ПОСТРОЕНИЮ: область чужого замера ВЛОЖЕНА в твою («всё X» против «X/<часть>»), поэтому число правдоподобно И истинно, опровергать нечего ⇒ переформулируй вопрос СВОЕГО критерия и сверь ОБЛАСТИ, прежде чем опереться; разные ⇒ мерить самому (обычно одна команда). ⚠ Tell: области не РАВНЫ, а ВЛОЖЕНЫ — надмножество про часть ничего не обещало, но читается как обещание |

⛤ **Справочник:** `c4347839-835d-4b53-8d35-9007226c41d6`, якоря **§A1-§A34**.
Рецепт адресного чтения (один на корпус) — `rule-carrier-follows-function` §«Читать справочник АДРЕСНО». ⛔ **Даты НЕ уникальны** (`2026-08-21` — десять разделов) ⇒ адресоваться НОМЕРОМ, не датой


## Cross-references

- `~/dotfiles/.claude/rules/verify-before-assert.md` — родитель (утверждение требует проверки);
  §A6 — количественный claim из одной точки замера. Здесь — специализация по **автору**: своё
  утверждение проверить нечем, потому что внешнего источника у него нет.
- `~/dotfiles/.claude/rules/decision-surface-must-derive-from-mechanism.md` §A2 — floor «писать
  ЧЕМ проверено, а не „проверено“» сформулирован там для **автономного воркера**; форма №1 выше —
  тот же floor, обращённый на себя.
- `~/dotfiles/.claude/rules/self-satisfying-metric-weak-verifier.md` §A23 — ярлык исхода несёт
  причинное утверждение, которого замер не делал (там — о наблюдаемом; здесь — о себе).
- `~/dotfiles/.claude/rules/activity-signal-is-not-progress.md` — индикатор ≠ работа; форма №3
  добавляет: индикатор ≠ то, что он сводит, и лог открывается за один вызов.
- `~/dotfiles/.claude/rules/retracted-claim-outranks-its-correction.md` — соседний класс: там
  утверждение **опровергнуто ниже в том же файле**; здесь оно не опровергнуто, а просто никогда
  не было установлено.
- `~/dotfiles/.claude/rules/bash-loop-eval-pitfalls.md` Г3b — `rc` после пайпа = код последней
  команды пайпа; техническая половина ошибки «прочитал не тот код возврата».

