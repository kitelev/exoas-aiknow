---
exo__Asset_uid: c4347839-835d-4b53-8d35-9007226c41d6
exo__Asset_createdAt: 2026-08-26T16:29:35
exo__Asset_updatedAt: 2026-09-13T02:02:05
exo__Instance_class:
  - "[[39a39239-2a97-483a-ba91-fb01bf5c85f3]]"
exo__Asset_createdBy: "[[4ef3962d-b8a7-42b5-bd28-88ec846f1d13]]"
exo__Asset_label: Ложный измеритель — 24 разбора (справочник к self-authored-claim-loses-its-provenance)
aliases:
  - Ложный измеритель — 24 разбора (справочник к self-authored-claim-loses-its-provenance)
exo__Asset_isDefinedBy: "[[2ce77ac1-3f78-4f64-9dc1-41ea01da1ef0]]"
inbox__ExoAssistantKnowledge_decay: "[[06a5b9f9-da93-4234-883f-3f0cf65b2fba]]"
inbox__ExoAssistantKnowledge_confidence: "[[227def30-f56f-4f5f-936d-f6a81ad73c96]]"
---

# Справочник: ложный измеритель — 24 разбора

⛤ Вынесено из `~/dotfiles/.claude/rules/self-authored-claim-loses-its-provenance.md`
(компрессия 2026-08-26: 104.9 КБ → ~34 КБ, −67 %). **Флор несёт ядро + таблицу
«чем соврал → floor»; здесь — механизм каждого случая.** Читать АДРЕСНО по §A.

⛔ **Ключ ДВОЙНОЙ (§A + дата)** — на правило ссылаются обеими формами. Даты НЕ уникальны:
`2026-08-21` — девять разделов, `2026-08-25` — три, `2026-08-19`/`2026-08-22`/`2026-08-24` — по
две-три. Разрешать по ТЕМЕ из индекса правила.

## Таблица перехода: дата → §A

| дата | §A |
|---|---|
| `2026-08-19` | §A1, §A3, §A10 |
| `2026-08-21` | §A2, §A12, §A13, §A16, §A17, §A18, §A19, §A20, §A21, §A22 |
| `2026-08-22` | §A8, §A9, §A11, §A15 |
| `2026-08-23` | §A4 |
| `2026-08-24` | §A23, §A24 |
| `2026-08-25` | §A5, §A6, §A7 |
| `2026-08-26` | §A14 |

## §A1 — СЕМЬ ложных счётчиков за одну сессию — таблица инструментов (`2026-08-19`)

### ⛔ Empirical 2026-08-19 — СЕМЬ ложных счётчиков за одну сессию, и все опознаны ОДНИМ признаком

За один рабочий день ad-hoc команды, написанные в моменте для проверки **собственной** работы,
соврали семь раз. Ни одна не была сложной; все пойманы **противоречием с тем, что я уже видел
глазами**:

| что мерил | сказал | было | чем врал |
|---|---|---|---|
| файлов в коммите (`grep -c` по `--stat`) | 1 | 2 | усечённые колонки `--stat` |
| осей в тесте (`grep -c 'it('`) | 5 | 2 | подстрока матчила не только `it(` |
| «безусловный остаток» разворота | 1 | 0 | считал присваивание, а не безусловность |
| частота flake по `/runs/{id}/jobs` | 0 % | 12 % | API отдаёт ПОСЛЕДНЮЮ попытку |
| выжил ли `\` (`echo … \| grep -F`) | нет | да | zsh-`echo` сам разворачивает escape |
| тело из `--dry-run` (`rfind('---')`) | 1 строка | 2 | поймал маркер `--- END PREVIEW ---` |
| красные оси (`grep '✕'`) | 0 | 2 | jest без `--verbose` глиф не печатает |
| мёртвые пути в правилах (регэксп с `json\|jsonl`) | 3 мёртвых | 0 | **порядок ветвей альтернации**: `json` матчится первым и обрезает `.jsonl` до `.json` |
| `updatedAt` ассета (`grep … \| sed 's/.*: *//'`) | `24` | `2026-09-06T10:56:24` | **жадный `.*:`** съедает всё до ПОСЛЕДНЕГО двоеточия, а в таймстемпе их три _(2026-09-12, vteam-8bd42343)_ |

⛤ **Последняя строка опаснее соседей по таблице: `sed 's/.*: *//'` — ходовой идиом извлечения
frontmatter, а таймстемпы несут двоеточия ПО ПОСТРОЕНИЮ.** Бьёт по полю, на котором держится
проверка «не пишет ли в ассет соседняя сессия» ([[graph-record-outlives-its-referent]] §6 ЗЕРКАЛО):
там сверяют `exo__Asset_updatedAt` до и после, и жадный срез даёт с обеих сторон одинаковый
хвост (`24`) ⇒ вердикт «не менялся» тождественно-истинный.
✅ Floor: `cut -d' ' -f2` (значение — второе поле) либо `sed 's/^[^:]*: *//'` (не-жадный класс).
⚠ Tell: извлечённое значение КОРОЧЕ ожидаемого и состоит из хвоста — «24» вместо даты.

## §A2 — ДЕВЯТЫЙ подвид — нормализация ключа схлопывает значащее различие (`2026-08-21`)

⛔ **ДЕВЯТЫЙ подвид — НОРМАЛИЗАЦИЯ КЛЮЧА схлопывает ЗНАЧАЩЕЕ различие, и образец показывает
первый попавшийся.** Группируя ошибки/события по «виду», их ключ нормализуют, чтобы вариативные
куски не плодили групп: `re.sub(r'\d+', 'N', first)`, `re.sub(r'[0-9a-f]{8,}', 'UID', …)`. ⛔ Но
часть чисел — **не шум, а сам предмет**: код возврата, номер версии, HTTP-статус, число попыток.
Схлопнув их, ты получаешь ОДНУ большую группу и печатаешь для неё **первый встреченный** образец —
то есть приписываешь всей группе свойство одного её члена.

- ⚠ **Отказ выглядит как СИЛЬНАЯ находка, а не как ошибка:** большая группа с конкретным
  образцом читается как «вот главный класс отказов, вот его пример». Чем крупнее группа, тем
  убедительнее — и тем сильнее ложный вывод.
- ✅ **Floor: нормализовать ТОЛЬКО то, что не несёт различия, о котором ты спрашиваешь.** Спроси
  себя: «если это число другое — ответ на мой вопрос меняется?» Да ⇒ оно часть ключа, не шум.
- ⛤ **Дискриминатор дешёвый: распечатай РАСПРЕДЕЛЕНИЕ внутри группы** (`Counter` по не-нормализованному
  полю). Однородная группа подтвердит вывод; разнородная — опровергнет мгновенно.
- ⚠ **Симптом-tell:** одна группа занимает 30 %+ выдачи, а её образец подозрительно конкретен
  (`Exit code 127`, `HTTP 429`, `attempt 3`). Конкретность образца при размере группы = сигнал, что
  различающее поле схлопнуто.

**Empirical 2026-08-21** (аудит частоты грабель, задача `0cab1d08`): ключ `\d+ → N` слил
`Exit code 1 / 2 / 127 / 143`, образец показал `127` ⇒ вывод «133 раза команда не найдена»
(31 % всех отказов, класс Г2 PATH-strip). Пере-замер с сохранённым кодом: `rc=127` — **3**,
`rc=1` — 111. То есть крупнейший «класс грабель» был артефактом нормализации, и работа по нему
ушла бы в починку несуществующего.

## §A3 — ВОСЬМОЙ подвид — ANSI-коды рвут якорь в цветном выводе (`2026-08-19`)

⛔ **ВОСЬМОЙ и самый коварный подвид — ЦВЕТНОЙ вывод: твой якорь разорван ANSI-кодами,
и детектор молчит на РАБОТАЮЩЕМ предмете.** Раннеры тестов, `git`, `gh`, `docker`
раскрашивают вывод, и escape-последовательности встают **между** словами, по которым ты
грепаешь: `× src/x.test.ts > describe > A3` в байтах есть
`× …test.ts\x1b[2m > \x1b[22mdescribe…`, поэтому `grep -oE '× .*> A[0-9]'` не матчит
ничего. Наблюдаемое — **пустой список красных при честно краснеющих тестах**, то есть
вывод «набор ничего не различает» о наборе, который различает всё.
- ⚠ **Читается наоборот и потому дорого:** пустота выглядит как свойство ПРЕДМЕТА
  («мутанты не работают»), и следующий шаг — «усиливать» исправные оси.
- ✅ **Floor: снимать ANSI ПЕРЕД разбором** — `| sed $'s/\033\[[0-9;]*m//g'` первым
  звеном пайпа, всегда, если вывод идёт от интерактивного инструмента.
- ⛤ **Дискриминатор за один вызов:** прогони предмет **без** своего фильтра и посмотри
  сырой хвост. Видишь `×` глазами, а фильтр даёт ноль ⇒ дело в разметке, не в предмете.
- ⚠ Тот же класс бьёт по счёту: `grep -c '✓'` на цветном выводе считает **строки-файлы**,
  а не тесты, — у меня «1 ✓» при четырёх зелёных тестах.

<!-- Added by /session-retrospective 2026-08-23 (vteam-8e2fc81e) — root: grep по журналу раннера молча вернул пустоту, я вывел «vteam-харнессы ни разу не гонялись» и собирался завести по этому тикет; опроверг только пере-замером с -a (218 вхождений). -->

## §A4 — ДЕСЯТЫЙ подвид — grep без -a молча подавляет вывод (битый UTF-8) (`2026-08-23`)

⛔ **ДЕСЯТЫЙ подвид, и он ТИШЕ ЦВЕТНОГО: `grep` МОЛЧА подавляет вывод на файле с невалидной UTF-8
последовательностью — БЕЗ строки `Binary file … matches`.** ВОСЬМОЙ подвид (ANSI) рвёт якорь и даёт
пустой список совпадений; здесь `grep` не печатает **ничего вообще** и выходит `rc=1`, то есть
неотличимо от честного «в файле этого нет».

- ⛤ **Механизм:** ОДНА оборванная многобайтовая последовательность делает файл «бинарным» для `grep`,
  и подавляется **весь** вывод, а не строка с ней. Источник в этой системе типовой — усечение
  **по БАЙТАМ** (`cut -c1-110`) причин, написанных по-русски и с эмодзи-маркерами (`⛔`, `⇒`): рез
  попадает в середину символа.
- ⚠ **Все дешёвые проверки «файл в порядке» ПОДТВЕРЖДАЮТ исправность:** `file` говорит `UTF-8 text`
  (он сэмплирует начало), `head`/`tail`/python читают нормально. ⇒ опровергнуть ложный ноль можно
  только пере-замером, а поводов к нему нет — ответ выглядит осмысленным.
- ✅ **Floor: вердикт-несущий `grep` по ЖУРНАЛУ — всегда `grep -a`.** Журнал = накопительный лог,
  чей байтовый состав пишет не ты (раннер, демон, чужой инструмент). Ноль без `-a` доказательством
  отсутствия **не является**.
- ⛤ **Дискриминатор за один вызов:** `grep -c X <файл>` пусто, а `grep -ac X <файл>` — число.
  Расхождение = кодировка, а не данные.
- ⛔ **Ложный ноль читается как ФАКТ О МИРЕ, а не о выборке:** «этот харнесс ни разу не гонялся»,
  «событие не происходило». Именно такой вывод и уходит в отчёт/тикет.
- ⛤ **Починка ПИСАТЕЛЯ этот floor НЕ отменяет:** журнал уже накопил битые последовательности, и
  читателю `-a` нужен и после фикса.
- ⛤ **ЗАМЕР 2026-08-26, из-за которого floor не «на всякий случай»: битых логов МАЛО, но это
  РОВНО ходовые, а грепов без `-a` — 90 %.** По `~/.claude/audit/*.log`: **5 из 165** несут
  невалидный UTF-8 — `alp-tick` · `self-compact` · `verify-harness-runner` · `founder-notify` ·
  `eka-watchdog`, то есть журналы петель, надзора и доставки, которые и читают при разборе.
  За 30 дней транскриптов: **214** грепов по этим путям, из них **193 (90 %) без `-a`**.
  ⇒ экспозиция не гипотетическая, а ежедневная.
- ⚠ **Битый байт лежит В ХВОСТЕ, и потому все дешёвые проверки честно говорят «файл цел»:**
  смещения 71808/741472 · 838401/862281 · 116753/159073 — то есть `file`, `head`, первые сотни
  строк и любой просмотр начала подтверждают исправность. ⛔ Отсюда же следует, что «раньше этот
  греп работал» — не аргумент: файл ПОРТИТСЯ по ходу дописывания, и вчерашний ноль был честным.

**Empirical 2026-08-23** (vteam `8e2fc81e`): `grep -c 'vteam' ~/.claude/audit/verify-harness-runner.log`
→ пусто; `grep -ac` → **218**, и `vteam-select-verify.py` прогонялся в тот же день 05:15-05:54 `rc=0`
(2346 с). Невалидный байт на смещении 116753 (одинокий `\xe2`) внесён `verify-harness-runner.sh:268/296`
(`cut -c1-100` / `cut -c1-110`). Починка раннера вынесена тикетом `a9719d2a`; ложный тикет «раннер не
покрывает vteam» **не заведён** — предмета нет.

<!-- Added by /session-retrospective 2026-08-25 (vteam-6440265e, MemberProductOwner) — root: `grep -nE '^(A|B|po__Work_|C):' <тикет>` дал НОЛЬ по четырём только что записанным po__Work_* при непустой выдаче по остальным ветвям ⇒ прочиталось как «переход этапа удалил поля»; опровергнуто дампом всего frontmatter. -->

## §A5 — ОДИННАДЦАТЫЙ подвид — общий суффикс альтернации убивает ветвь-префикс (`2026-08-25`)

⛔ **ОДИННАДЦАТЫЙ подвид — ОБЩИЙ СУФФИКС вне группы убивает ветвь-ПРЕФИКС, и выдача остаётся
ЧАСТИЧНО ВЕРНОЙ.** Десять подвидов выше врут **целиком** (ноль вместо числа, не тот образец,
подавленный вывод) и потому расходятся с наблюдением сразу. Здесь фильтр отвечает **верно по
части ветвей и молча пусто по одной** — то есть выглядит не поломкой, а **находкой о данных**.

- ⛤ **Механизм (проверено исполнением 2026-08-25):** в `^(A|B|prefix_):` суффикс `:` относится ко
  ВСЕЙ группе, поэтому ветвь-префикс требует литерального `prefix_:`, которого не существует.
  Замер на фикстуре из трёх строк: без `:` — **3** совпадения, с `:` — **1**, суффикс внесён
  ВНУТРЬ ветвей (`prefix_[a-zA-Z]+`) — снова **3**.
- ⚠ **Тель отличается от всех десяти: ноль по ЧАСТИ перечисленного при непустом ответе по
  остальному.** Читается не как «счётчик соврал», а как «эти поля исчезли» — то есть как
  **утрата данных**, и следующий ход по такому выводу пере-записывает уже корректное.
- ⛔ **Дефект вносится ПРАВКОЙ РАБОТАЮЩЕГО фильтра.** Тот же перечень **без** `:` отработал верно
  минутой раньше; `:` дописан, чтобы отсечь совпадения в теле, — и убил ровно ту ветвь, ради
  которой перечень составлялся. ⇒ подозревать надо не данные, а **последнюю правку предиката**.
- ✅ **Floor: члены альтернации под общим суффиксом обязаны быть ОДНОРОДНЫМИ** — либо все полные
  имена, либо суффикс внесён внутрь каждой ветви. ⛤ Ещё дешевле: на файле в десятки строк фильтр
  не чинить вовсе — распечатать предмет целиком (`sed -n '1,25p'`), одна команда, и она не врёт
  (тот же floor, что §Два ПОСЛЕДОВАТЕЛЬНЫХ уточнения: кандидатов единицы ⇒ читать, а не уточнять).
<!-- Added by /session-retrospective 2026-08-25 (vteam-9c2c39e1, MemberProductOwner) — root: дословный рецидив ТОГО ЖЕ ДНЯ у следующего исполнителя того же конвейера; правило было в always-on корпусе и в контексте. -->
- ⛔ **РЕЦИДИВ 2026-08-25 (в тот же день, следующий исполнитель того же конвейера): tell выше
  ПРОАКТИВНО не срабатывает — он ловит ПОСТФАКТУМ.** Правило написано грумингом `6440265e`; через
  считанные часы груминг `9c2c39e1` написал ровно ту же форму (`^(po__Work_|<полные имена>):` после
  перевода этапа, на том же классе работы). Правило было в always-on корпусе и в контексте.
  - ⛤ **Класс воспроизводится НЕ изредка, а на КАЖДОМ однотипном шаге:** «проверить frontmatter после
    мутации» — типовое действие груминга, и перечень там естественно смешивает **префикс своей группы**
    (`po__Work_`) с **полными именами** чужих полей (`ems__Effort_status`, `flow__WorkItem_stage`).
    Смешение возникает из самой задачи, а не из невнимательности.
  - ⚠ **Обнаружение оба раза — АРИФМЕТИЧЕСКОЕ ПРОТИВОРЕЧИЕ В ТОЙ ЖЕ ВЫДАЧЕ, а не чтение регэкспа:**
    у автора — дамп всего frontmatter, у меня — `yaml.safe_load` в том же выводе показал `rank= 4`
    при нуле совпадений грепа. ⇒ ✅ практический floor сильнее «пиши однородно»: **проверять
    результат мутации ПАРСЕРОМ, а не грепом** — `yaml.safe_load` не имеет этого класса отказов
    вообще и заодно ловит порчу frontmatter.
  - ⛔ Хук здесь невозможен по построению (корректная альтернация неотличима от дефектной) ⇒
    **terminal accept-baseline**: ценность записи — в частоте и в замене инструмента, не в напоминании.

**Empirical 2026-08-25** (груминг тикета `6440265e`, конвейер vteam): после перевода этапа
`grep -nE '^(flow__WorkItem_stage|flow__WorkItem_stageEnteredAt|po__Work_|ems__Effort_status|exo__Asset_updatedAt):'`
напечатал четыре строки и ни одной `po__Work_*` — при том что четыре таких поля были записаны
минутами ранее и CLI отчитался успехом. Вывод «переход снёс оценки» прожил один ход: дамп
`sed -n '1,25p'` показал все четыре на месте. Урон 0; поймано арифметическим противоречием
(§ТРЕТИЙ признак), а не чтением регэкспа.

<!-- Added by /session-retrospective 2026-08-25 (vteam-1bae87a4, MemberProductOwner) — root: `grep -c 'оценки записаны' child-lifecycle.jsonl` → 0 при заведомо отправленном пинге ⇒ вывод «событие ПОТЕРЯНО»; декодирование json дало 1. Файл валиден (в отличие от §ДЕСЯТОГО), литерала в нём нет ПО ПОСТРОЕНИЮ. -->

## §A6 — ДВЕНАДЦАТЫЙ подвид — json.dumps ensure_ascii: греп кириллицей = 0 (`2026-08-25`)

⛔ **ДВЕНАДЦАТЫЙ подвид — ЖУРНАЛ ХРАНИТ НЕ-ASCII `\uXXXX`-ЭКРАНИРОВАННЫМ: греп кириллицей даёт
ноль ВСЕГДА, а файл при этом валиден.** §ДЕСЯТЫЙ подвид описывает `grep`, который **подавляет
вывод** на битой UTF-8 последовательности; здесь противоположная механика — grep отрабатывает
штатно, а искомой подстроки в файле **не существует**: писатель сериализует поле через
`json.dumps()`, а его умолчание `ensure_ascii=True` превращает `оценки` в `оц…`.

- ⛔ **Ложный ноль читается как ФАКТ О МЕХАНИЗМЕ, а не о выборке:** «события нет в журнале» ⇒
  «пинг не доставлен» ⇒ вывод о том, что **канал наблюдаемости мёртв**. Это дороже обычного
  недосчёта: под подозрение попадает инструмент, которым пользуются все автономные воркеры.
- ⚠ **Симптом-tell:** ноль по строке, которую ты **сам только что отправил** и чей вызов вернул
  успех. Собственное недавнее действие — самый дешёвый контрпример; он же и единственный, если
  писателя ты не читал.
- ✅ **Floor: журнал разбирать ПАРСЕРОМ, а не грепом** — `json.loads` построчно, затем сравнение
  по декодированному полю. Одна форма, покрывает и кириллицу, и эмодзи, и любые будущие поля.
  ⛔ `grep -a` тут НЕ помогает (это лекарство §ДЕСЯТОГО): байты валидны, проблема в кодировке
  **содержимого**, а не файла.
- ⛤ **Класс шире `child-lifecycle.jsonl`:** так же ведёт себя ЛЮБОЙ JSONL, чей писатель зовёт
  `json.dumps` без `ensure_ascii=False` — журналы решений, реестр сессий, дайджесты. Дискриминатор
  один: открой писателя и посмотри на вызов сериализации (`grep -n 'json.dumps' <писатель>`).

**Empirical 2026-08-25** (vteam `1bae87a4`): `grep -c 'оценки записаны' ~/.claude/audit/child-lifecycle.jsonl`
→ **0**; декодирование `json.loads` по строкам → **1** (запись 18:17:05, `child: vteam-1bae87a4`).
Механизм прочитан в коде: `child-ping-progress.sh:68` — `python3 -c 'import json,sys;print(json.dumps(sys.stdin.read()))'`,
`ensure_ascii` не передан. Ложный вывод «мандатный пинг потерян» прожил один ход и был снят
декодированием — при том что предметом грумленного тикета была ровно эмиссия пингов.

<!-- Added by /session-retrospective 2026-08-25 (vteam-f3691c0f, StageCapture) — root: искал коррекцию ложного утверждения в СООБЩЕНИЯХ коммитов через `git log -S`, получил 0 при заведомо присутствующей строке; корпус зовёт `-S` в 4 правилах и НИ ОДНО не говорит, что он ищет по содержимому ФАЙЛОВ, а не по сообщениям. -->

## §A7 — ТРИНАДЦАТЫЙ подвид — git log -S ищет по содержимому ФАЙЛОВ, не по сообщениям (`2026-08-25`)

⛔ **ТРИНАДЦАТЫЙ подвид — `git log -S` ищет по СОДЕРЖИМОМУ ФАЙЛОВ, а не по СООБЩЕНИЯМ коммитов:
поиск цитаты из тела коммита даёт ноль ВСЕГДА.** `-S` (pickaxe) отвечает на вопрос «в каком коммите
изменилось число вхождений строки **в файлах**»; сообщение коммита он не читает вовсе. Значит любой
вопрос «кто и когда написал это В ТЕЛЕ КОММИТА» (найти коррекцию, найти обоснование, найти автора
формулировки) он отвечает **пустотой** — без ошибки и без предупреждения.

- ⛔ **Ложный ноль идёт в сторону ПОДТВЕРЖДЕНИЯ ЖЕЛАЕМОГО, и потому не перепроверяется.** Ищешь
  коррекцию, чтобы проверить «а не записана ли она уже» — ноль читается как «не записана», то есть
  как **усиление** твоего тикета/находки. Ровно тот случай, когда сомнению неоткуда взяться.
- ⚠ **Симптом-tell мгновенный и он же единственный:** ноль по строке, которую ты **видишь глазами**
  в `git log -1 --format='%B' <sha>`. Расхождение с прямым наблюдением (§ТРЕТИЙ признак) — здесь
  срабатывает сразу, потому что цитату ты только что оттуда и взял.
- ✅ **Floor: по СООБЩЕНИЯМ — `--grep=`; по СОДЕРЖИМОМУ файлов — `-S`; по ФАЙЛУ вперёд —
  `git log <sha>..HEAD -- <путь>`.** Три разных вопроса, три разных флага; корпус употребляет `-S`
  корректно (`commit-and-push` · `test-fixture-realism` · `verify-before-assert` §A1 —
  везде «строка диффа»/«символ»), и ошибка не в нём, а в переносе флага на четвёртый вопрос.
- ⛔ **Ноль от `--grep` тоже засчитывать ТОЛЬКО с канарейкой:** тот же `--grep` по заведомо
  присутствующему ключу обязан вернуть непусто. Иначе «связи нет» неотличимо от «предикат мёртв» —
  и второй ложный ноль ляжет поверх первого.
- ⛤ **Класс шире git: это перенос предиката на СОСЕДНИЙ вопрос.** `gh pr list --search` против
  `--json body`, `grep` по имени против `grep` по вызову, `-S` против `--grep` — во всех случаях
  инструмент отрабатывает штатно и отвечает **не на тот** вопрос.

**Empirical 2026-08-25** (тикет `f3691c0f`): `git log -S 'dispatch 14' --all` → **0 коммитов**, при
том что строка стоит в теле `1a95e01` (`git log -1 --format='%B' 1a95e01 | grep -n 'dispatch'` →
строка 24). Исправлено на `--grep=`; канарейка `--grep='шов dispatch'` → `a715600` ⇒ последующий
ноль по `--grep='dispatch 14'` был признан **настоящим** и лёг в замер тикета. Без канарейки два
ложных нуля были бы неразличимы.

## §A8 — Рецидив: три подряд «кто зовёт» — упоминание принято за вызов (`2026-08-22`)

⛤ **Рецидив 2026-08-22 — ТРИ подряд за час, и все три меряли ОДНО: «кто зовёт компонент».**
Форма новая и стоит того, чтобы её назвать: не «счётчик посчитал не то», а **предикат
включил не тот класс совпадений**.

| попытка | сказала | было | чем врала |
|---|---|---|---|
| `grep -rl <имя>` по прод-каталогу | 5 вызывающих | **0** | считала УПОМИНАНИЯ: комментарии соседей + строка присваивания пути |
| «умный» python-предикат позиции команды | нашёл вызов в plist | **нет** | имя лежало в XML-**комментарии** плейста |
| свип «инъектирует ли роль» по двум именам переменных | 4 харнесса не инъектируют | **1** | у трёх инъекция называлась ИНАЧЕ (`DLP_TEST_SESS`, `TSG_TEST_PANE`, `META_TEST_SESS`) |

⛔ **Третья строка — самая поучительная: узкий детектор врал в СТОРОНУ ТРЕВОГИ**, то есть
предъявлял к починке три исправных харнесса. Ложно-красный измеритель дороже ложно-зелёного
ровно тем, что расходует работу на исправное.

✅ **Floor, общий для всех трёх:** предикат, отбирающий по ИМЕНИ, обязан быть проверен
**контрпримером** — заведомо НЕ-вызовом (комментарий) и заведомо ИНАЧЕ названной формой.
Одна проба на каждый конец; без неё «нашёл N» и «не нашёл» одинаково недоказуемы.

## §A9 — Порядок ветвей альтернации: короткая ветвь обрезает длинную (`2026-08-22`)

⛔ **Отдельная форма, добавленная 2026-08-22 — ПОРЯДОК ВЕТВЕЙ АЛЬТЕРНАЦИИ.** Регэксп берёт
**первую подошедшую** ветвь, а не самую длинную: `\.(?:json|jsonl)` на `child-lifecycle.jsonl`
даёт `.json` ⇒ путь «не существует» ⇒ ложная находка. Класс шире расширений — он бьёт везде, где
одна альтернатива есть **префикс** другой (`md|markdown`, `ts|tsx`, `yml|yaml`, `Task|TaskPrototype`,
`Done|DoneLate`).
- ✅ **Floor: длинную ветвь ставить ПЕРВОЙ** (`jsonl|json`), либо якорить конец (`\b`, `$`, `(?![a-z])`).
- ⚠ **Симптом-tell:** находка на пути/имени, чей **хвост обрезан ровно до более короткой альтернативы**.
  Обрезка видна в самой выдаче — сравнить, что ты искал, с тем, что напечатано.
- ⛤ Тем же прогоном выяснилось второе: из трёх «мёртвых путей» третий (`dlp-tick.sh`) был
  **сознательной эмпирикой** — правило само несёт ⛤-пометку «носитель снесён, упоминания читать как
  датированную эмпирику». ⇒ у свипа по ссылкам обязателен вопрос «не помечен ли референт мёртвым
  НАМЕРЕННО», иначе он гонит чинить историю. Итог свипа: реальных мёртвых путей — **0**.

## §A10 — ЧЕТВЁРТЫЙ-ШЕСТОЙ: свип ключуется на ИМЕНИ переменной, а имена локальны (`2026-08-19`)

⛤ **Продолжение того же дня — ЧЕТВЁРТЫЙ, ПЯТЫЙ и ШЕСТОЙ, и все три промахнулись ОДИНАКОВО:
свип ключевался на ИМЯ ПЕРЕМЕННОЙ, а имена локальны.**

| свип | искал | промах | истина |
|---|---|---|---|
| инъекция роли в харнессах | два конкретных имени env | 4 «нарушителя» | 3 инжектят под своими именами |
| детект «матчит собственное имя» | путь против регэкспа детекта | 4 подозрительных | живая проба: **0** |
| «харнесс шлёт боевое уведомление» | `NOTIFY`/`founder-notify` | 3 «опасных» | все три заглушены под своими именами (`SENTINEL_NOTIFY`, `SDT_NOTIFY`, `NOTIFY_OK`) |

- ⛤ **Общий корень: имя переменной — СОГЛАШЕНИЕ, а не контракт.** Автор харнесса выбирает
  префикс под свой предмет (`SDT_`, `SENTINEL_`), и свип по «каноничному» имени видит меньшинство.
- ✅ **Floor: свип по имени даёт КАНДИДАТОВ, вердикт даёт ПРОБА.** Здесь она дёшева и однозначна:
  подать предмету вход и посмотреть исход. Все три раза проба перевернула вывод на противоположный.
- ⚠ **Симптом-tell:** свип нашёл «нарушителей» среди файлов, чьи авторы заведомо знали правило
  (харнессы пишет тот же, кто пишет floor'ы). Высокая доля нарушителей в дисциплинированной
  популяции = скорее промах метода, чем эпидемия.

## §A11 — Два последовательных уточнения счётчика — оба неверны (`2026-08-22`)

### ⛔ Два ПОСЛЕДОВАТЕЛЬНЫХ уточнения одного счётчика — оба неверны: сходимость к нулю НЕ значит приближение к истине

Обнаружив, что измеритель завышает, его уточняют — и получают второе число, которое кажется
надёжнее уже потому, что оно **меньше и добыто внимательнее**. ⛔ Признаком истины это не
является: уточнение сужает **один** ложный признак, оставляя остальные.

⛤ Замер 2026-08-22 (харнессы без доказанной различимости): грep по слову «мутант» дал **6**;
уточнение «плюс контрпримерные оси по словам-маркерам» дало **1**; чтение файла дало **0** —
у последнего кандидата пара «норма ↔ пустой вход» была реализована **без единого слова-маркера**,
зато с прямой формулировкой ассерты в комментарии.

- ⚠ **Симптом-tell:** ты правишь измеритель второй раз подряд, и каждый раз число падает.
  Падающий ряд означает, что ты **последовательно снимаешь ложные срабатывания**, — но не
  говорит, где он остановится. Останавливаться надо не на «правдоподобно малом», а на
  **прочитанном глазами** остатке.
- ✅ **Floor: когда кандидатов стало единицы — ЧИТАТЬ их, а не уточнять предикат.** Один файл
  читается быстрее, чем правится регэксп, и отвечает окончательно.
- ⛤ **Дешевле обоих: искать СВОЙСТВО через его РЕАЛИЗАЦИИ, а не через слова.** «Набор умеет
  покраснеть» реализуется тремя способами (внешняя спека мутантов · мутация внутри харнесса ·
  пара контрпримерных фикстур), и лексический поиск видит только первые два.

## §A12 — Отсутствие ВНЕШНЕГО проверяющего невидимо в артефакте (`2026-08-21`)

### ⛔ Отсутствие ВНЕШНЕГО проверяющего — структурное свойство прогона, и в артефакте оно НЕВИДИМО: называть вслух

Формы выше — про утверждения, которые ты произвёл сам. Предельный случай: **весь прогон**
произведён тобой одним. Автономная сессия, которая пишет код, ревьюит его, мержит и
релизит, — это L3-петля без verifier'а; и заметить это по её артефактам **нельзя**.

⛤ **Почему невидимо:** PR выглядит образцово именно тогда, когда автор старался, — есть
revert-verify, мутанты различают, гейты зелёные, тело описывает решения. Ни одно поле не
пусто. Отсутствует не **содержание**, а **вторая пара глаз**, и у неё нет места в
шаблоне.

- ⛔ **Revert-verify внешнего ревью НЕ заменяет.** Он доказывает, что тест различает
  предмет — то есть проверяет **мою** реализацию **моей** спекой. Класс ошибок, который
  он не ловит по построению: неверно понятая постановка, пропущенный потребитель, решение
  правильное для придуманной задачи. Ровно то, что ловит человек, читающий свежими глазами.
- ⛔ **Запрет может быть СРЕДОВЫМ, а не выбором.** «Не вызывать агентов, пока не попросили»
  — легитимное ограничение сессии (защита от троттла, стоимости, каскада). Тогда verifier
  недоступен **структурно**, и «в следующий раз позову» — не митигация.
- ✅ **Floor: назвать в теле PR/отчёта одной строкой** — «отгружено без внешнего ревью
  (agent-tool недоступен в этой сессии); проверено revert-verify + N гейтов». Тогда
  читатель знает, какого класса проверки НЕ было, и решает сам, нужен ли догоняющий проход.
- ✅ **Догоняющее ревью дёшево и не теряет ничего**: дифф зафиксирован, `code-reviewer` по
  коммиту постфактум даёт тот же вердикт, что дал бы до мержа — просто исправление
  приходит следующим PR (`skill-discipline-under-pressure` §A3b: перенос ревью на того,
  кому оно доступно, — это счёт АВТОРУ промпта, а не исполнителю).
- ⚠ **Симптом-tell:** ты пишешь «гейты зелёные, готово» про работу, которую **никто, кроме
  тебя, не читал**. Зелень гейтов и наличие читателя — ортогональны.

**Empirical 2026-08-21:** за одну автономную сессию отгружено четыре PR (#4178-#4181,
два релиза, два req в Active) — **ни одного** прохода `code-reviewer`: инструкция сессии
прямо запрещала вызов агентов без запроса пользователя. Каждый PR нёс revert-verify с
различающими мутантами и пять зелёных guard-скриптов, и по артефактам это неотличимо от
работы, прошедшей ревью. Отмечено в отчёте явно; догоняющий проход — открытая опция.

## §A13 — Измеритель с ПОДМЕНЯЕМЫМ входом — канарейка доставки (`2026-08-21`)

### ⛔ Измеритель с ПОДМЕНЯЕМЫМ входом обязан нести КАНАРЕЙКУ ДОСТАВКИ — иначе «предмет не изменился» неотличимо от «подмена не доехала»

Формы выше — про число, вывод и индикатор. Четвёртая, самая дорогая: измеритель, который
**подсовывает предмету другой вход** (мутант, фикстуру, временный конфиг, стаб) и судит по
изменению выдачи. У него есть отказ, которого нет у пассивного счётчика: **подмена может не
доехать**, и тогда выдача не меняется — что читается как **свойство предмета**.

⛔ Вердикт при этом звучит уверенно и указывает НЕ ТУДА: «мутант ничего не уронил ⇒ ось не
дискриминирует» обвиняет **ось**, хотя сломана **доставка**. Оба состояния дают побайтово
одинаковый вывод, поэтому отличить их чтением результата нельзя **по построению**.

- ⚠ **Симптом-tell, ловится счётом:** отрицательный вердикт у **ВСЕХ** осей/мутантов разом.
  Одна вакуумная ось — правдоподобно; все сразу — почти всегда общий слой, то есть доставка.
- ✅ **Floor: перед серией подмен — ОДНА проба заведомо непригодным входом** (пустой файл,
  несуществующий путь, битый конфиг). Выдача **обязана измениться**. Не изменилась ⇒
  терминальный `BROKEN` с указанием способа доставки, а не «оси не различают».
- ⛤ **Проба отвечает МЕХАНИЧЕСКИ на вопрос, который иначе решается рассуждением** — и стоит
  один прогон. Её отсутствие не видно ни в коде измерителя (он корректен), ни в его выводе.
- ⛔ **Способ доставки — часть контракта предмета, а не деталь измерителя.** Харнесс читает
  либо `env`, либо `argv`, либо файл в известном месте; выбрать «естественный» нельзя — надо
  прочитать, что он читает. Тот же класс, что env-override, который никто не консультирует.

**Empirical 2026-08-21** (три собственных измерителя за одну сессию, все три соврали
одинаково — «предмет плох», хотя плох был измеритель): (1) счёт процессов по имени скрипта
дал «два раннера» ⇒ трижды диагностировал рекурсию и построил три гварда против фантома —
лишний PID был суб-shell'ом `$(...)` (Г26); (2) драйвер revert-verify слал путь мутанта в
env `WD`, харнесс читал позиционный `$1` ⇒ три оси вакуумны **15 дней**, вердикт указывал на
предмет; (3) тот же драйвер распознавал красноту `startswith("❌")`, а харнесс печатал строку
**с отступом** ⇒ список красных пуст всегда. Каждый дефект по отдельности давал устойчивое
правдоподобное «оси не дискриминируют». Канарейка доставки закрывает (2) механически;
(1) и (3) — чтением того, что индикатор сводит.

<!-- Added by /session-retrospective 2026-08-26 (vteam-431820a5) — root: `grep '^❌' /tmp/out-mr1.txt` дал 0 при `FAIL=2`; харнесс печатает оси с ДВУМЯ ведущими пробелами. Floor §A13 адресует ДРАЙВЕР (ревьюемый артефакт), а промах жил в ad-hoc грепе, набранном в моменте — под тот floor он не подпадает. -->

### ⛤ Подслучай, в котором floor выше НЕИСПОЛНИМ: флип ожидаемо НЕ меняет выдачу

Floor §A13 требует «выдача **обязана измениться**». У **регрессионной** проверки («не сломал
ли мой фикс соседние оси») ожидается ровно **идентичность**, и она же есть желаемый результат
⇒ проба заведомо непригодным входом отвечает не на тот вопрос, а «регрессий 0» становится
неотличимо от «подмена не доехала».

- ✅ **Канарейка тут БЕСПЛАТНА, если харнесс печатает ИМЯ предмета:** судить не по `PASS/FAIL`,
  а по **СОСТАВУ** вердиктов — `diff` строк `✅/❌` двух прогонов. Единственное допустимое
  расхождение — строка, называющая файл под тестом; она и доказывает доставку подмены.
  **Ноль расхождений ⇒ `BROKEN`**, а не «чисто»: значит прогон шёл по боевому предмету.
- ⚠ **Tell:** пишешь «регрессий 0» после флипа, в котором ожидал идентичности, и не можешь
  назвать НИ ОДНОГО наблюдаемого следа того, что предмет был подменён.
- ⛔ **Не путать с [[integration-test-revert-verify]] §A42:** там идентичность половин —
  симптом ВАКУУМНОГО ОТКАТА (лечится явным извлечением из истории); здесь идентичность
  **законна**, и лечится маркером предмета в выводе.

**Empirical 2026-09-09** (`vteam-57de43cd`, StageSpecification): `vteam-supervise-verify.sh`
против боевого детектора и против копии-с-фиксом дал `PASS=115 FAIL=0 SKIP=0` **оба раза**;
`diff` состава вердиктов — ровно одна строка (`A0b читаем: detect-fixed.py` вместо
`…vteam-supervise-detect.py`). Она и была единственным доказательством, что
`VTEAM_SUP_PROD_PY` доехал; без неё вывод «регрессий 0» держался бы на предположении.

## §A14 — AD-HOC греп над чужим выводом строит предикат ПО ПАМЯТИ о формате (`2026-08-26`)

#### ⛔ Дефект (3) живёт НЕ ТОЛЬКО в драйвере: AD-HOC ГРЕП над чужим выводом строит предикат ПО ПАМЯТИ о формате

Дефект (3) выше описан у **драйвера** — кодового артефакта, который ревьюят и переиспользуют,
поэтому его floor звучит как «читай, что индикатор сводит». Тот же промах чаще случается там,
где артефакта нет вовсе: **однострочный `grep`/`awk`, набранный в моменте** над выводом чужого
инструмента. У него нет ни ревью, ни теста, ни второго прогона — вся его проверка это
правдоподобность результата.

- ⛤ **Механизм: предикат строится ПО ПАМЯТИ о формате, а не из файла.** Ты помнишь, что харнесс
  печатает `❌ R1: …`, и якоришь `^❌` — а печатает он `  ❌ R1: …` с отступом. Или помнишь
  разделитель `# ---`, а в файле `# ──` (box-drawing). Ошибки нет, выдача пуста, и пустота
  читается как **факт о предмете** («красных нет», «секции нет»).
- ⚠ **Симптом-tell тот же и он единственный работающий: АРИФМЕТИЧЕСКОЕ САМОПРОТИВОРЕЧИЕ В ОДНОЙ
  ВЫДАЧЕ** — `0 красных` при `FAIL=2` в том же файле. ⛔ Не «повезло, что мало» — пересчитай.
- ✅ **Floor: над выводом ЧУЖОГО инструмента — не якорить и не угадывать разделитель.** Прочитай
  файл целиком (`Read`), если он в десятки строк; нужен фильтр — бери **якорь из самого файла**
  (скопированную подстроку), а не из памяти. Дешевле грепа и не врёт.
- ⛔ **Хук здесь невозможен по построению:** легитимный `^`-якорь неотличим от ошибочного, а
  формат чужого вывода гварду неизвестен. ⇒ **terminal accept-baseline**; ценность записи — в
  симптоме-tell, а не в барьере.
- ⛤ **Отличать от §A15 (сама канарейка врёт):** там врёт **измеритель, который ты построил** (проба
  доставки); здесь — **однострочник, который ты набрал**. Первый чинится конструкцией, второй —
  только чтением предмета.

**Empirical 2026-08-26** (тикет `431820a5`, вердикт мутанта M-r1): `grep '^❌' /tmp/out-mr1.txt` →
**0** при последней строке `PASS=23 FAIL=2`. Харнесс печатает `  ❌ R1: rc=0 send-failed=0 sent=1`
(два ведущих пробела). Второй экземпляр в той же сессии: `awk '/^# --- K/'` не сматчил ничего —
в файле `# ── K.`. Оба пойманы противоречием, а не чтением своего предиката.

## §A15 — САМА канарейка доставки врёт — в сторону ложно-мёртвого (`2026-08-22`)

### ⛔ САМА КАНАРЕЙКА ДОСТАВКИ врёт — и врёт в сторону ЛОЖНО-МЁРТВОГО: она ФАБРИКУЕТ дефект, а не пропускает его

Секция выше требует канарейку и трактует её вердикт как надёжный. Но канарейка — такой же
код: она подсовывает предмету **заведомо непригодный** вход и ждёт, что выдача изменится.
⛔ «Непригодный» надо выбирать по тому, **как предмет запускается**, а не по интуиции
«пустой файл заведомо плох».

- ⛤ **Типовой промах: проба пишет пустой файл БЕЗ бита исполнения.** Харнесс со стартовым
  гвардом `[ -x "$SUT" ]` — обычная форма (в моём корпусе почти у всех 61) — отвергает его
  **своим** отказом (`BROKEN`, rc=2), а эта строка не матчит `red_re` (там `A\d+`) ⇒ выдача
  равна контролю ⇒ канарейка объявляет «подмена не доезжает» на харнессе, чья доставка
  **исправна**.
- ⛔ **Направление ошибки — худшее из двух.** Ложно-живая канарейка просто не защищает;
  ложно-мёртвая **уводит чинить доставку, которая не ломалась**, и вердикт при этом звучит
  технически убедительно («харнесс не читает подсунутый путь»).
- ✅ **Floor: непригодный вход обязан быть непригодным ПО СОДЕРЖИМОМУ, а не по правам** —
  пустой файл **с** битом исполнения. Тогда предмет запускается и честно проваливает оси,
  что и есть наблюдаемое изменение.
- ⚠ **Симптом-tell:** канарейка говорит «не доезжает» у харнесса, в котором ты **видишь**
  `"$SUT"` в строке запуска. Видимое использование пути опровергает вердикт немедленно —
  первое подозрение на канарейку, не на харнесс.
- ⛤ **Запломбировать парой:** ось «немой харнесс ⇒ прогон валится» **плюс** ось «харнесс с
  гвардом `[ -x ]` НЕ объявлен немым». Без второй половины «канарейка ловит» неотличимо от
  «канарейка кричит всегда».

**Empirical 2026-08-22:** дефект вскрылся на последнем из 61 харнесса, оставшемся без
доказательства различимости (`spawn-setting-sources-verify.sh`): вместо чисел мутантов
пришло `BROKEN: подмена не доезжает`. Один `chmod(0o755)` в пробе — и все три его мутанта
покраснили ровно свои оси. Пара A5c/A5d + мутант «канарейка снова пишет неисполнимую пробу»
запечатали класс.

## §A16 — Счёт по НАЛИЧИЮ АРТЕФАКТА вместо наличия СВОЙСТВА (`2026-08-21`)

### ⛔ Счёт по НАЛИЧИЮ АРТЕФАКТА вместо НАЛИЧИЯ СВОЙСТВА — и он врёт в ТРЕВОЖНУЮ сторону

Форма, которой нет в семёрке 2026-08-19: счётчик исправен, вход полон, арифметика верна —
а меряет он **носитель** свойства вместо самого свойства. Типовой вид: «сколько харнессов
без `.spec.json`», «сколько правил без секции X», «сколько джобов без plist-ключа Y».

⛔ **Ошибка систематически ЗАВЫШАЕТ проблему**, потому что у свойства почти всегда
**несколько носителей**, а счётчик знает один. Мой замер дал **22 из 56** харнессов «без
доказательства различимости»; проверка вторым признаком — **1**, да и та ложная (различимость
там обеспечена **парой контрпримерных осей**, а не мутантом). Настоящий ответ: **0**.

- ⚠ **Симптом-tell — правдоподобно ТРЕВОЖНАЯ доля.** «22 из 56» ощущается как находка и
  толкает заводить работу; «0 из 56» находкой не выглядит и потому не перепроверяется. ⇒
  подозрение к счётчику надо включать именно на **плохих** числах, а не только на подозрительно
  хороших (`self-satisfying-metric-weak-verifier` — там зеркало: метрика, которая не может
  покраснеть).
- ✅ **Floor: назови СВОЙСТВО словами, потом перечисли ЕГО НОСИТЕЛИ.** Здесь свойство —
  «набор умеет покраснеть»; носителей три: внешняя спека мутантов · мутация **внутри** харнесса ·
  **пара контрпримерных осей** (A1 «живой вход не дефект» ↔ A2 «мёртвый найден» — каждая
  контрпример другой). Счёт по одному носителю не является счётом свойства.
- ⛤ **Второй признак ищется расширением словаря, а не логики:** тот же счёт по
  `мутант|флип|revert|подмен|сломан` сдвинул 22 → 5. Значит первый промах был **лексическим**
  (искал одно слово), и это дешевле всего чинится перечислением синонимов **до** вывода.
- ⛔ **Пара контрпримерных осей — ПОЛНОЦЕННОЕ доказательство, а не «слабая форма».** Если
  фикстуры A1 и A2 различаются ровно проверяемым признаком, то сломанный в любую сторону
  предмет краснит одну из них: «перестал находить» → A2, «начал находить всё» → A1. Требовать
  сверх этого ещё и мутанта — требовать доказательства второй раз.

**Empirical 2026-08-21:** первый счёт «22 без спек» я собирался вынести как находку о состоянии
инфраструктуры. Поймало противоречие с прямым наблюдением: `vteam-select-verify.py` печатает
«мутантов 15» — то есть заведомо различает, будучи в списке «без доказательства». Побочно тот же
проход воспроизвёл **Г3e** (`grep -c … || echo 0` дал `"0\n0"` у пяти строк отчёта).

## §A17 — Ожидание по НАЛИЧИЮ строки вместо ПОЯВЛЕНИЯ новой (`2026-08-21`)

### ⛔ Ожидание по НАЛИЧИЮ строки вместо ПОЯВЛЕНИЯ новой — условие истинно с первой секунды

Floor «ждать АРТЕФАКТ, а не отсутствие процесса» (`event-driven-coordination`) снимает
самоматч `pgrep`, и на этом кажется исчерпанным. Он не покрывает вторую половину: артефакт
**накопительный**. Журнал, лог, каталог результатов хранят и **прошлые** вердикты, поэтому
`until grep -q '<маркер>' "$LOG"` истинно **до начала ожидания** — ждун возвращается
мгновенно и приносит **чужой, старый** результат.

- ⛔ **Отказ маскируется правдоподобием содержимого.** Возвращается не мусор, а настоящий
  вердикт настоящего прогона — просто **не того**. Числа осмысленные, формат верный, дата в
  строке есть и её никто не читает. У меня старый вердикт (13 часов назад) нёс `красных=4`
  против фактического `красных=1` — то есть картина была не просто устаревшей, а
  **противоположной по смыслу** («инфраструктура сыпется» вместо «один красный»).
- ✅ **Floor: ждать ПРИРАЩЕНИЯ, а не наличия.** Снять базу ДО ожидания и требовать больше:
  `bash
  BASE=$(grep -c '<маркер>' "$LOG")
  until [ "$(grep -c '<маркер>' "$LOG")" -gt "$BASE" ]; do sleep 30; done
  `
  Работает независимо от того, сколько прошлых записей уже лежит, и не требует парсить даты.
- ⚠ **Симптом-tell, ловится за секунду:** ждун, рассчитанный на десятки минут, вернулся
  **сразу**. Это тот же тель, что у §A8a в `gui-input-verify-then-poll` (поллер выходит на
  первом цикле, потому что маркер положил туда ты сам) — но корень другой: там **эхо своего
  ввода**, здесь **накопленная история чужих прогонов**.
- ⛤ Ошибка живуча тем, что floor формулируется словом «артефакт», а артефакт бывает
  **накопительным** (журнал, лог) и **одноразовым** (verdict-файл, перезаписываемый на прогон).
  Для второго `grep -q` корректен; для первого — нет. Спрашивать надо не «есть ли артефакт»,
  а **«переживает ли этот артефакт прошлые прогоны»**.

**Empirical 2026-08-21:** ждун вердикта раннера вернулся немедленно с записью 07:50
(`прогнано=16 красных=4`), тогда как текущий прогон шёл и на тот момент имел **1** красный из 60.
Ошибка в ждуне, написанном мною же минутой ранее — и написанном **по правильному floor'у**
(ждал артефакт, а не `pgrep`). Двенадцатый ложный измеритель за сессию.

## §A18 — ХУДШИЙ исход: ложный вывод уехал в durable-артефакты (`2026-08-21`)

### ⛔ ХУДШИЙ исход класса: ложный вывод УСПЕЛ УЕХАТЬ в durable-артефакты — и потребовал коррекции в двух местах

Пятнадцать случаев выше ловились **до** отгрузки: счётчик врал, я замечал противоречие,
чинил. Шестнадцатый прошёл дальше — потому что выглядел не «числом», а **объяснением**.

Механизм: я разбирал красный харнесс, увидел в его выводе `❌ H2` / `❌ H4`, затем в более
позднем фрагменте того же файла — `✅ H2` / `✅ H4`, и построил **связную гипотезу**
(«краснота от contention, в тишине зелено»). Гипотеза была правдоподобна, согласовывалась с
уже задокументированным классом и **объясняла всё наблюдаемое**. Я отгрузил её в правило и
в тело задачи.

⛔ **Фактически я читал вывод МУТАНТА, приняв его за контроль.** У мутантного харнесса вывод
устроен блоками: `=== КОНТРОЛЬ` → `✅ Контроль: N/N` → `=== M1 …` → `=== M2 …`, и краснота
внутри блока мутанта есть **ожидаемый исход**, а не дефект.

- ⚠ **Симптом-tell был и был пропущен:** `grep -c '❌'` по всему выводу дал **34** при контроле
  **27/27** — то есть больше, чем всего осей. Число, превышающее размер популяции, — прямое
  доказательство, что счёт смешивает две разные вещи.
- ⛤ **Почему прошло дальше остальных: ложным было не ЧИСЛО, а ГРАНИЦА.** Все пятнадцать
  предыдущих врали значением, и значение противоречило наблюдению. Здесь значения были верны
  (`❌ H2` действительно напечатано), неверна **атрибуция строки блоку** — а блок не виден,
  если смотреть на строки, а не на структуру.
- ✅ **Floor: у структурированного вывода резать по МАРКЕРУ БЛОКА, а не грепать целиком.**
  `awk '/^=== /{n++} n<=1'` либо чтение вердикта из строки `Контроль: N/N`. Для мутантного
  харнесса `grep -c` по файлу не значит **ничего**.
- ⛔ **Коррекцию ПОКАЗАТЬ в обоих носителях**, куда уехал вывод (правило и задача), а не
  переписать молча: иначе следующий читатель возьмёт из durable-артефакта ложный механизм
  (`retracted-claim-outranks-its-correction`).

**Empirical 2026-08-21:** коммит `954e478` («раннер САМ источник contention») отгрузил ложное
обоснование; `b4ebd00` его снял показанной формой, тело задачи `32859282` исправлено тем же
заходом. Настоящая причина `rc=1` в раннере осталась **не установленной** — и это тоже
сказано вслух, вместо второй правдоподобной гипотезы.

## §A19 — УЛИКА, перезаписываемая повторным прогоном (`2026-08-21`)

### ⛔ УЛИКА, перезаписываемая повторным прогоном, — САМОСТИРАЮЩИЙСЯ измеритель: повтор с ПУСТЫМ выводом уничтожает причину прошлого отказа

Сохранять полный вывод упавшего прогона — правильный floor (иначе причина стоит повторного
прогона). ⛔ Но если улика пишется **в один файл на предмет**, следующий прогон её **стирает** —
и хуже всего это работает в самом частом случае: повтор **сорван таймаутом**, вывода нет, улика
становится однобайтовой, а настоящая причина исчезает.

- ⚠ **Симптом читается как «улики не собирались вовсе»:** файл существует, размер 1 байт. Первая
  гипотеза — «правка сбора не отработала», и ты идёшь чинить исправный код.
- ⛤ **Дискриминатор — ЖУРНАЛ прогонов, а не файл улики:** `grep '<имя предмета>' <журнал>` покажет
  N пар START/END с разными rc и длительностями. Два и более END ⇒ улика перезаписана, и
  однобайтовость объясняется последним прогоном, а не сбором.
- ✅ **Floor: имя улики несёт МЕТКУ ВРЕМЕНИ** (`<предмет>.<YYYYmmddTHHMMSS>.out`). Тогда повтор
  добавляет файл, а не затирает; свидетельство прошлого отказа переживает любое число попыток.
- ⛤ **Проверяется двумя прогонами:** первый печатает причину, второй — пуст. После второго улик
  должно быть **две**, и ровно **одна** содержит причину. Одна улика ⇒ версионирование не работает.

**Empirical 2026-08-21** (`verify-harness-runner.sh`): причина `rc=1` у `vteam-record-verify.py`
(прогон 20:07, 1290 с) утрачена прогоном 21:47 (`rc=124`, 60 с, пустой вывод) — улика 1 байт.
Журнал показывал три пары START/END; без него однобайтовость выглядела бы дефектом сбора.

## §A20 — ДВА подряд ложных измерителя в ОДНОЙ пробе (`2026-08-21`)

### ⛔ ДВА подряд ложных измерителя в ОДНОЙ пробе: сперва не доехала ИНЪЕКЦИЯ, потом соврала ФИКСТУРА

Проба, доказывающая правку, сама состоит из измерителей — и они врут так же часто, как
измеряемое. Замер 2026-08-21: одна проба дала **два** ложных сигнала подряд, оба
правдоподобных.

| что видел | как прочёл | что было |
|---|---|---|
| `прогнано=10` при одной фикстуре | «раннер что-то путает» | ⛔ **инъекция не доехала**: переменная называется `VHR_DIR`, я передал `VHR_DIRS` ⇒ мерил БОЕВОЙ набор |
| «непустых улик 2» вместо 1 | «версионирование не работает» | ⛔ **фикстура не перезаписалась вовремя** ⇒ второй прогон напечатал старый вывод |

- ✅ **Floor к первому — КАНАРЕЙКА ДОСТАВКИ в самой пробе:** печатать не только исход, но и
  **размер входа** (`прогнано=N`, имя обработанного файла). Инъекция, не доехавшая до предмета,
  тогда объявляет себя сама, а не маскируется под дефект предмета.
- ✅ **Floor ко второму — сперва подозревать ФИКСТУРУ, а не предмет**, если ось краснеет **после**
  того, как ты только что доказал соседнюю ось на том же предмете. Предмет между двумя осями не
  менялся; менялась фикстура.
- ⚠ **Симптом-tell для обоих:** число в выводе пробы не совпадает с тем, что ты сам туда положил
  (одна фикстура → «прогнано=10»; пустой вывод → «непустой файл»). Расхождение с СОБСТВЕННЫМ
  входом — всегда измеритель, никогда не предмет.

## §A21 — МАРКЕР красноты в корпусе НЕ ОДИН (⛔ против ❌) (`2026-08-21`)

### ⛔ МАРКЕР, по которому ты ищешь красноту, в корпусе НЕ ОДИН — и разбор молча даёт «0 находок»

Разбирая чужой вывод (харнесс, линтер, аудит, чужой скрипт), ты грепаешь по маркеру, который
видел **у соседа**. ⛔ Корпус, писавшийся годами разными руками, маркер **не унифицирует**:
замер 2026-08-21 по 57 харнессам — **39** печатают `⛔`, **15** печатают `❌`, 3 не определяются.

- ⚠ **Отказ даёт «0 находок» при непустой выдаче** — то есть самый убедительный из ложных
  ответов: он совпадает с желаемым («всё зелено») и не выглядит ошибкой поиска.
- ⛤ **Дискриминатор — противоречие с ИТОГОМ, а не с ожиданием:** вердикт файла говорит `FAIL`,
  а твой счётчик красных даёт 0. Расхождение **внутри одного вывода** — всегда измеритель.
- ✅ **Floor: посчитать МАРКЕРЫ, прежде чем считать находки** —
  `grep -oE '⛔|❌|FAIL|ERROR' <файл> | sort | uniq -c`. Одна команда, и она же показывает,
  какой формой пользуется этот конкретный автор.
- ⛔ **И проверить СВОЙ инструмент, если он тоже грепает по маркеру.** Мой экстрактор причины в
  раннере знал только `⛔` ⇒ для пятнадцати наборов он **всегда** печатал «причина не извлечена»,
  и это читалось как «харнесс ничего не сказал», а не как «я не умею его читать».
- ⛤ **Унификация маркера — НЕ решение**: она требует править 15 чужих файлов ради удобства
  одного читателя. Дешевле и честнее — читатель, знающий обе формы.

**Empirical 2026-08-21** (разбор `rc=1` у `vteam-record-verify.py`): я трижды считал красноту
через `⛔` и получал 0 при `ИТОГ: FAIL`. Настоящая причина (мутант M9 краснил на 7 осей больше
ожидаемого) нашлась только после того, как противоречие «0 красных при FAIL» заставило прочитать,
каким символом харнесс вообще печатает отказ.

## §A22 — Маркер верификации, РАЗОРВАННЫЙ переносом строки (`2026-08-21`)

### ⛔ Маркер верификации, РАЗОРВАННЫЙ переносом строки, даёт ложный ноль — и «доехало ли» читается как «не доехало»

Verify-по-содержимому — правильный floor (проверять предмет, а не эхо команды). ⛔ Но сам маркер
выбирают глазами по исходнику, где текст перенесён по ширине, и фраза, целая **на экране**, в
файле разорвана `\n`. `grep -F` тогда честно возвращает 0.

- ⚠ **Ошибка в ТРЕВОЖНУЮ сторону, и потому дорогая по циклам:** ноль читается как «правка не
  доехала» ⇒ повторный push, повторная проверка, поиск несуществующего дефекта доставки.
- ✅ **Floor: маркер брать ОДНОСТРОЧНЫЙ и заведомо целый** — заголовок секции, имя файла, UID,
  команда. Не фразу из прозы, которую редактор переносит.
- ⛤ **Дешёвая страховка — ДВА маркера разной природы** (заголовок + идентификатор): совпадение
  обоих исключает и разрыв строки, и случайное совпадение подстроки.
- ⚠ **Симптом-tell:** первый маркер дал 0, второй — непустое. Значит проблема в **маркере**, а не
  в доставке; предмет доехал.

**Empirical 2026-08-21** (публикация задачи в граф): `grep -cF 'измеритель без харнесса'` на
remote → **0**, при том что файл на месте (4121 б) и два других маркера дали 4 и 1. Фраза в теле
разорвана переносом между «без» и «харнесса».

<!-- Added by /session-retrospective 2026-08-24 (проход 3) — root: моя УСТНАЯ сводка «5 гвардов дублируют то, что тик делает сам» разошлась с МОЕЙ ЖЕ письменной классификацией часом ранее («дубль — уже у исполнителей»), которая была верна; замер тика опроверг сводку, не артефакт. -->

## §A23 — ЧЕТВЁРТАЯ форма — ПЕРЕСКАЗ собственного письменного артефакта (`2026-08-24`)

### ⛔ ЧЕТВЁРТАЯ форма — ПЕРЕСКАЗ СОБСТВЕННОГО ПИСЬМЕННОГО АРТЕФАКТА: артефакт верен, пересказ нет, и правишь ты не то

Три формы выше — про утверждение, у которого **нет источника** (число в прозе, свой замер,
вывод из индикатора). Четвёртая инвертирована: источник **есть, он твой и он верен** — это
классификация/таблица/тело задачи, написанные тобой же час назад. Врёт **устная сводка** о нём.

⛤ **Почему это отдельный класс, а не невнимательность:** пересказ делается **по памяти о
намерении**, а не по тексту, и потому дрейфует в сторону **более сильного** утверждения
(«частично дублирует» → «дублирует», «у исполнителей» → «тик делает сам»). Проверять его
нечем: внешнего источника нет (форма №1), а внутренний ты считаешь известным.

- ⛔ **Дороже трёх предыдущих форм тем, что ведёт к ПРАВКЕ НЕ ТОГО.** Услышав от себя «дубль»,
  следующий шаг — снять дубликат; артефакт же говорил «перенесено к исполнителям», то есть
  снимать нельзя. Ошибка входит не в отчёт, а в **действие над системой**.
- ⚠ **Симптом-tell, ловится ДО действия:** ты произносишь вывод о собственной работе и **не
  открывал** артефакт в этом ходе. Дешевле, чем звучит: артефакт свой, путь известен, чтение —
  один `grep`.
- ✅ **Floor: перед действием, вытекающим из твоей же классификации, ПРОЦИТИРУЙ её дословно.**
  Совпало с пересказом ⇒ действуй. Разошлось ⇒ **артефакт авторитетнее**: он писался, глядя
  на предмет, а пересказ — глядя на память о нём.
- ⛤ **И проверь ПРЕДМЕТ, а не только артефакт:** у меня замер тика опроверг **сводку** и
  подтвердил **классификацию** — то есть предмет был третьей, независимой инстанцией. Когда
  артефакт и пересказ расходятся, замер предмета разрешает спор за один прогон.
- ⚠ **Коррекцию произнести вслух, а не молча заменить формулировку** — иначе следующий читатель
  твоего отчёта унаследует пересказ, а не артефакт (`retracted-claim-outranks-its-correction`).

**Empirical 2026-08-24**: устно — «5 гвардов дублируют то, что тик делает сам»; письменно, часом
ранее — «дубль — уже у исполнителей». Замер `vteam-dispatch.sh`: тик **не проверяет** ни темп, ни
слот, ни память, ни tmux (0 строк на каждое) ⇒ снимать эти пять было **нельзя**, и снял я ровно те
4, что классификация называла инертными/опасными. Артефакт был прав, пересказ — нет.

## §A24 — ТРЕТЬЯ форма §Двух уточнений — оракул классификации (`2026-08-24`)

### ⛔ ТРЕТЬЯ форма §Двух уточнений: у ОРАКУЛА КЛАССИФИКАЦИИ инверсия полярности НЕПРИМЕНИМА — floor «признать путь исчерпанным»

§Два ПОСЛЕДОВАТЕЛЬНЫХ уточнения (выше) описывает **счётчик**: ряд чисел сходится, и floor —
читать кандидатов, когда их стало единицы. `self-satisfying-metric-weak-verifier` §ПОЧИНКА
перечня перечнем описывает **гвард**: floor — инвертировать полярность (принимать всё, кроме
явно безопасного). Третий носитель — **оракул классификации** («реализовано ли X?»), и оба
floor'а к нему **не применяются**:

| носитель | что даёт уточнение | рабочий floor |
|---|---|---|
| счётчик | число **падает** (6 → 1 → 0) | читать кандидатов, когда их единицы |
| гвард | пропускает новую **форму входа** | инвертировать полярность |
| **оракул классификации** | **новый ВИД ложного срабатывания**, число не сходится | ⛔ обоих нет — **объявить механический путь исчерпанным** |

⛤ Инверсия здесь невозможна **по построению**: «считать реализованным всё, кроме явно
нереализованного» — то же самое утверждение с другой стороны, у него ровно те же ложные
срабатывания.

- ⚠ **Симптом-tell, отличающий от счётчика:** после сужения меняется не количество, а **природа**
  промаха. Первое сужение ловит «термин лишь упомянут», второе — «все термины упомянуты»,
  третье — «RFC вводит СВОЙСТВО, а не класс». Три разных вида подряд = whack-a-mole, и четвёртый
  фильтр к нему ничего не добавит.
- ⛔ **ОТРИЦАТЕЛЬНЫЙ ответ такого оракула тоже НЕ доказательство** — этого не говорит ни одна из
  двух соседних секций. Термин может отсутствовать в графе, а фича быть отгруженной **кодом** без
  TBox-записи. ⇒ у оракула по метке ненадёжны **обе** стороны, и «не нашёл ⇒ не сделано» ложно
  так же, как «нашёл ⇒ сделано».
- ✅ **Floor: остановиться после ВТОРОГО вида FP и сказать вслух «механический путь исчерпан»**,
  применив результат только к однозначным. Это не поражение: явно названная граница даёт фаундеру
  выбор («разобрать поштучно» / «закрыть приток»), а четвёртый фильтр даёт ложную уверенность.
- ⛤ **Дешевле, чем кажется: у остатка обычно есть ДРУГОЙ, не-оракульный ход.** У меня им было
  «закрыть приток» — команда, переводящая RFC в терминальный статус при отгрузке, то есть работа
  не над остатком, а над его **источником**.

**Empirical 2026-08-24** (117 Draft-RFC на `StageSanction`): оракул «термин из label RFC есть в
графе» сужался трижды — первый термин → все термины → «RFC ВВОДИТ термин»; каждое сужение давало
новый вид FP (`88bac31f` · `fd570e7e` · `74c5e336` «Extend `exocmd__Grounding`»). Флипнуто **4**
однозначных из 117; остальные 88 признаны неразрешимыми механически, и фаундер выбрал закрыть
приток. Обратная сторона предъявлена там же: `ui__RelationColumnSet` класса в графе не имеет, а
ассет говорит «v15.120.0 shipped».

## §A25 — ДВА ТВОИХ измерителя разошлись: ты объявил один испорченным и СОЧИНИЛ механизм (`2026-08-27`)

**Наблюдаемое.** Один и тот же предмет померен двумя каналами/инструментами, и числа разошлись
(у меня: `gh api …/contents --jq .content | base64 -d` → 3701 б против
`-H "Accept: application/vnd.github.raw"` → 5519 б на одном файле). Оба измерителя — **свои**,
внешней инстанции для сверки нет.

**Что я сделал неверно.** Объявил меньший канал **испорченным**, придумал ему механизм («молча
теряет байты на не-ASCII») и вывел из механизма **floor** — «брать raw-канал, `.content` не
использовать». Floor уехал в durable-артефакт (тело тикета, отчёт релиза) и **противоречил
корпусу**: `exosync-quarantine-triage` §A3c и `vault-asset-creation` §A2b предписывают ровно
`.content`-форму как разрешающий дискриминатор.

**Замер, опровергнувший механизм (одна команда, следующий же ход).** Воспроизведение ОБОИХ каналов
подряд: `.content` → **5519**, raw → **5519**, локальный файл → **5519**, `diff` идентичен.
История файла: один коммит, `size=5519` в нём же ⇒ версии меньшего размера не существовало
никогда. `base64 -d` на контрольной строке декодирует корректно. ⇒ механизм ложен целиком.

**Чего замер НЕ установил и почему это надо сказать вслух.** Откуда взялось 3701 — неизвестно:
команда того прогона не сохранилась. Соблазн — заменить одну гипотезу второй («значит, был
промежуточный push», «значит, обрезал пайп»). ⛔ Вторая гипотеза стоит ровно столько же, сколько
первая: ноль. Честная форма записи — «расхождение наблюдалось, причина не установлена,
предложенный механизм опровергнут».

**Floor.**

- ✅ **Разошлись два СВОИХ измерителя ⇒ первым делом воспроизвести ОБА в одном ходе**, а не
  объяснять разницу. Стоит одну команду и терминально: расхождение либо повторяется (тогда есть
  что исследовать), либо исчезает — и тогда объяснять было нечего.
- ✅ **Спросить ИСТОРИЮ предмета** (`commits?path=…` + `size` по каждому коммиту): она отвечает,
  существовало ли когда-нибудь состояние, дающее меньшее число. Не существовало ⇒ расхождение
  не про предмет, а про твой прогон.
- ⛔ **Не писать floor, отменяющий корпусную форму, из ОДНОГО расхождения.** Форма, живущая в
  нескольких правилах, пережила много прогонов; твоё единственное наблюдение против неё —
  подозреваемый, а не доказательство.
- ⚠ **Симптом-tell, ловится ДО записи:** ты формулируешь «канал X испорчен / инструмент Y врёт»
  и не можешь предъявить **повторный** прогон обоих. Утверждение о дефекте ЧУЖОГО инструмента —
  самый дорогой вид собственного claim'а: он уезжает в durable-артефакт как рекомендация.

**Чем отличается от соседей.** §A6 разбирает расхождение с **чужим** числом (сверять ПРОЦЕДУРУ —
единицу счёта, область, отбор). Здесь оба числа мои, процедура одна, и различает их только
**время прогона** ⇒ дискриминатор другой: не сверка определений, а повторный замер.
`decision-surface-must-derive-from-mechanism` §ТРЕТЬЯ ЧАСТЬ находки объясняет цену: опровержение
ПРИЧИНЫ роняет не находку, а **ФИКС**, — и фиксом здесь был ложный floor.

**Провенанс.** Найдено ретроспективой релиза тикета `52215d7f` (`StageRelease`), в Step 3a: греп
корпуса по `vnd.github` дал **0**, то есть мой floor был бы net-new — и именно это заставило
перепроверить его замером вместо записи. Коррекция показана в теле тикета парой
«⛔ снято → ✅ фактический замер», а не переписана молча.

⛤ **ЧЕТВЁРТЫЙ ход, СТРОГО СИЛЬНЕЕ воспроизведения, когда предмет ПОД КОНТРОЛЕМ ВЕРСИЙ.** Посылка выше («оба измерителя — свои, внешней инстанции для сверки нет») верна для API/remote и **ложна для файлов рабочего дерева**: там арбитр есть — `git status --porcelain -- <путь>`. Он отвечает ОДНОЙ командой и **терминально**, не требуя, чтобы расхождение воспроизвелось.

- ⚠ **Tell:** оба твоих измерителя считают ФАЙЛЫ (хеши, число, mtime) ⇒ спрашивать git ПЕРВЫМ, а «воспроизвести оба» оставить на случай, когда он молчит.
- ⛤ **Цена промаха — ложная находка в durable-артефакте.** Замер 2026-09-01 (`5531b98c`, приёмка): два моих счётчика дали **148 против 225** файлов и **3 против 35** launchd-джобов — разные предикаты (`shasum -a 256` против SHA-1 с неприменившимся `sed`; `com.kitelev.vteam-*` против всего флота). Вывод «BIN изменён» едва не ушёл в отчёт приёмки. `git status --porcelain -- .claude/bin` → **пусто** ⇒ каталог не тронут, расхождение целиком в моих предикатах.

## §A26 — КОМПАКЦИЯ стрипует провенанс: твоё наблюдение возвращается СТРОКОЙ САММАРИ и читается как установленный факт (`2026-08-27`)

**Наблюдаемое.** Ты собираешься исправить **всегда-загруженный** артефакт (правило / скилл) по факту
вида «тогда-то инструмент X отверг форму Y». Факт помнится отчётливо и формулируется уверенно — а
пришёл он **строкой компакт-саммари**, и назвать команду, которой он установлен, ты не можешь.

**Почему это ХУЖЕ обычной потери провенанса.** Ядро правила говорит: у своего утверждения нет
внешнего источника, потому что источник — ты. Компакция добавляет второй слой, которого нет ни у
одной из трёх форм ядра: наблюдение **переписывает СИСТЕМА** в сводку, где оно стоит рядом с
фактами, установленными замером, — тем же шрифтом, без пометки происхождения и без команды. То есть
исчезает даже память о том, ЧЕМ оно установлено, а вместе с ней и единственный повод перепроверить.

⛤ Дискриминатор ядра («могу ли я назвать ОДНОЙ строкой, чем это установлено?») здесь **работает**, но
задать его себе почти невозможно: сводка выглядит как чужой конспект, а не как твоё утверждение.
Спусковой крючок поэтому другой — **не источник, а АДРЕСАТ правки**: правишь корпус ⇒ проверь.

**Что я чуть не сделал.** Саммари несло строку «`--to 219d5e8f-…` отвергнут — не резолвится в этап;
исправлено на `--to StageImplementation`». Скилл `/onto-rfc` при этом дважды печатает форму
`--stage-transition <item-uid> --to <stage-uid>` — то есть **UID**. Вывод «инструкция скилла устарела,
надо править» напрашивался сам и лёг бы в always-on артефакт.

**Замер, опровергнувший его (один прогон, три формы).** `VTEAM_DRY_RUN=1 vteam-record.sh
--stage-transition <uid> --to <FORM>` для `219d5e8f-…` · `StageImplementation` ·
`flow__StageImplementation` — **все три** дали идентичный `rc=7` с одним и тем же **последующим**
сообщением («переход StageImplementation → StageImplementation НЕ ОБЪЯВЛЕН»), то есть резолюция
прошла у всех трёх. Чтение резолвера (`vteam-record.sh:665-671`) подтверждает: принимаются
**локальное имя · полная метка · UID**. ⇒ инструкция скилла **верна**, правка была бы ложной.

**Чего замер НЕ установил.** Почему тот, первый вызов отказал, — неизвестно: его точная форма не
сохранилась (правдоподобно — усечённый UID, не проходящий `UID_RE.fullmatch`, но это гипотеза).
⛔ Вторая гипотеза стоит ровно столько же, сколько первая. Честная запись — «расхождение
наблюдалось, причина не установлена, предложенная правка опровергнута замером».

**Floor.**

- ✅ **Адресат правки — корпус ⇒ пере-мерить предмет ОДНИМ прогоном ДО правки.** Не «вспомнить
  точнее», не «перечитать саммари внимательнее»: провенанс восстанавливается только замером.
- ✅ **Мерить ВСЕ формы разом, а не подозреваемую.** Три вызова в одном цикле стоят секунды и дают
  терминальный ответ; проверка одной формы отвечает «эта работает» и молчит об остальных.
- ✅ **Дискриминатор «резолюция прошла» — ПОСЛЕДУЮЩАЯ ошибка, а не `rc=0`.** Одинаковый *downstream*
  отказ у всех форм доказывает, что до него доехали все; ждать `rc=0` не нужно и часто невозможно
  (у меня предмет уже был на целевом этапе).
- ⛔ **Не писать коррекцию, пока замер её не подтвердил.** Ложная правка всегда-загруженного
  артефакта дороже пропущенной: она уводит **каждого** следующего исполнителя от рабочей формы.

**Цена промаха (если бы не замер).** В скилл роли конвейера уехало бы «`--to` требует имя, а не UID»
— измеримо ложное утверждение, снимающее рабочую форму вызова у всех последующих воркеров.

**Соседи.** §A25 (тот же день, та же семья) лечит случай, когда у тебя ДВА свежих измерителя и они
разошлись; здесь измеритель **один и протухший**, поэтому floor §A25 («воспроизвести оба») не
срабатывает — воспроизводить нечего, пока не поймёшь, что единственный источник есть сводка.
Соседний носитель того же перекоса — `verify-before-assert` §A16 (компакция урезает оценку хода
собственной работы); там предмет — метрика сессии, здесь — факт о поведении инструмента.

**Провенанс.** Найдено ретроспективой тикета `6632a011` (`StageSpecification`, роль MemberArchitect),
Step 3a: греп корпуса дал `self-authored-claim-loses-its-provenance` без единого упоминания
компакции — то есть floor был бы net-new, и именно это заставило перепроверить его замером вместо
записи.

## §A27 — КЛЮЧ СРАВНЕНИЯ двух ревизий несёт не только содержание (`2026-08-28`)

### ⛔ Сравниваешь ДВЕ ревизии — ключ обязан нести СОДЕРЖАНИЕ и ТОЛЬКО его: он ломается в ОБЕ стороны, и одна из них ИНВЕРТИРУЕТ вердикт

Дифференциал («до фикса против после») — базовая операция приёмки, и вывод из него делается по
**ключу сравнения**: имени файла, в который извлечена ревизия, либо свёртке её содержимого. Ключ
почти никогда не равен содержанию: он его **производная**, и производная ломается двумя
противоположными способами.

| как сломался ключ | что происходит | что читается |
|---|---|---|
| **потерял** различающую часть | два РАЗНЫХ входа схлопываются в ОДИН | ложное «половины одинаковы» |
| **приобрёл** несодержательную часть (координаты) | ОДИНАКОВЫЕ входы расходятся | ложное «предмет изменился» |

⛔ **Первая сторона ИНВЕРТИРУЕТ ВЕРДИКТ, а не шумит.** Если обе половины дифференциала — на деле
один и тот же предмет, флип не воспроизводится **по построению**; читается это как «фикс ничего
не меняет», то есть приёмка возвращает исправную поставку. Вторая сторона всего лишь шумит и
ловится пере-замером.

- ⚠ **Tell схлопывания — АРИФМЕТИЧЕСКИЙ и бесплатный:** размер одной из половин совпадает с
  размером ТРЕТЬЕЙ, заведомо известной величины (у меня — `HEAD`). Две разные ревизии не обязаны
  совпасть с ней обе; совпадение = ты смотришь на одну и ту же.
- ⚠ **Tell загрязнения:** различие обнаружено там, где твоя правка **не проходила**. Прежде чем
  объяснять его предметом, спроси, не сдвинулось ли что-то ВЫШЕ по файлу.
- ✅ **Floor 1 — извлекать в РАЗЛИЧИМЫЕ имена, не в производные от ревизии.** Деривация имени из
  строки ревизии — источник коллизии: любой транформ (`tr -d '^'`, `sed 's/[^a-z0-9]//g'`,
  `basename`) может стереть ровно тот символ, которым ревизии и различаются. Имена задавать
  ЛИТЕРАЛАМИ (`pre.sh` / `fix.sh` / `head.sh`).
- ✅ **Floor 2 — канарейка различности ВХОДА, один вызов:** `shasum` двух извлечённых файлов
  обязан **различаться**. Совпал ⇒ дифференциал вакуумен, и никакой прогон этого не покажет.
- ✅ **Floor 3 — сравнивать СОДЕРЖАНИЕ:** `grep -h` (⛔ не `-n`), `git show <rev>:<path>`,
  `diff` двух извлечённых файлов. ⛔ `--stat` и `grep -n` несут координаты, а координаты сдвигает
  любая правка соседнего участка.

⛤ **Отличать от §A19 по МЕХАНИЗМУ и по ЦЕНЕ.** §A19 — улику стирает ПОВТОРНЫЙ прогон (один файл
на предмет), цена — утраченная причина прошлого отказа. Здесь коллизия происходит **внутри одного
прохода**, между двумя РАЗНЫМИ входами, и цена — инвертированный вердикт о поставке. Floor у §A19
(«имя несёт метку времени») здесь **не помогает**: метка времени различила бы два прогона, а мне
надо различить две ревизии.

**Empirical 2026-08-28** (приёмка тикета `116ff9d1`, роль MemberTester, харнесс надзора vteam).
Оба экземпляра — в одном заходе, оба самопойманы, оба дали ложный вывод до пере-замера:

- **Схлопывание.** `for R in "25ceb52^" "25ceb52"` с извлечением в `/tmp/sv.$(... | tr -d '^').sh`
  — оба итерации дали `/tmp/sv.25ceb52.sh`, вторая затёрла первую. Поймано тем, что размер
  «до-фиксной» половины оказался равен размеру `HEAD` (146 857 б). Не поймай — обе половины
  дифференциала были бы FIX-ревизией, флип 3→2 не воспроизвёлся бы, и вердикт стал бы «фикс не
  различается» на исправной поставке.
- **Загрязнение.** Сверка блока оси `M21` между ревизиями через `grep -n 'M21' <файл> | md5`
  дала РАЗНЫЕ свёртки ⇒ вывод «правка задела M21» (то есть вышла за границы тикета). Пере-замер
  `grep -h` (только содержание) — свёртки **идентичны**; расходились номера строк, сдвинутые на
  +13 из-за ⛔-шапок комментариев, добавленных фиксом ВЫШЕ по файлу.

**Провенанс.** Найдено исполнением, не чтением: Step 3a-греп корпуса по «перезапись» и «деривация»
дал `§A19` (повторный прогон) — механизм соседний, но не этот, и его floor мою коллизию не
закрывает.

## §A28 — ЖДУН, чей шаблон РАСШИРЕН «на всякий случай», ловит ПРОГРЕСС-строку того же прогона (`2026-09-07`)

### ⛔ Ждать «маркер завершения» — верный floor; ломает его АЛЬТЕРНАЦИЯ, добавленная ради покрытия отказов

Корпус предписывает ждать **артефакт**, а не процесс (`until grep -q '<маркер завершения>' <лог>`),
и это верно. Отдельно предписывается **расширять** шаблон, чтобы не пропустить падение («silence
is not success» — floor Monitor-тула). Два верных совета вместе дают дефект: расширяя альтернацию,
ты добавляешь символ, которым производитель помечает **не только вердикт**.

| ждун | что предполагалось | что сматчило |
|---|---|---|
| `until grep -qiE 'violation\|conform\|✅\|❌' "$LOG"` | `✅ Vault conforms …` — вердикт | `✅ Loaded 459126 triples` — **строка прогресса**, напечатанная за минуты до вердикта |

⛤ **Отличать от §A17 и от Г28 — механизмы РАЗНЫЕ, и лечения не совпадают:**

| | откуда ложное срабатывание | лечение |
|---|---|---|
| **§A17** | журнал НАКОПИТЕЛЬНЫЙ ⇒ маркер от ПРОШЛЫХ прогонов | ждать ПРИРАЩЕНИЯ (`-gt "$BASE"`) |
| **Г28** | лог пуст, потому что вывод обёрнут пайпом | не пайпить вывод фоновой команды |
| **§A28** | маркер принадлежит **ЭТОМУ** прогону, но помечает **ПРОГРЕСС**, а не исход | якорить шаблон на ФОРМУ вердикта |

⇒ ни `BASE`-приращение, ни снятие пайпа здесь не помогают: журнал свежий, вывод не обёрнут,
маркер настоящий — просто он не про то.

- ⚠ **Симптом-tell, ловится ЧТЕНИЕМ ВЫДАЧИ ждуна, а не рассуждением:** уведомление пришло, а в
  напечатанном хвосте вердикта **нет** — только прогресс. ⛔ Не читать как «предмет ещё пишет»:
  ждун уже отработал и больше не сработает, то есть настоящий вердикт ты пропустишь.
- ⛔ **Цена ассиметрична и идёт в опасную сторону:** уведомление о срабатывании читается как
  «готово». Дальше строится вывод («SHACL прошёл», «прогон завершён») на строке, которая говорит
  ровно обратное — что работа ещё идёт.
- ✅ **Floor: у ждуна ДВА шаблона, а не один расширенный.**
  - **успех** — якорится на ФОРМУ терминальной строки, а не на символ: `'^ИТОГ:'`, `'conforms to'`,
    `'PASS=[0-9]'`. ⛔ Голый `✅`/`❌`/`OK` в альтернацию не класть: их производитель печатает и в
    прогрессе.
  - **отказ** — расширять свободно (`Traceback|Killed|OOM|FAILED`): ложное срабатывание там
    заставляет посмотреть лог, то есть ошибка уходит в безопасную сторону.
- ⛤ **Дешевле обоих — ждать НЕ маркер, а сам ПРОЦЕСС** (`run_in_background` у команды: её выход и
  есть терминальный факт). Маркер нужен, только когда предмет запущен не тобой.
- ⛔ **Проверять ждун СВОИМ ЖЕ шаблоном на УЖЕ НАПЕЧАТАННОМ хвосте** перед тем, как на него
  положиться: `grep -cE '<шаблон>' <лог>` при живом прогоне обязан дать **0**. Дал ≥1 ⇒ шаблон
  ловит прогресс, и ждун сработает немедленно.

**Empirical 2026-09-07** (тикет `c3dbbfbd`, `StageImplementation`): ждун на вердикт
`validate schema --shapes-mode` сработал через ~10 с после старта; в его выводе стояли три строки
загрузки и **ни одного** вердикта. Настоящий вердикт (`✅ Vault conforms to all shapes`) пришёл
на пять минут позже и был прочитан только потому, что я открыл напечатанный хвост, а не поверил
факту уведомления. Урон 0; второй ждун с шаблоном без `✅` отработал корректно.

## §A29 (2026-09-07) — ОСТАТОК: величина вычислена ВЕРНО, а её СОСТАВ ПРИПИСАН

Три формы ядра — про величину, которую ты **произвёл** (число в прозе · свой замер · вывод из
индикатора). Четвёртая: величина получена **вычитанием** (`целое − измеренные части`) и потому
арифметически безупречна — а **что в неё входит**, не измерялось ни разу.

- ⛔ **Тише трёх соседей, потому что проверку ПРОХОДИТ.** Арифметику можно пересчитать, и она
  сойдётся; ложна не она, а **интерпретация** остатка. У интерпретации источника нет вовсе —
  остаток по построению есть «всё прочее», то есть именно та часть, которую ты не смотрел.
- ⚠ **Tell, ловится при написании:** во фразе стоит «остальное — это …», «значит, туда входит …»,
  «на X приходится …», и замер ИМЕННО этой части назвать нельзя.
- ✅ **Floor: остаток называть остатком** («всё, что не корпус — 90 706»), а состав перечислять
  ТОЛЬКО наблюдением — прочитать, что фактически лежит в предмете.
- ⛤ **Наблюдение обычно бесплатно и лежит перед глазами:** схемы инструментов, листинги, инструкции
  видны прямо в промпте; лог открывается одной командой. Одно чтение опровергает вывод, который
  иначе живёт до чужой проверки.
- ⛤ **Цена выше, чем у соседей, из-за РОЛИ остатка в решении:** он всегда «всё, чем мы ещё не
  занимались», поэтому приписанный ему состав автоматически становится **рекомендацией номер один**.

**Empirical 2026-09-07** (сессия `standalone-1788709538`, вопрос «сколько занято контекста»):
префикс 604 997 замерен точно, документы по tiktoken 402 535 ⇒ остаток **202 462 (33 %)** объявлен
«схемами MCP — крупнейший неатакованный пласт» и ушёл фаундеру как главный рычаг диеты. Наблюдение
собственного промпта: MCP-тулы **deferred**, в контексте только ИМЕНА (~110 строк), схем нет вовсе.
Регрессия по 54 историческим сессиям (R²=0.99) дала остатку **90 706 (15 %)** ⇒ вывод был неверен
и по составу, и по величине — tiktoken занижал кириллический корпус на 30 %. Опровергнуто дважды:
наблюдением и замером; ни одна проверка арифметики этого бы не поймала.

## §A33 — ИЗОЛЯЦИЯ ПО ОДНОЙ ПЕРЕМЕННОЙ ПОДТВЕРДИЛА ЛОЖНУЮ ГИПОТЕЗУ: два разных вмешательства дают ОДИН сдвиг (`2026-09-10`)

**Наблюдаемое.** Мой замер разошёлся с **чужим** числом (постановка обещала `ЦИКЛ(7)` / 9 узлов при
четырёх `--move`; прогон дал `ЦИКЛ(8)` / 10). Расхождение — ровно на единицу, и лишний узел
(`exoas-person`) моей правки не касался вовсе.

**Что я сделал — и почему это выглядело безупречно.** Вместо того чтобы объяснять разницу словами,
я **поставил эксперимент**: убрал из набора одну переменную (`--move public/person`) и получил
ровно обещанные числа — `8/10 → 7/9`. Гипотеза «замер оркестратора шёл без `public/person`»
оказалась **проверяемой и подтверждённой**. Floor §A25 («не сочиняй механизм — воспроизведи»)
формально исполнен: я не рассуждал, я мерил.

**Чем это было на самом деле.** Причина лежала в **состоянии входа**: копия трёх vault снималась
с живых монтирований, а в отстающем `vault-tbank` не доехала дельта предыдущего листа
(`public/person/docs`). Ребро `person → exodoc` выживало и добавляло тот самый лишний узел.
Доводка копии дала `41/99, ЦИКЛ(7)/9` — совпадение с чужим числом побайтно.

**Почему эксперимент не различил гипотезы.** Оба вмешательства — «убрать `person` из `--move`» и
«довести отстающий vault» — убирают `exoas-person` из SCC, то есть дают **одинаковый сдвиг
результата**. Изоляция доказывает **связь** варьируемой переменной с исходом; она **не** доказывает,
что других переменных с тем же эффектом нет. Единственная переменная, которую я варьировал, была
не той, что варьировалась у автора чужого числа.

**Floor.**

- ✅ **Расхождение с ЧУЖИМ числом ⇒ первым делом сверить СОСТОЯНИЕ ВХОДА, а не процедуру.**
  Контрольные счётчики (сколько файлов в затронутых папках, есть ли каталог, снятый прошлой
  дельтой) стоят одну команду и отвечают на вопрос «мы вообще мерили одно и то же?».
  §A6 предписывает сверять **процедуру** — этого мало: процедуры могут совпасть, а входы разойтись.
- ⛔ **Подтверждённая изоляцией гипотеза — ещё не причина.** Спроси: «какие ЕЩЁ вмешательства дали
  бы тот же сдвиг?». Назвал хотя бы одно ⇒ эксперимент их не различает, и нужен **дискриминатор** —
  величина, чувствительная к одной гипотезе и нечувствительная к другой (здесь: число рёбер, `100`
  = отстающий вход, `99` = доведённый; набор `--move` менял его иначе — до `90`).
- ⚠ **Симптом-tell, ловится ДО объяснения:** в чужом-против-твоего расхождении фигурирует
  сущность, которой твоя правка **не касается вовсе**. Это признак разного ВХОДА, а не разной
  процедуры: своя правка не может добавить узел, которого она не трогала.
- ⛔ **Цена в этом классе выше обычной, потому что вывод обвиняет ПРЕДМЕТ:** «AC не достигнут»
  уезжает в durable-артефакт против уже принятого решения фаундера. Проверка входа стоит одну
  команду; отзыв ложного вердикта — цикл работы и потерянное доверие к замеру.

**Чем отличается от соседей.** §A25 — два **своих** измерителя, лечится воспроизведением обоих.
§A6 (`verify-before-assert`) — расхождение с чужим числом **в разы**, лечится сверкой процедуры.
Здесь расхождение **на единицу**, процедура воспроизведена честно, а разошлись **входы** — и
собственный эксперимент это скрыл, а не вскрыл.

**Empirical 2026-09-10** (лист F1.4 `b0ab4ec5`, программа `31c2bdee`): ложное объяснение прожило
~9 минут и успело попасть в durable-артефакт (правка RFC `fca39150` несла числа `9/11 → 8/10` и
утверждение «в четырёхчастном наборе остаётся `exoas-person`»). Снято тем же ходом после
дискриминатора «число рёбер», внесённого оркестратором; в RFC осталась запись о самой ловушке.

## §A34 — ЧУЖОЕ число ВЕРНО, расхождения НЕТ, а на ТВОЙ вопрос оно не отвечает: области ВЛОЖЕНЫ (`2026-09-10`)

Две половины класса уже описаны: **§A33** — floor ЧИТАТЕЛЯ, и он срабатывает на **расхождении**
(«мой замер разошёлся с чужим ⇒ сверь состояние входа»); **§ЗЕРКАЛО** — floor ПИШУЩЕГО («ярлык
области — часть числа»). Между ними остаётся третья позиция, и она **тише обеих**: ты читатель,
чужое число **верно**, твоё число ему **не противоречит** — просто спрашивали разное.

⛔ Механизм: область чужого замера **ВЛОЖЕНА** в твою (или наоборот), а не равна ей. Утверждение
«уникальных 0 против всего X» истинно и **ничего не обещает** про `X/<часть>`; читается же оно как
обещание, потому что предмет тот же и число выглядит исчерпывающим. Оба tell'а молчат по
построению: расхождения нет (§A33 нечего расследовать), ярлык области у автора может даже стоять
(§ЗЕРКАЛО исполнено) — и всё равно вывод неверен.

- ⚠ **Tell:** области не РАВНЫ, а **ВЛОЖЕНЫ** — «всё X» против «X/<часть>», «по всему репо» против
  «в этой папке», «за всё время» против «за окно». Вложенность и есть опасный случай: надмножество
  про часть ничего не обещало.
- ✅ **Floor:** переформулируй вопрос, который задаёт **ТВОЙ** приёмочный критерий, и сверь
  **ОБЛАСТИ** прежде, чем опереться на чужое число. Области разные ⇒ мерить самому; обычно это
  одна команда, а не работа.
- ⛤ Особенно у **ЭТАПА КОНВЕЙЕРА**: предшественник мерил под свой критерий, и его число попадает
  в постановку как факт. Твой критерий может спрашивать строже — тогда его число не «протухло»
  (§A33) и не «без ярлыка» (§ЗЕРКАЛО), оно просто **про другое множество**.

**Empirical 2026-09-10** (лист O4.3.2 `3341ba42`, программа `31c2bdee`): постановка несла замер
предшественника «67 UID в приёмниках, все присутствуют в `exoas-public`, уникальных **0**» — верный
против **всего** репозитория. Приёмочный критерий требовал **попапочного** `diff -r`
(`public/<ns>/` ↔ `exoas-<ns>/<ns>/`), и там уникальных оказалось **3**: `6627890d`, `ac54f2cc`,
`d3d00941` — файлы, которые лист F1.4 (коммит `6f58322`) увёл из `concept/` в `ims/`, тогда как
июньский снимок приёмника продолжал держать их под `concept/`. Расхождения с чужим числом при этом
**не возникло ни в одной точке** — оно и не могло: 67 против 3 не сравнимы, это разные множества.
Поймал только DoD, потребовавший попапочной сверки; без него три файла остались бы в
`exoas-concept` и после монтирования дали бы **два файла с одним `exo__Asset_uid`**.

## §A35 — ВОСПРОИЗВЕЛИСЬ ОБА измерителя ⇒ они мерят РАЗНЫЕ предикаты, и СУММЫ могут совпасть (`2026-09-10`)

§A25 разбирает «два твоих измерителя дали разные числа» и предписывает воспроизвести оба: расхождение
«чаще исчезает», а не воспроизвелось — писать «причина не установлена». ⛔ **Третий исход там не
перечислен, и он самый частый: воспроизводятся ОБА, и оба верны.** Тогда спор не о том, кто ошибся,
а о том, что числа отвечают на РАЗНЫЕ вопросы — расходятся КРИТЕРИЙ (популяция) и ОБЛАСТЬ.

- ✅ **Дискриминатор — СОСТАВ, а не сумма.** Выписать оба множества поимённо и предъявить разность.
  ⛔ «Пересчитать и взять большее» неверно в обе стороны: множества бывают **пересекающимися**, а не
  вложенными, и тогда ни одно не является ответом на вопрос другого.
- ⚠ **Ловушка-двойник, из-за которой сверка по сумме обманывает:** ОШИБОЧНЫЙ критерий может дать
  **то же число** при другом составе. Совпадение сумм читается как подтверждение гипотезы о причине
  расхождения — и закрывает разбор на неверном основании.
- ⛤ **Разводить с соседями ПО TELL'у, они не пересекаются:** не воспроизвелось ⇒ §A25 · расхождение
  есть, вход мог измениться ⇒ §A33 · расхождения НЕТ, области ВЛОЖЕНЫ ⇒ §A34 · разница «в разы, а не
  в процентах» ⇒ `adversarial-panel-before-foundational-rfc` §A6. Здесь: расхождение **есть**, оно
  **маленькое**, и оба числа **устояли** под пере-замером.
- ⛔ **Обе записи твои ⇒ внешнего арбитра нет** — потому и тянет «выбрать правильную». Выбор здесь
  всегда ошибка: правильны обе, неверен ВОПРОС, который им задан общим.

**Empirical 2026-09-10** (лист O4.3.3 `9867243c`): постановка называла расхождение «7 против 8» и
предлагала две гипотезы — «7 считалось до F1.4» и «7 считалось однчастным критерием». Оригинальный
`misplaced.py` на оригинальных CSV воспроизвёл **80 / public 7** дословно, свежий замер тем же
критерием дал **9**, а критерий узла — **8**. Устояли все три: 4 (CQ4 по четвёрке) · 9 (CQ4 по всему
`public`) · 8 (двухчастный по четвёрке); пересечение «7» и «8» — ровно **4** элемента, вложенности
нет. ⚠ Обе гипотезы постановки оказались ложными, причём вторая — **численно правдоподобной**:
критерий узла без ветви «пробел в имени» тоже даёт **7**, но с другим составом. Разрешило только
поимённое перечисление; сумма подтвердила бы неверную гипотезу.

## §A36 — ПРЕДИКАТ ПРОВЕРКИ `range` СРАВНИЛ КЛАСС ЦЕЛИ НА РАВЕНСТВО, не обойдя `exo__Class_superClass` (`2026-09-11`)

**Наблюдаемое.** Ты читаешь определение свойства (`exo__Property_range = <класс>`), берёшь `exo__Instance_class` цели, сравниваешь — не совпало — и объявляешь **нарушение range**. Вывод звучит прочнее любого прецедента («это не вкус, это нарушение схемы») и потому уходит в durable-артефакт как основание.

**Механизм.** `range` в этой онтологии — ограничение **по subsumption**, а не по равенству: цель законна, если её класс — сам range **ЛИБО любой его потомок** по цепочке `exo__Class_superClass`. Предикат без обхода цепочки врёт **в ОБЕ стороны**:

- **ложные нарушения** там, где subsumption работает (частый исход: доменные онтологии специализируют общие классы);
- **пропуск настоящих** там, где цепочку оборвали — а её рвут регулярно (`vault-asset-creation` §A1g: `superClass` перезаписали вместо append, и дефект **латентный**, гейтами не ловится).

⇒ у одного и того же дефектного предиката полярность ошибки зависит от данных, поэтому «проверю ещё раз внимательно» его не снимает.

**Почему tell'ы соседей молчат.** §A9 `migration-pre-verify-use-case` («доля дефектного неправдоподобно велика ⇒ сломан замер») ключуется на **неправдоподобии числа**; здесь число **правдоподобно** — 19 нарушителей на 1752 носителя, около процента, ровно столько, сколько ждёшь от накопленного долга. `shacl-validate-clear-cache` §маршрутизатор отвечает на «почему SHACL КРАСНЕЕТ», а здесь наблюдаемое обратное — **почему он МОЛЧИТ**, и молчание читается как слепота движка.

✅ **Floor — одна команда до вывода.** Проверять не равенство, а достижимость по цепочке:

```bash
# class-of-target -> ... -> range ?   (обход, а не сравнение)
U=<uid-цели>; R=<uid-из-Property_range>
C=$(find -L ~/vaults/vault-exodev -name "$U.md" | head -1)
while [ -n "$C" ]; do
  grep -m1 -A3 'exo__Class_superClass' "$C" | grep -oE '[0-9a-f]{8}-[0-9a-f-]{27}' | head -1
  C=$(find -L ~/vaults/vault-exodev -name "$(grep -m1 -A3 'exo__Class_superClass' "$C" | grep -oE '[0-9a-f]{8}-[0-9a-f-]{27}' | head -1).md" | head -1)
done
# любой шаг совпал с $R ⇒ range СОБЛЮДЁН, нарушения нет
```

⚠ **Симптом-tell, ловится ДО вывода:** ты пишешь «нарушение range» и **не называл** ни одного звена цепочки между классом цели и range. Назвать нечего ⇒ ты сравнивал, а не обходил.

⛔ **И зеркало на стороне вывода «нарушения нет»:** молчание SHACL про твою пару — это тоже claim. У него **два** объяснения (проверка прошла по subsumption / шейп до пары не дошёл), и различает их только чтение цепочки. Второй промах того же захода: искал упоминания свойства **только в ветке `violations`** JSON-отчёта и получил «0 упоминаний ⇒ шейп не работает»; полная выдача (`violations` + `warnings`) дала **94** записи `[vault-tbank]` и **85** `[vault-exodev]` — шейп работал, просто все его записи были `sh:Warning` вида `unresolvable-ref … not validated`.

**Empirical 2026-09-11** (узел F2.1 `0c4f19b9`, дедуп анкера `$ems/docs`). Оркестратор предъявил довод: `concept__Concept_genus` (`06d389ff`) имеет `range = dda12c48` (`concept__Concept`), цель `802030a1` несёт `ca92458c` (`exodoc__ClassConcept`) ⇒ «range нарушен, твоя правка его чинит». Довод выглядел прочнее исходного обоснования и был принят к записи в тело узла. Чтение цепочки: `ca92458c` → `52ecc2a9` (`exodoc__Concept`) → `dda12c48` — **подкласс**, range соблюдался, нарушения не было, SHACL молчал правильно. Мой встречный счёт «19 из 1752 носителей нарушают range» ложен **тем же** механизмом: мой предикат тоже сравнивал класс с range напрямую; все 19 указывали на `exodoc__ClassConcept` ⇒ нарушений **0**. Оба промаха — одна сессия, два независимых актора, один корень. Поймано только потому, что довод менял durable-артефакт и был перепроверен вместо принятия.
## §A37 — 2026-09-11 · ждун по ТРАНСКРИПТУ СУБАГЕНТА: floor §A28 на этом носителе НЕИСПОЛНИМ, а ждать не нужно вовсе

§A28 лечит ждун, чей шаблон поймал маркер **прогресса** вместо исхода, и его floor —
«проверять шаблон СВОИМ ЖЕ грепом по уже напечатанному хвосту». На одном носителе этот
floor неисполним **структурно**: ждун по `tasks/<agentId>.output` субагента, потому что
читать этот файл **запрещено** (полный JSONL-транскрипт, чтение переполняет контекст —
запрет напечатан в самом результате запуска `Agent`).

- ⚠ **Наблюдаемое:** монитор эмитит «FINISHED», а `ListAgents` показывает агента `running`.
  Предикат матчил `"type":"result"|"is_error"` — записи, которые в транскрипте появляются
  **по ходу** (ошибка внутри шага агента, промежуточный результат вложенного вызова).
- ⛔ **Подобрать шаблон точнее НЕЛЬЗЯ:** чтобы узнать, какой маркер терминален, надо
  прочитать транскрипт — то самое, что запрещено. Класс закрыт по построению.
- ✅ **Floor: ждуна по субагенту НЕ ставить вовсе.** Завершение приходит штатным
  уведомлением (`Agent` печатает это при запуске: «You will be notified automatically when
  it completes»), а до него — либо занимайся независимой работой, либо заверши ход:
  уведомление разбудит сессию. `ListAgents` даёт статус одним вызовом, если нужен именно он.
- ⚠ **Tell, ловится ДО написания предиката:** ты собираешься ждать артефакт, который
  инструмент запретил читать. Запрет на чтение и есть признак, что ждун здесь не строится.
- ⛤ **Цена ложного срабатывания здесь ВЫШЕ, чем у §A28:** там ждун молчит (событие пропущено),
  здесь он **утверждает завершение** работы, которая идёт — то есть производит ложный факт о
  ЧУЖОМ акторе, и следующий шаг строится на несуществующем результате. Поймано `ListAgents`;
  без него я бы писал отчёт по невернувшемуся ревью.

_Empirical 2026-09-11 (vteam-7358566c): Monitor с `until … grep -qE '"type":"result"|"is_error"'`
дал «REVIEWER-FINISHED» на 10-й минуте; агент в тот момент был `running` и завершился ещё
через ~4 мин. Урон 0 — проверил `ListAgents` прежде, чем действовать._

## §A38 — КВАНТОР («обе», «оба», «все») о множестве, НЕ названном поимённо: floor §ЗЕРКАЛО тут НЕИСПОЛНИМ, потому что области у квантора нет (`2026-09-12`)

**Наблюдаемое.** Пинг другому актору: «обе фактические зависимости цикла в реестре НЕ объявлены (CQ2: 0 при живой канарейке 1)». Замер под ним был **верен**, и в теле документа формулировка тоже была верна — там пара названа поимённо. Ложным оказался **пинг**: у цикла из трёх членов «обе» допускает два прочтения, и адресат взял второе.

- **Мой предмет:** `exocmd → public` и `public → lit` — оба фактические, оба **не** объявлены.
- **Прочтение адресата:** «обе стороны цикла» = `exocmd → public` и `lit → public`; второе **объявлено**, и его пере-замер дал расхождение.
- **Факт (сверен двумя акторами, точное сравнение имён, 77 резолвленных рёбер из 80):** из ЧЕТЫРЁХ рёбер цикла не объявлены **два** — `exocmd → public` и `public → lit`; `lit → public` и `public → exocmd` объявлены оба.

**Почему §ЗЕРКАЛО не срабатывает.** Оно лечит **число** ярлыком ОБЛАСТИ («54 дескриптора [клон]»). У квантора области нет вовсе — он не величина, а **утверждение об исчерпании множества**, и ошибается не в размере, а в ГРАНИЦЕ. Дописать к «обе» ярлык нечего.

⛔ **Цена выше, чем у неверного числа, по двум причинам.** Число адресат перепроверит арифметикой (§ТРЕТИЙ признак: две величины в одной выдаче не сходятся); квантор **не содержит величины**, сверять нечего — расхождение вскрывается только тем, что адресат независимо померил тот же предмет. И он **обвиняет чужую работу**: «обе не объявлены» читается как утверждение о состоянии реестра, то есть уезжает в durable-артефакт как факт о системе, а не как моя формулировка.

✅ **Floor: квантор заменяется СЧЁТОМ ИЗ ЦЕЛОГО плюс перечислением.** ⛔ Не «обе», а «**два ИЗ ЧЕТЫРЁХ**: `A` и `B`». Знаменатель здесь несущий наравне с именами: он сам предъявляет границу множества, о котором идёт речь, и делает утверждение фальсифицируемым без похода к предмету.

⚠ **Tell, ловится ПЕРЕД отправкой и не требует памяти:** во фразе стоит «обе / оба / все / ни один / каждый» + существительное во множественном, а элементы в той же фразе **не перечислены**. Перечислять дороже на пять слов; сверять потом — раунд у обоих акторов.

⛤ **Различать с соседями по симптому «моя формулировка соврала»:** §ЗЕРКАЛО — величина без области (лечится ярлыком) · §A34 — области ВЛОЖЕНЫ, оба числа истинны (лечится сверкой областей) · **§A38** — границы множества нет вовсе (лечится счётом из целого + именами). Общее у трёх: ни один не ловится перечитыванием, все три — только формой записи.

⛤ **Побочно: адресат был прав по своему прочтению и НЕправ по моему, и оба замера сошлись побайтно.** Когда сверка двух акторов даёт «мы согласны по методу, но не по вердикту» — сперва ищи квантор, а не дефект измерителя: у метода расхождения не было ни на одном ребре.

## §A39 — ТВОЙ ВЫВОД и ТВОЙ ЗАМЕР — ДВА разных артефакта: замер, снимающий вывод, вывод НЕ пересматривает (`2026-09-12`)

⛤ **Чем отличается от §Четвёртой формы ядра.** Там замер относится к СОСЕДНЕМУ механизму, и
дискриминатор «чем установлено?» отвечает ДА, усыпляя. Здесь замер относится **к твоему же
предмету** и **снимает твой вывод** — а вывод всё равно стоит, потому что он записан РАНЬШЕ и
пересматривается только вручную. То есть провенанс не утрачен, а **не потреблён**.

⛔ **Две формы наблюдались в ОДНОЙ сессии, у обеих корень один: «посчитано и напечатано ≠ учтено»**
(зеркало `self-satisfying-metric-weak-verifier` §A38, но потребитель — не ветка гварда, а **твоя
собственная проза**).

### Форма 1 — ДВА ПРЕДИКАТНЫХ МНОЖЕСТВА, два ИМЕНИ, и вычитание множеств между ними

Ты вычитаешь «объявленное» из «фактического» и печатаешь «N не объявлено». Оба множества сняты
верно. Ложна **операция**: множества построены по РАЗНЫМ предикатным наборам.

- ⚠ **Tell МЕХАНИЧЕСКИЙ, и он дешевле проверки подлежащих: у тебя ДВА ИМЕНИ предикатного набора** —
  одно у измерителя (константа в его коде: `TIER_DEFS`, `--tier defs`, список предикатов), другое
  у потребителя (имя предиката/функции в коде движка, которое ты только что грепал). **Имена
  разные ⇒ множества разные ⇒ вычитание не имеет смысла**, и это видно БЕЗ всякого замера.
- ⛔ Чтения кода потребителя **НЕДОСТАТОЧНО**: я прочитал `AssetSpaceDependsOn.ts`, понял, что это
  замыкание МОНТИРОВАНИЯ, и всё равно написал «58 не объявлено» по ярусу ОПРЕДЕЛЕНИЙ. Понимание
  потребителя не переносится на арифметику автоматически — переносить надо ЯВНО.
- ✅ **Floor: перед первым вычитанием назвать ОБА набора вслух и одной строкой** («меряю
  `TIER_DEFS`; потребитель читает замыкание монтирования»). Совпали ⇒ вычитай. Не совпали ⇒
  **вторая величина обязательна**, и ответ — ПОЛОСА, а не число.
- ⛤ **Полоса и есть продукт:** 58 (ярус определений) … 115 (+биндинги/ABox) … 246 (все
  кросс-ассетспейсные IRI). Одно число из полосы, поданное как «сколько не объявлено», ложно при
  любом выборе — ложен не выбор, а умолчание об оси.
- ⚠ **Канарейка измерителя обязательна, когда чисел несколько:** более широкий набор обязан
  воспроизвести эталон узкого (у меня `defs` → 112 рёбер / 2 цикла / 6 узлов). Не воспроизвёл ⇒
  числа сняты РАЗНЫМИ инструментами и несопоставимы (`self-satisfying-metric-weak-verifier` §A42).

### Форма 2 — ты ПРОГНАЛ симуляцию, снимающую твой вывод, и не сопоставил её с выводом

Я записал «требования DoD взаимоисключающи», обосновав симуляцией D (4 цикла). В том же прогоне
стояла симуляция E, дающая **2 цикла** и снимающая вывод целиком: лишние два цикла порождались
объявлениями БЕЗ факта, а не фактами. E была **исполнена и напечатана** — и не прочитана против
собственного вывода.

- ⚠ **Tell при написании вывода:** в выводе слово «взаимоисключающи» / «недостижимо» / «невозможно»,
  а в твоей же выдаче ≥2 варианта, различающиеся НЕ предметом, а **набором** включённых элементов.
  Сравни вывод с КАЖДОЙ строкой своей таблицы, а не с той, что его подтверждает.
- ✅ **Floor: вывод формулировать ПОСЛЕДНИМ действием**, перечитав свою таблицу целиком; и писать
  «недостижимо **при условии** X», где X — конкретная строка таблицы. Безусловное «недостижимо»
  требует, чтобы КАЖДАЯ строка его подтверждала.
- ⛤ **Верная формулировка обычно рядом и слабее:** не «требования конфликтуют», а «критерий написан
  под состояние, которого нет» — второе проверяемо и не обвиняет постановку.
- ⛔ **Цена асимметрична:** «взаимоисключающи» есть утверждение о **постановке задачи**, то есть о
  чужой работе, и оно уезжает в durable как основание сузить область. Ошибка в сторону «конфликт
  требований» дороже ошибки в сторону «я ещё не всё померил».

**Empirical 2026-09-12** (узел `F4.4` `1a1b20e3`, программа O4/F4): обе формы — в одной сессии, обе
поймал **оркестратор**, не я. Форма 1 стоила бы durable-записи «58 фактических рёбер не объявлено»
как факта о предикате, который движок не читает; фактическая полоса — 58/115/246, а по
mount-семантике граф вообще один SCC из 37 ассетспейсов и DAG'ом быть не может по построению.
Форма 2 стоила бы записи «DoD взаимоисключающи» при том, что снимающая её симуляция была прогнана
на десять минут раньше.

## §A40 `2026-09-12` — ярлык области В ПРОЗЕ не доезжает до следующего сравнения: если число производит инструмент, который правят параллельно, редакция измерителя обязана быть ПОЛЕМ записи

§ЗЕРКАЛО ядра требует ярлык области как часть числа («54 дескриптора [клон]»). У этого floor'а есть
предел: ярлык в ПРОЗЕ отчёта живёт один раунд. Число уезжает в базовую линию, в тело листа, в
следующий замер — а ярлык остаётся в пинге, и следующее сравнение идёт уже без него.

⛔ **Механизм.** Измеритель (хук retrieval) правился соседним листом ПОКА гейт описаний мерил на нём
распределение срабатываний. Первый замер — популяция 180, запас 0 у четырёх роутеров; финальный — 175
и у трёх, при редакции хука 059834668a20. Оба числа верны, оба — факт о СВОЕЙ редакции измерителя, и
линия, хранящая только числа, сравнила бы их как один ряд.

✅ **Floor.** Редакция измерителя — ПОЛЕ структуры, где хранится число (`hook: <sha12>` в каждой
записи базовой линии), печатается и в итоге отчёта, и в одиночном вердикте; классификатор дрейфа
называет смену редакции ОТДЕЛЬНЫМ исходом («РЕДАКЦИЯ ХУКА изменилась X → Y — числа померены разными
измерителями»), а не смешивает с дрейфом данных. Это производное от механизма
(`decision-surface-must-derive-from-mechanism` §выход №1), а не подпись рядом с числом.

⚠ **Tell:** два твоих замера одного предмета расходятся (§A25/§A35), а в записях нет поля, по
которому видно, ЧЕМ каждый померен. Второй tell — pre-spawn precheck сверял только множества
ЗАПИСИ: инструмент-измеритель попал в множество ЧТЕНИЯ соседа, и параллельность стала дрейфом
измерителя (`pre-spawn-parallel-session-conflict-check`).

⛔ **Границы.** Поле доказывает НАЛИЧИЕ ярлыка, а не СОВПАДЕНИЕ с линией: ось «редакция называется»
зелена и когда линия молча сравнивается с другой редакцией — нужна отдельная ось на РАСХОЖДЕНИЕ
(мутант «редакция не совпала с линией», а не «редакция не напечатана»).

_Empirical 2026-09-12 (лист 2a.6 пилота «правила из графа по симптому», `rules-graph-pilot-desc-gate`):
поле `hook` заведено в базовой линии `rules-router-desc-baseline.json`, заперто осью A19 гейта, осью
C5 корпус-харнесса и мутантом «редакция не называется»; ревью назвало ось на совпадение с линией
отсутствующей — вписано в AC листа 2a.7._

## §A41 `2026-09-12` — «минимальный набор», посчитанный ЖАДНО, есть артефакт ПОРЯДКА обхода, а не свойство данных

**Наблюдаемое.** Надо назвать, сколько элементов добавить, чтобы инвариант закрылся (объявления покрытия, набор правок, список зависимостей). Считаешь жадным проходом — «беру кандидата, если он ещё не покрыт текущим состоянием» — и получаешь число, которое читается как СВОЙСТВО ЗАДАЧИ («минимально нужно 24»).

⛔ **Оно им не является.** Порядок перебора кандидатов — обычно `sorted()`, то есть алфавит — **меняет ответ**. Замер 2026-09-12 (лист L1, 26 кандидатов): 20 случайных перестановок дали размеры **21 … 26**. Сортированный проход дал **24**, и это число ушло бы в приёмочный критерий как «дельта базиса».

✅ **Floor — разбиение ПО ОПРЕДЕЛЕНИЮ, без порядка:** кандидат **избыточен ⟺ он покрыт, когда добавлены ВСЕ остальные кандидаты** (`e избыточен ⟺ цель(e) ∈ замыкание(D ∪ (U \ {e}))`). Тогда «обязательных» — инвариант данных (у меня **20**), а не твоего цикла.

⛔ **Множество избыточных проверять ЦЕЛИКОМ, а не поэлементно:** по одному они избыточны, а вместе могут оказаться нужны (взаимное покрытие). Предъявлять прогоном: «добавлены только обязательные ⇒ непокрытых 0».

⚠ **Tell, ловится ДО отчёта:** собираешься написать «минимально нужно N», а в коде, который это N произвёл, стоит `for e in sorted(...)`. Сортировка тут — произвольный выбор, выданный за канон.

⛤ **Чем отличается от соседей по правилу:** у §A1-§A40 **источник соврал** (греп, парсер, индикатор, компакт-саммари). Здесь источник **честен** — ложна **устойчивость** его ответа: та же программа на тех же данных даёт другое число от перестановки входа. Метки происхождения у такого числа нет ПО ПОСТРОЕНИЮ, и дальше оно потребляется как замер.

⛤ **Проверка стоит один прогон:** тот же жадный проход на 10-20 случайных перестановках. Единственное значение ⇒ порядок не решает, число можно называть. Разброс ⇒ жадный ответ критерием быть не может, считай по определению.

⚠ **И не пиши рядом ярлык, которого не проверял:** мой же скрипт печатал «(единственное значение ⇒ разбиение порядко-НЕзависимо)» РЯДОМ со списком `[21, 22, 23, 24, 25, 26]` — подпись утверждала обратное данным в той же строке.

## §A42 `2026-09-12` — число и «потому что» написаны ДО команды: под конец длинного окна объяснения производятся быстрее, чем проверяются, и ловит их только арифметика

Форма №1 ядра — число в прозе без источника. Эта строка — о РЕЖИМЕ, в котором форма №1
рецидивирует пачкой: 25-й час окна оркестратора, четыре компакции, исполнение своими руками
после гибели ребёнка. За десять минут два самоопровержения подряд, оба в пингах верхнему.

⛔ **Механизм.** Пинг составляется ДО того, как команда, устанавливающая число, исполнена: sha12
трёх файлов написаны «как должны быть» (два из трёх не совпали с `shasum` через 20 с), а затем
объяснение совпадения третьего («коммит не менял этот файл») выдумано — `git show --stat`
показал 11 изменённых строк; истинный механизм был другим (снимок заморозки снят с правками уже
в дереве). Оба утверждения выглядели как факты, оба ушли адресату, и адресат переписал их в свой
журнал как факты — то есть чужая подпись потреблена без проверки на втором конце тоже.

✅ **Floor.** Любое число и любое «потому что» в пинге — только ПОСЛЕ команды, в том же вызове,
и текст пишется по её выводу, не до него. Адресат, переписывая число из пинга в durable, метит
его «из пинга, не замерено» либо перемеряет сам. «Буду внимательнее» как митигация опровергнуто
исполнением: оба самоопровержения поймала вторая величина в той же выдаче (§Третий признак), ни
одно — внимательность.

⚠ **Tell (для автора):** окно длиннее суток, и ты пишешь число, не видя его в выводе перед
глазами. **Tell (для адресата):** пинг с числами пришёл быстрее, чем могла отработать команда,
их устанавливающая; либо два пинга подряд с коррекцией.

⛤ Соседи: §A26 (компакт-саммари стрипует провенанс) · `decision-surface-must-derive-from-mechanism`
§ЗЕРКАЛО (вторая часть чужой находки — тоже claim; здесь адресат — потребитель) ·
`self-satisfying-metric-weak-verifier` §A21 (число из старого замера, под которое подгоняется бюджет).

_Empirical 2026-09-12 (оркестратор фазы 2 пилота, окно 25 ч 31 мин): пинги 22:51:26 и 22:51:46
(sha12: было baseline 3a3ca8c4b0b5 / sweep 8c6b9de4c5b6 — сочинено; факт c70ef6c9220c / 6d4db5aae09c),
22:52:08 (объяснение снято `git show --stat`). Верхний переписал оба в ретро как факты и исправил
показом. Основание для замены контекста оркестратора: «свойство режима, не оговорка»._
