---
exo__Asset_uid: c9e78f52-87e5-4046-b82d-e9dd4d5be228
exo__Asset_createdAt: 2026-08-11T22:03:41
exo__Asset_updatedAt: 2026-09-02T01:36:26
exo__Instance_class:
  - "[[39a39239-2a97-483a-ba91-fb01bf5c85f3]]"
exo__Asset_createdBy: "[[4ef3962d-b8a7-42b5-bd28-88ec846f1d13]]"
exo__Asset_label: "aggregate-hides-dead-stream: 2 разборов §A1-§A2 (справочник)"
aliases:
  - "aggregate-hides-dead-stream: 2 разборов §A1-§A2 (справочник)"
exo__Asset_isDefinedBy: "[[2ce77ac1-3f78-4f64-9dc1-41ea01da1ef0]]"
inbox__ExoAssistantKnowledge_decay: "[[06a5b9f9-da93-4234-883f-3f0cf65b2fba]]"
inbox__ExoAssistantKnowledge_confidence: "[[227def30-f56f-4f5f-936d-f6a81ad73c96]]"
---


# Разборы случаев — `aggregate-hides-dead-stream`

⛤ Вынесено из `~/.claude/rules/aggregate-hides-dead-stream.md` (диета always-on корпуса). Это **справочник**:
его читают, когда симптом УЖЕ опознан по индексу в самом правиле, а не до того.
Оперативная часть (принцип · floor · запреты · когда срабатывает) осталась в правиле.

## Переход: дата → §A

⛤ Внешние указатели ссылаются сюда **датой** (`§Addendum 2026-08-04`).
Дата не уникальна — при нескольких §A выбирать по теме из индекса правила.

| дата | §A |
|---|---|
| `2026-08-06` | §A1 |
| `2026-08-07/08` | §A1 (подсекция §A1b) |
| `2026-08-11` | §A2 |

## Разборы

## §A1 ⛔ Addendum 2026-08-06 — ТРЕТИЙ способ спрятать поток: не усечение, а ПОБЕДИТЕЛЬ БЕЗ ОБРАТНОЙ СВЯЗИ; отсев ничего не меняет, и это читается как «источник иссяк»

Тело правила разбирает две формы (поток гаснет молча · поток срезается общим лимитом).
Третья — там, где агрегат сводит потоки не суммой, а **выбором одного**: ранжирование,
«взять максимум», «самый весомый кластер». Такой отбор **детерминирован**: те же входы →
тот же победитель. Если вход меняется медленно, победитель не меняется **никогда** — а
44 проигравших не «отстают на чуть-чуть», они структурно недостижимы.

⛤ **Отличие от усечения — в том, где искать дефект.** При усечении виноват потолок, и
чинится он квотой. Здесь потолка нет вовсе, все входы живы, разбивка по источникам
здоровая — и всё равно на выходе один и тот же сюжет. Дефект не в отборе, а **в
отсутствии контура**: вердикт о выбранном (отсеян / реализован / не подошёл) никуда не
возвращается, поэтому следующий заход повторяет предыдущий дословно.

- ⛔ **Симптом маскируется под пустоту источника.** Наблюдаемое — «механизм раз за разом
  ничего не приносит», и первая гипотеза всегда про вход: коллектор беден, канал умер,
  гейт душит. Все три можно честно проверить и получить «здоров», не приблизившись к
  причине. У меня так и вышло: разбивка 7 источников ровная, гейт открыт, дедуп прав
  каждый раз — и ноль результата.
- ✅ **Floor: печатать не победителя, а ВЕРХ СПИСКА.** Одна распечатка `top-N` с весами
  отвечает сразу на два вопроса: насколько отрыв лидера (у меня — **15 %**, то есть
  выбор почти произволен) и что именно задавлено. Пока печатается только победитель,
  «45 кластеров» и «1 кластер» выглядят одинаково.
- ✅ **Вердикт обязан возвращаться в отбор** — карантин выбранного ключа на срок, за
  который решение может устареть. ⛤ Карантин, а не вечное исключение: тема, отсеянная
  как «уже заведено», через две недели может стать актуальной снова.
- ⛔ **Карантин обязан быть fail-open.** Съел все кластеры → отбор идёт как раньше, и это
  **громко** логируется. Иначе механизм встанет навсегда по причине, которую нечем
  увидеть — ровно тот класс, что тело правила и описывает.
- ⚠ **Tell, отличающий эту форму от двух предыдущих:** входы разнообразны и здоровы, а
  выход **дословно повторяется** от прогона к прогону. Если счётчик в заголовке темы
  ползёт («×113 → ×114 → ×116 → ×131»), а сама тема та же — это он.
- ⛤ **Проверять ось надо мутантом, а не зелёным прогоном.** «Победитель сменился при
  непустом карантине» зеленеет и на сломанном фильтре, если карантин пуст. Снять фильтр
  в копии → ось обязана покраснеть (`integration-test-revert-verify`).
⛤ **Подтверждение фикса + чего он НЕ чинит (2026-08-06, следующий прогон).** Карантин
отработал буквально: `deduped-keys.jsonl` = одна запись (ключ «отказ», поставлена прогоном
14:18), и прогон 17:39 выбрал **другой** ключ («правило»). ⛔ Но исход снова `deduped` — то
есть карантин лечит **повторение**, а не **долю уже-owned тем**: `runs.jsonl` = 3 прогона,
3 из 3 `deduped`, кандидатов не произведено ни одного.

- ⛤ **Гипотеза о причине — не коллектор, а СОСТАВ потока.** Сбор здоров и разнообразен
  (218 сигналов, 7/7 источников), поэтому рамка «петля меряет собственный выхлоп» на слое
  сбора **неверна**. Но **63 % потока самореферентны** (retrospective 60 + grooming 42 +
  ctx-metric 35 = 137/218), а у всего этого пространства **один владелец** — проект M4
  `47bdaa66` (Backlog, 5 открытых задач, среди них дословно «Измерить ценность корпуса по
  ctx-метрикам»). Лексический отбор над потоком, где большинство — про сам корпус, попадает
  на территорию M4 **по построению**, и дедуп его корректно убивает.
- ✅ **Дешёвый floor воркеру (стоит ~4 вызова и терминален):** тема про **метрику корпуса**
  (счётчик рецидивов, эффективность правила, покрытие хуками, размер корпуса) → сперва
  прочитать детей M4 `47bdaa66`, а не искать по словам темы. В прогоне 17:39 дедуп закрылся
  именно так: тема «bash-loop-eval-pitfalls: 101 повтор» ⇒ задача «Измерить ценность корпуса
  по ctx-метрикам» (Backlog) + инструмент `/grabli-audit` (построен 30.07) + топ-находка того
  же замера уже отгружена хуком `read-before-edit-guard.sh`.
- ⚠ Правка **селектора** (снизить вес самореферентных источников / проверять принадлежность
  владельцу) — **гипотеза на 3 точках**, не предписание (`pilot-tool-before-broad-rollout`).

### ⛔ Четвёртая точка (2026-08-07) — ВТОРОЙ подвид того же исхода: источник эмитит выход стадии, КОТОРУЮ ПЕТЛЯ САМА КОРМИТ ⇒ каждый победитель уже прошёл гейт фаундера ПО ПОСТРОЕНИЮ

Абзацы выше объясняют `deduped` **самореферентностью потока** (сигналы ПРО сам корпус → территория
M4) и дают floor «прочитай детей M4». Четвёртый прогон подряд дал тот же исход, но **этот floor к
нему неприменим**: механизм другой.

- ⛤ **Топологическая ошибка, а не шум.** Ключ `proposals_open_direction` — это выдача **PO-груминга**,
  то есть **нижестоящей** стадии конвейера: разведка предлагает RFC → фаундер решает → PO ранжирует
  **решённые**. Возвращая эту выдачу на вход разведки, петля **ест собственный downstream** ⇒ любой
  её победитель — артефакт с **уже пройденным** гейтом. Кандидатов из такого ключа не бывает не
  «часто», а **никогда**.
- ⚠ **Отпечаток такого источника виден в самом сигнале:** поля несут **UID существующих артефактов**
  (`{"uid":"7c7859d1", … "would_create":"ems__Project", "carried_from":"пилот"}`). Разведочный сигнал
  указывает на **симптом**; сигнал груминга — на **уже заведённую запись**.
- ✅ **Floor строго дешевле «прочитать детей M4»: сигнал несёт UID → дедуп решается ОДНИМ запросом —
  прочитай СТАТУС этого артефакта.** Семантическое суждение не нужно вовсе: `Approved`/`Deferred`/
  `Rejected` = гейт пройден = `deduped`, точка. Один запрос против четырёх.
- ⛤ **Селекторный предикат перестаёт быть эвристикой.** «Снизить вес самореферентных источников» —
  нечётко и требует пилота; «отбросить сигнал, чей UID указывает на артефакт вне `Proposed`» —
  **механически проверяемо**. Это по-прежнему решение фаундера (правка кода петли), но теперь у него
  есть точная форма, а не направление.

#### ⛔ Коррекция 2026-08-08 — предложенный выше floor «отбросить сигнал, чей UID вне `Proposed`» НЕИСПОЛНИМ: UID живёт в ДАЙДЖЕСТЕ, а не в сигнале. Различимое отбору — ИСТОЧНИК. И карантин КЛЮЧЕЙ по построению не лечит источник, эмитящий НЕСКОЛЬКО ключей

Показываю коррекцию, а не переписываю молча (`decision-surface-must-derive-from-mechanism` §форма №6): что именно звучало правдоподобно — ценнее готового ответа.

- ⛔ **Ложная посылка:** «⚠ поля несут UID существующих артефактов». Замер схемы `dlp-signal/v1` (218 записей): `schema · id · source · source_uid · collected_at · observed_at · title · body · evidence_ref · evidence_kind · weight · sensitive` — **UID артефакта отсутствует**. `source_uid` у всех grooming-сигналов ОДИН (`a7c55cdc` — identity дайджеста), `evidence_ref` = `…/po-digest-*.json#proposals_open_direction`. UID-ы лежат **внутри** дайджеста и видны ВОРКЕРУ; отбор их не видит. Я описал то, что читал воркер, и приписал это записи сигнала.
- ⛔ **Второй, более дорогой промах — уровень лечения.** Карантин ключей снимает **один** ключ за прогон. Если источник эмитит **несколько** ключей одной природы, отбор берёт сиблинга и отсеивается по той же причине. Замер: `grooming` даёт **ровно два** ключа — `proposals_open_direction` (20) и **`alerts` (22)**, оба суть имена секций дайджеста. Первый в карантине с 07.08; второй — **тяжелейший кластер во всём потоке** (вес 9.9 против 8.0 у ближайшего) ⇒ прогон №5 взял бы его и вернул пятый `deduped`. Карантин лечит симптом по одному, источник — болезнь.
- ✅ **Рабочий floor: отсев ПО ИСТОЧНИКУ, до кластеризации** (`DLP_EXCLUDE_SOURCES`, по умолчанию `grooming`), с тем же fail-open, что у карантина, и с печатью ЧИСЛА отсеянного. Список намеренно узкий — только измеренный случай; самореферентность `retrospective`/`ctx-metric` остаётся гипотезой на трёх точках (`pilot-tool-before-broad-rollout`), и расширение — правка ENV, а не кода.
- ⛤ **Мутант вскрыл то, чего не показывал ни один зелёный прогон: СЧЁТЧИК и ЭФФЕКТ ломаются независимо.** Сняв применение фильтра к строкам (`if not source_exhausted: rows=_kept`), получаешь выбор `alerts` **при `source_excluded=42`** — счётчик рапортует отсев, которого не было. ⇒ ось «сколько отсеяно» **не заменяет** ось «какой ключ выбран»; отчитываться одним счётчиком = тождественно-зелёный верификатор.
- ⚠ **Тель, который сработал бы раньше:** карантин снял ключ, а следующий выбор пришёл из **того же источника**. Если два подряд отсеянных ключа делят `source` — чинить надо источник, а не заводить третий карантин.

**Проверено** `~/.claude/bin/dlp-source-filter-verify.py` — 14 осей, блок отбора **извлекается из прода** и исполняется на РЕАЛЬНОМ потоке (218 сигналов), два семантических мутанта прода (снят эффект фильтра → краснеет ось ключа; обнулён счётчик → краснеет только счётная ось, ось ключа зелена ⇒ оси независимы), fail-open предъявлен исполнением.

**Empirical 2026-08-07** (`dlp-59741`, 4-й `deduped` подряд): кластер `proposals_open_direction` = 3
сигнала, все с UID RFC. `7c7859d1` — **Approved 03.08** (проект `38800c80` в `Doing`, под ним W5/W7/W8
уже `Backlog`-листья, т.е. видимы петле ПОСТАВКИ); `f2eccdd8` — **Approved** (проект `17c173dd`);
`6dfe2345` — **Deferred**. ⇒ 3/3 за гейтом, кандидат невозможен. Побочно (тот же класс, но
`graph-record-outlives-its-referent`): задача W8 в `Backlog` называет W8a «тривиально», тогда как
её механизм — блокирующий `PreToolUse`-хук `dogfood-cli-mutation.sh` — **живёт и срабатывает**
(дважды за этот же прогон). Верификация стоила 3 SPARQL + 2 чтения; карантин ключа тик ставит сам
(`dlp-tick.sh`, функция `quarantine_key()` — вызовы `quarantine_key … "worker-deduped"` и
`quarantine_key "$topic_key" "lexical-$dedup_out"`).

## §A2 ⛔ Addendum 2026-08-11 — ЧЕТВЁРТАЯ форма: агрегат, который гвард печатает ПРО СЕБЯ («известный долг: N»). Лечится ХРАПОВИКОМ по ИМЕНАМ, и его базовая линия обязана быть fail-loud В ОБЕ стороны

Три формы выше — про агрегат **над чужими потоками**. Четвёртая: гвард честно рапортует **собственную** неполноту — «N объектов проверить не смог / известный долг N». Число при этом стабильно и потому читается как «ничего не изменилось», хотя **состав** долга сменился: один объект починили, другой сломался. Регрессия охвата проходит молча, и её не видит **ни один** существующий гейт — гвард-то зелёный, он же сам про свой долг и написал.

- ⛤ **Лечение — не «печатать состав» (это диагностика), а СДЕЛАТЬ СОСТАВ ВЕРДИКТОМ.** Базовая линия = **множество имён**, зашитое в гвард; новое имя вне линии ⇒ **rc=1**. Пока состав только печатается, регрессия остаётся наблюдаемой, но не блокирующей — то есть ловится вниманием, а не механизмом.
- ⛔ **Сама базовая линия обязана быть fail-loud В ОБЕ стороны, и второе направление контринтуитивно:**

  | расхождение | что произошло | почему fail-loud |
  |---|---|---|
  | имя из линии **исчезло из корпуса** | объект переименован/удалён | линия описывает то, чего нет ⇒ её охват тихо сжался |
  | имя в линии, а **долг погашен** | объект починили и линию не сняли | ⛔ повторная поломка ТОГО ЖЕ объекта пройдёт как «известная» |

  Второе — **гейт, краснеющий на УЛУЧШЕНИИ**, и это правильно: непокрытая починка ослабляет храповик молча. ⚠ Оба расхождения — про **устаревание самого гварда**, а не про предмет ⇒ отдельный код возврата (`rc=2` / `BROKEN`), не `rc=1`; схлопнув их в «провал», получишь ложное обвинение корпуса (`self-satisfying-metric-weak-verifier` §«Три вердикта вместо двух» — там BROKEN схлопывают в `ok`, здесь зеркально соблазн схлопнуть в `fail`).
- ⛤ **Линия описывает КОНКРЕТНЫЙ корпус ⇒ применение к другому обязано быть загейчено И НАЗВАНО.** Прогон по фикстуре/подмножеству/под флагом сужения не должен судить чужой состав — но **молчаливое** отключение храповика неотличимо от «долга нет». Печатать статус явно (`храповик: выключен — корпус не тот`), иначе фикстурный прогон читается как чистый.
- ⚠ **Симптом-tell, ловится при написании гварда:** ты выводишь «известный долг: N» / «пропущено: N» / «не проверено: N». Спроси: **различит ли это число «те же N» и «один починен, другой сломан»?** Нет ⇒ у тебя агрегат про самого себя, и он врёт ровно там, где гвард обязан быть строже всего.
- ⛤ **Тестируемость требует инъекции в ДАННЫЕ, а не в корпус** — см. `integration-test-revert-verify` §Addendum 2026-08-11 (храповик, ключёванный к реальному корпусу).

⛤ Ссылки на правила корпуса записаны backtick'ами — они не vault-ассеты (`set-body` отвергает вики-скобки на не-ассеты).

## §A3 ФОРМА сигнала (монотонный счётчик) — карантин ключей отработал, а дедуп всё равно

⛤ **ТРЕТИЙ дискриминатор — ФОРМА сигнала (монотонный счётчик), и он нужен там, где первые два
бессильны ПО ПОСТРОЕНИЮ.** §A1a карантинит КЛЮЧ, §A1b отсеивает ИСТОЧНИК. Сигнал-счётчик уходит
от обоих: он **растёт**, поэтому каждый прогон предъявляет НОВЫЙ ключ (карантин мимо) из ТОГО ЖЕ
источника, отсечь который §A1c прямо запрещает (убьёт приток целиком).

- ⛔ **Такая тема дедуп-терминальна ДО отбора, а не по итогам проверки.** Счётчик рецидивов
  существует ровно потому, что корпус УЖЕ вынес по этой привычке вердикт («terminal
  accept-baseline» — хук не ужесточается) либо поставил вердикт в очередь под именованным
  владельцем. То есть отбор голосует за темы, чьё «да/нет» уже отвечено.
- ⚠ **Симптом-tell в самом заголовке темы:** в нём стоит РАСТУЩЕЕ ЧИСЛО («18 повторов»,
  «отказ инструмента ×131 за 7 сут»). Ключ у такой темы новый каждый раз, предмет — прежний.
- ✅ **Floor: требовать от счётчика не нового КЛЮЧА, а нового СОДЕРЖАНИЯ** — изменился ли вердикт
  владеющего правила/задачи с прошлого отбора (правило перестало быть accept-baseline · задача
  ушла из Backlog). Не изменился ⇒ пропускать ДО кластеризации, fail-open, с печатью числа
  пропущенного. ⛔ Это НЕ отсев источника: ретроспективный источник продолжает поставлять свои
  НЕ-счётчиковые сигналы.
- **Замер 2026-08-15** (8 прогонов DLP по каталогу `state/dlp/prompt-*.md`): **4 из 8** тем —
  счётчики (`отказ инструмента ×116` 08-03 · `×131` 08-06 — ОДНА И ТА ЖЕ метрика с бо́льшим
  числом · `bash-loop-eval-pitfalls: 101 повторов` 08-06 · `dogfood-vault-search: 18 повторов`
  08-15). У обоих правил в теле дословно стоит «terminal accept-baseline»; у метрики отказов
  владелец назван в корпусе (`ems__Task b20e6c2f` под M4 `47bdaa66`). Половина ёмкости петли
  ушла на темы, чей исход был предрешён до отбора.
- ⛤ **ПРЕДСКАЗАНИЕ ПОДТВЕРДИЛОСЬ через 8 часов после записи этого floor'а** (прогон 9, `dlp-32007`,
  2026-08-15 17:38): тема «`prefer-codegraph-for-structural-audits`: 24 повторов», ключ **`prefer`** —
  снова НОВЫЙ, поэтому карантин (а в нём уже лежат `отказ` · `правило` · `vault`) прошёл мимо ровно так,
  как сказано выше. Итог: **5 из 9** тем — счётчики, **9 `deduped` подряд**, `disc__Candidate` = **0**
  за всё время (канарейка `#RFC` = 308 тем же запросом ⇒ ноль настоящий, а не мёртвый запрос).
  ⛔ Отсюда то, чего замер выше не говорит: **записанный floor поведения НЕ меняет** — он живёт в
  правиле, а отбор делает `~/.claude/bin/dlp-tick.sh` (9 упоминаний карантина). Пока дискриминатор
  «новое СОДЕРЖАНИЕ, а не новый КЛЮЧ» не перенесён в СЕЛЕКТОР, цена начисляется каждым прогоном —
  то есть это `declaration-and-executable-carrier` в чистом виде: декларация обновлена, исполняемый
  носитель — нет.
- ⛔ **УТОЧНЕНИЕ МЕХАНИЗМА (прогон 10, `dlp-85899`, 2026-08-15 23:56) — ключ ротирует НЕ рост числа,
  а САМ КАРАНТИН, и это разворачивает направление фикса.** Формулировка выше («он **растёт**, поэтому
  каждый прогон предъявляет НОВЫЙ ключ») читается как «виноват счётчик». Замер парой `topic-*.json`
  по ОДНОМУ И ТОМУ ЖЕ файлу правила: прогон `92128` (08-06) — заголовок «правило
  «bash-loop-eval-pitfalls»: **101** повторов», ключ `правило` → отсужен `worker-deduped` и уложен в
  карантин; прогон `85899` (08-15) — заголовок «…: **121** повторов», ключ **`bash`**. Слова
  заголовка НЕ менялись — изменилось лишь то, что первое слово уже занято карантином, и селектор
  взял СЛЕДУЮЩЕЕ слово того же заголовка.
- ⛔ **Следствие, обратное интуиции: ужесточать карантин (больше ключей, длиннее срок) — ВРЕДНО.**
  Он не «проходит мимо» по слабости — он **работает** и потому мигрирует ключ: антидубль-механизм
  САМ производит видимость новизны, и каждый дедуп делает следующую встречу того же предмета
  ТРУДНЕЕ ловимой, монотонно. ⇒ карантинить надо **идентичность предмета** (источник + нормализованный
  заголовок), а не слово из него.
- ⚠ **Симптом-tell по самому ключу, до всякой проверки:** ключ прогона — служебное слово заголовка
  (`bash`, `правило`, `vault`), а не имя предмета. Значит выбран не предмет, а то, что осталось
  от заголовка после карантина.
- ⛔ **КОМПАУНД (прогон 14, `dlp-46727`, 2026-08-16 13:17): ротация гонит ключ к СЛУЖЕБНЫМ СЛОВАМ,
  а служебное слово — худший возможный ключ КЛАСТЕРИЗАЦИИ.** Симптом-tell выше называет служебное
  слово *признаком* промаха; недостающая половина — что по этому же ключу собирается САМ КЛАСТЕР,
  то есть у дефекта есть второй, усиливающий конец. Замер: ключ `между` — **предлог**, и по нему
  в потоке нашлось **6 сигналов из 4 источников**, попарно не связанных ничем (RFC мета-ассистента ·
  RFC про отпуска · расхождение семантики `displayName` · потеря задачи между пометкой и спавном ·
  вижн-док · дрейф SHACL-базлайна). Кластер «весит» больше, чем в нём предмета, и его `score`
  выигрывает отбор у настоящих тем.
  - ⛤ **Дефекты УМНОЖАЮТСЯ, а не складываются:** каждый дедуп сдвигает ключ на слово вправо, а чем
    правее слово в заголовке, тем выше шанс, что оно служебное ⇒ «новизна» ключа и «вес» кластера
    растут ВМЕСТЕ. Карантин не просто перестаёт защищать — он ухудшает ОТБОР, монотонно.
  - ✅ **Дешёвый гвард до всякой семантики: стоп-лист (предлоги, союзы, местоимения, голые числа) —
    ключ из него отбрасывать и брать следующее слово.** Не заменяет floor «карантинить идентичность
    предмета», но снимает худший класс кластеров одной строкой в селекторе.
  - ⚠ **И самое дорогое: дедуп-улика ЛЕЖАЛА В ТОМ ЖЕ КЛАСТЕРЕ.** Сигнал №1 — ретроспективный и
    дословно называет RFC, из-за которого тема дедуп («RFC: Мета-ассистент — мост между контекстами
    с человеком-замком»). Лексический слой сгруппировал его с темой и всё равно вынес пред-вердикт
    `Novel`: он матчит СЛОВА, а не смысл, поэтому прочитать собственный ответ в своём же кластере
    не может **по построению**. ⇒ ждать улучшения от лексического слоя бесполезно; работает только
    семантический шаг воркера (Шаг 1 промпта) — и он на этом прогоне сработал.
- **Итог на 2026-08-16 (14 прогонов): `disc__Candidate` = 0 за всё время** — замер с канарейкой
  (`#Candidate` → 0 при `#RFC` → 309 ⇒ ноль настоящий, а не мёртвый запрос).
- ⛔ **ЗАМЕР 2026-08-19 (прогон 16, `dlp-3442`) — ОБА предложенных гварда счётчиковую тему НЕ
  закрывают, и это свойство КОНСТРУКЦИИ, а не недоделка.** Селектор УЖЕ несёт «ярус 2» — отсев
  шаблонных токенов по доле внутри источника (`_BOIL_RATIO=0.6`, `_BOIL_MINN=10`, отгружен
  ~2026-08-14); ⚠ в корпусе правил он не описан **нигде** (`grep -rl 'boilerplate\|_BOIL'` по
  `rules/` → **0**), поэтому floor «стоп-лист» выше писался, не зная о соседе. Ярус 2 отработал
  штатно: `правило` 40/60 = **0.67** → отсеян, `отложенная` и `митигация` по 37/60 = **0.62** →
  отсеяны. Тема всё равно прошла — ключом стало **`повторов`**.
  - ⛤ **Механизм: счётчиковый подшаблон СЛИШКОМ РЕДОК, чтобы быть boilerplate.** У источника
    `retrospective` два подшаблона заголовка — «…: отложенная митигация» **37/60 (62 %)** и
    «…: N задокументированных повторов» **3/60 (5 %)**. Первый порог перешагнул и отсеян; второй
    ниже ЛЮБОГО разумного порога ⇒ его слова остаются валидными **специфичными** ключами и
    собирают счётчиковые темы в кластер. Фильтр частоты ищет ЧАСТОЕ — счётчик РЕДОК; он не может
    его поймать в принципе.
  - ⛔ **Стоп-лист (✅-floor §КОМПАУНД выше) здесь тоже бессилен:** `повторов` — содержательное
    существительное, не предлог и не союз. Ключ ротировался не «вправо к служебным», а **вбок к
    редким**: `правило` (карантин 08-06) → `bash` (карантин 08-15) → `prefer` (карантин 08-15) →
    **`повторов`**. Три гварда подряд (карантин · ярус 2 · стоп-лист) промахиваются мимо ОДНОЙ
    темы, потому что каждый ключуется на СВОЁМ признаке, а тема не несёт ни одного из них.
  - ✅ **Отсюда единственный работающий floor — распознавать ФОРМУ заголовка регэкспом**
    (`N задокументированных повторов` / `×N за N суток`), а не выводить её из частот. Это ровно
    то, что уже записано в Lesson `0211fee8` («требовать нового СОДЕРЖАНИЯ, а не нового КЛЮЧА») —
    замер добавляет к нему **доказательство невозможности** частотного пути.
  - ⚠ **Тот же файл выбран ЧЕТВЁРТЫЙ раз** (`101` 08-06 → `121` 08-15 → `124` 08-19, плюс
    `prefer-codegraph: 24` 08-15 вторым сигналом кластера — со **строго тем же** счётчиком, то
    есть предмет не менялся вовсе). ⛤ Lesson зафиксирован 2026-08-16, класс воспроизвёлся через
    3 дня ⇒ подтверждено прогоном: **декларация поведения не меняет**, floor живёт в правиле, а
    отбор делает `~/.claude/bin/dlp-tick.sh` (`declaration-and-executable-carrier`).

## §A4 ЛОЖНО-ПОЛОЖИТЕЛЬНЫЙ ДЕТЕКТОР ИСТОЧНИКА — у темы нет предмета вовсе

⛔ **ЧЕТВЁРТЫЙ дискриминатор — ЛОЖНО-ПОЛОЖИТЕЛЬНЫЙ ДЕТЕКТОР ИСТОЧНИКА; он даёт тему, у которой
предмета НЕТ ВОВСЕ, и потому исход у неё ДРУГОЙ.** Третий дискриминатор судит темы, чей предмет
реален, но вердикт по нему уже вынесен (`deduped`). Здесь сигнал сфабрикован самим сбором: детектор
матчит СЛОВО, употреблённое в другом смысле, и воркер тратит прогон на проверку выдумки
(`no-candidate`). Карантин ключей и отсев источника мимо по построению — источник живой, ключ
каждый раз новый, а ложным является **отдельный сигнал внутри него**.

- ⚠ **Симптом-tell, ловится ДО дедупа:** прочитал тела сигналов кластера — и ни одно не утверждает
  того, что обещает заголовок темы. Не «уже сделано», а «такого факта нет»: строка сматчена по
  слову, чей смысл в контексте **противоположен** искомому.
- ⛤ **Три класса ложного матча, и каждый — инверсия предмета** (замер по `DEFERRED_MARKERS`,
  `dlp-collect-signals.py:297`): (а) `accept-baseline` / `не вводится` = решение **ПРИНЯТО**, вопрос
  закрыт навсегда; (б) «отказ **отложенный**», «дефект **отложенный**» = свойство ДЕФЕКТА, не статус
  митигации; (в) «отложено в issues #3973-3976», «follow-up #3968» = долг **УЖЕ ЗАВЕДЁН**. Плюс
  родовое слово в чужом смысле («класс-**кандидат** на богатый displayName»).
- ⛔ **Ложные ВЫТЕСНЯЮТ настоящие через квоту, а не просто шумят.** У источника потолок
  (`MAX_PER_SOURCE=60`, из них `AIKNOW_SLOTS=20`), поэтому 38 ложных занимают **38 из 40**
  rule-слотов: настоящие сигналы того же источника до отбора не доезжают. Это тот же
  класс, что §Floor п.2 выше («лимит делится КВОТОЙ, а не „кто успел“»), но на ВХОДЕ, а не на выходе.
- ✅ **Floor: точность детектора мерить ПЕРЕЧИСЛЕНИЕМ ТЕЛ, а не долей источника.** Один проход по
  `signals-<дата>.jsonl` с печатью того, **что именно сматчило** (`m.group(1)`), и глазами по 30-40
  строкам. Здоровье источника (`ok 7/7`) и его объём об этом не говорят **ничего** — ровно как в
  задаче `700c061a` («манифест рапортовал ok: здоровье источника не равно его выходу»), только
  уровнем ниже: там мёртв канал, здесь **живой канал наполнен фальшивкой**.
- ⛤ **Следствие для плана починки:** фикс селектора по счётчикам (§ТРЕТИЙ) **недостаточен** — за
  ними стоит второй класс, и он вскрылся на первом же не-счётчиковом прогоне. Чинить надо ОБА
  конца: селектор (какие темы не брать) и коллектор (какие сигналы не производить).
- **Замер 2026-08-16** (прогон 11, `dlp-67749`, первая НЕ-счётчиковая тема за 11 прогонов): тема
  «правило `autonomous-mandate-drive-dont-idle`: отложенная митигация» — **все 3 её сигнала ложные**
  (два матча на `follow-up` в «follow-up hygiene» = вид работы из floor'а правила; третий — HTML-
  комментарий retro-атрибуции о митигации, которая **внесена**). По источнику: 38 из 60 сигналов
  (63 %) — «отложенная митигация» по 26 правилам, реально незакрытый долг — 1-2. Детектор написан
  2026-08-03 (`6d74cf1`, Ф2) и с тех пор не менялся: правились ротация окна и наблюдаемость
  источников, то есть **сколько** и **видно ли**, но не **что именно** ловим.
- ⛔ **И ГЛАВНОЕ СЛЕДСТВИЕ, которого выше не было: честный `no-candidate` ВОЗВРАЩАЕТ ту же ложную
  тему, потому что ключ карантинится ТОЛЬКО на `deduped`.** Прочитано в проде (`dlp-tick.sh:826-829`):
  `[ "$declared" = "deduped" ] && quarantine_key …`, с обоснованием «`no-candidate` не означает, что
  кластер исчерпан: тема может стать актуальной завтра». Для темы **с реальным предметом** это верно;
  для **сфабрикованной детектором** — нет: её предмет не появится завтра, потому что его не было
  никогда, а отбор детерминирован ⇒ тот же кластер выигрывает снова.
  - ⚠ **Отказ выглядит как ДВА независимых честных прогона**, а не как цикл: каждый воркер судил
    верно и закрылся штатным исходом. Увидеть можно только сверкой `topic-*.json` соседних прогонов —
    совпали `key` + `title` + состав `evidence` ⇒ это рецидив, а не новая тема.
  - ✅ **Floor воркеру: ложная тема закрывается `deduped`, а не `no-candidate`.** Формально подходят
    оба («сигналы есть, направления нет» ↔ «тема уже отклонялась»), но исходы **не эквивалентны по
    последствию**: первый оставляет тему в пуле, второй уводит ключ в карантин. Прецедент отклонения
    предыдущим прогоном и есть основание для `deduped` — это не натяжка, а точное попадание в
    формулировку «уже отклонялась».
  - ⛤ **Floor автору петли: у исхода есть ДЕЙСТВИЕ, а таблица промпта описывает только СОСТОЯНИЕ.**
    Пока «когда ставить» перечислено, а «что это сделает с отбором» — нет, воркер выбирает ярлык по
    смыслу и случайно роняет карантин. Называть последствие в той же строке таблицы.
  - **Замер 2026-08-16** (прогоны 11 → 12, `dlp-67749` 03:44 → `dlp-40461` 06:45): та же тема, тот же
    ключ `autonomous`, тот же кластер из 4 сигналов, интервал 3 ч. Прогон 11 закрылся `no-candidate`
    (верно по букве) ⇒ ключ не закарантинен ⇒ прогон 12 получил её снова. Закрыт `deduped` — ключ ушёл
    в карантин, рецидив ЭТОЙ темы прерван; класс остаётся, задача `f4abdc38` покрывает только ветки
    `size-capped` и в теле утверждает латентность класса (устарело — он сработал на `no-candidate`).

## §A5 ПОТРЕБЛЁННЫЙ ВХОД — сигнал честен, а работа по нему уже создана ИЗ НЕГО ЖЕ

⛔ **ПЯТЫЙ дискриминатор — ПОТРЕБЛЁННЫЙ ВХОД: сигнал ЧЕСТЕН, предмет РЕАЛЕН, а работа по нему уже
создана ИЗ ЭТОГО ЖЕ артефакта.** Три предыдущих разводятся по тому, что не так с темой: вердикт уже
вынесен (§ТРЕТИЙ, счётчики) · предмета нет вовсе (§ЧЕТВЁРТЫЙ, ложный матч). Здесь не так **с
источником**: он эмитит **бриф**, из которого проект и был материализован, — то есть спецификацию
уже сделанного, поданную как новую идею.

- ⛤ **Механизм структурный, а не настроечный:** `src_vision_doc` (`dlp-collect-signals.py:439`)
  глобает статические `~/Developer/*vision*.md`. У файла нет отметки «потреблён», а связь **односторонняя**:
  проект ссылается на бриф (§Референсы), бриф на проект — **ничем**. ⇒ ни коллектор, ни тик узнать
  не могут **по построению**, и документ переэмитится вечно, сколько бы раз его тему ни закрывали.
- ✅ **Дискриминатор воркеру — ОДИН запрос, и он терминальный: искать по ЗАГОЛОВКУ документа, а не по
  словам темы.** У брифа H1 обычно дословно совпадает с меткой проекта/RFC, потому что проект
  создавался из него. `SELECT ?s ?l WHERE { ?s exo:Asset_label ?l FILTER(CONTAINS(LCASE(STR(?l)),'<H1 брифа>')) }`
  → попадание = материализовано, дальше читать §Референсы найденного (там лежит путь брифа — это и есть
  недостающая обратная связь, восстановленная вручную).
- ⛔ **Не мерить «долей материализованных» — метрика тождественно-зелёная.** Замер 2026-08-16 матчером
  «≥2 содержательных слова заголовка в метке» дал **12 из 12** при явно шумных попаданиях
  (`exocortex-ksd-vision` → `inbox#ExoAssistantKnowledge_confidence`). 100 % — симптом сломанного
  верификатора (`self-satisfying-metric-weak-verifier` §A16), а не полноты. Судить поштучно по
  §Референсы конкретного кандидата.
- ⚠ **Тот же источник несёт и ложный матч §ЧЕТВЁРТОГО, в подстроке:** `"vision" in base.lower()`
  матчит **super·vision** ⇒ `loops-supervision-handoff-2026-08-14.md` (операционный handoff надзора)
  попадает как вижн-документ. Плюс 3 из 12 — `next-session-prompt-*` (эфемерны by design). Итого
  **4 из 12 файлов источника вижн-документами не являются**.
- ⛤ **ПРИОРИТЕТНОЕ следствие: задача `076de719` («Оцифровать вижн-документы в граф», Backlog,
  контейнер `bbac67ce`) — не гигиена находимости идей, а КОРЕНЬ этого дискриминатора.** Её тело
  описывает боль как «идею не нашли» и уже несёт нужный исход №2 («уже реализован → ссылка на
  проект/RFC»); недостающее — что тот же пробел кормит петлю разведки потреблёнными входами. Пока
  она открыта, дискриминатор придётся применять руками каждый прогон.
- **Замер 2026-08-16** (прогон `20260816T100108-71648`, тема «Concept Audit & Redistribution — Vision
  Locks + План»): бриф от **2026-07-26**, а проект `531dd440` создан из него **в тот же день** и живёт
  в `Doing` (Phase 0/1/2 Done — 2340 концептов редистрибутировано, VL10 отгружен в CLI как
  `audit ontology-membership` v16.194.0; Phase 3 Backlog). Оба сигнала кластера указывали на
  отгруженную работу: второй (`loops-supervision-handoff`) — на коммиты `535b37a`/`a05a34a`/`960d5d7`,
  проверены `git log`. Исход `deduped`, 12-й подряд.

## §A6 сигнал НЕ ОПИСЫВАЕТ работу, он ЕЮ ЯВЛЯЕТСЯ

⛔ **ШЕСТОЙ дискриминатор — сигнал НЕ ОПИСЫВАЕТ работу, он ЕЮ ЯВЛЯЕТСЯ; источник эмитит
зарегистрированные work-item'ы, поэтому тема, озаглавленная его меткой, дедуп-терминальна ПО
ОПРЕДЕЛЕНИЮ.** §ПЯТЫЙ разбирает **бриф**, из которого работа была материализована: там сигнал и
work-item — **разные** артефакты, связь односторонняя, и её приходится восстанавливать запросом.
Здесь восстанавливать нечего — сигнал **и есть** карточка задачи.

- ⛤ **Механизм — в ФИЛЬТРЕ самого источника, а не в отборе:** `src_executor_finding`
  (`dlp-collect-signals.py:386`) спрашивает `?s ems:Effort_status ?st` с
  `FILTER(CONTAINS(STR(?st), "753a44d5") || CONTAINS(STR(?st), "EffortStatusBacklog"))` под анкером
  `$exodev`. То есть предикат отбора **требует** статуса Backlog ⇒ **каждый** сигнал этого источника
  — открытая задача в очереди. Свойство, делающее тему дедуп-терминальной, гарантировано
  условием выборки.
- ✅ **Дискриминатор ДЕШЕВЛЕ, чем у §ПЯТЫЙ, и терминален: прочитать frontmatter цели, на которую
  сигнал УЖЕ указывает ссылкой.** Запроса по графу не требуется — ссылка `evidence_ref` даёт путь:
  ```bash
  grep -m1 -E '^(ems__Effort_status|ems__Effort_parent):' <путь из evidence_ref> | cat
  ```
  Есть `ems__Effort_status` ⇒ это **work-item**, а не идея ⇒ `deduped`, прогон закрывать. Нет ⇒
  сигнал указывает на прозу/разбор, и тему можно судить по существу.
- ⛔ **Источник при этом НЕ дефектен, и удалять его нельзя.** Замысел законен: несколько
  разрозненных находок исполнителей могут складываться в **направление**, которого в графе нет.
  Ломается **отбор**: лексический тик берёт заголовок ОДНОГО сигнала дословно, и тема вырождается
  из «что общего у этих находок» в «вот эта конкретная задача». ⇒ чинить в селекторе (тема из
  `executor-finding` обязана быть **обобщением ≥2 сигналов**, а не меткой одного), а не отсевом
  источника — иначе исчезнет единственный канал, видящий незакрытые находки.
- ⚠ **Симптом-tell, ловится ДО всякой проверки:** заголовок темы **дословно** совпадает с меткой
  одного из сигналов кластера. Совпадение слово-в-слово означает, что отбор не обобщал, а
  переименовал — и предмет темы уже существует как ассет.

**Замер 2026-08-19** (прогон `20260819T125314-51970`, тема «SPARQL-движок: три дефекта с молчаливо
неверным непустым результатом»): заголовок = `exo__Asset_label` задачи `0c24668f` дословно; задача
в `Backlog` под контейнером `bbac67ce` с 2026-08-02. Первый прогон петли, где доминирует
`executor-finding` — предыдущие 14 делились между счётчиками (§ТРЕТИЙ), ложным матчем (§ЧЕТВЁРТЫЙ)
и вижн-документами (§ПЯТЫЙ). Итог по `runs.jsonl`: **14 прогонов = 13 `deduped` + 1 `no-candidate`**,
`disc__Candidate` за всё время **0**; этот — 14-й `deduped`.

| # | дата | Тема (симптом ⇒ действие) |
|---|---|---|
