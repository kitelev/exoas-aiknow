---
exo__Asset_uid: 4247da94-4726-4deb-80d3-e15fc27ce976
exo__Asset_createdAt: 2026-09-14T10:31:37
exo__Asset_updatedAt: 2026-09-26T16:09:36
exo__Instance_class:
  - "[[39a39239-2a97-483a-ba91-fb01bf5c85f3]]"
exo__Asset_createdBy: "[[4ef3962d-b8a7-42b5-bd28-88ec846f1d13]]"
exo__Asset_label: "Правило: commit-and-push (полный текст; пилот rules-from-graph, 2026-09-06)"
aliases:
  - "Правило: commit-and-push (полный текст; пилот rules-from-graph, 2026-09-06)"
  - commit-and-push
exo__Asset_isDefinedBy: "[[2ce77ac1-3f78-4f64-9dc1-41ea01da1ef0]]"
inbox__ExoAssistantKnowledge_decay: "[[06a5b9f9-da93-4234-883f-3f0cf65b2fba]]"
inbox__ExoAssistantKnowledge_confidence: "[[227def30-f56f-4f5f-936d-f6a81ad73c96]]"
aiKnow__Memory_aboutConcept:
  - "[[b2371c70-1a34-4d3b-9e81-49eea29ac55f]]"
  - "[[4a24ddbc-8878-4cde-a259-cdc384d8d6ec]]"
  - "[[02ee588d-c806-4293-8552-52a4cc55e9bc]]"
  - "[[2c2087b4-4dfe-4ee6-b91d-2ac83d5c81a1]]"
  - "[[8ad1c75d-827d-44e6-b1bf-bace7c142e9b]]"
  - "[[fb98a5cf-4378-45f7-87ed-e508500d4776]]"
  - "[[58808620-7ed8-425b-abc0-a462daf7eeb1]]"
  - "[[e62145c2-e698-4de6-99fe-6821cad4f9a8]]"
---


# Commit & Push Rule

При изменении файлов в git-репозиториях (dotfiles и любых других; ⛤ каноничные vault **git-free** — там ExoSync, не git) — ВСЕГДА коммитить и пушить изменения перед тем как сообщить "Готово".

## Порядок
1. Изменить файлы
2. `git add` только изменённые тобой файлы (⛔ перечислять ФАЙЛЫ, не каталоги)
3. `git diff --cached --stat` — в индексе ровно твои файлы → `git commit <файл1> <файл2> -m "…"` **pathspec-формой**. ⛔ НЕ bare `git commit`: он уносит весь индекс, включая чужое pre-staged (§Bare `git commit`)
4. `git push`
5. Только после успешного push — "Готово"

## Исключения
- Репозитории без remote (локальные)
- Когда пользователь явно просит НЕ пушить

## ⛔ Трейлер `Claude-Session:` — часть КАЖДОГО коммита, и его пропуск НЕВИДИМ

Системный промпт предписывает завершать сообщение коммита строкой
`Claude-Session: https://claude.ai/code/session_<id>`, и конвенция **живая** (замер 2026-09-05:
**230 из 249** коммитов окна её несут). Пропуск при этом не даёт ни ошибки, ни красноты —
коммит выглядит образцовым.

⛔ **«ни хука» СНЯТО 2026-09-10 — хук есть:** `commit-session-trailer-guard.sh`
(PreToolUse:Bash, зарегистрирован) блокирует `git commit`, чьё сообщение извлекается из
`-m`/`--message`/`-F`/`--file` и не несёт трейлера; fail-open на формах, где сообщения в вызове
нет (интерактивный редактор · `--amend --no-edit` · `--dry-run` · `-C`/`--squash`/`--fixup`);
escape — трейлинг `# no-session-trailer: <причина>`. 19 осей + 4 мутанта
(`commit-session-trailer-guard-verify.sh`). ⛤ Замер, которым он обоснован (30 дней, `~/dotfiles`):
1412 коммитов, без трейлера **406**, из них машинных (auto-backup + vteam) 64 ⇒ **342 ручных
(24 %)** — пропуск систематический, а не разовый.
⚠ Хук не видит коммиты, которые делают СКРИПТЫ (launchd, auto-backup): они идут мимо Bash-тула.

- ⛔ **Цена — не косметика, а ПОТЕРЯ АТРИБУЦИИ:** по трейлеру разрешается «мой это проход или
  чужого этапа конвейера» (`session-retrospective` Step 3a) и восстанавливается, какая сессия
  внесла правку. Без него коммит неотличим от auto-backup и от соседней сессии.
- ⚠ **Симптом-tell, ловится ПОСЛЕ коммита одной командой:**
  `git show -s --format=%B <sha> | grep -c 'Claude-Session:'` → `0`. ⛔ Запушено — не амендить
  (общий remote): ставить трейлер в следующий коммит и сказать вслух.
- ✅ **Дешевле помнить: тело коммита писать в файл** (`git commit -F <файл>`), а трейлер держать
  последней строкой шаблона — тогда он не теряется при многострочном сообщении.

## ⛔ `git rm` + `git add` interaction (не передавать удалённый путь в git add)

`git rm <path>` **уже стейджит удаление**. Если потом передать тот же `<path>` (или путь
удалённой папки) в `git add` — `git` возвращает `fatal: pathspec '<path>' did not match any
files` и **прерывает весь `git add` целиком**, не застейдив остальные (Edit-модифицированные)
файлы. В результате `git commit` уносит только удаления (staged через `git rm`), а правки
выживающих файлов **молча теряются** из коммита.

**Правило:** после `git rm X` — в последующий `git add` включать **только отдельно
отредактированные файлы**, НИКОГДА не путь `X` (он уже staged). Проверка: `git status --short`
до commit — все ожидаемые `M`/`D` строки на месте.

<!-- Added by /session-retrospective 2026-06-27 — root: child al-settings-m1 git add включил deleted readEnabledChecks.ts → abort → validate-vault.ts/VaultCheckRunner.ts не закоммичены → broken push -->
## ⛔ `git add -A` в worktree после build/test сметает артефакты + submodule-указатели (selective-staging trap)

<!-- Added by /session-retrospective 2026-06-22 — root: child al-night-dogfood git add -A swept build/test residue + submodule pointers -->

`git add -A` (или `git add .`) кажется безопасным в «свежем worktree, всё моё» — но как только в worktree прогнаны **build** (tsc/esbuild dist-артефакты) и **tests** (flaky-reports, test-vault `main.js`), а также если worktree содержит **git submodules** (их gitlink-указатели могут «дрейфовать»), `git add -A` стейджит **всё это** вместе с твоими правками. На multi-package repo с submodules это особенно коварно: staged submodule-указатели + отсутствующий/несвежий `.gitmodules`-blob дают `error: invalid object … for '.gitmodules'` + `Error building trees` при commit (тот же класс симптома, что lint-staged-corruption — легко спутать).

**Правило:** в worktree, где прогонялись build/test, — **selective staging пофайлово** (`git add <path1> <path2> …`), НИКОГДА `git add -A`/`git add .`. Перед commit — `git diff --cached --name-only`: список должен быть **ровно** твои файлы (нет `dist/*.d.ts`, `flaky-report*.json`, `test-vault/**`, submodule-путей, которые ты не менял намеренно).

## ⛔ КАТАЛОГ в `git add` — тот же selective-staging trap, что `-A`, но выглядит «уже селективным»

`git add -A` настораживает; `git add .claude/rules/ .claude/skills/` — **нет**: перечислены конкретные каталоги, ощущение «я же указал, что именно». Но в общем dotfiles-репо каталог **шире твоей работы** — в нём живут файлы, которые прямо сейчас правит параллельная сессия, и каталожная форма забирает их **все**.

- ⛤ **Дискриминатор простой: `git add <каталог>` стейджит по СОСТОЯНИЮ дерева, `git add <файл> …` — по твоему СПИСКУ.** Первое — снимок чужой активности вместе с твоей; второе — утверждение о собственной работе. В репозитории с одним писателем разницы нет, в общем — это разные операции.
- ⚠ **Симптом-tell до коммита:** `git status --short` показывает файл, которого ты не открывал. ⛔ Не списывать на «наверное, я задел» — открой `git diff <файл>` и посмотри на `stat` mtime: свежий mtime (минуты) = живая чужая работа, а не твой след.
- ✅ **Floor: перечислять ФАЙЛЫ** (`git add <p1> <p2> …`) → `git diff --cached --name-only` (ровно твой список) → `git commit <p1> <p2> … -m` (pathspec-форма, игнорирует прочий индекс — §ниже).
- ⛔ **Каталожная форма особенно живуча, потому что её мандатят ИНСТРУКЦИИ.** `/session-retrospective` Step 8.5 печатает буквально `git add .claude/hooks/ .claude/skills/ .claude/rules/` — то есть шаг, требующий персистить митигации, сам же и бандлит чужое. Читать его как «перечисли свои файлы из Step 6.5», а не исполнять дословно (`declaration-and-executable-carrier`: правило объявляет selective staging, носитель мандатит каталог — побеждает носитель, потому что его читают в момент работы).
## ⛔ Даже `git add <ФАЙЛ>` забирает чужое: `git status` показывает `M`, но НЕ показывает ЧЬЁ

Секции выше лечат **область** staging (каталог вместо файлов, bare `commit` поверх чужого индекса). Остаётся третий вход, который они не закрывают: ты стейджишь **ровно один файл**, дисциплина соблюдена — а в файле лежит **твоя правка ПОВЕРХ чужой незакоммиченной**. `git status --short` печатает `M <путь>` в обоих случаях; признак авторства в нём отсутствует **по построению**.

- ⛤ **Механизм появления чужой правки:** автономный воркер (петля, демон) правит общий файл и **умирает до коммита** — его работа остаётся в рабочем дереве как обычная модификация (`anthropic-api-rate-limit-child-death` §Addendum 2026-08-10). Живых сессий рядом может не быть вовсе, поэтому «сейчас никто не работает» ложным чувством безопасности **усиливает** промах.
- ⚠ **Симптом-tell до коммита, стоит один вызов:** `git diff <файл>` показывает hunks, которых ты не писал. ⛔ Не списывать на «наверное, задел рефакторингом» — посмотреть их содержательно: чужой код обычно решает **другую** задачу, чем твоя правка.
- ✅ **Floor для файла, который трогают автономные акторы:** перед `git add` — `git diff <файл> | grep -c '^+'` против числа строк, которые ты сам добавил. Расхождение = чужая работа в дереве; решать её судьбу **отдельно** (проверить харнессом, закоммитить с атрибуцией), а не заметать в свой коммит.
- ⛔ **Уже уехало — не переписывать историю** (на remote, force-push запрещён): атрибуция фиксируется **следующим** коммитом с явным «код уехал в `<sha>` по моей ошибке staging». Обнаруживается постфактум одной командой: `git log -S '<строка чужого диффа>' -- <файл>` покажет **твой** коммит там, где строку ты не писал.
<!-- Added by /session-retrospective 2026-08-14 — root: ALP-воркер 2c73c41f взял 4 файла, увидел в `git status --short` ровно их и закоммитил, не открыв `git diff`; в дельте лежала работа воркера alp-17ed32f2, умершего в 07:57 (за 16 мин до спавна). Floor выше («сравни `grep -c '^+'` с числом своих строк») требует помнить своё число ДО коммита — здесь сработала более дешёвая форма того же счёта, доступная ПОСЛЕ. -->
- ⛤ **Самый дешёвый tell — ДИФФСТАТ вывода `git commit`, и он работает ПОСТФАКТУМ, когда floor выше уже пропущен.** Floor требует свериться до `git add` (то есть помнить, сколько строк ты добавил); диффстат печатается сам и сверяется с порядком величины: «правил один блок и два комментария» ≈ десятки строк, а `343 insertions(+)` — нет. **Расхождение НА ПОРЯДОК = в коммите чужое**, и проверяется одной командой: `git show HEAD -- <файл> | grep '^+' | grep -v '<маркеры своей темы>'`.
  - ⚠ Работает и там, где `git diff` до коммита **бесполезен**: чужая правка может быть в **том же** файле и внешне однородна твоей (комментарии того же стиля, соседний блок) — глазами в диффе она не выделяется, а в счёте выделяется сразу.
  - ✅ **Не запушено ⇒ чинится дёшево и без переписывания истории:** `git commit --amend -F <файл>` с честной атрибуцией (чья работа, из какой задачи, почему подобрана). Запушено ⇒ следующим коммитом, как выше.
  - ⛤ **Найдя чужое — проверь, не осталась ли его задача в `Doing`**, и допиши в её тело, где теперь лежит работа: иначе её преемник увидит чистое дерево и начнёт с нуля (`anthropic-api-rate-limit-child-death` §A3).
<!-- Added by /session-retrospective 2026-08-12 — root: воркер ALP-задачи 0274ca15 сделал `git rebase origin/main` ПРЕВЕНТИВНО (отказ `Please commit or stash them` из-за 5 чужих файлов при behind=0), а следом получил КРАСНЫЙ мутант на петле, которую не трогал — чужой коммит 2305364 ужесточил общий с ней код. Обе половины стоили по циклу диагностики; обе лечатся одной командой. -->
## ⛔ Чужой коммит в общем репо бьёт ДВАЖДЫ: превентивный `rebase` отказывает, а КРАСНЕЕТ у тебя ЧУЖОЕ

Секции выше — про **staging** (не забрать чужое в свой коммит). Оставшиеся два входа не про staging вовсе, и оба возникают в общем репо (dotfiles правят все параллельные сессии + hourly auto-backup), где рабочее дерево почти никогда не чистое:

1. ⛔ **Не делать `git fetch && git rebase` ПРЕВЕНТИВНО — сперва посчитать `behind`.** `rebase` **требует чистого дерева**, поэтому при живых чужих правках он отказывает **штатно** (`cannot rebase: You have unstaged changes` / `Please commit or stash them`) — и толкает ровно к запрещённому: `git stash` чужого WIP. При этом он чаще всего не нужен: `behind = 0` ⇒ push и так пройдёт fast-forward.
   ```bash
   git -C <repo> rev-list --left-right --count origin/main...HEAD   # "<behind>\t<ahead>"
   ```
   `behind = 0` ⇒ rebase не нужен · `ahead = 0` ⇒ коммит уже на remote (вероятно, забрал auto-backup) · rebase только при `behind > 0` **и** чистом дереве.
   ⛤ Прежде это знание жило **только** в `session-retrospective` §Step 8.5 — то есть доезжало к моменту ретро, а не к моменту работы, когда push и делается (`rule-carrier-follows-function`: барьер обязан стоять там, где действие).

2. ⛔ **Красный тест / мутант на компоненте, который ты НЕ ТРОГАЛ, при зелёном проде — сперва `git log` по этому пути, а не поиск своей регрессии.** В общем репо между твоей базовой линией и повторным прогоном мог лечь чужой коммит; если он правил код, **общий** с твоим (вторая петля, соседний адаптер, разделяемый хелпер), краснеет у тебя — а причина в чужой правке.
   ```bash
   git -C <repo> log --oneline -3 -- <путь-который-покраснел> | cat
   ```
   - ⚠ **Симптом-tell:** красное там, куда твой диф не заходил **вообще**. Не «наверное, я задел» — посмотри лог: коммит с чужой темой и временем внутри твоей сессии закрывает вопрос за секунды.
   - ⛔ **Проверь, не затираешь ли ты их правку:** твой рабочий файл мог разойтись с HEAD (`git show HEAD:<путь> | diff - <путь>`). Их коммит мог обновить то, что ты сейчас переписываешь.
   - ⛤ **Чинить — можно и нужно, если красное блокирует ТВОЙ DoD** (мутант — часть твоей приёмки). Но чинить **минимально** и сказать вслух, чью правку это восстанавливает; ⛔ не расширять правку на их свежую поверхность — заведи остаток задачей.

## ⛔ Rebase УЖЕ ЗАПУШЕННОЙ ветки: force-push запрещён ⇒ rebased-история уезжает НОВОЙ веткой, старая — «влита, удаление за фаундером»

`main` ушёл ⇒ `git rebase origin/main` в worktree переписывает ветку, которую ты уже запушил ⇒ `git push` отказывает non-fast-forward, а `--force`/`--force-with-lease` **блокирует хук** `git-force-push-blocker` (директива 2026-04-18 — без исключений для своих feature-веток).

- ✅ **Floor:** rebased-историю пушить **под новым именем** — `git push -u origin <branch>:<branch>-r2` — и ff-мержить ИЗ неё (`git merge --ff-only origin/<branch>-r2`); в WAITING_DECISION/отчёте назвать обе ветки.
- ⛔ **Старую ветку (до-rebase) и `-r2` НЕ удалять:** `git push --delete` / `:<ref>` — решение фаундера (CLAUDE.md §Auto-execute matrix «Deleting branch — ask user first»); с 2026-09-16 это блокирует хук `git-remote-branch-delete-blocker`. Статус в отчёте: «влиты, удаление — за фаундером».
- ⚠ **Tell ДО rebase:** ветка уже на remote (`git rev-parse origin/<branch>` резолвится) ⇒ после rebase понадобится новое имя; альтернатива без rebase — `git merge origin/main` в ветку (тогда ff в main невозможен, нужен merge-коммит — по промпту решать заранее).

_Empirical 2026-09-16 (34e52481): rebase на 1 чужой коммит → `--force-with-lease` заблокирован → `session-kind-r2` → ff-merge; обе remote-ветки снёс по строке ревью «снести после merge», верхний указал на матрицу — восстановлены из объектов на те же sha (`git push origin <sha>:refs/heads/<name>`), урон 0._

## ⛔ `git diff` ПОСЛЕ `git add` — ПУСТО: спасая работу патчем перед сносом worktree, бери `git diff HEAD`

Стандартный сценарий потери: pre-commit не пропустил коммит (тесты красные по независимой причине), обходить его нельзя, работу надо сохранить — делаешь `git diff > /tmp/x.patch`, кладёшь патч куда-то durable, сносишь worktree. ⛔ Если перед этим был `git add`, патч выйдет **пустым**: `git diff` без аргументов показывает только **неиндексированное**, а твои правки уже в индексе.

- ⚠ **Отказ тихий:** `git diff` возвращает `rc=0` и пустой файл — команда «выглядит» отработавшей. Дальше `worktree remove --force` уносит и дерево, и индекс.
- ✅ **Floor: `git diff HEAD`** (всё против последнего коммита — и staged, и нет). `--cached` покрывает только индекс, то есть половину случая.
- ✅ **И проверять размер:** `[ -s patch ] || { echo "⛔ патч пуст"; exit 1; }` — одна строка, ловит класс целиком.
- ⛤ **Патч часто вообще не нужен: механическую правку дешевле описать РЕЦЕПТОМ.** Замена «N файлов × 1 вхождение» воспроизводится скриптом с `assert n == 1`, и такой рецепт **не протухает** от смены контекста вокруг, в отличие от diff'а.
- ⚠ **Ветка при этом выживает и вводит в заблуждение:** `git branch --list` покажет её живой, но она указывает на коммит **без** твоих правок — они были только в индексе снесённого worktree.

**Empirical 2026-08-20** (задел `quotingType → quoteStyle` ×7 под #4156): pre-commit корректно отверг коммит (CLI-тесты красные из-за js-yaml 5), `git add -u` уже отработал, `git diff > patch` дал **0 строк**, worktree снесён. Работа восстановлена только потому, что была механической — в issue ушёл исполнимый рецепт вместо патча.

## ⛔ Bare `git commit` сметает PRE-staged файлы из чужих сессий (selective-commit trap)

`git add <file1> <file2>` стейджит твои файлы, но индекс мог **уже содержать** staged-изменения, оставленные **другой/прошлой сессией** (или процессом). Bare `git commit` (без pathspec) коммитит **весь индекс** → подметает эти pre-staged файлы в твой коммит под твоим (нерелевантным им) commit-message. В лучшем случае — blemish трассируемости; в худшем — забандлишь чужую WIP / sensitive файл.

**Правило (selective commit):**
- Перед commit — **`git diff --cached --stat`**: убедись, что staged ровно твои файлы. Если видишь чужой staged файл — реши осознанно (committed by you? leave for owner?).
- Коммить **pathspec'ом**: `git commit <file1> <file2> -m "..."` — коммитит ТОЛЬКО перечисленные пути из индекса, игнорируя прочие staged. Это надёжнее, чем `git add <files> && git commit`.

## ⛔ `git diff --cached <ref>` — ДВУХТОЧЕЧНАЯ форма: в worktree, чья база УЕХАЛА, чужой merged-файл печатается как ТВОЁ удаление

Гвард этого правила («`--cached --name-status | grep -c '^D'` обязан быть **0**») и приёмка «в индексе
ровно твои файлы» **не называют КОМПАРАНД** — и со ссылкой тот же гвард **инвертируется**:
`git diff --cached --stat origin/main` сравнивает индекс с **ТЕКУЩИМ** деревом ссылки, поэтому файл,
влитый СОСЕДНИМ PR после создания твоей ветки, выходит **твоим удалением**. Читается как «индекс несёт
чужое удаление, коммитить нельзя» — ложно-КРАСНЫЙ на исправном индексе.

- ✅ **Floor: сравнивать с MERGE-BASE, не роняя `--cached`** — `git diff --cached --merge-base origin/main --stat`
  (форма поддержана, замер 2026-09-20: `rc=0`). ⛔ `--cached` с `...` **несовместим** — git отвечает
  usage-блоком, отказ громкий.
- ⚠ **Tell:** удаления появились в диффе после ЧУЖОГО мержа в main, а `git status` их не показывает.
- ⛤ Гвард на `^D` в data-репо (§выше) безопаснее гнать **без ссылки вовсе** (индекс против HEAD) —
  тогда компаранд не может уехать.

_(2026-09-20, PR #4300: `--cached --stat origin/main` показал 277-строчный файл PR #4299 моим удалением.)_

## Empirical reference

- **Date:** 2026-06-09 (session retrospective, RFC umbrella `78c2b7d0`).
- **Incident:** `git add verify-before-assert.md rfc-quality-check/SKILL.md && git commit -m "retro…"` — но `interactive-interview/SKILL.md` был **pre-staged в индексе** другой сессией (`ebfcf3a5`, легитимный empirical ref). Bare commit подмёл его (+8 строк) в retro-коммит `5d0e9a0` под message, который его не описывает. Контент был benign (нужный dotfiles-change), потому history не переписывал. Пойман самим Step 8.5 retro через `git show --stat HEAD`.
- **Lesson:** при selective commit — `git diff --cached --stat` ДО commit ИЛИ `git commit <pathspec>`.

### Recurrence 2026-06-14 (child `eka-materialize-hang`, retro Step 8.5) — high-blast-radius variant

- **Incident:** В vault-exodev superproject (live EKA M2 16-repo миграция шла параллельной сессией) сделал `git add assetspaces/kitelev/exoas-exoass` (только мой submodule-pointer) → затем **bare** `git commit` → коммит `4dc832a` подмёл **pre-staged** изменения чужой сессии: `.gitmodules` (−27 строк) + удаление gitlink'ов **9 submodule'ов** (exoas-adapters/agent/ims/inbox/kitelev-class-relations/kpc/period-commands/place/strategy) — staged EKA M2 submodule-removal. Запушено в shared main до обнаружения.
- **Почему хуже benign-кейса 2026-06-09:** не +8 строк документации, а **чужая mid-migration submodule-removal** под моим retro-message → coordination disruption (параллельная сессия теряет группировку своего коммита) + audit-traceability урон (filter `git log --grep "retro"` поднимает миграцию). Data loss = 0 (мой `git add`+`commit` не трогает working tree; removals были intentional staging другой сессии).
- **Не исправлял history:** push в shared main + ⛔ no-force-push → rewrite запрещён и опаснее блемиша. Surfaced оркестратору.
- **Root:** Пропустил **mandatory** `git diff --cached --stat` ПЕРЕД bare commit (этот rule прямо требует). Это HABIT: rule существовал, hook не энфорсит (и не может — не знает intent AI), знал — не применил. На multi-session машине с активной миграцией bare commit особенно опасен: индекс почти всегда содержит чужой pre-staged WIP.
- **Reinforced:** на vault-репозиториях, где параллельно может идти миграция, — **ВСЕГДА** `git commit <pathspec>` (не `git add <path> && git commit`). Pathspec-commit игнорирует прочий staged индекс атомарно. Cost: 1 загрязнённый shared commit (необратимо без force-push).

#### Victim-side дополнение (child `eka-vault-hygiene`, тот же инцидент 4dc832a — обратная сторона)

Это та же сессия, но с точки зрения **жертвы** sweep'а. `eka-vault-hygiene` застейджил 9 submodule-removal в vault-exodev и потратил ~10 tool-call на верификацию **ПЕРЕД** commit; в этом окне sibling-bare-commit (`4dc832a`) забрал его staged-индекс себе. Урок для жертвы:

- **Минимизируй staged→commit окно.** Тяжёлую верификацию делай ДО staging, затем stage→commit→push **атомарно**. Долгий staged-but-uncommitted интервал на multi-session vault = окно, где чужой commit заберёт твою работу.
- **HEAD-stability check перед КАЖДЫМ commit:** `git rev-parse --short HEAD` == значение с прошлого наблюдения. Сдвинулся → sibling закоммитил → `git show <new-HEAD> --stat` проверить, не подмёл ли твоё staged.
- **Recovery, если подмели:** data loss = 0 — работа закоммичена (под чужим message). `git merge-base --is-ancestor <твой-recovery-tag> <их-commit>` подтверждает линейность; ⛔ не переписывай pushed shared main. Перенеси оставшиеся коммиты pathspec'ом.
- aiKnow `[[0decc7ce-1fd4-49f2-ae11-89f29453b481]]`.
<!-- Added by /session-retrospective 2026-06-14 — root: bare commit в vault-exodev подмёл чужую EKA M2 submodule-removal staging (9 submodule + .gitmodules) под retro-message, запушено в shared main. HABIT: пропустил mandatory git diff --cached --stat. Reinforced: git commit <pathspec> на migration-active vault-репо. -->

⛤ **Эмпирика** (4 фрагм.: 2 секц. + 2 абз.) → `~/.claude/rules-archive/commit-and-push.md`

## ⛔ Переименование (`git mv`) в pathspec-коммите обязано нести ОБА пути — иначе коммит кладёт КОПИЮ без удаления

`git commit <pathspec>` берёт из индекса только записи, попадающие в pathspec. У переименования их **две** — удаление старого пути и добавление нового; pathspec из одних новых путей уносит **добавления**, а staged-удаления остаются в индексе ⇒ в дереве коммита лежат **оба** файла (дубль UID / basename), и push доставит дубль на remote (`vault-operations` §File Movement — тот же класс, что «перенос без удаления»).

- ⚠ **Tell ловится диффстатом ДО push:** `git show --stat HEAD` даёт insertions размером с полные файлы (у меня 714 при ожидаемых 112) и `git status --short` после коммита по-прежнему непуст (staged `D`).
- ✅ **Floor:** список путей для `git commit --pathspec-from-file` = **новые + старые** пути переносов + правленые файлы; проверка — `git show --name-status -M HEAD` печатает `R` (rename), а не `A` + оставшиеся `D`.
- ⛤ Не пушен ⇒ `git reset --soft HEAD~1` и коммит заново (не `--amend`: rule запрещает его как класс, а reset локального непушенного коммита — нет). ⛔ Многострочную переменную путей в `git add $VAR` zsh НЕ расщепляет (Г1) — `--pathspec-from-file`.

_(2026-09-14, exoas-tbank `d34616a`: 39 rename + 8 M; первый коммит с pathspec из новых путей дал 47 файлов и +714/−5 при 39 staged-`D` в остатке; после reset --soft и pathspec из 86 путей — +112/−79, `gh api commits`: 8 modified + 39 renamed.)_

## ⛔ Push ОТКЛОНЁН (non-fast-forward) в клоне data-репо: `reset --soft origin/main` тащит ИНДЕКС старой базы — чужой НОВЫЙ файл уезжает в твой коммит УДАЛЕНИЕМ

Симптом: между твоим `fetch` и `push` в data-репо лёг чужой коммит (mirror-тикет, exosync соседней сессии). Рефлекс «`git reset --soft origin/main` → re-copy → commit» **опасен**: `--soft` двигает HEAD на НОВЫЙ main, а индекс остаётся снимком твоего коммита на СТАРОЙ базе ⇒ файл, который сосед ДОБАВИЛ, в индексе отсутствует и `git status` показывает его как `D` — pathspec-коммит его не спасает (pathspec добавляет ТВОИ пути, удаление уже стоит в индексе).

- ✅ **Floor: после отклонённого push — `git fetch origin main && git reset -q --hard origin/main`** (индекс = новый main), затем re-copy своих файлов из vault, `git add --pathspec-from-file`, коммит заново. Свой коммит терять не жалко — он весь восстанавливается копированием.
- ✅ **Гвард ПЕРЕД коммитом, для любого «аддитивного» коммита в data-репо:** `git diff --cached --name-status | grep -c '^D'` обязан быть **0**; не ноль ⇒ индекс несёт чужое удаление, коммитить нельзя.
- ⚠ **Tell (ловится в `git status --short` ДО коммита):** строка `D  <путь>` у файла, которого ты **не открывал**. ⛔ Не читать как «сосед сам удалил»: сосед его только что ДОБАВИЛ.
- ⛤ Восстановление, если уже ушло: `git checkout <чужой-sha> -- <путь>` + коммит «restore … deleted by mistake» + проверить на remote `contents/<path>?ref=main` размер/sha. Урон окна — минуты, пока чужой exosync не подтянул удаление.

_(2026-09-19, child codebatch6e, exoas-exodev: `e95728e` унёс `007b7b48` (mirror #4272) удалением; замечено по `D` в статусе после коммита, восстановлено `1379ae2` через ~1 мин; net-diff 567ba13..1379ae2 = ровно мои 3 файла.)_

## ⛔ ssh к GitHub таймаутит (`Could not read from remote repository`) — пушить по HTTPS с кредами `gh`, remote НЕ менять

Симптом: `git push`/`fetch` падают `fatal: Could not read from remote repository … repository exists`, при том что `gh api` и `npm view` работают (сеть жива, лежит ssh-канал). ⛔ Не менять `origin` на https и не ждать «пока отпустит» — одна команда пушит по https с токеном из `gh`, без утечки PAT в командную строку:

```bash
git -c credential.helper='!gh auth git-credential' push https://github.com/<owner>/<repo>.git HEAD:refs/heads/<branch>
gh api repos/<owner>/<repo>/branches/<branch> --jq '.commit.sha[0:8]'   # verify по API, не по выводу push
```

⚠ Tell: два ssh-push подряд с одним и тем же `fatal`, а параллельный `gh api` отвечает. ⛤ Фикс лечит только транспорт: ветка, pathspec-коммит, запрет force — всё остальное правило в силе. _(2026-09-19, child #4264: три push из четырёх ушли https-обходом; ssh ожил сам через ~20 мин.)_

⛔ **Уточнение замером 2026-09-19 (child codebatch6e): на этой машине `~/.gitconfig` несёт `url.git@github.com:.insteadOf https://github.com/` ⇒ команда выше ПЕРЕПИСЫВАЕТСЯ в ssh и падает тем же `ssh: connect to host github.com port 22`.** Замер: `git push https://github.com/kitelev/exoas-exo-reqs.git HEAD:main` → ssh-таймаут; проверка — `git config --global --show-origin --get-regexp 'url\.'`. ⛔ Конфиг не править (правило safety). ✅ Обход ПРЕФИКСА — URL с явным портом, который `insteadOf` не матчит, плюс инлайн-хелпер (токен не попадает в argv):
```bash
git -c credential.helper='!f() { echo username=kitelev; echo "password=$(gh auth token)"; }; f' \
  push https://github.com:443/<owner>/<repo>.git HEAD:refs/heads/<branch>
```
(`!gh auth git-credential` на `:443`-форме вернул «could not read Username» — замер один, состояние сети в тот момент не установлено.) ⛤ **Если и `github.com:443` мёртв, а `api.github.com` жив** (curl `https://github.com/` таймаут при `api.github.com` 200 — замер 2026-09-19 ~06:5xZ), однофайловый коммит в data-репо идёт через Contents API: `gh api -X PUT repos/<o>/<r>/contents/<path> --input <json: message (с трейлером Claude-Session), content base64, branch>` → verify `gh api repos/<o>/<r>/commits/main`; локальный клон потом `fetch` + `reset --hard`. Ветку кодового репо так не толкнуть (Git Data API) — ждать/повторять с интервалом (в тот раз ssh вернулся через ~15 мин, push прошёл с 3-й попытки по 60 с).

⛔ **Форма `git -c 'url.git@github.com:.insteadof=' … push https://…` ОБХОДОМ НЕ ЯВЛЯЕТСЯ** (замер 2026-09-19 18:2x, dotfiles): пустое значение `-c url.<base>.insteadOf=` глобальный `insteadOf` не снимает — push ушёл в ssh и упал тем же `connect to host github.com port 22: Operation timed out`; предыдущие «успехи» этой формы совпали с живым ssh, а не с обходом. ✅ **Самая короткая рабочая форма — отключить глобальный конфиг целиком на один вызов:** `GIT_CONFIG_GLOBAL=/dev/null git -c credential.helper='!gh auth git-credential' push https://github.com/<owner>/<repo>.git HEAD:main` (rc=0, `gh api repos/<o>/<r>/commits/main` показал новый sha). `!gh auth git-credential` в этой форме работает (в отличие от `:443`-формы выше); порт и inline-хелпер не нужны. ⚠ Tell: в выводе push стоит `To github.com:<o>/<r>` (ssh-форма) при заданном `https://` — переписал `insteadOf`. Fetch той же формой: `GIT_CONFIG_GLOBAL=/dev/null git -c credential.helper=… fetch https://github.com/<o>/<r>.git main:refs/remotes/origin/main`.

⛤ **Замер 2026-09-19 20:03 и 21:04 (child 0218844b): github.com недоступен по 22 И по 443 ОДНОВРЕМЕННО, окнами 1–3 мин** (`curl -m 15 https://github.com/` → timeout, `ssh -o ConnectTimeout=8 -T git@github.com` → timeout, тот же `ls-remote` по https с `GIT_CONFIG_GLOBAL=/dev/null` → rc=124 за 40 с). ⇒ ⛔ выбор формы транспорта тут НИЧЕГО не лечит: все https-обходы выше — для случая «22 мёртв, 443 жив», а «висит 76 с» той формы в 20:03 был сетевым таймаутом, не потерей credential helper. ✅ Дискриминатор ДО обхода — два зонда: `curl -sS -m 10 -o /dev/null -w '%{http_code}' https://github.com/` И `ssh -o ConnectTimeout=8 -T git@github.com`; оба мертвы ⇒ ждать 1–2 мин и повторить ОБЫЧНЫЙ push с `GIT_SSH_COMMAND='ssh -o ConnectTimeout=20'` (за сессию 4 из 4 push прошли со 2-й попытки); мёртв только 22 ⇒ https-обход. ⚠ Tell: два транспорта падают одинаковым таймаутом в одну и ту же минуту.

⛤ **МЕХАНИЗМ окон 1–3 мин (замер 2026-09-19 21:0x, child #4277): DNS отдаёт для `github.com` ОДИН A-адрес из пула, и мёртв именно ОН, а не GitHub.** `dig +short github.com` → `140.82.121.4`; `curl --resolve github.com:443:<ip>` по соседям пула (`140.82.121.3`, `.112.3/.4`, `.114.3/.4`, `.116.3/.4`) → 200 за 1–4 с, `.121.4` → таймаут; `api.github.com` (другой хост, `.121.6`) жив. Окно = TTL ротации (~60 с): через одну-две минуты `dig` вернул `.121.3`, и тот же push прошёл. ⚠ Tell: системный `curl https://github.com/` может дать 200 (резолвер отдал другой адрес), а `git-remote-https` в ту же минуту — connect-таймаут 75 с: у них РАЗНЫЕ резолвы, и «curl 200 ⇒ git пройдёт» ложно. ✅ Floor: не менять `/etc/hosts`/конфиг и не переписывать URL на IP — ждун `until curl -m 8 https://github.com/ = 200; sleep 20` с повтором push внутри (elapsed по часам, Г55), верификация по API. Data-репо при долгом окне — Contents API (выше).

✅ **Обход, не зависящий от того, какой A-адрес `github.com` мёртв (проверен ORCH и child 2026-09-20, 22 и 443 у `github.com` лежали одновременно ~10 мин): SSH через `ssh.github.com:443` — ДРУГОЙ хост и другой IP, host key уже в `known_hosts`, auth та же.** Форма для push / fetch / clone data-репо, remote не менять:

```bash
git -C <repo> push ssh://git@ssh.github.com:443/kitelev/<repo>.git HEAD:refs/heads/<branch> > <лог> 2>&1; echo rc=$?
git clone --depth 3 ssh://git@ssh.github.com:443/kitelev/<repo>.git <dir>
gh api repos/kitelev/<repo>/branches/<branch> --jq .commit.sha    # verify: api.github.com — отдельный хост
```

⚠ Tell: `ssh: connect to host github.com port 22` И `Failed to connect to github.com port 443` в одном окне при живом `gh api` ⇒ мёртв A-адрес, а не сервис — сперва эта форма, retry-петля по 60 с только после неё. _(2026-09-20, child codebatch6k: https:443 прошёл со 2-й попытки, ssh.github.com:443 — с первой, 4 push подряд.)_
## ⛔ СЕЛЕКТИВНЫЙ push клоном: предикат «в клоне только мои файлы» ТОЖДЕСТВЕННО ИСТИНЕН — судить надо ДИФФ, и отдельным вызовом

Приём «клонировать data-репо, скопировать свои файлы, запушить» выбран ровно
ради селективности — он обходит `exosync push`, который идёт по ВСЕМУ vault.
Но его обычная проверка (`git -C <клон> status --short` ⇒ «изменены ровно мои
файлы») **не может показать иное**: изменены те файлы, которые ты скопировал.
Она подтверждает твоё же действие, а не состав поставки.

⛔ Состав задаёт **локальный файл**, а он несёт и чужие правки, сделанные в него
параллельными сессиями и ещё не доехавшие на remote. Ты пушишь их под своим
сообщением — присвоение чужой работы без злого умысла и без следа.

- ✅ **Floor: предикат — `git -C <клон> diff --numstat`, и вердикт читается ДО
  коммита, ОТДЕЛЬНЫМ вызовом.** Ожидаемое число строк выводить ЗАРАНЕЕ (правил
  одну строку индекса ⇒ `1 1`, с бампом `updatedAt` ⇒ `2 1`). Не сошлось —
  открыть `git diff` и назвать чужие строки ПОИМЁННО: либо не пушить их, либо
  сказать о них в сообщении коммита.
- ⚠ **Tell, ловится ДО Enter:** в одном Bash-вызове стоят и `diff --numstat`,
  и `commit`/`push` — тогда вердикт печатается ВЫШЕ подтверждения пуша и
  выглядит учтённым, а пуш уходит независимо от него
  ([[self-verification]] §проверка и необратимое в одном вызове).
- ⛤ Тот же счёт отвечает и на вопрос «мой ли это файл целиком»: у чужих строк
  своя дата и свой номер `§A`.

_Empirical 2026-09-26: клон `exoas-aiknow`, правил §A128 + §A51 ⇒ ждал `2 1` и
`2 1`; numstat дал `4 1` и `2 1`, и это было напечатано в ТОМ ЖЕ вызове, что
`push`. Уехали §A126 и §A127 двух чужих сессий — аддитивно и без потерь, но
под моим сообщением._
